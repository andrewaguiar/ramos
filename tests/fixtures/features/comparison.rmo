# comparison.rmo — comparison operators, each returning a bool.

equal = 1 == 1
not_equal = 1 != 2
less = 1 < 2
greater = 1 > 2
less_or_equal = 1 <= 2
greater_or_equal = 1 >= 2

# Comparison binds looser than arithmetic, so the sum happens first.
in_range = 1 + 2 * 3 == 7
