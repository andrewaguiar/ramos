# private_functions.rmo — `helper` defines a private function, callable only from
# inside its own module. `function` is public.

module Accounts
  function create(name)
    (:ok, normalize(name))   # a public function may call a private one

  helper normalize(name)
    String.downcase(name)
