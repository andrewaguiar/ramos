{"modules":["Actor","Config","Date","DateTime","Dir","File","Float","Global","Integer","Kernel","List","Map","Module","NaiveDateTime","String","Struct","Test","Thread","Time","TimeZone","Tuple"],"guides":{"guide":true,"examples":true,"programs":true},"pages":{"Actor":{"title":"Actor — Ramos","heading":"Actor","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Actor — a named process that holds state and answers messages.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Actor — a named process that holds state and answers messages.</p>\n<p>A module implements <code><a href=\"#/Actor\">Actor</a></code> by defining one function, <code>call</code>, which takes the operation, its arguments, the actor&#39;s current state and its config, and returns a <code>(reply, new_state)</code> tuple. The actor&#39;s state is whatever the last <code>call</code> returned; nothing mutates, the actor simply holds the newest value.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Counter</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Actor</span></code></pre>\n<pre><code><span class=\"tok-kw\">function</span> call(f, args, state, config)\n  <span class=\"tok-kw\">case</span> f\n    <span class=\"tok-lit\">:bump</span> -&gt; (state + <span class=\"tok-lit\">1</span>, state + <span class=\"tok-lit\">1</span>)\n    <span class=\"tok-lit\">:read</span> -&gt; (state, state)</code></pre>\n<pre><code><span class=\"tok-kw\">function</span> start()\n  start_actor(<span class=\"tok-lit\">:counter</span>, <span class=\"tok-type\">Counter</span>, <span class=\"tok-lit\">0</span>, {})</code></pre>\n<pre><code><span class=\"tok-kw\">function</span> bump()\n  call_actor(<span class=\"tok-lit\">:counter</span>, <span class=\"tok-type\">Counter</span>, <span class=\"tok-lit\">:bump</span>, [])</code></pre>\n<p>The convention is to keep <code>call</code> as the server half and wrap each operation in a plain function — the client half — so callers never write <code>call_actor</code> themselves.</p>\n<p>Each actor runs on <em></em>its own thread<em></em>, with its own memory: a fresh scope that cannot see the caller&#39;s bindings, and state reachable only by sending a message. One actor handles its own messages one at a time, in order, but different actors run in parallel.</p>\n<p>That memory does not include the actor registry, so a handler cannot send messages to another actor or to itself.</p>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#cast/4\" class=\"fn-sig\"><code>function cast(f, args, state, config)</code></a>\n    <span class=\"fn-summary\">Handle one message sent with <code>cast_actor</code>, returning only the new state — a cast has no reply to send anywhere, which is the whole difference from <code>call</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#call/4\" class=\"fn-sig\"><code>function call(f, args, state, config)</code></a>\n    <span class=\"fn-summary\">Handle one message. Required — every implementer defines it.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"cast/4\">\n  <h3 class=\"fn-title\"><code>function cast(f, args, state, config)</code> <a class=\"anchor\" href=\"#cast/4\">¶</a></h3>\n<p>Handle one message sent with <code>cast_actor</code>, returning only the new state — a cast has no reply to send anywhere, which is the whole difference from <code>call</code>.</p>\n<p>The default runs the module&#39;s own <code>call</code> and discards the reply, so an actor that already handles an operation synchronously can be cast to without writing anything extra. Override it for work that only makes sense fire-and-forget.</p>\n<p><code>cast_actor</code> returns as soon as the message is sent, so the handler may still be running when the next statement starts.</p>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>the operation, conventionally a symbol</dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of arguments for the operation</dd>\n  <dt><code>state</code></dt>\n  <dd>the actor&#39;s current state</dd>\n  <dt><code>config</code></dt>\n  <dd>the config given to <code>start_actor</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the actor&#39;s new state</p>\n</div>\n<div class=\"fn-detail\" id=\"call/4\">\n  <h3 class=\"fn-title\"><code>function call(f, args, state, config)</code> <a class=\"anchor\" href=\"#call/4\">¶</a></h3>\n<p>Handle one message. Required — every implementer defines it.</p>\n<pre><code>actor&#39;s lifetime\nand `new_state` becomes the actor&#39;s state</code></pre>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>the operation, conventionally a symbol such as <code>:get</code></dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of arguments for the operation</dd>\n  <dt><code>state</code></dt>\n  <dd>the actor&#39;s current state</dd>\n  <dt><code>config</code></dt>\n  <dd>the config given to <code>start_actor</code>, unchanged for the</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code>(reply, new_state)</code> tuple: the reply goes back to the caller</p>\n</div>\n</section>\n"},"Config":{"title":"Config — Ramos","heading":"Config","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Config — the environment&#39;s <code>.env</code> file, read once and answered from memory.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Config — the environment&#39;s <code>.env</code> file, read once and answered from memory.</p>\n<p>One actor named <code>:config</code> holds the settings for this run. <code>start</code> picks the file from the <code>APP_ENV</code> environment variable, reads it, parses it, and hands the result to the actor; from then on <code>get</code> is the only operation. Nothing writes — the actor is <em></em>read-only<em></em>, so what a program sees at the end of a run is what it saw at the start.</p>\n<p>The examples here and below are written against this <code>.env</code>:</p>\n<pre><code><span class=\"tok-com\"># ramos doctest setup</span>\nsettings =\n  <span class=\"tok-str\">&quot;&quot;&quot;\n    [database]\n    host = &quot;db.internal&quot;\n    port = 5432\n  &quot;&quot;&quot;</span>\n<span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;.env&quot;</span>, settings)</code></pre>\n<p>Read back, that is:</p>\n<pre><code><span class=\"tok-type\">Config</span>.start()                   <span class=\"tok-com\"># == :ok</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;host&quot;</span>)   <span class=\"tok-com\"># == &quot;db.internal&quot;</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;port&quot;</span>)   <span class=\"tok-com\"># == &quot;5432&quot;</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;user&quot;</span>)   <span class=\"tok-com\"># == nil</span></code></pre>\n<p><em></em>Which file.<em></em> <code>APP_ENV</code> names the environment and the file is <code>.env</code> followed by its downcased name — <code>APP_ENV=PROD</code> reads <code>.env.prod</code>, <code>APP_ENV=staging</code> reads <code>.env.staging</code>. With <code>APP_ENV</code> unset or empty the plain <code>.env</code> is read. <code>path()</code> reports the choice without starting anything. The path is relative, so it resolves against the working directory the program was run from.</p>\n<p><em></em>The format<em></em> is TOML, of the subset a settings file actually uses: <code>[section]</code> headers, <code>key = value</code> pairs, <code>#</code> comments, blank lines. Every value comes back as a <code><a href=\"#/String\">String</a></code> — <code>port = 5432</code> reads as <code>&quot;5432&quot;</code> — because the file cannot say what a value was meant to be and guessing would make <code>get</code> return a different type for the same key on a different deploy. Use <code>to_integer</code> on the way out when you need a number.</p>\n<p>Quotes around a value are stripped, single or double. An unquoted value ends at a trailing <code> #</code> comment; a quoted one does not, so a <code>#</code> inside quotes is part of the value. A key that appears twice takes its last value, and a key written before the first <code>[section]</code> has no section to be asked for, so it is skipped.</p>\n<p><em></em>Missing files are not an error to <code>get</code>.<em></em> <code>start</code> reports an unreadable file as <code>(:error, reason)</code> in <code><a href=\"#/File\">File</a></code>&#39;s vocabulary, so the caller decides whether a run without config is fatal:</p>\n<pre><code><span class=\"tok-kw\">case</span> <span class=\"tok-type\">Config</span>.start()\n  <span class=\"tok-lit\">:ok</span> -&gt; <span class=\"tok-kw\">run</span>()\n  (<span class=\"tok-lit\">:error</span>, <span class=\"tok-lit\">:enoent</span>) -&gt; run_with_defaults()</code></pre>\n<p><code><a href=\"#/Config\">Config</a></code> is an ordinary <code><a href=\"#/Actor\">Actor</a></code> implementation, so <code>list_actors</code>, <code>actor_stats(:config)</code> and <code>stop_actor(:config)</code> all work on it. For state that changes as a program runs, see <code><a href=\"#/Global\">Global</a></code>.</p>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#call/4\" class=\"fn-sig\"><code>function call(f, args, state, config)</code></a>\n    <span class=\"fn-summary\">The server half. <code>:get</code> is the only operation — the settings are fixed for the run, so there is nothing else to ask for.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#start/0\" class=\"fn-sig\"><code>function start()</code></a>\n    <span class=\"fn-summary\">Read the environment&#39;s file and start the <code>:config</code> actor on it. Call it once, before anything reads; starting it twice is an error.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#get/2\" class=\"fn-sig\"><code>function get(section, key)</code></a>\n    <span class=\"fn-summary\">The value of <code>key</code> under <code>[section]</code>, as a <code><a href=\"#/String\">String</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#path/0\" class=\"fn-sig\"><code>function path()</code></a>\n    <span class=\"fn-summary\">The file <code>start</code> will read, chosen from <code>APP_ENV</code>: <code>.env.&lt;downcased&gt;</code> when it is set, and <code>.env</code> when it is not. Reads nothing itself.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#get_impl/2\" class=\"fn-sig\"><code>helper get_impl(args, state)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#parse/1\" class=\"fn-sig\"><code>helper parse(text)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#parse_line/2\" class=\"fn-sig\"><code>helper parse_line(acc, raw)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#is_skippable/1\" class=\"fn-sig\"><code>helper is_skippable(line)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#is_header/1\" class=\"fn-sig\"><code>helper is_header(line)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#header_name/1\" class=\"fn-sig\"><code>helper header_name(line)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#put_pair/3\" class=\"fn-sig\"><code>helper put_pair(sections, section, line)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#put_entry/4\" class=\"fn-sig\"><code>helper put_entry(sections, section, key, value)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#key_of/2\" class=\"fn-sig\"><code>helper key_of(line, split)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#value_of/2\" class=\"fn-sig\"><code>helper value_of(line, split)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#unquote/1\" class=\"fn-sig\"><code>helper unquote(value)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#take_quoted/2\" class=\"fn-sig\"><code>helper take_quoted(value, quote)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#drop_comment/1\" class=\"fn-sig\"><code>helper drop_comment(value)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"call/4\">\n  <h3 class=\"fn-title\"><code>function call(f, args, state, config)</code> <a class=\"anchor\" href=\"#call/4\">¶</a></h3>\n<p>The server half. <code>:get</code> is the only operation — the settings are fixed for the run, so there is nothing else to ask for.</p>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>the operation — <code>:get</code></dd>\n  <dt><code>args</code></dt>\n  <dd><code>[section, key]</code>, the pair to look up</dd>\n  <dt><code>state</code></dt>\n  <dd>the parsed settings, section by section</dd>\n  <dt><code>config</code></dt>\n  <dd>unused — the file is read before the actor starts</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code>(value, state)</code> tuple; a read leaves the settings alone</p>\n</div>\n<div class=\"fn-detail\" id=\"start/0\">\n  <h3 class=\"fn-title\"><code>function start()</code> <a class=\"anchor\" href=\"#start/0\">¶</a></h3>\n<p>Read the environment&#39;s file and start the <code>:config</code> actor on it. Call it once, before anything reads; starting it twice is an error.</p>\n<p>Reading happens here rather than inside the actor, so a missing or unreadable file surfaces at the call that started it.</p>\n<pre><code>`File`&#39;s reasons, so `:enoent` is &quot;no such file&quot;</code></pre>\n<pre><code><span class=\"tok-type\">Config</span>.start()   <span class=\"tok-com\"># == :ok</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;host&quot;</span>)   <span class=\"tok-com\"># == &quot;db.internal&quot;</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> when the file cannot be read —</p>\n</div>\n<div class=\"fn-detail\" id=\"get/2\">\n  <h3 class=\"fn-title\"><code>function get(section, key)</code> <a class=\"anchor\" href=\"#get/2\">¶</a></h3>\n<p>The value of <code>key</code> under <code>[section]</code>, as a <code><a href=\"#/String\">String</a></code>.</p>\n<pre><code><span class=\"tok-kw\">not</span> in the file</code></pre>\n<pre><code><span class=\"tok-type\">Config</span>.start()\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;host&quot;</span>)   <span class=\"tok-com\"># == &quot;db.internal&quot;</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;nope&quot;</span>)   <span class=\"tok-com\"># == nil</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;nope&quot;</span>, <span class=\"tok-str\">&quot;host&quot;</span>)       <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>section</code></dt>\n  <dd>the section name, without its brackets</dd>\n  <dt><code>key</code></dt>\n  <dd>the key to look up within that section</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the value as a <code><a href=\"#/String\">String</a></code>, or <code>nil</code> when the section or the key is</p>\n</div>\n<div class=\"fn-detail\" id=\"path/0\">\n  <h3 class=\"fn-title\"><code>function path()</code> <a class=\"anchor\" href=\"#path/0\">¶</a></h3>\n<p>The file <code>start</code> will read, chosen from <code>APP_ENV</code>: <code>.env.&lt;downcased&gt;</code> when it is set, and <code>.env</code> when it is not. Reads nothing itself.</p>\n<p>With <code>APP_ENV=PROD</code> this reads <code>.env.prod</code>; unset, as here, it is <code>.env</code>:</p>\n<pre><code><span class=\"tok-type\">Config</span>.path()   <span class=\"tok-com\"># == &quot;.env&quot;</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the path as a <code><a href=\"#/String\">String</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"get_impl/2\">\n  <h3 class=\"fn-title\"><code>helper get_impl(args, state)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#get_impl/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"parse/1\">\n  <h3 class=\"fn-title\"><code>helper parse(text)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#parse/1\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"parse_line/2\">\n  <h3 class=\"fn-title\"><code>helper parse_line(acc, raw)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#parse_line/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"is_skippable/1\">\n  <h3 class=\"fn-title\"><code>helper is_skippable(line)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#is_skippable/1\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"is_header/1\">\n  <h3 class=\"fn-title\"><code>helper is_header(line)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#is_header/1\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"header_name/1\">\n  <h3 class=\"fn-title\"><code>helper header_name(line)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#header_name/1\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"put_pair/3\">\n  <h3 class=\"fn-title\"><code>helper put_pair(sections, section, line)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#put_pair/3\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"put_entry/4\">\n  <h3 class=\"fn-title\"><code>helper put_entry(sections, section, key, value)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#put_entry/4\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"key_of/2\">\n  <h3 class=\"fn-title\"><code>helper key_of(line, split)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#key_of/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"value_of/2\">\n  <h3 class=\"fn-title\"><code>helper value_of(line, split)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#value_of/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"unquote/1\">\n  <h3 class=\"fn-title\"><code>helper unquote(value)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#unquote/1\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"take_quoted/2\">\n  <h3 class=\"fn-title\"><code>helper take_quoted(value, quote)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#take_quoted/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"drop_comment/1\">\n  <h3 class=\"fn-title\"><code>helper drop_comment(value)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#drop_comment/1\">¶</a></h3>\n</div>\n</section>\n"},"Date":{"title":"Date — Ramos","heading":"Date","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Date — a calendar date: year, month, and day, with no time-of-day or time zone. Built on the proleptic Gregorian calendar.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Date — a calendar date: year, month, and day, with no time-of-day or time zone. Built on the proleptic Gregorian calendar.</p>\n<p>Construct with <code>new</code>, <code>today</code>, <code>from_epoch_day</code>, or <code>parse</code>; read a field with dot access, like any struct:</p>\n<pre><code>d = <span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">29</span>)\nd.year    <span class=\"tok-com\"># == 2024</span>\nd.month   <span class=\"tok-com\"># == 2</span></code></pre>\n<p>An out-of-range <code>day</code> normalizes rather than raising — <code>day: 0</code> is the last day of the previous month, one past the last day rolls into the next — so &quot;the last day of this month&quot; is just <code><a href=\"#/Date:new\">Date.new(year, month + 1, 0)</a></code>. <code>month</code> itself must be <code>1</code>..<code>12</code>.</p>\n<pre><code><span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">0</span>)    <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 29}</span>\n<span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">32</span>)   <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 1}</span></code></pre>\n<p>Two dates compare structurally with <code>==</code>, like any other struct — but there is no built-in <code>&lt;</code>/<code>&gt;</code>, since arbitrary structs are not ordered. Use <code>compare</code> instead, or sort by <code>to_epoch_day</code>:</p>\n<pre><code><span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>) == <span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>)               <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Date</span>.compare(<span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>), <span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>))   <span class=\"tok-com\"># == -1</span></code></pre>\n<p>Conventions:</p>\n<ul><li>every date is proleptic Gregorian, with no time zone — <code>today</code> reads the wall clock as UTC.</li><li><code>compare</code> follows <code><a href=\"#/Integer:compare\">Integer.compare</a></code>/<code><a href=\"#/Float:compare\">Float.compare</a></code>: <code>-1</code>, <code>0</code>, <code>1</code>.</li><li>the attributes default to the epoch, <code>1970-01-01</code>, so <code>Date{}</code> is a real (if arbitrary) date rather than a half-built value.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#new/3\" class=\"fn-sig\"><code>function new(year, month, day)</code></a>\n    <span class=\"fn-summary\">Builds a <code><a href=\"#/Date\">Date</a></code>. <code>day</code> normalizes when it falls outside the month — <code>0</code> is the last day of the previous month, one past the last day rolls into the next.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#today/0\" class=\"fn-sig\"><code>function today()</code></a>\n    <span class=\"fn-summary\">Today&#39;s date, read from the wall clock as UTC.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_epoch_day/1\" class=\"fn-sig\"><code>function from_epoch_day(epoch_day)</code></a>\n    <span class=\"fn-summary\">The <code><a href=\"#/Date\">Date</a></code> that is <code>epoch_day</code> days after 1970-01-01 (day <code>0</code>). Negative values reach before the epoch.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#parse/1\" class=\"fn-sig\"><code>function parse(text)</code></a>\n    <span class=\"fn-summary\">Parses an ISO-8601 calendar date, <code>&quot;YYYY-MM-DD&quot;</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_epoch_day/1\" class=\"fn-sig\"><code>function to_epoch_day(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> as the number of days since 1970-01-01 — the bridge to arithmetic and comparison.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_iso/1\" class=\"fn-sig\"><code>function to_iso(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> rendered as <code>&quot;YYYY-MM-DD&quot;</code> (ISO-8601), zero-padded.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_days/2\" class=\"fn-sig\"><code>function add_days(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> days — negative moves backward.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_years/2\" class=\"fn-sig\"><code>function add_years(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> years, keeping the month and day — except a leap day (<code>month: 2, day: 29</code>) landing on a non-leap year, which rolls to March 1, the same normalization <code>new</code> does for any other overflowing day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_weeks/2\" class=\"fn-sig\"><code>function add_weeks(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> weeks (<code>n * 7</code> days) — negative moves backward.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_months/2\" class=\"fn-sig\"><code>function add_months(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> months, carrying into the year and normalizing an overflowing day the same way <code>new</code> does — &quot;same day next month&quot; for <code>day: 31</code> run against a 30-day month rolls into the one after.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(self, other)</code></a>\n    <span class=\"fn-summary\">Orders two dates as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> and <code><a href=\"#/Float:compare\">Float.compare</a></code> use, since there is no built-in <code>&lt;</code>/<code>&gt;</code> for structs.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#diff_days/2\" class=\"fn-sig\"><code>function diff_days(self, other)</code></a>\n    <span class=\"fn-summary\">The number of days from <code>other</code> to <code>self</code> — positive when <code>self</code> is later, negative when earlier.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#day_of_week/1\" class=\"fn-sig\"><code>function day_of_week(self)</code></a>\n    <span class=\"fn-summary\">The day of the week <code>self</code> falls on.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_leap_year/1\" class=\"fn-sig\"><code>function is_leap_year(year)</code></a>\n    <span class=\"fn-summary\">Whether <code>year</code> has a February 29th, by the proleptic Gregorian rule: divisible by 4, except centuries, unless also divisible by 400.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#days_in_month/1\" class=\"fn-sig\"><code>function days_in_month(self)</code></a>\n    <span class=\"fn-summary\">How many days are in <code>self</code>&#39;s month.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#pad/2\" class=\"fn-sig\"><code>helper pad(n, width)</code></a>\n    <span class=\"fn-summary\">(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"new/3\">\n  <h3 class=\"fn-title\"><code>function new(year, month, day)</code> <a class=\"anchor\" href=\"#new/3\">¶</a></h3>\n<p>Builds a <code><a href=\"#/Date\">Date</a></code>. <code>day</code> normalizes when it falls outside the month — <code>0</code> is the last day of the previous month, one past the last day rolls into the next.</p>\n<pre><code><span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">29</span>)   <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 29}</span>\n<span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">0</span>)    <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 29}</span>\n<span class=\"tok-type\">Date</span>.new(<span class=\"tok-lit\">2023</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">29</span>)   <span class=\"tok-com\"># == Date{year: 2023, month: 3, day: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>year</code></dt>\n  <dd>the calendar year, any integer</dd>\n  <dt><code>month</code></dt>\n  <dd>the month, <code>1</code>..<code>12</code></dd>\n  <dt><code>day</code></dt>\n  <dd>the day of month; out-of-range values roll over</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"today/0\">\n  <h3 class=\"fn-title\"><code>function today()</code> <a class=\"anchor\" href=\"#today/0\">¶</a></h3>\n<p>Today&#39;s date, read from the wall clock as UTC.</p>\n<pre><code><span class=\"tok-type\">Date</span>.today().year &gt; <span class=\"tok-lit\">2000</span>   <span class=\"tok-com\"># == true</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the current <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_epoch_day/1\">\n  <h3 class=\"fn-title\"><code>function from_epoch_day(epoch_day)</code> <a class=\"anchor\" href=\"#from_epoch_day/1\">¶</a></h3>\n<p>The <code><a href=\"#/Date\">Date</a></code> that is <code>epoch_day</code> days after 1970-01-01 (day <code>0</code>). Negative values reach before the epoch.</p>\n<pre><code><span class=\"tok-type\">Date</span>.from_epoch_day(<span class=\"tok-lit\">0</span>)    <span class=\"tok-com\"># == Date{year: 1970, month: 1, day: 1}</span>\n<span class=\"tok-type\">Date</span>.from_epoch_day(-<span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == Date{year: 1969, month: 12, day: 31}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>epoch_day</code></dt>\n  <dd>days since 1970-01-01, as an <code><a href=\"#/Integer\">Integer</a></code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"parse/1\">\n  <h3 class=\"fn-title\"><code>function parse(text)</code> <a class=\"anchor\" href=\"#parse/1\">¶</a></h3>\n<p>Parses an ISO-8601 calendar date, <code>&quot;YYYY-MM-DD&quot;</code>.</p>\n<pre><code><span class=\"tok-type\">Date</span>.parse(<span class=\"tok-str\">&quot;2024-02-29&quot;</span>)   <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 29}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>text</code></dt>\n  <dd>a date in <code>&quot;YYYY-MM-DD&quot;</code> form</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"to_epoch_day/1\">\n  <h3 class=\"fn-title\"><code>function to_epoch_day(self)</code> <a class=\"anchor\" href=\"#to_epoch_day/1\">¶</a></h3>\n<p><code>self</code> as the number of days since 1970-01-01 — the bridge to arithmetic and comparison.</p>\n<pre><code><span class=\"tok-type\">Date</span>.to_epoch_day(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">1970</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})     <span class=\"tok-com\"># == 0</span>\n<span class=\"tok-type\">Date</span>.to_epoch_day(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">1969</span>, month: <span class=\"tok-lit\">12</span>, day: <span class=\"tok-lit\">31</span>})   <span class=\"tok-com\"># == -1</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> day count, negative before the epoch</p>\n</div>\n<div class=\"fn-detail\" id=\"to_iso/1\">\n  <h3 class=\"fn-title\"><code>function to_iso(self)</code> <a class=\"anchor\" href=\"#to_iso/1\">¶</a></h3>\n<p><code>self</code> rendered as <code>&quot;YYYY-MM-DD&quot;</code> (ISO-8601), zero-padded.</p>\n<pre><code><span class=\"tok-type\">Date</span>.to_iso(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>})   <span class=\"tok-com\"># == &quot;2024-02-09&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date to render</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the ISO-8601 string</p>\n</div>\n<div class=\"fn-detail\" id=\"add_days/2\">\n  <h3 class=\"fn-title\"><code>function add_days(self, n)</code> <a class=\"anchor\" href=\"#add_days/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> days — negative moves backward.</p>\n<pre><code><span class=\"tok-type\">Date</span>.add_days(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">28</span>}, <span class=\"tok-lit\">1</span>)    <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 29}</span>\n<span class=\"tok-type\">Date</span>.add_days(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>}, -<span class=\"tok-lit\">1</span>)    <span class=\"tok-com\"># == Date{year: 2023, month: 12, day: 31}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date</dd>\n  <dt><code>n</code></dt>\n  <dd>how many days to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_years/2\">\n  <h3 class=\"fn-title\"><code>function add_years(self, n)</code> <a class=\"anchor\" href=\"#add_years/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> years, keeping the month and day — except a leap day (<code>month: 2, day: 29</code>) landing on a non-leap year, which rolls to March 1, the same normalization <code>new</code> does for any other overflowing day.</p>\n<pre><code><span class=\"tok-type\">Date</span>.add_years(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">29</span>}, <span class=\"tok-lit\">1</span>)    <span class=\"tok-com\"># == Date{year: 2025, month: 3, day: 1}</span>\n<span class=\"tok-type\">Date</span>.add_years(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2020</span>, month: <span class=\"tok-lit\">6</span>, day: <span class=\"tok-lit\">15</span>}, -<span class=\"tok-lit\">5</span>)   <span class=\"tok-com\"># == Date{year: 2015, month: 6, day: 15}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date</dd>\n  <dt><code>n</code></dt>\n  <dd>how many years to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_weeks/2\">\n  <h3 class=\"fn-title\"><code>function add_weeks(self, n)</code> <a class=\"anchor\" href=\"#add_weeks/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> weeks (<code>n * 7</code> days) — negative moves backward.</p>\n<pre><code><span class=\"tok-type\">Date</span>.add_weeks(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">1</span>)    <span class=\"tok-com\"># == Date{year: 2024, month: 1, day: 8}</span>\n<span class=\"tok-type\">Date</span>.add_weeks(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">8</span>}, -<span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == Date{year: 2024, month: 1, day: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date</dd>\n  <dt><code>n</code></dt>\n  <dd>how many weeks to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_months/2\">\n  <h3 class=\"fn-title\"><code>function add_months(self, n)</code> <a class=\"anchor\" href=\"#add_months/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> months, carrying into the year and normalizing an overflowing day the same way <code>new</code> does — &quot;same day next month&quot; for <code>day: 31</code> run against a 30-day month rolls into the one after.</p>\n<pre><code><span class=\"tok-type\">Date</span>.add_months(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">15</span>}, <span class=\"tok-lit\">1</span>)    <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 15}</span>\n<span class=\"tok-type\">Date</span>.add_months(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">11</span>, day: <span class=\"tok-lit\">15</span>}, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == Date{year: 2025, month: 2, day: 15}</span>\n<span class=\"tok-type\">Date</span>.add_months(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">15</span>}, -<span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == Date{year: 2023, month: 12, day: 15}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date</dd>\n  <dt><code>n</code></dt>\n  <dd>how many months to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Date\">Date</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(self, other)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two dates as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> and <code><a href=\"#/Float:compare\">Float.compare</a></code> use, since there is no built-in <code>&lt;</code>/<code>&gt;</code> for structs.</p>\n<pre><code><span class=\"tok-type\">Date</span>.compare(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">2</span>})   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">Date</span>.compare(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">2</span>}, <span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Date</span>.compare(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the first date</dd>\n  <dt><code>other</code></dt>\n  <dd>the second date</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>self</code> is earlier, <code>1</code> when later, <code>0</code> when equal</p>\n</div>\n<div class=\"fn-detail\" id=\"diff_days/2\">\n  <h3 class=\"fn-title\"><code>function diff_days(self, other)</code> <a class=\"anchor\" href=\"#diff_days/2\">¶</a></h3>\n<p>The number of days from <code>other</code> to <code>self</code> — positive when <code>self</code> is later, negative when earlier.</p>\n<pre><code><span class=\"tok-type\">Date</span>.diff_days(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">10</span>}, <span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 9</span>\n<span class=\"tok-type\">Date</span>.diff_days(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">10</span>})   <span class=\"tok-com\"># == -9</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the later (or earlier) date</dd>\n  <dt><code>other</code></dt>\n  <dd>the date to measure from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> day count</p>\n</div>\n<div class=\"fn-detail\" id=\"day_of_week/1\">\n  <h3 class=\"fn-title\"><code>function day_of_week(self)</code> <a class=\"anchor\" href=\"#day_of_week/1\">¶</a></h3>\n<p>The day of the week <code>self</code> falls on.</p>\n<pre><code>`:thursday`, `:friday`, `:saturday`</code></pre>\n<pre><code><span class=\"tok-type\">Date</span>.day_of_week(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">1970</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == :thursday</span>\n<span class=\"tok-type\">Date</span>.day_of_week(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == :monday</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> one of <code>:sunday</code>, <code>:monday</code>, <code>:tuesday</code>, <code>:wednesday</code>,</p>\n</div>\n<div class=\"fn-detail\" id=\"is_leap_year/1\">\n  <h3 class=\"fn-title\"><code>function is_leap_year(year)</code> <a class=\"anchor\" href=\"#is_leap_year/1\">¶</a></h3>\n<p>Whether <code>year</code> has a February 29th, by the proleptic Gregorian rule: divisible by 4, except centuries, unless also divisible by 400.</p>\n<pre><code><span class=\"tok-type\">Date</span>.is_leap_year(<span class=\"tok-lit\">2024</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Date</span>.is_leap_year(<span class=\"tok-lit\">1900</span>)   <span class=\"tok-com\"># == false</span>\n<span class=\"tok-type\">Date</span>.is_leap_year(<span class=\"tok-lit\">2000</span>)   <span class=\"tok-com\"># == true</span></code></pre>\n<dl class=\"params\">\n  <dt><code>year</code></dt>\n  <dd>the calendar year to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>year</code> is a leap year</p>\n</div>\n<div class=\"fn-detail\" id=\"days_in_month/1\">\n  <h3 class=\"fn-title\"><code>function days_in_month(self)</code> <a class=\"anchor\" href=\"#days_in_month/1\">¶</a></h3>\n<p>How many days are in <code>self</code>&#39;s month.</p>\n<pre><code><span class=\"tok-type\">Date</span>.days_in_month(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 29</span>\n<span class=\"tok-type\">Date</span>.days_in_month(<span class=\"tok-type\">Date</span>{year: <span class=\"tok-lit\">2023</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 28</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date whose month to measure</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>28</code>-<code>31</code></p>\n</div>\n<div class=\"fn-detail\" id=\"pad/2\">\n  <h3 class=\"fn-title\"><code>helper pad(n, width)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#pad/2\">¶</a></h3>\n<p>(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</p>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the non-negative integer to render</dd>\n  <dt><code>width</code></dt>\n  <dd>the minimum number of digits</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>n</code>&#39;s decimal digits, zero-padded to <code>width</code></p>\n</div>\n</section>\n"},"DateTime":{"title":"DateTime — Ramos","heading":"DateTime","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>DateTime — a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> plus a fixed offset from UTC, so it names a precise instant rather than just a wall-clock reading.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>DateTime — a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> plus a fixed offset from UTC, so it names a precise instant rather than just a wall-clock reading.</p>\n<p>There is no time zone database here (no dependency ships one) — an <code>offset_minutes</code> is a plain number, like <code>-300</code> for UTC-5 or <code>330</code> for UTC+5:30, not a zone name that shifts with daylight saving. The fields (<code>year</code>..<code>millisecond</code>) are the <em>local</em> reading at that offset:</p>\n<pre><code>utc = <span class=\"tok-type\">DateTime</span>.now()\nny = <span class=\"tok-type\">DateTime</span>.now_at(-<span class=\"tok-lit\">300</span>)\nutc.offset_minutes   <span class=\"tok-com\"># == 0</span>\nny.offset_minutes    <span class=\"tok-com\"># == -300</span></code></pre>\n<p>Two <code><a href=\"#/DateTime\">DateTime</a></code>s at different offsets can still name the same instant — <code>compare</code> and <code>diff_millis</code> account for the offset, so they agree with what a clock actually measures, not just the printed fields:</p>\n<pre><code>a = <span class=\"tok-type\">DateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">12</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>)     <span class=\"tok-com\"># noon UTC</span>\nb = <span class=\"tok-type\">DateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">7</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>, -<span class=\"tok-lit\">300</span>)   <span class=\"tok-com\"># 07:00 at UTC-5 — the same instant</span>\n<span class=\"tok-type\">DateTime</span>.compare(a, b)   <span class=\"tok-com\"># == 0</span></code></pre>\n<p><code>==</code> does not know this — it compares fields structurally like any other struct, so <code>a == b</code> above is <code>false</code> even though they name one instant. Convert with <code>with_offset</code> (or <code>to_utc</code>) before comparing fields directly.</p>\n<p><code>now_in</code> and <code>in_time_zone</code> take a <code><a href=\"#/TimeZone\">TimeZone</a></code> (a name plus a fixed offset) rather than a bare number, when a name is worth carrying:</p>\n<pre><code>ist = <span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;IST&quot;</span>, <span class=\"tok-lit\">330</span>)\n<span class=\"tok-type\">DateTime</span>.now_in(ist).offset_minutes   <span class=\"tok-com\"># == 330</span></code></pre>\n<p>Conventions:</p>\n<ul><li><code>compare</code> orders by instant (the UTC equivalent), not by the printed local fields.</li><li><code>to_iso</code> renders the offset as <code>Z</code> for UTC or <code>±HH:MM</code> otherwise (via <code><a href=\"#/TimeZone:offset_text\">TimeZone.offset_text</a></code>), and <code>parse</code> reads the same shape back.</li><li>see <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> for the zone-free version of this shape, <code><a href=\"#/Date\">Date</a></code> for the calendar day alone, and <code><a href=\"#/TimeZone\">TimeZone</a></code> for the named-offset type.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#new/7\" class=\"fn-sig\"><code>function new(year, month, day, hour, minute, second, offset_minutes)</code></a>\n    <span class=\"fn-summary\">Builds a <code><a href=\"#/DateTime\">DateTime</a></code> from local fields at <code>offset_minutes</code> from UTC, with <code>millisecond: 0</code>. Every field past <code>year</code> normalizes when it overflows or underflows its natural range, the same way <code><a href=\"#/Date:new\">Date.new</a></code>&#39;s <code>day</code> does.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#now/0\" class=\"fn-sig\"><code>function now()</code></a>\n    <span class=\"fn-summary\">The current instant, as UTC (<code>offset_minutes: 0</code>).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#now_at/1\" class=\"fn-sig\"><code>function now_at(offset_minutes)</code></a>\n    <span class=\"fn-summary\">The current instant, as local fields at <code>offset_minutes</code> from UTC.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#now_in/1\" class=\"fn-sig\"><code>function now_in(time_zone)</code></a>\n    <span class=\"fn-summary\">The current instant, as local fields in <code>time_zone</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_epoch_millis/2\" class=\"fn-sig\"><code>function from_epoch_millis(utc_ms, offset_minutes)</code></a>\n    <span class=\"fn-summary\">The <code><a href=\"#/DateTime\">DateTime</a></code> for the instant <code>utc_ms</code> milliseconds after the epoch, with its fields expressed at <code>offset_minutes</code> from UTC.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#from_epoch_millis_raw/2\" class=\"fn-sig\"><code>helper from_epoch_millis_raw(utc_ms, offset_minutes)</code></a>\n    <span class=\"fn-summary\">(private) <code>from_epoch_millis</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_naive/2\" class=\"fn-sig\"><code>function from_naive(naive, offset_minutes)</code></a>\n    <span class=\"fn-summary\">Tags a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code>&#39;s fields as the local time at <code>offset_minutes</code> — a relabeling, not a conversion: the fields are copied as they are.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#parse/1\" class=\"fn-sig\"><code>function parse(text)</code></a>\n    <span class=\"fn-summary\">Parses <code>&quot;YYYY-MM-DDTHH:MM:SS±HH:MM&quot;</code> (or a trailing <code>&quot;Z&quot;</code> for UTC) — either a <code>T</code> or a plain space between the date and the time, as <code>to_iso</code> renders or as the <code>U&quot;...&quot;</code> sigil is written. The seconds&#39; fractional part is optional and any number of digits (padded or truncated to milliseconds), the same as <code><a href=\"#/NaiveDateTime:parse\">NaiveDateTime.parse</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#date/1\" class=\"fn-sig\"><code>function date(self)</code></a>\n    <span class=\"fn-summary\">The <code><a href=\"#/Date\">Date</a></code> half of <code>self</code>&#39;s local reading, dropping the time of day and the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_naive/1\" class=\"fn-sig\"><code>function to_naive(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code>&#39;s local fields as a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code>, dropping the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_utc_epoch_millis/1\" class=\"fn-sig\"><code>function to_utc_epoch_millis(self)</code></a>\n    <span class=\"fn-summary\">The instant <code>self</code> names, as milliseconds since the epoch (UTC) — the bridge to arithmetic and comparison, and what makes two <code><a href=\"#/DateTime\">DateTime</a></code>s at different offsets comparable.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_utc/1\" class=\"fn-sig\"><code>function to_utc(self)</code></a>\n    <span class=\"fn-summary\">The same instant as <code>self</code>, with its fields expressed at UTC (<code>offset_minutes: 0</code>).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#with_offset/2\" class=\"fn-sig\"><code>function with_offset(self, offset_minutes)</code></a>\n    <span class=\"fn-summary\">The same instant as <code>self</code>, with its fields re-expressed at <code>offset_minutes</code> — the way to convert between offsets.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#in_time_zone/2\" class=\"fn-sig\"><code>function in_time_zone(self, time_zone)</code></a>\n    <span class=\"fn-summary\">The same instant as <code>self</code>, with its fields re-expressed at <code>time_zone</code>&#39;s offset — <code>with_offset</code>, taking a <code><a href=\"#/TimeZone\">TimeZone</a></code> instead of a bare number.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#time_zone/1\" class=\"fn-sig\"><code>function time_zone(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code>&#39;s offset as a <code><a href=\"#/TimeZone\">TimeZone</a></code>, named after the offset itself (<code>&quot;UTC&quot;</code> at <code>0</code>, otherwise <code>±HH:MM</code>) — <code>self</code> only ever keeps the number, never the name a zone arrived with.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_iso/1\" class=\"fn-sig\"><code>function to_iso(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> rendered as <code>&quot;YYYY-MM-DDTHH:MM:SS.mmm&quot;</code> followed by <code>Z</code> (when <code>offset_minutes</code> is <code>0</code>) or a signed <code>±HH:MM</code> offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_days/2\" class=\"fn-sig\"><code>function add_days(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> days, keeping the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_weeks/2\" class=\"fn-sig\"><code>function add_weeks(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> weeks (<code>n * 7</code> days), keeping the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_months/2\" class=\"fn-sig\"><code>function add_months(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> months, keeping the time of day and the offset — <code><a href=\"#/Date:add_months\">Date.add_months</a></code> applied to the local date half, with the day-overflow normalization that implies.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_hours/2\" class=\"fn-sig\"><code>function add_hours(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> hours, keeping the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_minutes/2\" class=\"fn-sig\"><code>function add_minutes(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> minutes, keeping the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_seconds/2\" class=\"fn-sig\"><code>function add_seconds(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> seconds, keeping the offset.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(self, other)</code></a>\n    <span class=\"fn-summary\">Orders two date-times by the instant they name (their UTC equivalent), as <code>-1</code>, <code>0</code> or <code>1</code> — not by their printed local fields, so two <code><a href=\"#/DateTime\">DateTime</a></code>s at different offsets compare correctly.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#diff_millis/2\" class=\"fn-sig\"><code>function diff_millis(self, other)</code></a>\n    <span class=\"fn-summary\">The number of milliseconds from the instant <code>other</code> names to the one <code>self</code> names — positive when <code>self</code> is later, negative when earlier.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#day_of_week/1\" class=\"fn-sig\"><code>function day_of_week(self)</code></a>\n    <span class=\"fn-summary\">The day of the week <code>self</code>&#39;s local date falls on. Delegates to <code><a href=\"#/Date:day_of_week\">Date.day_of_week</a></code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#raw_millis/7\" class=\"fn-sig\"><code>helper raw_millis(year, month, day, hour, minute, second, millisecond)</code></a>\n    <span class=\"fn-summary\">(private) Milliseconds since the epoch for the given fields, as if they were UTC — before the offset is applied. Shared arithmetic behind <code>new</code>, <code>parse</code> and <code>to_utc_epoch_millis</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#shift/2\" class=\"fn-sig\"><code>helper shift(self, millis)</code></a>\n    <span class=\"fn-summary\">(private) <code>self</code> moved by <code>millis</code> milliseconds of real time, keeping its offset. Backs the <code>add_*</code> family.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#floor_div/2\" class=\"fn-sig\"><code>helper floor_div(a, b)</code></a>\n    <span class=\"fn-summary\">(private) <code>a</code> divided by <code>b</code>, rounding toward negative infinity — unlike <code>/</code>, which truncates toward zero. <code>b</code> is assumed positive.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#offset_start/1\" class=\"fn-sig\"><code>helper offset_start(rest)</code></a>\n    <span class=\"fn-summary\">(private) The index in <code>rest</code> (the text after the date) where its offset begins — the trailing <code>&quot;Z&quot;</code>, or the <code>+</code>/<code>-</code> of a <code>±HH:MM</code>. The clock text before it never contains any of those three characters, so whichever appears first is unambiguous. Backs <code>parse</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#millis_from_fraction/1\" class=\"fn-sig\"><code>helper millis_from_fraction(digits)</code></a>\n    <span class=\"fn-summary\">(private) A fractional-second string of any length as milliseconds: <code>&quot;5&quot;</code> is <code>500</code>, <code>&quot;123456&quot;</code> truncates to <code>123</code>. Backs <code>parse</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#pad/2\" class=\"fn-sig\"><code>helper pad(n, width)</code></a>\n    <span class=\"fn-summary\">(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"new/7\">\n  <h3 class=\"fn-title\"><code>function new(year, month, day, hour, minute, second, offset_minutes)</code> <a class=\"anchor\" href=\"#new/7\">¶</a></h3>\n<p>Builds a <code><a href=\"#/DateTime\">DateTime</a></code> from local fields at <code>offset_minutes</code> from UTC, with <code>millisecond: 0</code>. Every field past <code>year</code> normalizes when it overflows or underflows its natural range, the same way <code><a href=\"#/Date:new\">Date.new</a></code>&#39;s <code>day</code> does.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">9</span>, <span class=\"tok-lit\">13</span>, <span class=\"tok-lit\">45</span>, <span class=\"tok-lit\">30</span>, <span class=\"tok-lit\">0</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 45, second: 30, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>year</code></dt>\n  <dd>the calendar year, any integer</dd>\n  <dt><code>month</code></dt>\n  <dd>the month, <code>1</code>..<code>12</code></dd>\n  <dt><code>day</code></dt>\n  <dd>the day of month; out-of-range values roll over</dd>\n  <dt><code>hour</code></dt>\n  <dd>the hour, <code>0</code>..<code>23</code>; out-of-range values roll over</dd>\n  <dt><code>minute</code></dt>\n  <dd>the minute, <code>0</code>..<code>59</code>; out-of-range values roll over</dd>\n  <dt><code>second</code></dt>\n  <dd>the second, <code>0</code>..<code>59</code>; out-of-range values roll over</dd>\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC, in minutes (e.g. <code>-300</code> for UTC-5)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"now/0\">\n  <h3 class=\"fn-title\"><code>function now()</code> <a class=\"anchor\" href=\"#now/0\">¶</a></h3>\n<p>The current instant, as UTC (<code>offset_minutes: 0</code>).</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.now().offset_minutes   <span class=\"tok-com\"># == 0</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the current <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"now_at/1\">\n  <h3 class=\"fn-title\"><code>function now_at(offset_minutes)</code> <a class=\"anchor\" href=\"#now_at/1\">¶</a></h3>\n<p>The current instant, as local fields at <code>offset_minutes</code> from UTC.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.now_at(-<span class=\"tok-lit\">300</span>).offset_minutes   <span class=\"tok-com\"># == -300</span></code></pre>\n<dl class=\"params\">\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC, in minutes</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the current <code><a href=\"#/DateTime\">DateTime</a></code>, expressed at that offset</p>\n</div>\n<div class=\"fn-detail\" id=\"now_in/1\">\n  <h3 class=\"fn-title\"><code>function now_in(time_zone)</code> <a class=\"anchor\" href=\"#now_in/1\">¶</a></h3>\n<p>The current instant, as local fields in <code>time_zone</code>.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.now_in(<span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;IST&quot;</span>, <span class=\"tok-lit\">330</span>)).offset_minutes   <span class=\"tok-com\"># == 330</span></code></pre>\n<dl class=\"params\">\n  <dt><code>time_zone</code></dt>\n  <dd>the <code><a href=\"#/TimeZone\">TimeZone</a></code> to express the current instant in</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the current <code><a href=\"#/DateTime\">DateTime</a></code>, at <code>time_zone</code>&#39;s offset</p>\n</div>\n<div class=\"fn-detail\" id=\"from_epoch_millis/2\">\n  <h3 class=\"fn-title\"><code>function from_epoch_millis(utc_ms, offset_minutes)</code> <a class=\"anchor\" href=\"#from_epoch_millis/2\">¶</a></h3>\n<p>The <code><a href=\"#/DateTime\">DateTime</a></code> for the instant <code>utc_ms</code> milliseconds after the epoch, with its fields expressed at <code>offset_minutes</code> from UTC.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.from_epoch_millis(<span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>)\n<span class=\"tok-com\"># == DateTime{year: 1970, month: 1, day: 1, hour: 0, minute: 0, second: 0, millisecond: 0, offset_minutes: 0}</span>\n<span class=\"tok-type\">DateTime</span>.from_epoch_millis(<span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">60</span>)\n<span class=\"tok-com\"># == DateTime{year: 1970, month: 1, day: 1, hour: 1, minute: 0, second: 0, millisecond: 0, offset_minutes: 60}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>utc_ms</code></dt>\n  <dd>the instant, as milliseconds since the epoch (UTC)</dd>\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC to render the fields at</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_epoch_millis_raw/2\">\n  <h3 class=\"fn-title\"><code>helper from_epoch_millis_raw(utc_ms, offset_minutes)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#from_epoch_millis_raw/2\">¶</a></h3>\n<p>(private) <code>from_epoch_millis</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</p>\n</div>\n<div class=\"fn-detail\" id=\"from_naive/2\">\n  <h3 class=\"fn-title\"><code>function from_naive(naive, offset_minutes)</code> <a class=\"anchor\" href=\"#from_naive/2\">¶</a></h3>\n<p>Tags a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code>&#39;s fields as the local time at <code>offset_minutes</code> — a relabeling, not a conversion: the fields are copied as they are.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.from_naive(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">13</span>}, -<span class=\"tok-lit\">300</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 0, second: 0, millisecond: 0, offset_minutes: -300}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>naive</code></dt>\n  <dd>the wall-clock reading to tag</dd>\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC those fields are read at</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/DateTime\">DateTime</a></code> with <code>naive</code>&#39;s fields and the given offset</p>\n</div>\n<div class=\"fn-detail\" id=\"parse/1\">\n  <h3 class=\"fn-title\"><code>function parse(text)</code> <a class=\"anchor\" href=\"#parse/1\">¶</a></h3>\n<p>Parses <code>&quot;YYYY-MM-DDTHH:MM:SS±HH:MM&quot;</code> (or a trailing <code>&quot;Z&quot;</code> for UTC) — either a <code>T</code> or a plain space between the date and the time, as <code>to_iso</code> renders or as the <code>U&quot;...&quot;</code> sigil is written. The seconds&#39; fractional part is optional and any number of digits (padded or truncated to milliseconds), the same as <code><a href=\"#/NaiveDateTime:parse\">NaiveDateTime.parse</a></code>.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.parse(<span class=\"tok-str\">&quot;2024-02-09T13:45:30.500Z&quot;</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 45, second: 30, millisecond: 500, offset_minutes: 0}</span>\n<span class=\"tok-type\">DateTime</span>.parse(<span class=\"tok-str\">&quot;2024-02-09 13:45:30.123456-05:00&quot;</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 45, second: 30, millisecond: 123, offset_minutes: -300}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>text</code></dt>\n  <dd>a date-time in <code>&quot;YYYY-MM-DD[T ]HH:MM:SS[.fraction](Z|±HH:MM)&quot;</code> form</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"date/1\">\n  <h3 class=\"fn-title\"><code>function date(self)</code> <a class=\"anchor\" href=\"#date/1\">¶</a></h3>\n<p>The <code><a href=\"#/Date\">Date</a></code> half of <code>self</code>&#39;s local reading, dropping the time of day and the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.date(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">13</span>})   <span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 9}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Date\">Date</a></code> self&#39;s local fields fall on</p>\n</div>\n<div class=\"fn-detail\" id=\"to_naive/1\">\n  <h3 class=\"fn-title\"><code>function to_naive(self)</code> <a class=\"anchor\" href=\"#to_naive/1\">¶</a></h3>\n<p><code>self</code>&#39;s local fields as a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code>, dropping the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.to_naive(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">13</span>, offset_minutes: -<span class=\"tok-lit\">300</span>})\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> with the same local fields</p>\n</div>\n<div class=\"fn-detail\" id=\"to_utc_epoch_millis/1\">\n  <h3 class=\"fn-title\"><code>function to_utc_epoch_millis(self)</code> <a class=\"anchor\" href=\"#to_utc_epoch_millis/1\">¶</a></h3>\n<p>The instant <code>self</code> names, as milliseconds since the epoch (UTC) — the bridge to arithmetic and comparison, and what makes two <code><a href=\"#/DateTime\">DateTime</a></code>s at different offsets comparable.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.to_utc_epoch_millis(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">1970</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>, hour: <span class=\"tok-lit\">1</span>, offset_minutes: <span class=\"tok-lit\">60</span>})   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> millisecond count, negative before the epoch</p>\n</div>\n<div class=\"fn-detail\" id=\"to_utc/1\">\n  <h3 class=\"fn-title\"><code>function to_utc(self)</code> <a class=\"anchor\" href=\"#to_utc/1\">¶</a></h3>\n<p>The same instant as <code>self</code>, with its fields expressed at UTC (<code>offset_minutes: 0</code>).</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.to_utc(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">8</span>, offset_minutes: -<span class=\"tok-lit\">300</span>})\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 0, second: 0, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an equivalent <code><a href=\"#/DateTime\">DateTime</a></code> at <code>offset_minutes: 0</code></p>\n</div>\n<div class=\"fn-detail\" id=\"with_offset/2\">\n  <h3 class=\"fn-title\"><code>function with_offset(self, offset_minutes)</code> <a class=\"anchor\" href=\"#with_offset/2\">¶</a></h3>\n<p>The same instant as <code>self</code>, with its fields re-expressed at <code>offset_minutes</code> — the way to convert between offsets.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.with_offset(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">13</span>, offset_minutes: <span class=\"tok-lit\">0</span>}, -<span class=\"tok-lit\">300</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 8, minute: 0, second: 0, millisecond: 0, offset_minutes: -300}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to convert</dd>\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC to render the fields at</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an equivalent <code><a href=\"#/DateTime\">DateTime</a></code> at the new offset</p>\n</div>\n<div class=\"fn-detail\" id=\"in_time_zone/2\">\n  <h3 class=\"fn-title\"><code>function in_time_zone(self, time_zone)</code> <a class=\"anchor\" href=\"#in_time_zone/2\">¶</a></h3>\n<p>The same instant as <code>self</code>, with its fields re-expressed at <code>time_zone</code>&#39;s offset — <code>with_offset</code>, taking a <code><a href=\"#/TimeZone\">TimeZone</a></code> instead of a bare number.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.in_time_zone(<span class=\"tok-type\">DateTime</span>{offset_minutes: <span class=\"tok-lit\">0</span>}, <span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;IST&quot;</span>, <span class=\"tok-lit\">330</span>)).offset_minutes   <span class=\"tok-com\"># == 330</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to convert</dd>\n  <dt><code>time_zone</code></dt>\n  <dd>the <code><a href=\"#/TimeZone\">TimeZone</a></code> to render the fields in</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an equivalent <code><a href=\"#/DateTime\">DateTime</a></code> at that zone&#39;s offset</p>\n</div>\n<div class=\"fn-detail\" id=\"time_zone/1\">\n  <h3 class=\"fn-title\"><code>function time_zone(self)</code> <a class=\"anchor\" href=\"#time_zone/1\">¶</a></h3>\n<p><code>self</code>&#39;s offset as a <code><a href=\"#/TimeZone\">TimeZone</a></code>, named after the offset itself (<code>&quot;UTC&quot;</code> at <code>0</code>, otherwise <code>±HH:MM</code>) — <code>self</code> only ever keeps the number, never the name a zone arrived with.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.time_zone(<span class=\"tok-type\">DateTime</span>{offset_minutes: -<span class=\"tok-lit\">300</span>}).name   <span class=\"tok-com\"># == &quot;-05:00&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/TimeZone\">TimeZone</a></code> matching <code>self</code>&#39;s offset</p>\n</div>\n<div class=\"fn-detail\" id=\"to_iso/1\">\n  <h3 class=\"fn-title\"><code>function to_iso(self)</code> <a class=\"anchor\" href=\"#to_iso/1\">¶</a></h3>\n<p><code>self</code> rendered as <code>&quot;YYYY-MM-DDTHH:MM:SS.mmm&quot;</code> followed by <code>Z</code> (when <code>offset_minutes</code> is <code>0</code>) or a signed <code>±HH:MM</code> offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.to_iso(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">3</span>, minute: <span class=\"tok-lit\">5</span>, second: <span class=\"tok-lit\">9</span>, millisecond: <span class=\"tok-lit\">7</span>, offset_minutes: <span class=\"tok-lit\">0</span>})\n<span class=\"tok-com\"># == &quot;2024-02-09T03:05:09.007Z&quot;</span>\n<span class=\"tok-type\">DateTime</span>.to_iso(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">3</span>, minute: <span class=\"tok-lit\">5</span>, second: <span class=\"tok-lit\">9</span>, millisecond: <span class=\"tok-lit\">7</span>, offset_minutes: -<span class=\"tok-lit\">300</span>})\n<span class=\"tok-com\"># == &quot;2024-02-09T03:05:09.007-05:00&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to render</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the ISO-8601 string</p>\n</div>\n<div class=\"fn-detail\" id=\"add_days/2\">\n  <h3 class=\"fn-title\"><code>function add_days(self, n)</code> <a class=\"anchor\" href=\"#add_days/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> days, keeping the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.add_days(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">28</span>, hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 29, hour: 12, minute: 0, second: 0, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many days to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_weeks/2\">\n  <h3 class=\"fn-title\"><code>function add_weeks(self, n)</code> <a class=\"anchor\" href=\"#add_weeks/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> weeks (<code>n * 7</code> days), keeping the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.add_weeks(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>, hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 1, day: 8, hour: 12, minute: 0, second: 0, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many weeks to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_months/2\">\n  <h3 class=\"fn-title\"><code>function add_months(self, n)</code> <a class=\"anchor\" href=\"#add_months/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> months, keeping the time of day and the offset — <code><a href=\"#/Date:add_months\">Date.add_months</a></code> applied to the local date half, with the day-overflow normalization that implies.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.add_months(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">15</span>, hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 15, hour: 12, minute: 0, second: 0, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many months to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_hours/2\">\n  <h3 class=\"fn-title\"><code>function add_hours(self, n)</code> <a class=\"anchor\" href=\"#add_hours/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> hours, keeping the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.add_hours(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">23</span>}, <span class=\"tok-lit\">2</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 10, hour: 1, minute: 0, second: 0, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many hours to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_minutes/2\">\n  <h3 class=\"fn-title\"><code>function add_minutes(self, n)</code> <a class=\"anchor\" href=\"#add_minutes/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> minutes, keeping the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.add_minutes(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">0</span>, minute: <span class=\"tok-lit\">30</span>}, <span class=\"tok-lit\">45</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, hour: 1, minute: 15, second: 0, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many minutes to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_seconds/2\">\n  <h3 class=\"fn-title\"><code>function add_seconds(self, n)</code> <a class=\"anchor\" href=\"#add_seconds/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> seconds, keeping the offset.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.add_seconds(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, second: <span class=\"tok-lit\">45</span>}, <span class=\"tok-lit\">30</span>)\n<span class=\"tok-com\"># == DateTime{year: 2024, month: 2, day: 9, minute: 1, second: 15, millisecond: 0, offset_minutes: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many seconds to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/DateTime\">DateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(self, other)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two date-times by the instant they name (their UTC equivalent), as <code>-1</code>, <code>0</code> or <code>1</code> — not by their printed local fields, so two <code><a href=\"#/DateTime\">DateTime</a></code>s at different offsets compare correctly.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.compare(<span class=\"tok-type\">DateTime</span>{hour: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">DateTime</span>{hour: <span class=\"tok-lit\">2</span>})                        <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">DateTime</span>.compare(<span class=\"tok-type\">DateTime</span>{hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-type\">DateTime</span>{hour: <span class=\"tok-lit\">7</span>, offset_minutes: -<span class=\"tok-lit\">300</span>})  <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the first date-time</dd>\n  <dt><code>other</code></dt>\n  <dd>the second date-time</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>self</code> is earlier, <code>1</code> when later, <code>0</code> when equal</p>\n</div>\n<div class=\"fn-detail\" id=\"diff_millis/2\">\n  <h3 class=\"fn-title\"><code>function diff_millis(self, other)</code> <a class=\"anchor\" href=\"#diff_millis/2\">¶</a></h3>\n<p>The number of milliseconds from the instant <code>other</code> names to the one <code>self</code> names — positive when <code>self</code> is later, negative when earlier.</p>\n<pre><code><span class=\"tok-type\">DateTime</span>.diff_millis(<span class=\"tok-type\">DateTime</span>{second: <span class=\"tok-lit\">10</span>}, <span class=\"tok-type\">DateTime</span>{second: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 9000</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the later (or earlier) date-time</dd>\n  <dt><code>other</code></dt>\n  <dd>the date-time to measure from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> millisecond count</p>\n</div>\n<div class=\"fn-detail\" id=\"day_of_week/1\">\n  <h3 class=\"fn-title\"><code>function day_of_week(self)</code> <a class=\"anchor\" href=\"#day_of_week/1\">¶</a></h3>\n<p>The day of the week <code>self</code>&#39;s local date falls on. Delegates to <code><a href=\"#/Date:day_of_week\">Date.day_of_week</a></code>.</p>\n<pre><code>`:thursday`, `:friday`, `:saturday`</code></pre>\n<pre><code><span class=\"tok-type\">DateTime</span>.day_of_week(<span class=\"tok-type\">DateTime</span>{year: <span class=\"tok-lit\">1970</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == :thursday</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> one of <code>:sunday</code>, <code>:monday</code>, <code>:tuesday</code>, <code>:wednesday</code>,</p>\n</div>\n<div class=\"fn-detail\" id=\"raw_millis/7\">\n  <h3 class=\"fn-title\"><code>helper raw_millis(year, month, day, hour, minute, second, millisecond)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#raw_millis/7\">¶</a></h3>\n<p>(private) Milliseconds since the epoch for the given fields, as if they were UTC — before the offset is applied. Shared arithmetic behind <code>new</code>, <code>parse</code> and <code>to_utc_epoch_millis</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"shift/2\">\n  <h3 class=\"fn-title\"><code>helper shift(self, millis)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#shift/2\">¶</a></h3>\n<p>(private) <code>self</code> moved by <code>millis</code> milliseconds of real time, keeping its offset. Backs the <code>add_*</code> family.</p>\n</div>\n<div class=\"fn-detail\" id=\"floor_div/2\">\n  <h3 class=\"fn-title\"><code>helper floor_div(a, b)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#floor_div/2\">¶</a></h3>\n<p>(private) <code>a</code> divided by <code>b</code>, rounding toward negative infinity — unlike <code>/</code>, which truncates toward zero. <code>b</code> is assumed positive.</p>\n</div>\n<div class=\"fn-detail\" id=\"offset_start/1\">\n  <h3 class=\"fn-title\"><code>helper offset_start(rest)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#offset_start/1\">¶</a></h3>\n<p>(private) The index in <code>rest</code> (the text after the date) where its offset begins — the trailing <code>&quot;Z&quot;</code>, or the <code>+</code>/<code>-</code> of a <code>±HH:MM</code>. The clock text before it never contains any of those three characters, so whichever appears first is unambiguous. Backs <code>parse</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"millis_from_fraction/1\">\n  <h3 class=\"fn-title\"><code>helper millis_from_fraction(digits)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#millis_from_fraction/1\">¶</a></h3>\n<p>(private) A fractional-second string of any length as milliseconds: <code>&quot;5&quot;</code> is <code>500</code>, <code>&quot;123456&quot;</code> truncates to <code>123</code>. Backs <code>parse</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"pad/2\">\n  <h3 class=\"fn-title\"><code>helper pad(n, width)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#pad/2\">¶</a></h3>\n<p>(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</p>\n</div>\n</section>\n"},"Dir":{"title":"Dir — Ramos","heading":"Dir","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Dir — inspecting and manipulating directories on the host filesystem.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Dir — inspecting and manipulating directories on the host filesystem.</p>\n<p>Like <code><a href=\"#/File\">File</a></code>, every function here is <em>effectful</em> and reports outcome as a value you pattern match rather than by raising:</p>\n<ul><li>fallible operations return <code>(:ok, value)</code> or <code>(:error, reason)</code></li><li>operations with nothing to hand back return the bare <code>:ok</code></li><li><code>exists</code> and <code>is_dir</code> return a plain <code>true</code>/<code>false</code></li></ul>\n<p><code>reason</code> is a lowercase symbol — <code>:enoent</code> (no such directory), <code>:eacces</code> (permission denied), <code>:eexist</code>, or <code>:io_error</code>:</p>\n<pre><code><span class=\"tok-kw\">case</span> <span class=\"tok-type\">Dir</span>.ls(<span class=\"tok-str\">&quot;src&quot;</span>)\n  (<span class=\"tok-lit\">:ok</span>, entries) -&gt; <span class=\"tok-type\">List</span>.each(entries, <span class=\"tok-kw\">do</span> e -&gt; print(e))\n  (<span class=\"tok-lit\">:error</span>, <span class=\"tok-lit\">:enoent</span>) -&gt; print(<span class=\"tok-str\">&quot;no such directory&quot;</span>)\n  (<span class=\"tok-lit\">:error</span>, reason) -&gt; (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:dir_failed</span>, reason))</code></pre>\n<p>The current working directory is process-wide state: <code>cd</code> changes it for every later relative path (in <code><a href=\"#/Dir\">Dir</a></code> and <code><a href=\"#/File\">File</a></code> alike), and <code>cwd</code> reads it.</p>\n<p>Conventions:</p>\n<ul><li>paths are host-native strings; relative paths resolve against the cwd.</li><li><code>ls</code> returns bare entry names (not full paths), sorted.</li><li><code>mkdir</code> creates parent directories as needed and is happy if the directory already exists.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#cwd/0\" class=\"fn-sig\"><code>function cwd()</code></a>\n    <span class=\"fn-summary\">Returns the process&#39;s current working directory.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#cd/1\" class=\"fn-sig\"><code>function cd(path)</code></a>\n    <span class=\"fn-summary\">Changes the process&#39;s current working directory to <code>path</code>. Affects every relative path used afterwards, in both <code><a href=\"#/Dir\">Dir</a></code> and <code><a href=\"#/File\">File</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#mkdir/1\" class=\"fn-sig\"><code>function mkdir(path)</code></a>\n    <span class=\"fn-summary\">Creates the directory at <code>path</code>, including any missing parent directories (like <code>mkdir -p</code>). Succeeds if the directory already exists.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#ls/1\" class=\"fn-sig\"><code>function ls(path)</code></a>\n    <span class=\"fn-summary\">Lists the entries of the directory at <code>path</code> — the bare names of the files and subdirectories it contains (not full paths), sorted.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#exists/1\" class=\"fn-sig\"><code>function exists(path)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if something exists at <code>path</code> (a file or a directory), else <code>false</code>. Use <code>is_dir</code> to require that it is a directory.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_dir/1\" class=\"fn-sig\"><code>function is_dir(path)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>path</code> exists and is a directory, else <code>false</code> (including when <code>path</code> is a regular file or does not exist).</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"cwd/0\">\n  <h3 class=\"fn-title\"><code>function cwd()</code> <a class=\"anchor\" href=\"#cwd/0\">¶</a></h3>\n<p>Returns the process&#39;s current working directory.</p>\n<pre><code><span class=\"tok-type\">Dir</span>.cwd()   <span class=\"tok-com\"># (:ok, &quot;/home/andrew/dev/ramos&quot;) — whatever it happens to be</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> <code>(:ok, path)</code>, or <code>(:error, reason)</code> if it cannot be determined</p>\n</div>\n<div class=\"fn-detail\" id=\"cd/1\">\n  <h3 class=\"fn-title\"><code>function cd(path)</code> <a class=\"anchor\" href=\"#cd/1\">¶</a></h3>\n<p>Changes the process&#39;s current working directory to <code>path</code>. Affects every relative path used afterwards, in both <code><a href=\"#/Dir\">Dir</a></code> and <code><a href=\"#/File\">File</a></code>.</p>\n<pre><code><span class=\"tok-type\">Dir</span>.mkdir(<span class=\"tok-str\">&quot;src&quot;</span>)\n<span class=\"tok-type\">Dir</span>.cd(<span class=\"tok-str\">&quot;src&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the directory to change into</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> (e.g. <code>:enoent</code>) on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"mkdir/1\">\n  <h3 class=\"fn-title\"><code>function mkdir(path)</code> <a class=\"anchor\" href=\"#mkdir/1\">¶</a></h3>\n<p>Creates the directory at <code>path</code>, including any missing parent directories (like <code>mkdir -p</code>). Succeeds if the directory already exists.</p>\n<pre><code><span class=\"tok-type\">Dir</span>.mkdir(<span class=\"tok-str\">&quot;build/cache&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the directory to create</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"ls/1\">\n  <h3 class=\"fn-title\"><code>function ls(path)</code> <a class=\"anchor\" href=\"#ls/1\">¶</a></h3>\n<p>Lists the entries of the directory at <code>path</code> — the bare names of the files and subdirectories it contains (not full paths), sorted.</p>\n<pre><code><span class=\"tok-type\">Dir</span>.mkdir(<span class=\"tok-str\">&quot;stdlib&quot;</span>)\n<span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;stdlib/dir.rmo&quot;</span>, <span class=\"tok-str\">&quot;&quot;</span>)\n<span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;stdlib/file.rmo&quot;</span>, <span class=\"tok-str\">&quot;&quot;</span>)\n<span class=\"tok-type\">Dir</span>.ls(<span class=\"tok-str\">&quot;stdlib&quot;</span>)   <span class=\"tok-com\"># == (:ok, [&quot;dir.rmo&quot;, &quot;file.rmo&quot;])</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the directory to list</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>(:ok, names)</code> (a sorted list of strings), or <code>(:error, reason)</code></p>\n</div>\n<div class=\"fn-detail\" id=\"exists/1\">\n  <h3 class=\"fn-title\"><code>function exists(path)</code> <a class=\"anchor\" href=\"#exists/1\">¶</a></h3>\n<p>Returns <code>true</code> if something exists at <code>path</code> (a file or a directory), else <code>false</code>. Use <code>is_dir</code> to require that it is a directory.</p>\n<pre><code><span class=\"tok-type\">Dir</span>.mkdir(<span class=\"tok-str\">&quot;stdlib&quot;</span>)\n<span class=\"tok-type\">Dir</span>.exists(<span class=\"tok-str\">&quot;stdlib&quot;</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Dir</span>.exists(<span class=\"tok-str\">&quot;missing&quot;</span>)  <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>path</code> exists, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_dir/1\">\n  <h3 class=\"fn-title\"><code>function is_dir(path)</code> <a class=\"anchor\" href=\"#is_dir/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>path</code> exists and is a directory, else <code>false</code> (including when <code>path</code> is a regular file or does not exist).</p>\n<pre><code><span class=\"tok-type\">Dir</span>.mkdir(<span class=\"tok-str\">&quot;stdlib&quot;</span>)\n<span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;stdlib/dir.rmo&quot;</span>, <span class=\"tok-str\">&quot;&quot;</span>)\n<span class=\"tok-type\">Dir</span>.is_dir(<span class=\"tok-str\">&quot;stdlib&quot;</span>)           <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Dir</span>.is_dir(<span class=\"tok-str\">&quot;stdlib/dir.rmo&quot;</span>)  <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>path</code> is a directory, else <code>false</code></p>\n</div>\n</section>\n"},"File":{"title":"File — Ramos","heading":"File","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>File — reading, writing, and managing files on the host filesystem.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>File — reading, writing, and managing files on the host filesystem.</p>\n<p>This is Ramos&#39;s window onto mutable state: files change over time. Every function here is therefore <em>effectful</em> and reports success or failure as a value you pattern match, never by raising:</p>\n<ul><li>fallible operations return <code>(:ok, value)</code> or <code>(:error, reason)</code></li><li>operations with nothing to hand back return the bare <code>:ok</code></li><li><code>exists</code> and <code>is_file</code> return a plain <code>true</code>/<code>false</code></li></ul>\n<p><code>reason</code> is a lowercase symbol — <code>:enoent</code> (no such file), <code>:eacces</code> (permission denied), <code>:eexist</code>, <code>:io_error</code>, or <code>:unsupported</code> — so branches read cleanly:</p>\n<pre><code><span class=\"tok-kw\">case</span> <span class=\"tok-type\">File</span>.read(<span class=\"tok-str\">&quot;config.txt&quot;</span>)\n  (<span class=\"tok-lit\">:ok</span>, text) -&gt; parse(text)\n  (<span class=\"tok-lit\">:error</span>, <span class=\"tok-lit\">:enoent</span>) -&gt; <span class=\"tok-lit\">:use_defaults</span>\n  (<span class=\"tok-lit\">:error</span>, reason) -&gt; (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:config_unreadable</span>, reason))</code></pre>\n<p>Every function takes a path; there are no open file handles. <code>read</code> and <code>write</code> move the whole file at once, which keeps the model simple and the values immutable.</p>\n<p>Conventions:</p>\n<ul><li>paths are host-native strings; relative paths resolve against the cwd.</li><li><code>write</code> truncates; <code>append</code> adds to the end; both create the file if it is missing.</li><li><code>mv</code> is the one way to move a file: it tries a rename first and falls back to copy + delete, so it works across filesystems.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#read/1\" class=\"fn-sig\"><code>function read(path)</code></a>\n    <span class=\"fn-summary\">Reads the entire file at <code>path</code> into a string.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#write/2\" class=\"fn-sig\"><code>function write(path, contents)</code></a>\n    <span class=\"fn-summary\">Writes <code>contents</code> to <code>path</code>, replacing any existing content and creating the file if it does not exist.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#append/2\" class=\"fn-sig\"><code>function append(path, contents)</code></a>\n    <span class=\"fn-summary\">Appends <code>contents</code> to the end of <code>path</code>, creating the file if it does not exist. Unlike <code>write</code>, existing content is preserved.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#size/1\" class=\"fn-sig\"><code>function size(path)</code></a>\n    <span class=\"fn-summary\">Returns the size of <code>path</code> in bytes.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#rm/1\" class=\"fn-sig\"><code>function rm(path)</code></a>\n    <span class=\"fn-summary\">Deletes the file at <code>path</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#cp/2\" class=\"fn-sig\"><code>function cp(source, dest)</code></a>\n    <span class=\"fn-summary\">Copies the file at <code>source</code> to <code>dest</code>, overwriting <code>dest</code> if it exists.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#mv/2\" class=\"fn-sig\"><code>function mv(source, dest)</code></a>\n    <span class=\"fn-summary\">Moves the file at <code>source</code> to <code>dest</code>. Works across filesystems: it tries a rename first, then falls back to a copy followed by deleting <code>source</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#exists/1\" class=\"fn-sig\"><code>function exists(path)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if something exists at <code>path</code> (a file or a directory), else <code>false</code>. Use <code>is_file</code> to require that it is a regular file.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_file/1\" class=\"fn-sig\"><code>function is_file(path)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>path</code> exists and is a regular file, else <code>false</code> (including when <code>path</code> is a directory or does not exist).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#touch/1\" class=\"fn-sig\"><code>function touch(path)</code></a>\n    <span class=\"fn-summary\">Creates <code>path</code> if it does not exist, and updates its modified time to now. Existing content is preserved — the file-manager equivalent of <code>touch(1)</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#chmod/2\" class=\"fn-sig\"><code>function chmod(path, mode)</code></a>\n    <span class=\"fn-summary\">Sets the permission bits of <code>path</code>. <code>mode</code> is the numeric permission — for example <code>493</code> is <code>rwxr-xr-x</code> and <code>420</code> is <code>rw-r--r--</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"read/1\">\n  <h3 class=\"fn-title\"><code>function read(path)</code> <a class=\"anchor\" href=\"#read/1\">¶</a></h3>\n<p>Reads the entire file at <code>path</code> into a string.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;greeting.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.read(<span class=\"tok-str\">&quot;greeting.txt&quot;</span>)   <span class=\"tok-com\"># == (:ok, &quot;hello\\n&quot;)</span>\n<span class=\"tok-type\">File</span>.read(<span class=\"tok-str\">&quot;missing.txt&quot;</span>)    <span class=\"tok-com\"># == (:error, :enoent)</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>(:ok, contents)</code>, or <code>(:error, reason)</code> if it cannot be read</p>\n</div>\n<div class=\"fn-detail\" id=\"write/2\">\n  <h3 class=\"fn-title\"><code>function write(path, contents)</code> <a class=\"anchor\" href=\"#write/2\">¶</a></h3>\n<p>Writes <code>contents</code> to <code>path</code>, replacing any existing content and creating the file if it does not exist.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;out.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to write</dd>\n  <dt><code>contents</code></dt>\n  <dd>the string to write</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"append/2\">\n  <h3 class=\"fn-title\"><code>function append(path, contents)</code> <a class=\"anchor\" href=\"#append/2\">¶</a></h3>\n<p>Appends <code>contents</code> to the end of <code>path</code>, creating the file if it does not exist. Unlike <code>write</code>, existing content is preserved.</p>\n<pre><code><span class=\"tok-type\">File</span>.append(<span class=\"tok-str\">&quot;log.txt&quot;</span>, <span class=\"tok-str\">&quot;another line\\n&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to append to</dd>\n  <dt><code>contents</code></dt>\n  <dd>the string to append</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"size/1\">\n  <h3 class=\"fn-title\"><code>function size(path)</code> <a class=\"anchor\" href=\"#size/1\">¶</a></h3>\n<p>Returns the size of <code>path</code> in bytes.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;greeting.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.size(<span class=\"tok-str\">&quot;greeting.txt&quot;</span>)   <span class=\"tok-com\"># == (:ok, 6)</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to measure</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>(:ok, bytes)</code>, or <code>(:error, reason)</code> if it cannot be read</p>\n</div>\n<div class=\"fn-detail\" id=\"rm/1\">\n  <h3 class=\"fn-title\"><code>function rm(path)</code> <a class=\"anchor\" href=\"#rm/1\">¶</a></h3>\n<p>Deletes the file at <code>path</code>.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;out.txt&quot;</span>, <span class=\"tok-str\">&quot;bye\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.rm(<span class=\"tok-str\">&quot;out.txt&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to delete</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> (e.g. <code>:enoent</code> if it is not there)</p>\n</div>\n<div class=\"fn-detail\" id=\"cp/2\">\n  <h3 class=\"fn-title\"><code>function cp(source, dest)</code> <a class=\"anchor\" href=\"#cp/2\">¶</a></h3>\n<p>Copies the file at <code>source</code> to <code>dest</code>, overwriting <code>dest</code> if it exists.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;a.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.cp(<span class=\"tok-str\">&quot;a.txt&quot;</span>, <span class=\"tok-str\">&quot;b.txt&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>source</code></dt>\n  <dd>the file to copy from</dd>\n  <dt><code>dest</code></dt>\n  <dd>the path to copy to</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"mv/2\">\n  <h3 class=\"fn-title\"><code>function mv(source, dest)</code> <a class=\"anchor\" href=\"#mv/2\">¶</a></h3>\n<p>Moves the file at <code>source</code> to <code>dest</code>. Works across filesystems: it tries a rename first, then falls back to a copy followed by deleting <code>source</code>.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;draft.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.mv(<span class=\"tok-str\">&quot;draft.txt&quot;</span>, <span class=\"tok-str\">&quot;final.txt&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>source</code></dt>\n  <dd>the file to move from</dd>\n  <dt><code>dest</code></dt>\n  <dd>the path to move to</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"exists/1\">\n  <h3 class=\"fn-title\"><code>function exists(path)</code> <a class=\"anchor\" href=\"#exists/1\">¶</a></h3>\n<p>Returns <code>true</code> if something exists at <code>path</code> (a file or a directory), else <code>false</code>. Use <code>is_file</code> to require that it is a regular file.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;out.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.exists(<span class=\"tok-str\">&quot;out.txt&quot;</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">File</span>.exists(<span class=\"tok-str\">&quot;missing&quot;</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>path</code> exists, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_file/1\">\n  <h3 class=\"fn-title\"><code>function is_file(path)</code> <a class=\"anchor\" href=\"#is_file/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>path</code> exists and is a regular file, else <code>false</code> (including when <code>path</code> is a directory or does not exist).</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;out.txt&quot;</span>, <span class=\"tok-str\">&quot;hello\\n&quot;</span>)\n<span class=\"tok-type\">Dir</span>.mkdir(<span class=\"tok-str\">&quot;src&quot;</span>)\n<span class=\"tok-type\">File</span>.is_file(<span class=\"tok-str\">&quot;out.txt&quot;</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">File</span>.is_file(<span class=\"tok-str\">&quot;src&quot;</span>)       <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>path</code> is a regular file, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"touch/1\">\n  <h3 class=\"fn-title\"><code>function touch(path)</code> <a class=\"anchor\" href=\"#touch/1\">¶</a></h3>\n<p>Creates <code>path</code> if it does not exist, and updates its modified time to now. Existing content is preserved — the file-manager equivalent of <code>touch(1)</code>.</p>\n<pre><code><span class=\"tok-type\">File</span>.touch(<span class=\"tok-str\">&quot;build.stamp&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to touch</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, or <code>(:error, reason)</code> on failure</p>\n</div>\n<div class=\"fn-detail\" id=\"chmod/2\">\n  <h3 class=\"fn-title\"><code>function chmod(path, mode)</code> <a class=\"anchor\" href=\"#chmod/2\">¶</a></h3>\n<p>Sets the permission bits of <code>path</code>. <code>mode</code> is the numeric permission — for example <code>493</code> is <code>rwxr-xr-x</code> and <code>420</code> is <code>rw-r--r--</code>.</p>\n<pre><code><span class=\"tok-type\">File</span>.write(<span class=\"tok-str\">&quot;run.sh&quot;</span>, <span class=\"tok-str\">&quot;echo hi\\n&quot;</span>)\n<span class=\"tok-type\">File</span>.chmod(<span class=\"tok-str\">&quot;run.sh&quot;</span>, <span class=\"tok-lit\">493</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>path</code></dt>\n  <dd>the path to change</dd>\n  <dt><code>mode</code></dt>\n  <dd>the numeric permission bits</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, <code>(:error, reason)</code>, or <code>(:error, :unsupported)</code> off Unix</p>\n</div>\n</section>\n"},"Float":{"title":"Float — Ramos","heading":"Float","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Float — operations on floating-point numbers.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Float — operations on floating-point numbers.</p>\n<p><code>3.14</code> is a <code><a href=\"#/Float\">Float</a></code>. Arithmetic lives in the operators (<code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>**</code>); this module holds the rounding, the constants, and the tests that read better as named functions.</p>\n<pre><code><span class=\"tok-type\">Float</span>.round(<span class=\"tok-lit\">2.6</span>)   <span class=\"tok-com\"># == 3</span>\n<span class=\"tok-type\">Float</span>.pi()         <span class=\"tok-com\"># == 3.141592653589793</span></code></pre>\n<p>An <code><a href=\"#/Integer\">Integer</a></code> and a <code><a href=\"#/Float\">Float</a></code> never compare equal — <code>1 == 1.0</code> is <code>false</code> — so convert explicitly when you mean to compare across the two.</p>\n<p>Rounding is built on Kernel&#39;s <code>to_integer</code> (which truncates toward zero), and the special values fall out of the arithmetic itself — no native needed for either. <code>exp</code>/<code>log</code>/<code>log_two</code>/<code>log_ten</code>/<code>sin</code>/<code>cos</code>/<code>tan</code> are the exception: transcendental functions have no operator to build on, so those seven are the only natives behind this module. (No digit may appear in a Ramos identifier, which is why the base-2 and base-10 logs are spelled out rather than <code>log2</code>/<code>log10</code>.)</p>\n<p>Kernel already converts and truncates, so this module does not repeat it: use <code>to_integer(x)</code> to drop the fraction, <code>to_float(text)</code> to parse, and <code>to_string(x)</code> to render.</p>\n<p>Conventions:</p>\n<ul><li><code>floor</code>, <code>ceil</code> and <code>round</code> all return an <code><a href=\"#/Integer\">Integer</a></code>.</li><li><code>round</code> goes half <em>away from zero</em>: <code>2.5</code> rounds to <code>3</code>, <code>-2.5</code> to <code>-3</code>.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#pi/0\" class=\"fn-sig\"><code>function pi()</code></a>\n    <span class=\"fn-summary\">The ratio of a circle&#39;s circumference to its diameter.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#e/0\" class=\"fn-sig\"><code>function e()</code></a>\n    <span class=\"fn-summary\">Euler&#39;s number, the base of the natural logarithm.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#infinity/0\" class=\"fn-sig\"><code>function infinity()</code></a>\n    <span class=\"fn-summary\">Positive infinity. Negate it for the other end: <code>-Float.infinity()</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#nan/0\" class=\"fn-sig\"><code>function nan()</code></a>\n    <span class=\"fn-summary\">Not-a-number, the result of an undefined operation. It equals nothing, including itself, so test it with <code>is_nan</code> rather than <code>==</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#floor/1\" class=\"fn-sig\"><code>function floor(x)</code></a>\n    <span class=\"fn-summary\">The largest integer not greater than <code>x</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#ceil/1\" class=\"fn-sig\"><code>function ceil(x)</code></a>\n    <span class=\"fn-summary\">The smallest integer not less than <code>x</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#round/1\" class=\"fn-sig\"><code>function round(x)</code></a>\n    <span class=\"fn-summary\"><code>x</code> rounded to the nearest integer, halves going away from zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#abs/1\" class=\"fn-sig\"><code>function abs(x)</code></a>\n    <span class=\"fn-summary\">The magnitude of <code>x</code>, without its sign.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sqrt/1\" class=\"fn-sig\"><code>function sqrt(x)</code></a>\n    <span class=\"fn-summary\">The square root of <code>x</code>, via <code>x ** 0.5</code>. A negative <code>x</code> yields <code>nan</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#min/2\" class=\"fn-sig\"><code>function min(a, b)</code></a>\n    <span class=\"fn-summary\">The smaller of two floats.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#max/2\" class=\"fn-sig\"><code>function max(a, b)</code></a>\n    <span class=\"fn-summary\">The larger of two floats.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#clamp/3\" class=\"fn-sig\"><code>function clamp(x, low, high)</code></a>\n    <span class=\"fn-summary\"><code>x</code> held inside the range <code>low</code>..<code>high</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(a, b)</code></a>\n    <span class=\"fn-summary\">Orders two floats as <code>-1</code>, <code>0</code> or <code>1</code> — the shape sorting wants.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_nan/1\" class=\"fn-sig\"><code>function is_nan(x)</code></a>\n    <span class=\"fn-summary\">Whether <code>x</code> is the not-a-number value. <code>nan</code> is the one value not equal to itself, which is exactly how this is tested.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_infinite/1\" class=\"fn-sig\"><code>function is_infinite(x)</code></a>\n    <span class=\"fn-summary\">Whether <code>x</code> is positive or negative infinity.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_finite/1\" class=\"fn-sig\"><code>function is_finite(x)</code></a>\n    <span class=\"fn-summary\">Whether <code>x</code> is an ordinary number — neither infinite nor <code>nan</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sign/1\" class=\"fn-sig\"><code>function sign(x)</code></a>\n    <span class=\"fn-summary\"><code>-1.0</code>, <code>0.0</code> or <code>1.0</code> according to the sign of <code>x</code>. Neither infinity nor <code>nan</code> gets special treatment: <code>sign</code> on an infinity follows its direction, same as any other float, and <code>nan &lt; 0.0</code>/<code>nan &gt; 0.0</code> are both false so <code>sign(Float.nan())</code> is <code>0.0</code> — the same convention <code>compare</code> already uses for <code>nan</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_positive/1\" class=\"fn-sig\"><code>function is_positive(x)</code></a>\n    <span class=\"fn-summary\">Whether <code>x</code> is strictly greater than zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_negative/1\" class=\"fn-sig\"><code>function is_negative(x)</code></a>\n    <span class=\"fn-summary\">Whether <code>x</code> is strictly less than zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#exp/1\" class=\"fn-sig\"><code>function exp(x)</code></a>\n    <span class=\"fn-summary\">Euler&#39;s number <code>e</code> raised to the power <code>x</code>. The one direction <code>**</code> cannot express directly, since the base there is the fixed operand.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#log/1\" class=\"fn-sig\"><code>function log(x)</code></a>\n    <span class=\"fn-summary\">The natural logarithm of <code>x</code> (base <code>e</code>) — the inverse of <code>exp</code>. Negative <code>x</code> yields <code>nan</code>; <code>0.0</code> yields <code>-Float.infinity()</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#log_two/1\" class=\"fn-sig\"><code>function log_two(x)</code></a>\n    <span class=\"fn-summary\">The base-2 logarithm of <code>x</code>. Same domain as <code>log</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#log_ten/1\" class=\"fn-sig\"><code>function log_ten(x)</code></a>\n    <span class=\"fn-summary\">The base-10 logarithm of <code>x</code>. Same domain as <code>log</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sin/1\" class=\"fn-sig\"><code>function sin(x)</code></a>\n    <span class=\"fn-summary\">The sine of <code>x</code>, in radians.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#cos/1\" class=\"fn-sig\"><code>function cos(x)</code></a>\n    <span class=\"fn-summary\">The cosine of <code>x</code>, in radians.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#tan/1\" class=\"fn-sig\"><code>function tan(x)</code></a>\n    <span class=\"fn-summary\">The tangent of <code>x</code>, in radians.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"pi/0\">\n  <h3 class=\"fn-title\"><code>function pi()</code> <a class=\"anchor\" href=\"#pi/0\">¶</a></h3>\n<p>The ratio of a circle&#39;s circumference to its diameter.</p>\n<pre><code><span class=\"tok-type\">Float</span>.pi()   <span class=\"tok-com\"># == 3.141592653589793</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Float\">Float</a></code> pi</p>\n</div>\n<div class=\"fn-detail\" id=\"e/0\">\n  <h3 class=\"fn-title\"><code>function e()</code> <a class=\"anchor\" href=\"#e/0\">¶</a></h3>\n<p>Euler&#39;s number, the base of the natural logarithm.</p>\n<pre><code><span class=\"tok-type\">Float</span>.e()   <span class=\"tok-com\"># == 2.718281828459045</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Float\">Float</a></code> e</p>\n</div>\n<div class=\"fn-detail\" id=\"infinity/0\">\n  <h3 class=\"fn-title\"><code>function infinity()</code> <a class=\"anchor\" href=\"#infinity/0\">¶</a></h3>\n<p>Positive infinity. Negate it for the other end: <code>-Float.infinity()</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_infinite(<span class=\"tok-type\">Float</span>.infinity())   <span class=\"tok-com\"># == true</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Float\">Float</a></code> infinity</p>\n</div>\n<div class=\"fn-detail\" id=\"nan/0\">\n  <h3 class=\"fn-title\"><code>function nan()</code> <a class=\"anchor\" href=\"#nan/0\">¶</a></h3>\n<p>Not-a-number, the result of an undefined operation. It equals nothing, including itself, so test it with <code>is_nan</code> rather than <code>==</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_nan(<span class=\"tok-type\">Float</span>.nan())   <span class=\"tok-com\"># == true</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Float\">Float</a></code> nan</p>\n</div>\n<div class=\"fn-detail\" id=\"floor/1\">\n  <h3 class=\"fn-title\"><code>function floor(x)</code> <a class=\"anchor\" href=\"#floor/1\">¶</a></h3>\n<p>The largest integer not greater than <code>x</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.floor(<span class=\"tok-lit\">2.9</span>)    <span class=\"tok-com\"># == 2</span>\n<span class=\"tok-type\">Float</span>.floor(-<span class=\"tok-lit\">2.1</span>)   <span class=\"tok-com\"># == -3</span>\n<span class=\"tok-type\">Float</span>.floor(<span class=\"tok-lit\">2.0</span>)    <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to round down</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> — <code>x</code> rounded toward negative infinity</p>\n</div>\n<div class=\"fn-detail\" id=\"ceil/1\">\n  <h3 class=\"fn-title\"><code>function ceil(x)</code> <a class=\"anchor\" href=\"#ceil/1\">¶</a></h3>\n<p>The smallest integer not less than <code>x</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.ceil(<span class=\"tok-lit\">2.1</span>)    <span class=\"tok-com\"># == 3</span>\n<span class=\"tok-type\">Float</span>.ceil(-<span class=\"tok-lit\">2.9</span>)   <span class=\"tok-com\"># == -2</span>\n<span class=\"tok-type\">Float</span>.ceil(<span class=\"tok-lit\">2.0</span>)    <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to round up</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> — <code>x</code> rounded toward positive infinity</p>\n</div>\n<div class=\"fn-detail\" id=\"round/1\">\n  <h3 class=\"fn-title\"><code>function round(x)</code> <a class=\"anchor\" href=\"#round/1\">¶</a></h3>\n<p><code>x</code> rounded to the nearest integer, halves going away from zero.</p>\n<pre><code><span class=\"tok-type\">Float</span>.round(<span class=\"tok-lit\">2.4</span>)    <span class=\"tok-com\"># == 2</span>\n<span class=\"tok-type\">Float</span>.round(<span class=\"tok-lit\">2.6</span>)    <span class=\"tok-com\"># == 3</span>\n<span class=\"tok-type\">Float</span>.round(<span class=\"tok-lit\">2.5</span>)    <span class=\"tok-com\"># == 3</span>\n<span class=\"tok-type\">Float</span>.round(-<span class=\"tok-lit\">2.5</span>)   <span class=\"tok-com\"># == -3</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to round</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> — the nearest integer to <code>x</code></p>\n</div>\n<div class=\"fn-detail\" id=\"abs/1\">\n  <h3 class=\"fn-title\"><code>function abs(x)</code> <a class=\"anchor\" href=\"#abs/1\">¶</a></h3>\n<p>The magnitude of <code>x</code>, without its sign.</p>\n<pre><code><span class=\"tok-type\">Float</span>.abs(-<span class=\"tok-lit\">7.5</span>)   <span class=\"tok-com\"># == 7.5</span>\n<span class=\"tok-type\">Float</span>.abs(<span class=\"tok-lit\">7.5</span>)    <span class=\"tok-com\"># == 7.5</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to take the absolute value of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>x</code> when it is zero or positive, <code>-x</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"sqrt/1\">\n  <h3 class=\"fn-title\"><code>function sqrt(x)</code> <a class=\"anchor\" href=\"#sqrt/1\">¶</a></h3>\n<p>The square root of <code>x</code>, via <code>x ** 0.5</code>. A negative <code>x</code> yields <code>nan</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.sqrt(<span class=\"tok-lit\">9.0</span>)    <span class=\"tok-com\"># == 3.0</span>\n<span class=\"tok-type\">Float</span>.sqrt(<span class=\"tok-lit\">2.25</span>)   <span class=\"tok-com\"># == 1.5</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to take the square root of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the square root as a <code><a href=\"#/Float\">Float</a></code>, or <code>nan</code> when <code>x</code> is negative</p>\n</div>\n<div class=\"fn-detail\" id=\"min/2\">\n  <h3 class=\"fn-title\"><code>function min(a, b)</code> <a class=\"anchor\" href=\"#min/2\">¶</a></h3>\n<p>The smaller of two floats.</p>\n<pre><code><span class=\"tok-type\">Float</span>.min(<span class=\"tok-lit\">1.5</span>, <span class=\"tok-lit\">2.5</span>)   <span class=\"tok-com\"># == 1.5</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first float</dd>\n  <dt><code>b</code></dt>\n  <dd>the second float</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whichever of <code>a</code> and <code>b</code> is smaller</p>\n</div>\n<div class=\"fn-detail\" id=\"max/2\">\n  <h3 class=\"fn-title\"><code>function max(a, b)</code> <a class=\"anchor\" href=\"#max/2\">¶</a></h3>\n<p>The larger of two floats.</p>\n<pre><code><span class=\"tok-type\">Float</span>.max(<span class=\"tok-lit\">1.5</span>, <span class=\"tok-lit\">2.5</span>)   <span class=\"tok-com\"># == 2.5</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first float</dd>\n  <dt><code>b</code></dt>\n  <dd>the second float</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whichever of <code>a</code> and <code>b</code> is larger</p>\n</div>\n<div class=\"fn-detail\" id=\"clamp/3\">\n  <h3 class=\"fn-title\"><code>function clamp(x, low, high)</code> <a class=\"anchor\" href=\"#clamp/3\">¶</a></h3>\n<p><code>x</code> held inside the range <code>low</code>..<code>high</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.clamp(<span class=\"tok-lit\">5.0</span>, <span class=\"tok-lit\">1.0</span>, <span class=\"tok-lit\">3.0</span>)   <span class=\"tok-com\"># == 3.0</span>\n<span class=\"tok-type\">Float</span>.clamp(<span class=\"tok-lit\">0.5</span>, <span class=\"tok-lit\">1.0</span>, <span class=\"tok-lit\">3.0</span>)   <span class=\"tok-com\"># == 1.0</span>\n<span class=\"tok-type\">Float</span>.clamp(<span class=\"tok-lit\">2.0</span>, <span class=\"tok-lit\">1.0</span>, <span class=\"tok-lit\">3.0</span>)   <span class=\"tok-com\"># == 2.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to clamp</dd>\n  <dt><code>low</code></dt>\n  <dd>the lower bound</dd>\n  <dt><code>high</code></dt>\n  <dd>the upper bound</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>low</code> when <code>x</code> is below it, <code>high</code> when above, else <code>x</code></p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(a, b)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two floats as <code>-1</code>, <code>0</code> or <code>1</code> — the shape sorting wants.</p>\n<pre><code><span class=\"tok-type\">Float</span>.compare(<span class=\"tok-lit\">1.0</span>, <span class=\"tok-lit\">2.0</span>)   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">Float</span>.compare(<span class=\"tok-lit\">2.0</span>, <span class=\"tok-lit\">1.0</span>)   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Float</span>.compare(<span class=\"tok-lit\">1.0</span>, <span class=\"tok-lit\">1.0</span>)   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first float</dd>\n  <dt><code>b</code></dt>\n  <dd>the second float</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>a &lt; b</code>, <code>1</code> when <code>a &gt; b</code>, <code>0</code> when they are equal</p>\n</div>\n<div class=\"fn-detail\" id=\"is_nan/1\">\n  <h3 class=\"fn-title\"><code>function is_nan(x)</code> <a class=\"anchor\" href=\"#is_nan/1\">¶</a></h3>\n<p>Whether <code>x</code> is the not-a-number value. <code>nan</code> is the one value not equal to itself, which is exactly how this is tested.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_nan(<span class=\"tok-type\">Float</span>.nan())   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Float</span>.is_nan(<span class=\"tok-lit\">1.0</span>)         <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>x</code> is <code>nan</code>, <code>false</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"is_infinite/1\">\n  <h3 class=\"fn-title\"><code>function is_infinite(x)</code> <a class=\"anchor\" href=\"#is_infinite/1\">¶</a></h3>\n<p>Whether <code>x</code> is positive or negative infinity.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_infinite(<span class=\"tok-type\">Float</span>.infinity())    <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Float</span>.is_infinite(-<span class=\"tok-type\">Float</span>.infinity())   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Float</span>.is_infinite(<span class=\"tok-lit\">1.0</span>)               <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>x</code> is infinite, <code>false</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"is_finite/1\">\n  <h3 class=\"fn-title\"><code>function is_finite(x)</code> <a class=\"anchor\" href=\"#is_finite/1\">¶</a></h3>\n<p>Whether <code>x</code> is an ordinary number — neither infinite nor <code>nan</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_finite(<span class=\"tok-lit\">1.0</span>)               <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Float</span>.is_finite(<span class=\"tok-type\">Float</span>.infinity())    <span class=\"tok-com\"># == false</span>\n<span class=\"tok-type\">Float</span>.is_finite(<span class=\"tok-type\">Float</span>.nan())         <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>x</code> is finite, <code>false</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"sign/1\">\n  <h3 class=\"fn-title\"><code>function sign(x)</code> <a class=\"anchor\" href=\"#sign/1\">¶</a></h3>\n<p><code>-1.0</code>, <code>0.0</code> or <code>1.0</code> according to the sign of <code>x</code>. Neither infinity nor <code>nan</code> gets special treatment: <code>sign</code> on an infinity follows its direction, same as any other float, and <code>nan &lt; 0.0</code>/<code>nan &gt; 0.0</code> are both false so <code>sign(Float.nan())</code> is <code>0.0</code> — the same convention <code>compare</code> already uses for <code>nan</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.sign(-<span class=\"tok-lit\">7.5</span>)   <span class=\"tok-com\"># == -1.0</span>\n<span class=\"tok-type\">Float</span>.sign(<span class=\"tok-lit\">7.5</span>)    <span class=\"tok-com\"># == 1.0</span>\n<span class=\"tok-type\">Float</span>.sign(<span class=\"tok-lit\">0.0</span>)    <span class=\"tok-com\"># == 0.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1.0</code> when <code>x</code> is negative, <code>1.0</code> when positive, else <code>0.0</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_positive/1\">\n  <h3 class=\"fn-title\"><code>function is_positive(x)</code> <a class=\"anchor\" href=\"#is_positive/1\">¶</a></h3>\n<p>Whether <code>x</code> is strictly greater than zero.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_positive(<span class=\"tok-lit\">7.5</span>)    <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Float</span>.is_positive(<span class=\"tok-lit\">0.0</span>)    <span class=\"tok-com\"># == false</span>\n<span class=\"tok-type\">Float</span>.is_positive(-<span class=\"tok-lit\">7.5</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>x &gt; 0.0</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_negative/1\">\n  <h3 class=\"fn-title\"><code>function is_negative(x)</code> <a class=\"anchor\" href=\"#is_negative/1\">¶</a></h3>\n<p>Whether <code>x</code> is strictly less than zero.</p>\n<pre><code><span class=\"tok-type\">Float</span>.is_negative(-<span class=\"tok-lit\">7.5</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Float</span>.is_negative(<span class=\"tok-lit\">0.0</span>)    <span class=\"tok-com\"># == false</span>\n<span class=\"tok-type\">Float</span>.is_negative(<span class=\"tok-lit\">7.5</span>)    <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>x &lt; 0.0</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"exp/1\">\n  <h3 class=\"fn-title\"><code>function exp(x)</code> <a class=\"anchor\" href=\"#exp/1\">¶</a></h3>\n<p>Euler&#39;s number <code>e</code> raised to the power <code>x</code>. The one direction <code>**</code> cannot express directly, since the base there is the fixed operand.</p>\n<pre><code><span class=\"tok-type\">Float</span>.exp(<span class=\"tok-lit\">0.0</span>)   <span class=\"tok-com\"># == 1.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the exponent</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>e ** x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"log/1\">\n  <h3 class=\"fn-title\"><code>function log(x)</code> <a class=\"anchor\" href=\"#log/1\">¶</a></h3>\n<p>The natural logarithm of <code>x</code> (base <code>e</code>) — the inverse of <code>exp</code>. Negative <code>x</code> yields <code>nan</code>; <code>0.0</code> yields <code>-Float.infinity()</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.log(<span class=\"tok-lit\">1.0</span>)   <span class=\"tok-com\"># == 0.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to take the logarithm of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the natural logarithm of <code>x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"log_two/1\">\n  <h3 class=\"fn-title\"><code>function log_two(x)</code> <a class=\"anchor\" href=\"#log_two/1\">¶</a></h3>\n<p>The base-2 logarithm of <code>x</code>. Same domain as <code>log</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.log_two(<span class=\"tok-lit\">8.0</span>)   <span class=\"tok-com\"># == 3.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to take the logarithm of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the base-2 logarithm of <code>x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"log_ten/1\">\n  <h3 class=\"fn-title\"><code>function log_ten(x)</code> <a class=\"anchor\" href=\"#log_ten/1\">¶</a></h3>\n<p>The base-10 logarithm of <code>x</code>. Same domain as <code>log</code>.</p>\n<pre><code><span class=\"tok-type\">Float</span>.log_ten(<span class=\"tok-lit\">100.0</span>)   <span class=\"tok-com\"># == 2.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the float to take the logarithm of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the base-10 logarithm of <code>x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"sin/1\">\n  <h3 class=\"fn-title\"><code>function sin(x)</code> <a class=\"anchor\" href=\"#sin/1\">¶</a></h3>\n<p>The sine of <code>x</code>, in radians.</p>\n<pre><code><span class=\"tok-type\">Float</span>.sin(<span class=\"tok-lit\">0.0</span>)   <span class=\"tok-com\"># == 0.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the angle, in radians</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the sine of <code>x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"cos/1\">\n  <h3 class=\"fn-title\"><code>function cos(x)</code> <a class=\"anchor\" href=\"#cos/1\">¶</a></h3>\n<p>The cosine of <code>x</code>, in radians.</p>\n<pre><code><span class=\"tok-type\">Float</span>.cos(<span class=\"tok-lit\">0.0</span>)   <span class=\"tok-com\"># == 1.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the angle, in radians</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the cosine of <code>x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"tan/1\">\n  <h3 class=\"fn-title\"><code>function tan(x)</code> <a class=\"anchor\" href=\"#tan/1\">¶</a></h3>\n<p>The tangent of <code>x</code>, in radians.</p>\n<pre><code><span class=\"tok-type\">Float</span>.tan(<span class=\"tok-lit\">0.0</span>)   <span class=\"tok-com\"># == 0.0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>x</code></dt>\n  <dd>the angle, in radians</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the tangent of <code>x</code>, as a <code><a href=\"#/Float\">Float</a></code></p>\n</div>\n</section>\n"},"Global":{"title":"Global — Ramos","heading":"Global","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Global — one process-wide map, held by an actor.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Global — one process-wide map, held by an actor.</p>\n<p><code><a href=\"#/Global\">Global</a></code> is a <code><a href=\"#/Map\">Map</a></code> that every part of a program shares: one actor named <code>:global</code> holds it, and <code>get</code> / <code>put</code> / <code>clear</code> are messages to that actor. Nothing mutates — each <code>put</code> hands the actor a new map, and the actor keeps the newest one.</p>\n<pre><code><span class=\"tok-type\">Global</span>.start()\n<span class=\"tok-type\">Global</span>.put(<span class=\"tok-lit\">:user</span>, <span class=\"tok-str\">&quot;andrew&quot;</span>)\n<span class=\"tok-type\">Global</span>.get(<span class=\"tok-lit\">:user</span>)             <span class=\"tok-com\"># == &quot;andrew&quot;</span></code></pre>\n<p>Only <code>get</code> waits. A write has no answer worth waiting for, so <code>put</code> and <code>clear</code> are casts: <code>:ok</code> comes back as soon as the message is sent. That costs nothing in correctness, because the actor takes its mailbox in order — a <code>get</code> sent after a <code>put</code> is handled after it, and sees the write.</p>\n<p><em></em>Use it with caution.<em></em> It is shared mutable state wearing an actor&#39;s clothes, and it carries every cost that implies:</p>\n<ul><li><em></em>It hides a dependency.<em></em> A function that reads <code><a href=\"#/Global\">Global</a></code> says nothing about it in its signature, so callers cannot see what it needs or substitute anything for a test. A parameter is almost always better.</li><li><em></em>It is a bottleneck.<em></em> One actor handles one message at a time, so every reader and writer in the program queues behind the same thread.</li><li><em></em>It has no scoping.<em></em> One flat key space for the whole program, so two unrelated parts picking the same key silently overwrite each other.</li><li><em></em>A read is a guess.<em></em> Between a <code>get</code> and the <code>put</code> that acts on it, another caller may have written the key; only what happens inside one handler is atomic.</li><li><em></em>Actors cannot reach it.<em></em> An actor&#39;s thread cannot see the actor registry, so a handler cannot call <code><a href=\"#/Global:get\">Global.get</a></code> — a message is the only way state gets into an actor.</li></ul>\n<p>Reach for it for what is genuinely process-wide and written once: config read at startup, a feature flag, a run id. For anything else, pass the value or give it its own actor with a name that says what it holds.</p>\n<p><code><a href=\"#/Global\">Global</a></code> is an ordinary <code><a href=\"#/Actor\">Actor</a></code> implementation, so <code>list_actors</code>, <code>actor_stats(:global)</code> and <code>stop_actor(:global)</code> all work on it.</p>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#call/4\" class=\"fn-sig\"><code>function call(f, args, state, config)</code></a>\n    <span class=\"fn-summary\">The server half of the one operation with something to say back: <code>:get</code>. Sent by <code>get</code>, never called directly.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#cast/4\" class=\"fn-sig\"><code>function cast(f, args, state, config)</code></a>\n    <span class=\"fn-summary\">The server half of the two writes, <code>:put</code> and <code>:clear</code>. Neither replies, so the new map is the whole result.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#start/0\" class=\"fn-sig\"><code>function start()</code></a>\n    <span class=\"fn-summary\">Start the <code>:global</code> actor with an empty map. Call it once, before anything reads or writes; starting it twice is an error.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#get/1\" class=\"fn-sig\"><code>function get(key)</code></a>\n    <span class=\"fn-summary\">The value stored under <code>key</code>, waiting for the actor to answer. Every write sent before it has been handled by the time it reads.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#put/2\" class=\"fn-sig\"><code>function put(key, value)</code></a>\n    <span class=\"fn-summary\">Store <code>value</code> under <code>key</code>, replacing whatever was there, without waiting for the actor. <code>:ok</code> comes back at once and the next <code>get</code> still sees the write, because the actor handles its messages in order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#clear/1\" class=\"fn-sig\"><code>function clear(key)</code></a>\n    <span class=\"fn-summary\">Drop <code>key</code> without waiting for the actor, on the same terms as <code>put</code>. Clearing a key that is not there is fine.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#get_impl/2\" class=\"fn-sig\"><code>helper get_impl(args, state)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#put_impl/2\" class=\"fn-sig\"><code>helper put_impl(args, state)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#clear_impl/2\" class=\"fn-sig\"><code>helper clear_impl(args, state)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"call/4\">\n  <h3 class=\"fn-title\"><code>function call(f, args, state, config)</code> <a class=\"anchor\" href=\"#call/4\">¶</a></h3>\n<p>The server half of the one operation with something to say back: <code>:get</code>. Sent by <code>get</code>, never called directly.</p>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>the operation — <code>:get</code></dd>\n  <dt><code>args</code></dt>\n  <dd><code>[key]</code>, the key to look up</dd>\n  <dt><code>state</code></dt>\n  <dd>the map as it stands</dd>\n  <dt><code>config</code></dt>\n  <dd>unused — <code><a href=\"#/Global\">Global</a></code> has nothing to configure</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code>(value, state)</code> tuple; a read leaves the map alone</p>\n</div>\n<div class=\"fn-detail\" id=\"cast/4\">\n  <h3 class=\"fn-title\"><code>function cast(f, args, state, config)</code> <a class=\"anchor\" href=\"#cast/4\">¶</a></h3>\n<p>The server half of the two writes, <code>:put</code> and <code>:clear</code>. Neither replies, so the new map is the whole result.</p>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>the operation — <code>:put</code> or <code>:clear</code></dd>\n  <dt><code>args</code></dt>\n  <dd><code>[key, value]</code> for <code>:put</code>, <code>[key]</code> for <code>:clear</code></dd>\n  <dt><code>state</code></dt>\n  <dd>the map as it stands</dd>\n  <dt><code>config</code></dt>\n  <dd>unused</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the map the write produced</p>\n</div>\n<div class=\"fn-detail\" id=\"start/0\">\n  <h3 class=\"fn-title\"><code>function start()</code> <a class=\"anchor\" href=\"#start/0\">¶</a></h3>\n<p>Start the <code>:global</code> actor with an empty map. Call it once, before anything reads or writes; starting it twice is an error.</p>\n<pre><code><span class=\"tok-type\">Global</span>.start()   <span class=\"tok-com\"># == :ok</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code></p>\n</div>\n<div class=\"fn-detail\" id=\"get/1\">\n  <h3 class=\"fn-title\"><code>function get(key)</code> <a class=\"anchor\" href=\"#get/1\">¶</a></h3>\n<p>The value stored under <code>key</code>, waiting for the actor to answer. Every write sent before it has been handled by the time it reads.</p>\n<pre><code><span class=\"tok-type\">Global</span>.start()\n<span class=\"tok-type\">Global</span>.put(<span class=\"tok-lit\">:port</span>, <span class=\"tok-lit\">8080</span>)\n<span class=\"tok-type\">Global</span>.get(<span class=\"tok-lit\">:port</span>)     <span class=\"tok-com\"># == 8080</span>\n<span class=\"tok-type\">Global</span>.get(<span class=\"tok-lit\">:host</span>)     <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>key</code></dt>\n  <dd>the key to look up — an <code><a href=\"#/Integer\">Integer</a></code>, <code><a href=\"#/String\">String</a></code> or <code>Symbol</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the value under <code>key</code>, or <code>nil</code> when there is none</p>\n</div>\n<div class=\"fn-detail\" id=\"put/2\">\n  <h3 class=\"fn-title\"><code>function put(key, value)</code> <a class=\"anchor\" href=\"#put/2\">¶</a></h3>\n<p>Store <code>value</code> under <code>key</code>, replacing whatever was there, without waiting for the actor. <code>:ok</code> comes back at once and the next <code>get</code> still sees the write, because the actor handles its messages in order.</p>\n<pre><code><span class=\"tok-type\">Global</span>.start()\n<span class=\"tok-type\">Global</span>.put(<span class=\"tok-lit\">:user</span>, <span class=\"tok-str\">&quot;andrew&quot;</span>)   <span class=\"tok-com\"># == :ok</span></code></pre>\n<dl class=\"params\">\n  <dt><code>key</code></dt>\n  <dd>the key to set — an <code><a href=\"#/Integer\">Integer</a></code>, <code><a href=\"#/String\">String</a></code> or <code>Symbol</code></dd>\n  <dt><code>value</code></dt>\n  <dd>the value to store</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, as soon as the message is sent</p>\n</div>\n<div class=\"fn-detail\" id=\"clear/1\">\n  <h3 class=\"fn-title\"><code>function clear(key)</code> <a class=\"anchor\" href=\"#clear/1\">¶</a></h3>\n<p>Drop <code>key</code> without waiting for the actor, on the same terms as <code>put</code>. Clearing a key that is not there is fine.</p>\n<pre><code><span class=\"tok-type\">Global</span>.start()\n<span class=\"tok-type\">Global</span>.put(<span class=\"tok-lit\">:tmp</span>, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-type\">Global</span>.clear(<span class=\"tok-lit\">:tmp</span>)   <span class=\"tok-com\"># == :ok</span>\n<span class=\"tok-type\">Global</span>.get(<span class=\"tok-lit\">:tmp</span>)     <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>key</code></dt>\n  <dd>the key to drop</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, as soon as the message is sent</p>\n</div>\n<div class=\"fn-detail\" id=\"get_impl/2\">\n  <h3 class=\"fn-title\"><code>helper get_impl(args, state)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#get_impl/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"put_impl/2\">\n  <h3 class=\"fn-title\"><code>helper put_impl(args, state)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#put_impl/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"clear_impl/2\">\n  <h3 class=\"fn-title\"><code>helper clear_impl(args, state)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#clear_impl/2\">¶</a></h3>\n</div>\n</section>\n"},"Integer":{"title":"Integer — Ramos","heading":"Integer","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Integer — operations on whole numbers.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Integer — operations on whole numbers.</p>\n<p><code>42</code> is an <code><a href=\"#/Integer\">Integer</a></code>. Arithmetic lives in the operators (<code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>%</code>, <code>**</code>) rather than here; this module holds what reads better as a named function, especially the predicates passed to <code><a href=\"#/List\">List</a></code>:</p>\n<pre><code>[<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]\n| <span class=\"tok-type\">List</span>.filter(<span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == [2, 4]</span></code></pre>\n<p>Kernel already converts, so this module does not repeat it: use <code>to_integer(text)</code> to parse, <code>to_float(n)</code> to widen, <code>to_string(n)</code> to render.</p>\n<p>Conventions:</p>\n<ul><li>integer division truncates toward zero, and <code>%</code> follows the sign of the divisor — both are operator behaviour, documented in the README.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#is_even/1\" class=\"fn-sig\"><code>function is_even(n)</code></a>\n    <span class=\"fn-summary\">Whether <code>n</code> is divisible by two.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_odd/1\" class=\"fn-sig\"><code>function is_odd(n)</code></a>\n    <span class=\"fn-summary\">Whether <code>n</code> leaves a remainder when divided by two.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#abs/1\" class=\"fn-sig\"><code>function abs(n)</code></a>\n    <span class=\"fn-summary\">The magnitude of <code>n</code>, without its sign.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sign/1\" class=\"fn-sig\"><code>function sign(n)</code></a>\n    <span class=\"fn-summary\"><code>-1</code>, <code>0</code> or <code>1</code> according to the sign of <code>n</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_positive/1\" class=\"fn-sig\"><code>function is_positive(n)</code></a>\n    <span class=\"fn-summary\">Whether <code>n</code> is strictly greater than zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_negative/1\" class=\"fn-sig\"><code>function is_negative(n)</code></a>\n    <span class=\"fn-summary\">Whether <code>n</code> is strictly less than zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_zero/1\" class=\"fn-sig\"><code>function is_zero(n)</code></a>\n    <span class=\"fn-summary\">Whether <code>n</code> is exactly zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#min/2\" class=\"fn-sig\"><code>function min(a, b)</code></a>\n    <span class=\"fn-summary\">The smaller of two integers.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#max/2\" class=\"fn-sig\"><code>function max(a, b)</code></a>\n    <span class=\"fn-summary\">The larger of two integers.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#clamp/3\" class=\"fn-sig\"><code>function clamp(n, low, high)</code></a>\n    <span class=\"fn-summary\"><code>n</code> held inside the range <code>low</code>..<code>high</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(a, b)</code></a>\n    <span class=\"fn-summary\">Orders two integers as <code>-1</code>, <code>0</code> or <code>1</code> — the shape sorting wants.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#gcd/2\" class=\"fn-sig\"><code>function gcd(a, b)</code></a>\n    <span class=\"fn-summary\">The greatest common divisor of <code>a</code> and <code>b</code>, by Euclid&#39;s algorithm. The result is never negative.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#lcm/2\" class=\"fn-sig\"><code>function lcm(a, b)</code></a>\n    <span class=\"fn-summary\">The least common multiple of <code>a</code> and <code>b</code>. Zero with either operand zero.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#times/2\" class=\"fn-sig\"><code>function times(n, f)</code></a>\n    <span class=\"fn-summary\">Applies <code>f</code> to each of <code>0</code>, <code>1</code>, ..., <code>n - 1</code>, collecting the results. Empty when <code>n</code> is zero or negative — checked directly, since <code><a href=\"#/List:range\">List.range(0, n - 1)</a></code> alone would count <em>down</em> from <code>0</code> once <code>n - 1</code> goes negative, rather than stopping at an empty list.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#digits/1\" class=\"fn-sig\"><code>function digits(n)</code></a>\n    <span class=\"fn-summary\">The decimal digits of <code>n</code>, most significant first. The sign is dropped, and zero is a single <code>0</code> digit.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"is_even/1\">\n  <h3 class=\"fn-title\"><code>function is_even(n)</code> <a class=\"anchor\" href=\"#is_even/1\">¶</a></h3>\n<p>Whether <code>n</code> is divisible by two.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.is_even(<span class=\"tok-lit\">4</span>)    <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Integer</span>.is_even(-<span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>n</code> is even, <code>false</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"is_odd/1\">\n  <h3 class=\"fn-title\"><code>function is_odd(n)</code> <a class=\"anchor\" href=\"#is_odd/1\">¶</a></h3>\n<p>Whether <code>n</code> leaves a remainder when divided by two.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.is_odd(<span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Integer</span>.is_odd(<span class=\"tok-lit\">4</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>n</code> is odd, <code>false</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"abs/1\">\n  <h3 class=\"fn-title\"><code>function abs(n)</code> <a class=\"anchor\" href=\"#abs/1\">¶</a></h3>\n<p>The magnitude of <code>n</code>, without its sign.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.abs(-<span class=\"tok-lit\">7</span>)   <span class=\"tok-com\"># == 7</span>\n<span class=\"tok-type\">Integer</span>.abs(<span class=\"tok-lit\">7</span>)    <span class=\"tok-com\"># == 7</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to take the absolute value of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>n</code> when it is zero or positive, <code>-n</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"sign/1\">\n  <h3 class=\"fn-title\"><code>function sign(n)</code> <a class=\"anchor\" href=\"#sign/1\">¶</a></h3>\n<p><code>-1</code>, <code>0</code> or <code>1</code> according to the sign of <code>n</code>.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.sign(-<span class=\"tok-lit\">7</span>)   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">Integer</span>.sign(<span class=\"tok-lit\">7</span>)    <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Integer</span>.sign(<span class=\"tok-lit\">0</span>)    <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>n</code> is negative, <code>1</code> when positive, else <code>0</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_positive/1\">\n  <h3 class=\"fn-title\"><code>function is_positive(n)</code> <a class=\"anchor\" href=\"#is_positive/1\">¶</a></h3>\n<p>Whether <code>n</code> is strictly greater than zero.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.is_positive(<span class=\"tok-lit\">7</span>)    <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Integer</span>.is_positive(<span class=\"tok-lit\">0</span>)    <span class=\"tok-com\"># == false</span>\n<span class=\"tok-type\">Integer</span>.is_positive(-<span class=\"tok-lit\">7</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>n &gt; 0</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_negative/1\">\n  <h3 class=\"fn-title\"><code>function is_negative(n)</code> <a class=\"anchor\" href=\"#is_negative/1\">¶</a></h3>\n<p>Whether <code>n</code> is strictly less than zero.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.is_negative(-<span class=\"tok-lit\">7</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Integer</span>.is_negative(<span class=\"tok-lit\">0</span>)    <span class=\"tok-com\"># == false</span>\n<span class=\"tok-type\">Integer</span>.is_negative(<span class=\"tok-lit\">7</span>)    <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>n &lt; 0</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_zero/1\">\n  <h3 class=\"fn-title\"><code>function is_zero(n)</code> <a class=\"anchor\" href=\"#is_zero/1\">¶</a></h3>\n<p>Whether <code>n</code> is exactly zero.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.is_zero(<span class=\"tok-lit\">0</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Integer</span>.is_zero(<span class=\"tok-lit\">7</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>n == 0</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"min/2\">\n  <h3 class=\"fn-title\"><code>function min(a, b)</code> <a class=\"anchor\" href=\"#min/2\">¶</a></h3>\n<p>The smaller of two integers.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.min(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == 1</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first integer</dd>\n  <dt><code>b</code></dt>\n  <dd>the second integer</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whichever of <code>a</code> and <code>b</code> is smaller</p>\n</div>\n<div class=\"fn-detail\" id=\"max/2\">\n  <h3 class=\"fn-title\"><code>function max(a, b)</code> <a class=\"anchor\" href=\"#max/2\">¶</a></h3>\n<p>The larger of two integers.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.max(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first integer</dd>\n  <dt><code>b</code></dt>\n  <dd>the second integer</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whichever of <code>a</code> and <code>b</code> is larger</p>\n</div>\n<div class=\"fn-detail\" id=\"clamp/3\">\n  <h3 class=\"fn-title\"><code>function clamp(n, low, high)</code> <a class=\"anchor\" href=\"#clamp/3\">¶</a></h3>\n<p><code>n</code> held inside the range <code>low</code>..<code>high</code>.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.clamp(<span class=\"tok-lit\">5</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == 3</span>\n<span class=\"tok-type\">Integer</span>.clamp(<span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Integer</span>.clamp(<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to clamp</dd>\n  <dt><code>low</code></dt>\n  <dd>the lower bound</dd>\n  <dt><code>high</code></dt>\n  <dd>the upper bound</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>low</code> when <code>n</code> is below it, <code>high</code> when above, else <code>n</code></p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(a, b)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two integers as <code>-1</code>, <code>0</code> or <code>1</code> — the shape sorting wants.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.compare(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">Integer</span>.compare(<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Integer</span>.compare(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first integer</dd>\n  <dt><code>b</code></dt>\n  <dd>the second integer</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>a &lt; b</code>, <code>1</code> when <code>a &gt; b</code>, <code>0</code> when they are equal</p>\n</div>\n<div class=\"fn-detail\" id=\"gcd/2\">\n  <h3 class=\"fn-title\"><code>function gcd(a, b)</code> <a class=\"anchor\" href=\"#gcd/2\">¶</a></h3>\n<p>The greatest common divisor of <code>a</code> and <code>b</code>, by Euclid&#39;s algorithm. The result is never negative.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.gcd(<span class=\"tok-lit\">12</span>, <span class=\"tok-lit\">18</span>)   <span class=\"tok-com\"># == 6</span>\n<span class=\"tok-type\">Integer</span>.gcd(<span class=\"tok-lit\">7</span>, <span class=\"tok-lit\">13</span>)    <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Integer</span>.gcd(-<span class=\"tok-lit\">12</span>, <span class=\"tok-lit\">18</span>)  <span class=\"tok-com\"># == 6</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first integer</dd>\n  <dt><code>b</code></dt>\n  <dd>the second integer</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the largest integer dividing both</p>\n</div>\n<div class=\"fn-detail\" id=\"lcm/2\">\n  <h3 class=\"fn-title\"><code>function lcm(a, b)</code> <a class=\"anchor\" href=\"#lcm/2\">¶</a></h3>\n<p>The least common multiple of <code>a</code> and <code>b</code>. Zero with either operand zero.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.lcm(<span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">6</span>)   <span class=\"tok-com\"># == 12</span>\n<span class=\"tok-type\">Integer</span>.lcm(<span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">5</span>)   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first integer</dd>\n  <dt><code>b</code></dt>\n  <dd>the second integer</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the smallest non-negative integer both divide</p>\n</div>\n<div class=\"fn-detail\" id=\"times/2\">\n  <h3 class=\"fn-title\"><code>function times(n, f)</code> <a class=\"anchor\" href=\"#times/2\">¶</a></h3>\n<p>Applies <code>f</code> to each of <code>0</code>, <code>1</code>, ..., <code>n - 1</code>, collecting the results. Empty when <code>n</code> is zero or negative — checked directly, since <code><a href=\"#/List:range\">List.range(0, n - 1)</a></code> alone would count <em>down</em> from <code>0</code> once <code>n - 1</code> goes negative, rather than stopping at an empty list.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.times(<span class=\"tok-lit\">3</span>, <span class=\"tok-kw\">do</span> i -&gt; i * <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == [0, 2, 4]</span>\n<span class=\"tok-type\">Integer</span>.times(<span class=\"tok-lit\">0</span>, <span class=\"tok-kw\">do</span> i -&gt; i)       <span class=\"tok-com\"># == []</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>how many times to apply <code>f</code></dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda taking the zero-based index</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of the <code>n</code> results, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"digits/1\">\n  <h3 class=\"fn-title\"><code>function digits(n)</code> <a class=\"anchor\" href=\"#digits/1\">¶</a></h3>\n<p>The decimal digits of <code>n</code>, most significant first. The sign is dropped, and zero is a single <code>0</code> digit.</p>\n<pre><code><span class=\"tok-type\">Integer</span>.digits(<span class=\"tok-lit\">123</span>)    <span class=\"tok-com\"># == [1, 2, 3]</span>\n<span class=\"tok-type\">Integer</span>.digits(-<span class=\"tok-lit\">123</span>)   <span class=\"tok-com\"># == [1, 2, 3]</span>\n<span class=\"tok-type\">Integer</span>.digits(<span class=\"tok-lit\">0</span>)      <span class=\"tok-com\"># == [0]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>n</code></dt>\n  <dd>the integer to split</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of the digits, most significant first</p>\n</div>\n</section>\n"},"Kernel":{"title":"Kernel — Ramos","heading":"Kernel","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Kernel — the implicit, always-in-scope module of built-ins.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Kernel — the implicit, always-in-scope module of built-ins.</p>\n<p><code><a href=\"#/Kernel\">Kernel</a></code> is auto-imported into every scope, so its functions are the only ones you call <em>bare</em> — <code>print(x)</code> is sugar for <code><a href=\"#/Kernel:print\">Kernel.print(x)</a></code>, and no <code>alias</code> is ever needed for it. Every other module must be named or <code>alias</code>ed.</p>\n<p>It is the seam between Ramos code and the host runtime. Its functions have no Ramos bodies: each delegates to a native handler via <code>native(str, args)</code>, which the interpreter intercepts and dispatches. As a result <code><a href=\"#/Kernel\">Kernel</a></code> collects the primitives that can&#39;t reasonably be expressed in Ramos itself:</p>\n<ul><li>console I/O      — <code>print</code>, <code>println</code>, <code>new_line</code>, <code>read</code>, <code>read_all</code>, <code>read_password</code>, <code>eprint</code>, <code>eprintln</code></li><li>process          — <code>get_args</code>, <code>get_arg</code>, <code>get_env</code>, <code>sleep</code>, <code>exit</code></li><li>time             — <code>now</code> (wall clock), <code>monotonic</code> (elapsed)</li><li>randomness       — <code>random</code>, <code>random_int</code> (not cryptographic)</li><li>collection ops   — <code>size</code> (List/Tuple/Map), <code>at</code> (List/Tuple), <code>to_list</code>, <code>is_empty</code> (also strings)</li><li>conversions      — <code>to_string</code> (display), <code>inspect</code> (debug), <code>to_integer</code>, <code>to_float</code>, <code>type_of</code> (there is deliberately no <code>to_symbol</code> — see below)</li><li>calling          — <code>apply(f, args)</code>, <code>module_apply(m, fn_name, args)</code> (see the <code><a href=\"#/Module\">Module</a></code> module)</li><li>actors           — <code>start_actor</code>, <code>call_actor</code>, <code>cast_actor</code>, <code>list_actors</code>, <code>actor_stats</code>, <code>is_actor_started</code>, <code>stop_actor</code></li><li>diagnostics      — <code>current_stacktrace</code></li><li>errors           — <code>error(type, message)</code> (with a stacktrace) and <code>exception(type, message)</code> (without, for business logic) — see &quot;Error handling&quot; in the README</li><li>type predicates  — <code>is_integer</code>, <code>is_string</code>, <code>is_list</code>, …</li><li>truthiness       — <code>is_truthy</code> (pure Ramos, no native; <code>not</code> inverts it)</li></ul>\n<p>Because <code><a href=\"#/Kernel\">Kernel</a></code> is always in scope, these read as language-level built-ins rather than library calls:</p>\n<pre><code>print(<span class=\"tok-str\">&quot;hi&quot;</span>)\nn = size([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])\n<span class=\"tok-kw\">cond</span>\n  is_tuple(value) -&gt; ...</code></pre>\n<p>Conventions:</p>\n<ul><li>out-of-range <code>at</code> returns <code>nil</code>.</li><li>the <code>is_*</code> predicates mirror the built-in type modules listed in the README&#39;s Types table (plus <code>is_lambda</code> for anonymous functions).</li><li>there is no <code>to_symbol</code>, by design. A symbol lives for as long as the program does, so building one from runtime data — a request field, a line of input — is an unbounded leak. Symbols are written as literals (<code>:ok</code>), which keeps the set of them finite and visible in the source. To carry runtime text, use a <code><a href=\"#/String\">String</a></code>; to branch on it, match the string and yield a literal symbol.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#native/2\" class=\"fn-sig\"><code>function native(str, args)</code></a>\n    <span class=\"fn-summary\">Invokes a host-provided (native) function by name.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#print/1\" class=\"fn-sig\"><code>function print(value)</code></a>\n    <span class=\"fn-summary\">Writes <code>value</code> to standard out <em>without</em> a trailing newline, then flushes. <code>value</code> is rendered with its <code><a href=\"#/String\">String</a></code> representation.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#println/1\" class=\"fn-sig\"><code>function println(value)</code></a>\n    <span class=\"fn-summary\">Writes <code>value</code> to standard out <em>followed by a newline</em>. Like <code>print(value); new_line()</code>, but the text and its newline are written in a single call, so when threads print at once one line never lands inside another (see the <code><a href=\"#/Thread\">Thread</a></code> module).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#new_line/0\" class=\"fn-sig\"><code>function new_line()</code></a>\n    <span class=\"fn-summary\">Writes a single newline (<code>\\n</code>) to standard out and flushes. Useful for terminating a sequence of <code>print</code> calls.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#read/0\" class=\"fn-sig\"><code>function read()</code></a>\n    <span class=\"fn-summary\">Reads a single line from standard in (up to and including the newline, which is stripped) and returns it as a <code><a href=\"#/String\">String</a></code>. Blocks until a line is available; returns <code>nil</code> at end-of-input.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#read_password/0\" class=\"fn-sig\"><code>function read_password()</code></a>\n    <span class=\"fn-summary\">Reads a line from standard in <em>without echoing</em> it to the terminal, and returns it as a <code><a href=\"#/String\">String</a></code>. Intended for secrets like passwords. Returns <code>nil</code> at end-of-input. Behavior falls back to plain echoing <code>read</code> when the input stream is not a TTY.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#read_all/0\" class=\"fn-sig\"><code>function read_all()</code></a>\n    <span class=\"fn-summary\">Reads <em>all</em> of standard in, to end of input, as a single <code><a href=\"#/String\">String</a></code> — the whole-pipe counterpart to <code>read</code>, which takes one line. Returns <code>&quot;&quot;</code> when the input is empty.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#eprint/1\" class=\"fn-sig\"><code>function eprint(value)</code></a>\n    <span class=\"fn-summary\">Writes <code>value</code> to standard <em>error</em> without a trailing newline. Use it for diagnostics, so a program&#39;s real output on standard out stays clean enough to pipe.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#eprintln/1\" class=\"fn-sig\"><code>function eprintln(value)</code></a>\n    <span class=\"fn-summary\"><code>eprint</code> with a trailing newline.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#get_env/1\" class=\"fn-sig\"><code>function get_env(name)</code></a>\n    <span class=\"fn-summary\">Looks up an environment variable by <code>name</code> and returns its <code><a href=\"#/String\">String</a></code> value, or <code>nil</code> if it is not set.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#get_args/0\" class=\"fn-sig\"><code>function get_args()</code></a>\n    <span class=\"fn-summary\">Returns the program&#39;s command-line arguments — everything after the script path — as a <code><a href=\"#/List\">List</a></code> of <code><a href=\"#/String\">String</a></code>s. Empty when none were given.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#get_arg/1\" class=\"fn-sig\"><code>function get_arg(index)</code></a>\n    <span class=\"fn-summary\">Returns the command-line argument at zero-based <code>index</code>, or <code>nil</code> if there is no argument at that position. Negative indexes are not supported.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sleep/1\" class=\"fn-sig\"><code>function sleep(ms)</code></a>\n    <span class=\"fn-summary\">Suspends the program for <code>ms</code> milliseconds, then returns <code>nil</code>. A value of <code>0</code> or less returns immediately.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#exit/1\" class=\"fn-sig\"><code>function exit(code)</code></a>\n    <span class=\"fn-summary\">Ends the program with <code>code</code> as its exit status — <code>0</code> for success, any other value for failure. Nothing after the call runs.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#now/0\" class=\"fn-sig\"><code>function now()</code></a>\n    <span class=\"fn-summary\">The wall-clock time as milliseconds since the Unix epoch.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#monotonic/0\" class=\"fn-sig\"><code>function monotonic()</code></a>\n    <span class=\"fn-summary\">Milliseconds from a fixed point in the program&#39;s life. The value only ever moves forward, which is what makes it the right clock for measuring how long something took.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#random/0\" class=\"fn-sig\"><code>function random()</code></a>\n    <span class=\"fn-summary\">A random <code><a href=\"#/Float\">Float</a></code> in <code>[0.0, 1.0)</code> — zero is possible, one is not.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#random_int/2\" class=\"fn-sig\"><code>function random_int(low, high)</code></a>\n    <span class=\"fn-summary\">A random <code><a href=\"#/Integer\">Integer</a></code> in <code>low..high</code>, <em></em>inclusive at both ends<em></em>. The bounds may be given either way round.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#apply/2\" class=\"fn-sig\"><code>function apply(f, args)</code></a>\n    <span class=\"fn-summary\">Calls the lambda <code>f</code> with <code>args</code>, a list of its arguments. Use it when the arity is not known where the call is written — otherwise call <code>f</code> directly.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#module_apply/3\" class=\"fn-sig\"><code>function module_apply(m, fn_name, args)</code></a>\n    <span class=\"fn-summary\">Resolves <code>fn_name</code> on module <code>m</code> and calls it with <code>args</code>, a list. <code><a href=\"#/Module:apply\">Module.apply</a></code> is the client-side name for this — prefer it.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#start_actor/4\" class=\"fn-sig\"><code>function start_actor(id, actor_module, initial_state, config)</code></a>\n    <span class=\"fn-summary\">Starts an actor under <code>id</code>, handled by <code>module</code> — which must implement the <code><a href=\"#/Actor\">Actor</a></code> trait — holding <code>initial_state</code>. <code>config</code> is fixed for the actor&#39;s lifetime and passed to every <code>call</code> and <code>cast</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#call_actor/4\" class=\"fn-sig\"><code>function call_actor(id, actor_module, op, args)</code></a>\n    <span class=\"fn-summary\">Sends <code>op</code> to the actor registered under <code>id</code> and waits for the reply. The module&#39;s <code>call(f, args, state, config)</code> handles it and returns a <code>(reply, new_state)</code> tuple: the reply comes back here, and <code>new_state</code> is what the actor holds from then on.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#cast_actor/4\" class=\"fn-sig\"><code>function cast_actor(id, actor_module, op, args)</code></a>\n    <span class=\"fn-summary\">Sends <code>op</code> to the actor registered under <code>id</code> <em></em>without<em></em> waiting for it to be handled, and returns <code>:ok</code> at once. The message goes on the actor&#39;s mailbox and runs later: before the next <code>call_actor</code> on that actor, or when the program ends, whichever comes first.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#list_actors/0\" class=\"fn-sig\"><code>function list_actors()</code></a>\n    <span class=\"fn-summary\">Every running actor as an <code>(id, module)</code> pair, sorted by id so the order is stable from one call to the next. The id is the value it was started with, and the module is the module itself — so a pair can be handed straight to <code>call_actor</code> or <code>cast_actor</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#actor_stats/1\" class=\"fn-sig\"><code>function actor_stats(id)</code></a>\n    <span class=\"fn-summary\">What is known about one running actor, as a <code><a href=\"#/Map\">Map</a></code>:</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_actor_started/1\" class=\"fn-sig\"><code>function is_actor_started(id)</code></a>\n    <span class=\"fn-summary\">Whether <code>id</code> names a running actor. False for an id that was never started, and for one that has been stopped.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#stop_actor/1\" class=\"fn-sig\"><code>function stop_actor(id)</code></a>\n    <span class=\"fn-summary\">Lets the actor finish everything already sent to it, then ends its thread and forgets the id. Nothing in flight is discarded: the stop is queued behind the messages already in the mailbox.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#size/1\" class=\"fn-sig\"><code>function size(collection)</code></a>\n    <span class=\"fn-summary\">Returns the number of elements in <code>collection</code> — the length of a <code><a href=\"#/List\">List</a></code>, the arity of a <code><a href=\"#/Tuple\">Tuple</a></code>, or the number of key/value pairs in a <code><a href=\"#/Map\">Map</a></code>. Strings are not collections: <code>size</code> on a <code><a href=\"#/String\">String</a></code> is an error — use <code><a href=\"#/String:length\">String.length</a></code> instead.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#at/2\" class=\"fn-sig\"><code>function at(collection, index)</code></a>\n    <span class=\"fn-summary\">Returns the element of <code>collection</code> at zero-based <code>index</code> (<code><a href=\"#/List\">List</a></code> or <code><a href=\"#/Tuple\">Tuple</a></code>), or <code>nil</code> if out of range. Negative indexes are not supported. Strings are not collections: <code>at</code> on a <code><a href=\"#/String\">String</a></code> is an error — use <code><a href=\"#/String:at\">String.at</a></code> instead.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_string/1\" class=\"fn-sig\"><code>function to_string(value)</code></a>\n    <span class=\"fn-summary\">Converts <code>value</code> to its <code><a href=\"#/String\">String</a></code> representation — the same rendering that <code>print</code> uses. Numbers, symbols, tuples, lists, and maps all stringify; <code>nil</code> becomes <code>&quot;nil&quot;</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_integer/1\" class=\"fn-sig\"><code>function to_integer(value)</code></a>\n    <span class=\"fn-summary\">Converts <code>value</code> to an <code><a href=\"#/Integer\">Integer</a></code>. A <code><a href=\"#/String\">String</a></code> is parsed as a whole number; a <code><a href=\"#/Float\">Float</a></code> is truncated toward zero. Returns <code>nil</code> if <code>value</code> can&#39;t be parsed.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_float/1\" class=\"fn-sig\"><code>function to_float(value)</code></a>\n    <span class=\"fn-summary\">Converts <code>value</code> to a <code><a href=\"#/Float\">Float</a></code>. An <code><a href=\"#/Integer\">Integer</a></code> widens; a <code><a href=\"#/String\">String</a></code> is parsed as a decimal number. Returns <code>nil</code> if <code>value</code> can&#39;t be parsed.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_list/1\" class=\"fn-sig\"><code>function to_list(value)</code></a>\n    <span class=\"fn-summary\">Converts <code>value</code> to a <code><a href=\"#/List\">List</a></code>. A <code><a href=\"#/Tuple\">Tuple</a></code> yields its elements in order; a <code><a href=\"#/Map\">Map</a></code> yields its <em>values</em> (keys are dropped — reach for the <code><a href=\"#/Map\">Map</a></code> module when you need them). Already-list values are returned unchanged.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#current_stacktrace/0\" class=\"fn-sig\"><code>function current_stacktrace()</code></a>\n    <span class=\"fn-summary\">Returns the current call stacktrace — the chain of Ramos calls leading to this point — as a list of <code>(file, line)</code> tuples, most recent call first and the outermost caller (<code>main</code>, or wherever the trail runs out) last.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#error/2\" class=\"fn-sig\"><code>function error(type, message)</code></a>\n    <span class=\"fn-summary\">Builds the standard error tuple: <code>(:error, (type, message, stacktrace))</code>. This — together with <code>exception</code>, its stackless twin — is the one way Ramos code reports failure: there is no <code>raise</code> and no <code>catch</code>. A fallible function returns <code>(:ok, value)</code> on success and whatever this builds on failure, and the caller reads either with <code>case</code> like any other value.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#exception/2\" class=\"fn-sig\"><code>function exception(type, message)</code></a>\n    <span class=\"fn-summary\">Builds the business-logic error tuple: <code>(:error, (type, message))</code> — the same shape as <code>error</code>, minus the stacktrace.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_integer/1\" class=\"fn-sig\"><code>function is_integer(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is an <code><a href=\"#/Integer\">Integer</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_float/1\" class=\"fn-sig\"><code>function is_float(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Float\">Float</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_string/1\" class=\"fn-sig\"><code>function is_string(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/String\">String</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_symbol/1\" class=\"fn-sig\"><code>function is_symbol(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code>Symbol</code> (e.g. <code>:ok</code>).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_bool/1\" class=\"fn-sig\"><code>function is_bool(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code>Bool</code> (<code>true</code> or <code>false</code>).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_nil/1\" class=\"fn-sig\"><code>function is_nil(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is <code>nil</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_list/1\" class=\"fn-sig\"><code>function is_list(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/List\">List</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_tuple/1\" class=\"fn-sig\"><code>function is_tuple(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Tuple\">Tuple</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_map/1\" class=\"fn-sig\"><code>function is_map(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Map\">Map</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_module/1\" class=\"fn-sig\"><code>function is_module(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Module\">Module</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_struct/1\" class=\"fn-sig\"><code>function is_struct(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Struct\">Struct</a></code> instance.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#inspect/1\" class=\"fn-sig\"><code>function inspect(value)</code></a>\n    <span class=\"fn-summary\">The <em>debug</em> rendering of <code>value</code>: strings keep their quotes and symbols their colon, so a value reads back as the source that would build it. This is what the REPL prints and what error messages quote.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#type_of/1\" class=\"fn-sig\"><code>function type_of(value)</code></a>\n    <span class=\"fn-summary\">The module <code>value</code> belongs to — the one the README&#39;s Types table lists.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_empty/1\" class=\"fn-sig\"><code>function is_empty(value)</code></a>\n    <span class=\"fn-summary\">Whether <code>value</code> holds nothing — an empty list, map, tuple or string.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_truthy/1\" class=\"fn-sig\"><code>function is_truthy(value)</code></a>\n    <span class=\"fn-summary\">Whether <code>value</code> counts as true in a condition. Only <code>false</code> and <code>nil</code> are falsy — every other value, including <code>0</code>, <code>&quot;&quot;</code> and <code>[]</code>, is truthy. This is the rule <code>if</code>, <code>cond</code>, <code>and</code>, <code>or</code> and <code>not</code> all use.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_lambda/1\" class=\"fn-sig\"><code>function is_lambda(value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is a <code>Lambda</code> (an anonymous function).</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"native/2\">\n  <h3 class=\"fn-title\"><code>function native(str, args)</code> <a class=\"anchor\" href=\"#native/2\">¶</a></h3>\n<p>Invokes a host-provided (native) function by name.</p>\n<p><code>str</code> is the name of the native function to call, and <code>args</code> is a list of arguments to forward to it. This function has no Ramos body — the interpreter intercepts the call and dispatches it to the corresponding native handler in the host runtime.</p>\n<pre><code>native(<span class=\"tok-str\">&quot;print&quot;</span>, [<span class=\"tok-str\">&quot;hello&quot;</span>])   <span class=\"tok-com\"># handled by the interpreter</span></code></pre>\n<dl class=\"params\">\n  <dt><code>str</code></dt>\n  <dd>the name of the native handler to invoke</dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of arguments forwarded to the handler</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whatever the native handler returns</p>\n</div>\n<div class=\"fn-detail\" id=\"print/1\">\n  <h3 class=\"fn-title\"><code>function print(value)</code> <a class=\"anchor\" href=\"#print/1\">¶</a></h3>\n<p>Writes <code>value</code> to standard out <em>without</em> a trailing newline, then flushes. <code>value</code> is rendered with its <code><a href=\"#/String\">String</a></code> representation.</p>\n<pre><code>print(<span class=\"tok-str\">&quot;loading&quot;</span>)        <span class=\"tok-com\"># -&gt; loading</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to write (rendered as a <code><a href=\"#/String\">String</a></code>)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"println/1\">\n  <h3 class=\"fn-title\"><code>function println(value)</code> <a class=\"anchor\" href=\"#println/1\">¶</a></h3>\n<p>Writes <code>value</code> to standard out <em>followed by a newline</em>. Like <code>print(value); new_line()</code>, but the text and its newline are written in a single call, so when threads print at once one line never lands inside another (see the <code><a href=\"#/Thread\">Thread</a></code> module).</p>\n<pre><code>println(<span class=\"tok-str\">&quot;done&quot;</span>)         <span class=\"tok-com\"># -&gt; done\\n</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to write (rendered as a <code><a href=\"#/String\">String</a></code>)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"new_line/0\">\n  <h3 class=\"fn-title\"><code>function new_line()</code> <a class=\"anchor\" href=\"#new_line/0\">¶</a></h3>\n<p>Writes a single newline (<code>\\n</code>) to standard out and flushes. Useful for terminating a sequence of <code>print</code> calls.</p>\n<pre><code>print(<span class=\"tok-str\">&quot;a&quot;</span>)\nprint(<span class=\"tok-str\">&quot;b&quot;</span>)\nnew_line()             <span class=\"tok-com\"># -&gt; ab\\n</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"read/0\">\n  <h3 class=\"fn-title\"><code>function read()</code> <a class=\"anchor\" href=\"#read/0\">¶</a></h3>\n<p>Reads a single line from standard in (up to and including the newline, which is stripped) and returns it as a <code><a href=\"#/String\">String</a></code>. Blocks until a line is available; returns <code>nil</code> at end-of-input.</p>\n<pre><code>name = read()</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the line read as a <code><a href=\"#/String\">String</a></code>, or <code>nil</code> at end-of-input</p>\n</div>\n<div class=\"fn-detail\" id=\"read_password/0\">\n  <h3 class=\"fn-title\"><code>function read_password()</code> <a class=\"anchor\" href=\"#read_password/0\">¶</a></h3>\n<p>Reads a line from standard in <em>without echoing</em> it to the terminal, and returns it as a <code><a href=\"#/String\">String</a></code>. Intended for secrets like passwords. Returns <code>nil</code> at end-of-input. Behavior falls back to plain echoing <code>read</code> when the input stream is not a TTY.</p>\n<pre><code>pw = read_password()</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the line read as a <code><a href=\"#/String\">String</a></code>, or <code>nil</code> at end-of-input</p>\n</div>\n<div class=\"fn-detail\" id=\"read_all/0\">\n  <h3 class=\"fn-title\"><code>function read_all()</code> <a class=\"anchor\" href=\"#read_all/0\">¶</a></h3>\n<p>Reads <em>all</em> of standard in, to end of input, as a single <code><a href=\"#/String\">String</a></code> — the whole-pipe counterpart to <code>read</code>, which takes one line. Returns <code>&quot;&quot;</code> when the input is empty.</p>\n<pre><code>text = read_all()      <span class=\"tok-com\"># `cat file | ramos run app.rmo`</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> everything on standard in as a <code><a href=\"#/String\">String</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"eprint/1\">\n  <h3 class=\"fn-title\"><code>function eprint(value)</code> <a class=\"anchor\" href=\"#eprint/1\">¶</a></h3>\n<p>Writes <code>value</code> to standard <em>error</em> without a trailing newline. Use it for diagnostics, so a program&#39;s real output on standard out stays clean enough to pipe.</p>\n<p>Unlike <code>print</code>, standard error is never buffered — a message is on screen as soon as it is written.</p>\n<pre><code>eprint(<span class=\"tok-str\">&quot;warning: &quot;</span>)</code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to write</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"eprintln/1\">\n  <h3 class=\"fn-title\"><code>function eprintln(value)</code> <a class=\"anchor\" href=\"#eprintln/1\">¶</a></h3>\n<p><code>eprint</code> with a trailing newline.</p>\n<pre><code>eprintln(<span class=\"tok-str\">&quot;could not read config, using defaults&quot;</span>)</code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to write</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"get_env/1\">\n  <h3 class=\"fn-title\"><code>function get_env(name)</code> <a class=\"anchor\" href=\"#get_env/1\">¶</a></h3>\n<p>Looks up an environment variable by <code>name</code> and returns its <code><a href=\"#/String\">String</a></code> value, or <code>nil</code> if it is not set.</p>\n<pre><code>get_env(<span class=\"tok-str\">&quot;HOME&quot;</span>)        <span class=\"tok-com\"># == &quot;/home/andrew&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>name</code></dt>\n  <dd>the environment variable name to look up</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the variable&#39;s <code><a href=\"#/String\">String</a></code> value, or <code>nil</code> if unset</p>\n</div>\n<div class=\"fn-detail\" id=\"get_args/0\">\n  <h3 class=\"fn-title\"><code>function get_args()</code> <a class=\"anchor\" href=\"#get_args/0\">¶</a></h3>\n<p>Returns the program&#39;s command-line arguments — everything after the script path — as a <code><a href=\"#/List\">List</a></code> of <code><a href=\"#/String\">String</a></code>s. Empty when none were given.</p>\n<pre><code><span class=\"tok-com\"># ramos run app.rmo alice 42</span>\nget_args()             <span class=\"tok-com\"># == [&quot;alice&quot;, &quot;42&quot;]</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of the argument <code><a href=\"#/String\">String</a></code>s (empty when none were given)</p>\n</div>\n<div class=\"fn-detail\" id=\"get_arg/1\">\n  <h3 class=\"fn-title\"><code>function get_arg(index)</code> <a class=\"anchor\" href=\"#get_arg/1\">¶</a></h3>\n<p>Returns the command-line argument at zero-based <code>index</code>, or <code>nil</code> if there is no argument at that position. Negative indexes are not supported.</p>\n<pre><code><span class=\"tok-com\"># ramos run app.rmo alice 42</span>\nget_arg(<span class=\"tok-lit\">0</span>)             <span class=\"tok-com\"># == &quot;alice&quot;</span>\nget_arg(<span class=\"tok-lit\">9</span>)             <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>index</code></dt>\n  <dd>the zero-based position of the argument to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the argument <code><a href=\"#/String\">String</a></code> at <code>index</code>, or <code>nil</code> if there is none</p>\n</div>\n<div class=\"fn-detail\" id=\"sleep/1\">\n  <h3 class=\"fn-title\"><code>function sleep(ms)</code> <a class=\"anchor\" href=\"#sleep/1\">¶</a></h3>\n<p>Suspends the program for <code>ms</code> milliseconds, then returns <code>nil</code>. A value of <code>0</code> or less returns immediately.</p>\n<pre><code>sleep(<span class=\"tok-lit\">250</span>)             <span class=\"tok-com\"># pause for a quarter second</span></code></pre>\n<dl class=\"params\">\n  <dt><code>ms</code></dt>\n  <dd>how many milliseconds to sleep (<code>0</code> or less returns at once)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"exit/1\">\n  <h3 class=\"fn-title\"><code>function exit(code)</code> <a class=\"anchor\" href=\"#exit/1\">¶</a></h3>\n<p>Ends the program with <code>code</code> as its exit status — <code>0</code> for success, any other value for failure. Nothing after the call runs.</p>\n<p>Output written with <code>print</code> is flushed on the way out, so a program can print its result and exit in the next breath without losing it.</p>\n<pre><code>exit(<span class=\"tok-lit\">0</span>)                <span class=\"tok-com\"># success</span>\nexit(<span class=\"tok-lit\">1</span>)                <span class=\"tok-com\"># failure</span></code></pre>\n<dl class=\"params\">\n  <dt><code>code</code></dt>\n  <dd>the process exit status</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> never returns</p>\n</div>\n<div class=\"fn-detail\" id=\"now/0\">\n  <h3 class=\"fn-title\"><code>function now()</code> <a class=\"anchor\" href=\"#now/0\">¶</a></h3>\n<p>The wall-clock time as milliseconds since the Unix epoch.</p>\n<p>This is the clock to <em>timestamp</em> with. It can jump backwards when the system clock is adjusted, so measure elapsed time with <code>monotonic</code>.</p>\n<pre><code>started = now()</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> — milliseconds since 1970-01-01 UTC</p>\n</div>\n<div class=\"fn-detail\" id=\"monotonic/0\">\n  <h3 class=\"fn-title\"><code>function monotonic()</code> <a class=\"anchor\" href=\"#monotonic/0\">¶</a></h3>\n<p>Milliseconds from a fixed point in the program&#39;s life. The value only ever moves forward, which is what makes it the right clock for measuring how long something took.</p>\n<p>The origin is arbitrary, so a single reading means nothing on its own — only the difference between two does.</p>\n<pre><code>started = monotonic()\n<span class=\"tok-com\"># ... work ...</span>\nelapsed = monotonic() - started</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> — milliseconds since an arbitrary fixed point</p>\n</div>\n<div class=\"fn-detail\" id=\"random/0\">\n  <h3 class=\"fn-title\"><code>function random()</code> <a class=\"anchor\" href=\"#random/0\">¶</a></h3>\n<p>A random <code><a href=\"#/Float\">Float</a></code> in <code>[0.0, 1.0)</code> — zero is possible, one is not.</p>\n<p>Not cryptographic: this is for sampling, shuffling and test data, never for secrets or tokens.</p>\n<pre><code>chance = random()</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Float\">Float</a></code> in <code>[0.0, 1.0)</code></p>\n</div>\n<div class=\"fn-detail\" id=\"random_int/2\">\n  <h3 class=\"fn-title\"><code>function random_int(low, high)</code> <a class=\"anchor\" href=\"#random_int/2\">¶</a></h3>\n<p>A random <code><a href=\"#/Integer\">Integer</a></code> in <code>low..high</code>, <em></em>inclusive at both ends<em></em>. The bounds may be given either way round.</p>\n<p>Not cryptographic — see <code>random</code>.</p>\n<pre><code>roll = random_int(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">6</span>)</code></pre>\n<dl class=\"params\">\n  <dt><code>low</code></dt>\n  <dd>one end of the range</dd>\n  <dt><code>high</code></dt>\n  <dd>the other end of the range</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> between the two, inclusive</p>\n</div>\n<div class=\"fn-detail\" id=\"apply/2\">\n  <h3 class=\"fn-title\"><code>function apply(f, args)</code> <a class=\"anchor\" href=\"#apply/2\">¶</a></h3>\n<p>Calls the lambda <code>f</code> with <code>args</code>, a list of its arguments. Use it when the arity is not known where the call is written — otherwise call <code>f</code> directly.</p>\n<p>Like <code>native</code>, this function has no Ramos body: the interpreter intercepts the call, because running a lambda needs the evaluator and a native handler cannot reach back into it. Also like <code>native</code>, it is answered for the bare call <code>apply(...)</code>, not for <code><a href=\"#/Kernel:apply\">Kernel.apply(...)</a></code>.</p>\n<pre><code>apply(<span class=\"tok-kw\">do</span> x, y -&gt; x + y, [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>])   <span class=\"tok-com\"># == 3</span>\napply(<span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">42</span>, [])               <span class=\"tok-com\"># == 42</span></code></pre>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>the lambda to call</dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of the arguments to pass</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whatever <code>f</code> returns</p>\n</div>\n<div class=\"fn-detail\" id=\"module_apply/3\">\n  <h3 class=\"fn-title\"><code>function module_apply(m, fn_name, args)</code> <a class=\"anchor\" href=\"#module_apply/3\">¶</a></h3>\n<p>Resolves <code>fn_name</code> on module <code>m</code> and calls it with <code>args</code>, a list. <code><a href=\"#/Module:apply\">Module.apply</a></code> is the client-side name for this — prefer it.</p>\n<p>Enforces the same <code>helper</code> visibility a written <code>m.fn_name(...)</code> call would: a private function is reachable only from inside its own module, so this cannot reach around that rule from the outside.</p>\n<p>Like <code>apply</code>, this function has no Ramos body: the interpreter intercepts the call, since resolving <code>fn_name</code> and running it needs the evaluator. Also like <code>apply</code>, it is answered for the bare call <code>module_apply(...)</code>, not for <code><a href=\"#/Kernel:module_apply\">Kernel.module_apply(...)</a></code>.</p>\n<pre><code>module_apply(<span class=\"tok-type\">Integer</span>, <span class=\"tok-str\">&quot;abs&quot;</span>, [-<span class=\"tok-lit\">7</span>])   <span class=\"tok-com\"># == 7</span></code></pre>\n<dl class=\"params\">\n  <dt><code>m</code></dt>\n  <dd>the module to call a function on</dd>\n  <dt><code>fn_name</code></dt>\n  <dd>the function&#39;s name, as a <code><a href=\"#/String\">String</a></code></dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of the arguments to pass</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whatever the resolved function returns</p>\n</div>\n<div class=\"fn-detail\" id=\"start_actor/4\">\n  <h3 class=\"fn-title\"><code>function start_actor(id, actor_module, initial_state, config)</code> <a class=\"anchor\" href=\"#start_actor/4\">¶</a></h3>\n<p>Starts an actor under <code>id</code>, handled by <code>module</code> — which must implement the <code><a href=\"#/Actor\">Actor</a></code> trait — holding <code>initial_state</code>. <code>config</code> is fixed for the actor&#39;s lifetime and passed to every <code>call</code> and <code>cast</code>.</p>\n<p>The <code>id</code> is what identifies a running actor, so one module can back many of them, each with its own state. Starting the same <code>id</code> twice is an error.</p>\n<p>Like <code>apply</code>, this function has no Ramos body: the interpreter intercepts the call, because dispatching a message runs Ramos code and a native handler cannot reach back into the evaluator. Also like <code>apply</code>, it is answered for the bare call <code>start_actor(...)</code>, not <code><a href=\"#/Kernel:start_actor\">Kernel.start_actor(...)</a></code>.</p>\n<pre><code>start_actor(<span class=\"tok-lit\">:global</span>, <span class=\"tok-type\">Global</span>, {}, {})</code></pre>\n<dl class=\"params\">\n  <dt><code>id</code></dt>\n  <dd>the name to register the actor under, conventionally a symbol</dd>\n  <dt><code>actor_module</code></dt>\n  <dd>the module implementing <code><a href=\"#/Actor\">Actor</a></code></dd>\n  <dt><code>initial_state</code></dt>\n  <dd>the state the actor starts with</dd>\n  <dt><code>config</code></dt>\n  <dd>fixed configuration handed to every callback</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code></p>\n</div>\n<div class=\"fn-detail\" id=\"call_actor/4\">\n  <h3 class=\"fn-title\"><code>function call_actor(id, actor_module, op, args)</code> <a class=\"anchor\" href=\"#call_actor/4\">¶</a></h3>\n<p>Sends <code>op</code> to the actor registered under <code>id</code> and waits for the reply. The module&#39;s <code>call(f, args, state, config)</code> handles it and returns a <code>(reply, new_state)</code> tuple: the reply comes back here, and <code>new_state</code> is what the actor holds from then on.</p>\n<p>Any messages sent earlier with <code>cast_actor</code> are handled first, in the order they were sent, so a call always sees everything sent before it.</p>\n<p>Has no Ramos body, for the reason given on <code>start_actor</code>.</p>\n<pre><code>call_actor(<span class=\"tok-lit\">:global</span>, <span class=\"tok-type\">Global</span>, <span class=\"tok-lit\">:get</span>, [<span class=\"tok-str\">&quot;key&quot;</span>])</code></pre>\n<dl class=\"params\">\n  <dt><code>id</code></dt>\n  <dd>the actor&#39;s id, as given to <code>start_actor</code></dd>\n  <dt><code>actor_module</code></dt>\n  <dd>the module handling that actor</dd>\n  <dt><code>op</code></dt>\n  <dd>the operation, conventionally a symbol such as <code>:get</code></dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of arguments for the operation</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whatever the handler put in the reply position</p>\n</div>\n<div class=\"fn-detail\" id=\"cast_actor/4\">\n  <h3 class=\"fn-title\"><code>function cast_actor(id, actor_module, op, args)</code> <a class=\"anchor\" href=\"#cast_actor/4\">¶</a></h3>\n<p>Sends <code>op</code> to the actor registered under <code>id</code> <em></em>without<em></em> waiting for it to be handled, and returns <code>:ok</code> at once. The message goes on the actor&#39;s mailbox and runs later: before the next <code>call_actor</code> on that actor, or when the program ends, whichever comes first.</p>\n<p>The module&#39;s <code>cast(f, args, state, config)</code> handles it and returns only the new state — a cast has no reply to send anywhere. Unless the module defines its own, the <code><a href=\"#/Actor\">Actor</a></code> trait&#39;s default runs <code>call</code> and discards the reply.</p>\n<p>Because the handler runs later, a failure inside it surfaces later too: at the drain, after the statement that sent the message.</p>\n<p>Has no Ramos body, for the reason given on <code>start_actor</code>.</p>\n<pre><code>cast_actor(<span class=\"tok-lit\">:global</span>, <span class=\"tok-type\">Global</span>, <span class=\"tok-lit\">:clear</span>, [<span class=\"tok-str\">&quot;key&quot;</span>])</code></pre>\n<dl class=\"params\">\n  <dt><code>id</code></dt>\n  <dd>the actor&#39;s id, as given to <code>start_actor</code></dd>\n  <dt><code>actor_module</code></dt>\n  <dd>the module handling that actor</dd>\n  <dt><code>op</code></dt>\n  <dd>the operation, conventionally a symbol</dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of arguments for the operation</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code>, immediately</p>\n</div>\n<div class=\"fn-detail\" id=\"list_actors/0\">\n  <h3 class=\"fn-title\"><code>function list_actors()</code> <a class=\"anchor\" href=\"#list_actors/0\">¶</a></h3>\n<p>Every running actor as an <code>(id, module)</code> pair, sorted by id so the order is stable from one call to the next. The id is the value it was started with, and the module is the module itself — so a pair can be handed straight to <code>call_actor</code> or <code>cast_actor</code>.</p>\n<p>Has no Ramos body, for the reason given on <code>start_actor</code>.</p>\n<pre><code>list_actors()   <span class=\"tok-com\"># == []</span></code></pre>\n<pre><code>[(id, mod) | _] = list_actors()\ncall_actor(id, mod, <span class=\"tok-lit\">:ping</span>, [])</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code>(id, module)</code> tuples</p>\n</div>\n<div class=\"fn-detail\" id=\"actor_stats/1\">\n  <h3 class=\"fn-title\"><code>function actor_stats(id)</code> <a class=\"anchor\" href=\"#actor_stats/1\">¶</a></h3>\n<p>What is known about one running actor, as a <code><a href=\"#/Map\">Map</a></code>:</p>\n<ul><li><code>id</code>      — the id it was started with</li><li><code>module</code>  — the name of the module handling it, as a <code><a href=\"#/String\">String</a></code></li><li><code>calls</code>   — how many <code>call_actor</code> messages have been sent to it</li><li><code>casts</code>   — how many <code>cast_actor</code> messages have been sent to it</li><li><code>pending</code> — casts sent whose outcome has not been collected yet</li><li><code>alive</code>   — whether its thread is still running</li></ul>\n<p><code>calls</code> and <code>casts</code> count messages <em>sent</em>. A cast is asynchronous, so it may still be running: <code>pending</code> is the part not accounted for yet, and drops to zero once the handler has finished and its result been collected.</p>\n<p>Has no Ramos body, for the reason given on <code>start_actor</code>.</p>\n<pre><code>actor_stats(<span class=\"tok-lit\">:global</span>)</code></pre>\n<dl class=\"params\">\n  <dt><code>id</code></dt>\n  <dd>the actor&#39;s id, as given to <code>start_actor</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Map\">Map</a></code> describing the actor</p>\n</div>\n<div class=\"fn-detail\" id=\"is_actor_started/1\">\n  <h3 class=\"fn-title\"><code>function is_actor_started(id)</code> <a class=\"anchor\" href=\"#is_actor_started/1\">¶</a></h3>\n<p>Whether <code>id</code> names a running actor. False for an id that was never started, and for one that has been stopped.</p>\n<p>Has no Ramos body, for the reason given on <code>start_actor</code>.</p>\n<pre><code>is_actor_started(<span class=\"tok-lit\">:nobody</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>id</code></dt>\n  <dd>the id to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when an actor is running under <code>id</code></p>\n</div>\n<div class=\"fn-detail\" id=\"stop_actor/1\">\n  <h3 class=\"fn-title\"><code>function stop_actor(id)</code> <a class=\"anchor\" href=\"#stop_actor/1\">¶</a></h3>\n<p>Lets the actor finish everything already sent to it, then ends its thread and forgets the id. Nothing in flight is discarded: the stop is queued behind the messages already in the mailbox.</p>\n<p>Afterwards the id is free, so <code>start_actor</code> may reuse it. Sending to a stopped actor is an error, the same as sending to one never started.</p>\n<p>Has no Ramos body, for the reason given on <code>start_actor</code>.</p>\n<pre><code>stop_actor(<span class=\"tok-lit\">:global</span>)</code></pre>\n<dl class=\"params\">\n  <dt><code>id</code></dt>\n  <dd>the actor&#39;s id, as given to <code>start_actor</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>:ok</code></p>\n</div>\n<div class=\"fn-detail\" id=\"size/1\">\n  <h3 class=\"fn-title\"><code>function size(collection)</code> <a class=\"anchor\" href=\"#size/1\">¶</a></h3>\n<p>Returns the number of elements in <code>collection</code> — the length of a <code><a href=\"#/List\">List</a></code>, the arity of a <code><a href=\"#/Tuple\">Tuple</a></code>, or the number of key/value pairs in a <code><a href=\"#/Map\">Map</a></code>. Strings are not collections: <code>size</code> on a <code><a href=\"#/String\">String</a></code> is an error — use <code><a href=\"#/String:length\">String.length</a></code> instead.</p>\n<pre><code>size([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])            <span class=\"tok-com\"># == 3</span>\nsize((<span class=\"tok-lit\">:ok</span>, <span class=\"tok-lit\">42</span>))            <span class=\"tok-com\"># == 2</span>\nsize({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>})         <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>collection</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code>, <code><a href=\"#/Tuple\">Tuple</a></code>, or <code><a href=\"#/Map\">Map</a></code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Integer\">Integer</a></code> element count</p>\n</div>\n<div class=\"fn-detail\" id=\"at/2\">\n  <h3 class=\"fn-title\"><code>function at(collection, index)</code> <a class=\"anchor\" href=\"#at/2\">¶</a></h3>\n<p>Returns the element of <code>collection</code> at zero-based <code>index</code> (<code><a href=\"#/List\">List</a></code> or <code><a href=\"#/Tuple\">Tuple</a></code>), or <code>nil</code> if out of range. Negative indexes are not supported. Strings are not collections: <code>at</code> on a <code><a href=\"#/String\">String</a></code> is an error — use <code><a href=\"#/String:at\">String.at</a></code> instead.</p>\n<pre><code>at([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">1</span>)      <span class=\"tok-com\"># == 2</span>\nat((<span class=\"tok-lit\">:ok</span>, <span class=\"tok-lit\">42</span>), <span class=\"tok-lit\">1</span>)      <span class=\"tok-com\"># == 42</span>\nat([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">9</span>)      <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>collection</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> or <code><a href=\"#/Tuple\">Tuple</a></code></dd>\n  <dt><code>index</code></dt>\n  <dd>the zero-based position to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the element at <code>index</code>, or <code>nil</code> if out of range</p>\n</div>\n<div class=\"fn-detail\" id=\"to_string/1\">\n  <h3 class=\"fn-title\"><code>function to_string(value)</code> <a class=\"anchor\" href=\"#to_string/1\">¶</a></h3>\n<p>Converts <code>value</code> to its <code><a href=\"#/String\">String</a></code> representation — the same rendering that <code>print</code> uses. Numbers, symbols, tuples, lists, and maps all stringify; <code>nil</code> becomes <code>&quot;nil&quot;</code>.</p>\n<pre><code>to_string(<span class=\"tok-lit\">42</span>)          <span class=\"tok-com\"># == &quot;42&quot;</span>\nto_string(<span class=\"tok-lit\">:ok</span>)         <span class=\"tok-com\"># == &quot;:ok&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to render</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/String\">String</a></code> representation of <code>value</code></p>\n</div>\n<div class=\"fn-detail\" id=\"to_integer/1\">\n  <h3 class=\"fn-title\"><code>function to_integer(value)</code> <a class=\"anchor\" href=\"#to_integer/1\">¶</a></h3>\n<p>Converts <code>value</code> to an <code><a href=\"#/Integer\">Integer</a></code>. A <code><a href=\"#/String\">String</a></code> is parsed as a whole number; a <code><a href=\"#/Float\">Float</a></code> is truncated toward zero. Returns <code>nil</code> if <code>value</code> can&#39;t be parsed.</p>\n<pre><code>to_integer(<span class=\"tok-str\">&quot;42&quot;</span>)     <span class=\"tok-com\"># == 42</span>\nto_integer(<span class=\"tok-lit\">3.9</span>)      <span class=\"tok-com\"># == 3</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>a <code><a href=\"#/String\">String</a></code>, <code><a href=\"#/Float\">Float</a></code>, or <code><a href=\"#/Integer\">Integer</a></code> to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Integer\">Integer</a></code> value, or <code>nil</code> if it can&#39;t be parsed</p>\n</div>\n<div class=\"fn-detail\" id=\"to_float/1\">\n  <h3 class=\"fn-title\"><code>function to_float(value)</code> <a class=\"anchor\" href=\"#to_float/1\">¶</a></h3>\n<p>Converts <code>value</code> to a <code><a href=\"#/Float\">Float</a></code>. An <code><a href=\"#/Integer\">Integer</a></code> widens; a <code><a href=\"#/String\">String</a></code> is parsed as a decimal number. Returns <code>nil</code> if <code>value</code> can&#39;t be parsed.</p>\n<pre><code>to_float(<span class=\"tok-lit\">3</span>)          <span class=\"tok-com\"># == 3.0</span>\nto_float(<span class=\"tok-str\">&quot;3.14&quot;</span>)     <span class=\"tok-com\"># == 3.14</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>an <code><a href=\"#/Integer\">Integer</a></code>, <code><a href=\"#/String\">String</a></code>, or <code><a href=\"#/Float\">Float</a></code> to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Float\">Float</a></code> value, or <code>nil</code> if it can&#39;t be parsed</p>\n</div>\n<div class=\"fn-detail\" id=\"to_list/1\">\n  <h3 class=\"fn-title\"><code>function to_list(value)</code> <a class=\"anchor\" href=\"#to_list/1\">¶</a></h3>\n<p>Converts <code>value</code> to a <code><a href=\"#/List\">List</a></code>. A <code><a href=\"#/Tuple\">Tuple</a></code> yields its elements in order; a <code><a href=\"#/Map\">Map</a></code> yields its <em>values</em> (keys are dropped — reach for the <code><a href=\"#/Map\">Map</a></code> module when you need them). Already-list values are returned unchanged.</p>\n<pre><code>to_list((<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>))         <span class=\"tok-com\"># == [1, 2, 3]</span>\nto_list({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>})      <span class=\"tok-com\"># == [1, 2]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>a <code><a href=\"#/Tuple\">Tuple</a></code>, <code><a href=\"#/Map\">Map</a></code>, or <code><a href=\"#/List\">List</a></code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> — a tuple&#39;s elements, a map&#39;s values, or the list itself</p>\n</div>\n<div class=\"fn-detail\" id=\"current_stacktrace/0\">\n  <h3 class=\"fn-title\"><code>function current_stacktrace()</code> <a class=\"anchor\" href=\"#current_stacktrace/0\">¶</a></h3>\n<p>Returns the current call stacktrace — the chain of Ramos calls leading to this point — as a list of <code>(file, line)</code> tuples, most recent call first and the outermost caller (<code>main</code>, or wherever the trail runs out) last.</p>\n<p>A self-recursive <em></em>tail<em></em> call is not a frame of its own: it loops in constant space rather than recursing, so however many times it has looped, it still contributes exactly one entry — the line the loop is currently at.</p>\n<pre><code>print(current_stacktrace())</code></pre>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code>(file, line)</code> tuples, most recent call first</p>\n</div>\n<div class=\"fn-detail\" id=\"error/2\">\n  <h3 class=\"fn-title\"><code>function error(type, message)</code> <a class=\"anchor\" href=\"#error/2\">¶</a></h3>\n<p>Builds the standard error tuple: <code>(:error, (type, message, stacktrace))</code>. This — together with <code>exception</code>, its stackless twin — is the one way Ramos code reports failure: there is no <code>raise</code> and no <code>catch</code>. A fallible function returns <code>(:ok, value)</code> on success and whatever this builds on failure, and the caller reads either with <code>case</code> like any other value.</p>\n<p><code>type</code> is a lowercase symbol naming <em>what</em> went wrong — the part code matches on. <code>message</code> is for the human reading the output: a <code><a href=\"#/String\">String</a></code>, or a struct when a plain string is not enough detail. The stacktrace is <code>current_stacktrace()</code>, captured where <code>error</code> is called, so a failure carries where it came from without a caller having to ask for it.</p>\n<p>Reach for <code>error</code> where that trace earns its cost — a lower-level or unexpected failure worth tracing back. Ordinary business-rule failures (a bad request, a declined charge) happen too often and too predictably for a trace to be useful — use <code>exception</code> for those.</p>\n<pre><code>built = error(<span class=\"tok-lit\">:insufficient_funds</span>, <span class=\"tok-str\">&quot;balance is too low&quot;</span>)\n(<span class=\"tok-lit\">:error</span>, (type, message, _)) = built\n(type, message)   <span class=\"tok-com\"># == (:insufficient_funds, &quot;balance is too low&quot;)</span></code></pre>\n<dl class=\"params\">\n  <dt><code>type</code></dt>\n  <dd>a symbol naming what went wrong</dd>\n  <dt><code>message</code></dt>\n  <dd>a <code><a href=\"#/String\">String</a></code>, or a struct carrying further detail</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>(:error, (type, message, stacktrace))</code></p>\n</div>\n<div class=\"fn-detail\" id=\"exception/2\">\n  <h3 class=\"fn-title\"><code>function exception(type, message)</code> <a class=\"anchor\" href=\"#exception/2\">¶</a></h3>\n<p>Builds the business-logic error tuple: <code>(:error, (type, message))</code> — the same shape as <code>error</code>, minus the stacktrace.</p>\n<p>An expected failure — a bad request, a declined charge, a value that fails validation — happens in the ordinary run of a program and is handled a line or two away, so a stacktrace would only be noise. Use <code>exception</code> for those, and save <code>error</code> (which captures one) for a failure worth tracing back to where it happened.</p>\n<pre><code>exception(<span class=\"tok-lit\">:insufficient_funds</span>, <span class=\"tok-str\">&quot;balance is too low&quot;</span>)\n<span class=\"tok-com\"># == (:error, (:insufficient_funds, &quot;balance is too low&quot;))</span></code></pre>\n<dl class=\"params\">\n  <dt><code>type</code></dt>\n  <dd>a symbol naming what went wrong</dd>\n  <dt><code>message</code></dt>\n  <dd>a <code><a href=\"#/String\">String</a></code>, or a struct carrying further detail</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>(:error, (type, message))</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_integer/1\">\n  <h3 class=\"fn-title\"><code>function is_integer(value)</code> <a class=\"anchor\" href=\"#is_integer/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is an <code><a href=\"#/Integer\">Integer</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is an <code><a href=\"#/Integer\">Integer</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_float/1\">\n  <h3 class=\"fn-title\"><code>function is_float(value)</code> <a class=\"anchor\" href=\"#is_float/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Float\">Float</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/Float\">Float</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_string/1\">\n  <h3 class=\"fn-title\"><code>function is_string(value)</code> <a class=\"anchor\" href=\"#is_string/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/String\">String</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/String\">String</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_symbol/1\">\n  <h3 class=\"fn-title\"><code>function is_symbol(value)</code> <a class=\"anchor\" href=\"#is_symbol/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code>Symbol</code> (e.g. <code>:ok</code>).</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code>Symbol</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_bool/1\">\n  <h3 class=\"fn-title\"><code>function is_bool(value)</code> <a class=\"anchor\" href=\"#is_bool/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code>Bool</code> (<code>true</code> or <code>false</code>).</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code>Bool</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_nil/1\">\n  <h3 class=\"fn-title\"><code>function is_nil(value)</code> <a class=\"anchor\" href=\"#is_nil/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is <code>nil</code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is <code>nil</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_list/1\">\n  <h3 class=\"fn-title\"><code>function is_list(value)</code> <a class=\"anchor\" href=\"#is_list/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/List\">List</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/List\">List</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_tuple/1\">\n  <h3 class=\"fn-title\"><code>function is_tuple(value)</code> <a class=\"anchor\" href=\"#is_tuple/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Tuple\">Tuple</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/Tuple\">Tuple</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_map/1\">\n  <h3 class=\"fn-title\"><code>function is_map(value)</code> <a class=\"anchor\" href=\"#is_map/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Map\">Map</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/Map\">Map</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_module/1\">\n  <h3 class=\"fn-title\"><code>function is_module(value)</code> <a class=\"anchor\" href=\"#is_module/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Module\">Module</a></code>.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/Module\">Module</a></code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_struct/1\">\n  <h3 class=\"fn-title\"><code>function is_struct(value)</code> <a class=\"anchor\" href=\"#is_struct/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code><a href=\"#/Struct\">Struct</a></code> instance.</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code><a href=\"#/Struct\">Struct</a></code> instance, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"inspect/1\">\n  <h3 class=\"fn-title\"><code>function inspect(value)</code> <a class=\"anchor\" href=\"#inspect/1\">¶</a></h3>\n<p>The <em>debug</em> rendering of <code>value</code>: strings keep their quotes and symbols their colon, so a value reads back as the source that would build it. This is what the REPL prints and what error messages quote.</p>\n<p><code>to_string</code> gives the <em>display</em> form instead, which renders a string bare. The two differ only at the top level — a string nested inside a list is quoted either way.</p>\n<pre><code>inspect(<span class=\"tok-str\">&quot;hi&quot;</span>)        <span class=\"tok-com\"># == &quot;\\&quot;hi\\&quot;&quot;</span>\nto_string(<span class=\"tok-str\">&quot;hi&quot;</span>)      <span class=\"tok-com\"># == &quot;hi&quot;</span>\ninspect([<span class=\"tok-lit\">1</span>, <span class=\"tok-str\">&quot;two&quot;</span>])  <span class=\"tok-com\"># == &quot;[1, \\&quot;two\\&quot;]&quot;</span>\ninspect(<span class=\"tok-lit\">:ok</span>)         <span class=\"tok-com\"># == &quot;:ok&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to render</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/String\">String</a></code> — the debug rendering</p>\n</div>\n<div class=\"fn-detail\" id=\"type_of/1\">\n  <h3 class=\"fn-title\"><code>function type_of(value)</code> <a class=\"anchor\" href=\"#type_of/1\">¶</a></h3>\n<p>The module <code>value</code> belongs to — the one the README&#39;s Types table lists.</p>\n<p>A <code><a href=\"#/String\">String</a></code> rather than a <code>Symbol</code>: module names are CamelCase and symbol names are snake_case, so <code>:Integer</code> is not a symbol you could write, and a value you cannot write is a poor thing to compare against.</p>\n<pre><code>type_of(<span class=\"tok-lit\">42</span>)        <span class=\"tok-com\"># == &quot;Integer&quot;</span>\ntype_of(<span class=\"tok-lit\">3.14</span>)      <span class=\"tok-com\"># == &quot;Float&quot;</span>\ntype_of(<span class=\"tok-str\">&quot;hi&quot;</span>)      <span class=\"tok-com\"># == &quot;String&quot;</span>\ntype_of([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>])    <span class=\"tok-com\"># == &quot;List&quot;</span>\ntype_of(<span class=\"tok-lit\">nil</span>)       <span class=\"tok-com\"># == &quot;Nil&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/String\">String</a></code> naming the value&#39;s module</p>\n</div>\n<div class=\"fn-detail\" id=\"is_empty/1\">\n  <h3 class=\"fn-title\"><code>function is_empty(value)</code> <a class=\"anchor\" href=\"#is_empty/1\">¶</a></h3>\n<p>Whether <code>value</code> holds nothing — an empty list, map, tuple or string.</p>\n<p><code>size</code> deliberately rejects strings (they count code points, not elements), so this is the one question that spans all four.</p>\n<pre><code>is_empty([])       <span class=\"tok-com\"># == true</span>\nis_empty({})       <span class=\"tok-com\"># == true</span>\nis_empty(<span class=\"tok-str\">&quot;&quot;</span>)       <span class=\"tok-com\"># == true</span>\nis_empty([<span class=\"tok-lit\">1</span>])      <span class=\"tok-com\"># == false</span>\nis_empty(<span class=\"tok-str\">&quot;hi&quot;</span>)     <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>a list, map, tuple or string</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when it holds no elements or characters</p>\n</div>\n<div class=\"fn-detail\" id=\"is_truthy/1\">\n  <h3 class=\"fn-title\"><code>function is_truthy(value)</code> <a class=\"anchor\" href=\"#is_truthy/1\">¶</a></h3>\n<p>Whether <code>value</code> counts as true in a condition. Only <code>false</code> and <code>nil</code> are falsy — every other value, including <code>0</code>, <code>&quot;&quot;</code> and <code>[]</code>, is truthy. This is the rule <code>if</code>, <code>cond</code>, <code>and</code>, <code>or</code> and <code>not</code> all use.</p>\n<p>There is no native behind this one: a <code>cond</code> arm already tests a value for truthiness, so the function is that rule written down.</p>\n<pre><code>is_truthy(<span class=\"tok-lit\">0</span>)       <span class=\"tok-com\"># == true</span>\nis_truthy(<span class=\"tok-str\">&quot;&quot;</span>)      <span class=\"tok-com\"># == true</span>\nis_truthy([])      <span class=\"tok-com\"># == true</span>\nis_truthy(<span class=\"tok-lit\">nil</span>)     <span class=\"tok-com\"># == false</span>\nis_truthy(<span class=\"tok-lit\">false</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>value</code> is truthy, <code>false</code> when it is <code>false</code> or <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"is_lambda/1\">\n  <h3 class=\"fn-title\"><code>function is_lambda(value)</code> <a class=\"anchor\" href=\"#is_lambda/1\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is a <code>Lambda</code> (an anonymous function).</p>\n<dl class=\"params\">\n  <dt><code>value</code></dt>\n  <dd>the value to test</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is a <code>Lambda</code>, else <code>false</code></p>\n</div>\n</section>\n"},"List":{"title":"List — Ramos","heading":"List","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>List — operations on persistent, immutable lists.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>List — operations on persistent, immutable lists.</p>\n<p>Lists in Ramos are singly-linked and pattern matched against the empty list <code>[]</code> and the cons cell <code>[head | tail]</code>. Every function here returns a new list rather than mutating its input — values in Ramos are immutable.</p>\n<p>The module is built around a few small combinators — <code>map</code>, <code>filter</code>, <code>reduce</code> — from which most of the rest are derived. This keeps the implementation tiny and makes the functions compose cleanly through pipes:</p>\n<pre><code>[<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]\n| <span class=\"tok-type\">List</span>.filter(<span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> x -&gt; x * <span class=\"tok-lit\">10</span>)            <span class=\"tok-com\"># == [20, 40]</span></code></pre>\n<p>Conventions:</p>\n<ul><li>predicates use Ramos truthiness — only <code>false</code> and <code>nil</code> are falsy.</li><li>search functions that may miss return <code>nil</code>.</li><li>index arguments are zero-based.</li><li>a <code>_by</code> suffix (<code>sort_by</code>, <code>max_by</code>, <code>min_by</code>, <code>dedup_by</code>) means the plain version&#39;s comparison is replaced by a key <code>f</code> returns, not the elements themselves.</li><li>a function whose own recursive call is not the last thing it does (<code>[f(head)] ++ recurse(tail)</code>, say) is written instead as a private <code>_loop</code> helper carrying a reverse-order accumulator, so the recursion is tail-recursive and runs in constant native-stack space; the public function then <code>reverse</code>s the result once. <code>each</code>, <code>reduce</code>, <code>find</code>, <code>drop</code>, and the other functions whose own recursive call already <em>is</em> the last thing they do need no such helper.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#range/2\" class=\"fn-sig\"><code>function range(from, to)</code></a>\n    <span class=\"fn-summary\">The integers from <code>from</code> to <code>to</code>, inclusive at both ends, moving in whichever direction gets there: ascending when <code>from &lt;= to</code>, descending when <code>from &gt; to</code> — the one function for both counting up and counting down.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#range_loop/4\" class=\"fn-sig\"><code>helper range_loop(current, stop, step, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>range</code>, counting from <code>current</code> to <code>stop</code> by <code>step</code> and building the run in reverse for <code>range</code> to <code>reverse</code> back into order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#map/2\" class=\"fn-sig\"><code>function map(list, f)</code></a>\n    <span class=\"fn-summary\">Applies <code>f</code> to every element of <code>list</code>, returning a new list of the results. Preserves order and length.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#map_loop/3\" class=\"fn-sig\"><code>helper map_loop(list, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>map</code>, building the mapped results in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#each/2\" class=\"fn-sig\"><code>function each(list, f)</code></a>\n    <span class=\"fn-summary\">Calls <code>f</code> once per element, discarding the results. Intended for side effects (printing, etc.); returns <code>nil</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#with_index/1\" class=\"fn-sig\"><code>function with_index(list)</code></a>\n    <span class=\"fn-summary\">Pairs every element of <code>list</code> with its zero-based index.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#with_index_loop/3\" class=\"fn-sig\"><code>helper with_index_loop(list, index, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper carrying the current <code>index</code> and a reverse-order accumulator for <code>with_index</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#map_with_index/2\" class=\"fn-sig\"><code>function map_with_index(list, f)</code></a>\n    <span class=\"fn-summary\">Like <code>map</code>, but <code>f</code> also receives the zero-based index of the element it is applied to.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#map_with_index_loop/4\" class=\"fn-sig\"><code>helper map_with_index_loop(list, f, index, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper carrying the current <code>index</code> and a reverse-order accumulator for <code>map_with_index</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#each_with_index/2\" class=\"fn-sig\"><code>function each_with_index(list, f)</code></a>\n    <span class=\"fn-summary\">Like <code>each</code>, but <code>f</code> also receives the zero-based index of the element it is called with. Intended for side effects; returns <code>nil</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#each_with_index_loop/3\" class=\"fn-sig\"><code>helper each_with_index_loop(list, f, index)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper carrying the current <code>index</code> for <code>each_with_index</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#reduce/3\" class=\"fn-sig\"><code>function reduce(list, acc, f)</code></a>\n    <span class=\"fn-summary\">Folds <code>list</code> left-to-right, threading an accumulator. <code>f</code> receives the accumulator and the current element, and its return becomes the next accumulator. Returns the final accumulator.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sum/1\" class=\"fn-sig\"><code>function sum(list)</code></a>\n    <span class=\"fn-summary\">Returns the sum of the elements of <code>list</code> (numeric). The sum of <code>[]</code> is 0.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#product/1\" class=\"fn-sig\"><code>function product(list)</code></a>\n    <span class=\"fn-summary\">Returns the product of the elements of <code>list</code> (numeric). The product of <code>[]</code> is 1.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#count/2\" class=\"fn-sig\"><code>function count(list, f)</code></a>\n    <span class=\"fn-summary\">Returns the number of elements for which <code>f</code> is truthy.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#min/1\" class=\"fn-sig\"><code>function min(list)</code></a>\n    <span class=\"fn-summary\">Returns the smallest element of <code>list</code>, or <code>nil</code> if empty.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#max/1\" class=\"fn-sig\"><code>function max(list)</code></a>\n    <span class=\"fn-summary\">Returns the largest element of <code>list</code>, or <code>nil</code> if empty.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#min_of/2\" class=\"fn-sig\"><code>helper min_of(a, b)</code></a>\n    <span class=\"fn-summary\">(private) Two-argument minimum that tolerates a <code>nil</code> operand, used as the reducing step for <code>min</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#max_of/2\" class=\"fn-sig\"><code>helper max_of(a, b)</code></a>\n    <span class=\"fn-summary\">(private) Two-argument maximum that tolerates a <code>nil</code> operand, used as the reducing step for <code>max</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#filter/2\" class=\"fn-sig\"><code>function filter(list, f)</code></a>\n    <span class=\"fn-summary\">Returns a new list containing only the elements of <code>list</code> for which <code>f</code> is truthy, in their original order.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#filter_loop/3\" class=\"fn-sig\"><code>helper filter_loop(list, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>filter</code>, building the kept elements in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#reject/2\" class=\"fn-sig\"><code>function reject(list, f)</code></a>\n    <span class=\"fn-summary\">Inverse of <code>filter</code>: returns the elements for which <code>f</code> is falsy.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#partition/2\" class=\"fn-sig\"><code>function partition(list, f)</code></a>\n    <span class=\"fn-summary\">Splits <code>list</code> into a tuple of two lists: those for which <code>f</code> is truthy and those for which it is falsy. Relative order is preserved in each half.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#uniq/1\" class=\"fn-sig\"><code>function uniq(list)</code></a>\n    <span class=\"fn-summary\">Returns a new list with duplicate values removed, keeping the first occurrence of each. Equality uses <code>==</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#uniq_loop/2\" class=\"fn-sig\"><code>helper uniq_loop(list, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>uniq</code>, building the kept first-occurrences in reverse. Order in <code>acc</code> does not matter for the membership check, only for the caller&#39;s final <code>reverse</code>, so a value is kept the first time it is seen and skipped every time after.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#already_kept/2\" class=\"fn-sig\"><code>helper already_kept(acc, value)</code></a>\n    <span class=\"fn-summary\">(private) Whether <code>value</code> is already in <code>acc</code>, by <code>==</code> — <code>uniq_loop</code>&#39;s own membership check, since it cannot reach the public <code>contains</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#dedup/1\" class=\"fn-sig\"><code>function dedup(list)</code></a>\n    <span class=\"fn-summary\">Returns a new list with <em>consecutive</em> duplicate values collapsed to one, using <code>==</code>. Unlike <code>uniq</code>, a repeat elsewhere in the list (not adjacent to its first occurrence) is left alone.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#dedup_from/3\" class=\"fn-sig\"><code>helper dedup_from(list, last, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>dedup</code>, carrying the last element kept — so an arbitrarily long run collapses to its first element rather than comparing only adjacent pairs — and a reverse-order accumulator.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#dedup_by/2\" class=\"fn-sig\"><code>function dedup_by(list, f)</code></a>\n    <span class=\"fn-summary\">Like <code>dedup</code>, but two elements collapse when <code>f</code> returns the same key for both, rather than when they are themselves <code>==</code>. Keeps the first element of each run, like <code>dedup</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#dedup_by_from/4\" class=\"fn-sig\"><code>helper dedup_by_from(list, key, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>dedup_by</code>, carrying the key of the last element kept and a reverse-order accumulator.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#concat/1\" class=\"fn-sig\"><code>function concat(list)</code></a>\n    <span class=\"fn-summary\">Flattens one level: given a list of lists, concatenates them in order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#flatten/1\" class=\"fn-sig\"><code>function flatten(list)</code></a>\n    <span class=\"fn-summary\">Flattens <code>list</code> all the way down, however deeply its elements are themselves nested lists — unlike <code>concat</code>, which only drops one level. A non-list element is kept as-is.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#flatten_loop/2\" class=\"fn-sig\"><code>helper flatten_loop(list, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>flatten</code>: walks <code>list</code> one element at a time, so stack use is bounded by <code>list</code>&#39;s own length rather than its total (flattened) element count — a nested list still recurses into <code>flatten_loop</code> itself, bounded instead by its nesting depth. <code>flatten_loop(head, [])</code> is already <code>reverse(flatten(head))</code> — the public round trip through <code>flatten</code> would reverse it twice — so a nested list&#39;s own reversed accumulator prepends directly onto <code>acc</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#flat_map/2\" class=\"fn-sig\"><code>function flat_map(list, f)</code></a>\n    <span class=\"fn-summary\">Maps <code>f</code> over <code>list</code> and flattens the result one level. Equivalent to <code>concat(map(list, f))</code> — handy when <code>f</code> returns a list per element.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#zip/2\" class=\"fn-sig\"><code>function zip(a, b)</code></a>\n    <span class=\"fn-summary\">Pairs up <code>a</code> and <code>b</code> element-wise. Stops at the shorter list — a trailing tail on the longer one has nothing to pair with, so it is dropped.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#zip_loop/3\" class=\"fn-sig\"><code>helper zip_loop(a, b, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>zip</code>, building pairs in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#zip_with/3\" class=\"fn-sig\"><code>function zip_with(a, b, f)</code></a>\n    <span class=\"fn-summary\">Combines <code>a</code> and <code>b</code> element-wise through <code>f</code> instead of pairing them into tuples. Stops at the shorter list, same as <code>zip</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#zip_with_loop/4\" class=\"fn-sig\"><code>helper zip_with_loop(a, b, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>zip_with</code>, building results in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#unzip/1\" class=\"fn-sig\"><code>function unzip(pairs)</code></a>\n    <span class=\"fn-summary\">Splits a list of <code>(x, y)</code> tuples into a tuple of two lists — the inverse of <code>zip</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#reverse/1\" class=\"fn-sig\"><code>function reverse(list)</code></a>\n    <span class=\"fn-summary\">Returns a new list with the elements of <code>list</code> in reverse order.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#reverse_loop/2\" class=\"fn-sig\"><code>helper reverse_loop(list, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>reverse</code>. Also the reversal other helpers reach for when they need to flip an accumulator of their own — they cannot call the public <code>reverse</code> — so this stays a plain, two-arg loop rather than folding into <code>reverse</code> itself.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sort/1\" class=\"fn-sig\"><code>function sort(list)</code></a>\n    <span class=\"fn-summary\">Returns a new list with the elements of <code>list</code> sorted in ascending order using <code>&lt;</code>. Elements must be mutually comparable.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#sorted_insert/2\" class=\"fn-sig\"><code>helper sorted_insert(list, x)</code></a>\n    <span class=\"fn-summary\">(private) Inserts <code>x</code> into the already-sorted <code>list</code> at the first position where it is <code>&lt;=</code> the current head, preserving order. Named apart from the public <code>insert</code>, which places a value at an index.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#sorted_insert_loop/3\" class=\"fn-sig\"><code>helper sorted_insert_loop(list, x, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>sorted_insert</code>, carrying the already-passed prefix in reverse until it finds where <code>x</code> belongs.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sort_by/2\" class=\"fn-sig\"><code>function sort_by(list, f)</code></a>\n    <span class=\"fn-summary\">Returns a new list sorted in ascending order of <code>f(element)</code>, rather than the elements themselves — the way to sort a list of structs or tuples by one field.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#sorted_insert_by/3\" class=\"fn-sig\"><code>helper sorted_insert_by(list, x, f)</code></a>\n    <span class=\"fn-summary\">(private) Inserts <code>x</code> into the already-<code>sort_by</code>-sorted <code>list</code> at the first position where <code>f(x)</code> is <code>&lt;=</code> the current head&#39;s key.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#sorted_insert_by_loop/4\" class=\"fn-sig\"><code>helper sorted_insert_by_loop(list, x, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>sorted_insert_by</code>, carrying the already-passed prefix in reverse until it finds where <code>x</code> belongs.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#max_by/2\" class=\"fn-sig\"><code>function max_by(list, f)</code></a>\n    <span class=\"fn-summary\">Returns the element of <code>list</code> for which <code>f</code> is largest, or <code>nil</code> if empty. The way to find the largest of a list of structs or tuples by one field.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#min_by/2\" class=\"fn-sig\"><code>function min_by(list, f)</code></a>\n    <span class=\"fn-summary\">Returns the element of <code>list</code> for which <code>f</code> is smallest, or <code>nil</code> if empty.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#max_of_by/3\" class=\"fn-sig\"><code>helper max_of_by(a, b, f)</code></a>\n    <span class=\"fn-summary\">(private) Two-argument, key-based maximum tolerating a <code>nil</code> operand — the reducing step for <code>max_by</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#min_of_by/3\" class=\"fn-sig\"><code>helper min_of_by(a, b, f)</code></a>\n    <span class=\"fn-summary\">(private) Two-argument, key-based minimum tolerating a <code>nil</code> operand — the reducing step for <code>min_by</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#find/2\" class=\"fn-sig\"><code>function find(list, f)</code></a>\n    <span class=\"fn-summary\">Returns the first element for which <code>f</code> is truthy, or <code>nil</code> if none match.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#find_index/2\" class=\"fn-sig\"><code>function find_index(list, f)</code></a>\n    <span class=\"fn-summary\">Returns the zero-based index of the first element for which <code>f</code> is truthy, or <code>nil</code> if none match.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#find_index_loop/3\" class=\"fn-sig\"><code>helper find_index_loop(list, f, index)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper carrying the current <code>index</code> for <code>find_index</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#index_of/2\" class=\"fn-sig\"><code>function index_of(list, value)</code></a>\n    <span class=\"fn-summary\">Returns the zero-based index of the first element equal to <code>value</code> (using <code>==</code>), or <code>nil</code> if it is not present. <code>find_index</code> takes a predicate; this is the by-value shorthand, matching <code>contains</code>&#39;s by-value search.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#contains/2\" class=\"fn-sig\"><code>function contains(list, value)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>value</code> is present in <code>list</code> (using <code>==</code>), else <code>false</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#all/2\" class=\"fn-sig\"><code>function all(list, f)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>f</code> is truthy for every element (trivially <code>true</code> for an empty list). Short-circuits on the first falsy element.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#any/2\" class=\"fn-sig\"><code>function any(list, f)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>f</code> is truthy for at least one element (<code>false</code> for an empty list). Short-circuits on the first truthy element.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#first/1\" class=\"fn-sig\"><code>function first(list)</code></a>\n    <span class=\"fn-summary\">Returns the first element of <code>list</code>, or <code>nil</code> if empty.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#last/1\" class=\"fn-sig\"><code>function last(list)</code></a>\n    <span class=\"fn-summary\">Returns the last element of <code>list</code>, or <code>nil</code> if empty. Walks the whole list.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#insert/3\" class=\"fn-sig\"><code>function insert(list, index, value)</code></a>\n    <span class=\"fn-summary\">Returns a new list with <code>value</code> placed at zero-based <code>index</code>, moving the element currently there and everything after it one position along.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#delete/2\" class=\"fn-sig\"><code>function delete(list, value)</code></a>\n    <span class=\"fn-summary\">Returns a new list with the first element equal to <code>value</code> (using <code>==</code>) removed. Every later occurrence, if any, is left alone — unlike <code>filter(list, do x -&gt; x != value)</code>, which removes every match.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#delete_loop/3\" class=\"fn-sig\"><code>helper delete_loop(list, value, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>delete</code>, carrying the already-passed prefix in reverse until it finds <code>value</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#delete_at/2\" class=\"fn-sig\"><code>function delete_at(list, index)</code></a>\n    <span class=\"fn-summary\">Returns a new list with the element at zero-based <code>index</code> removed — the removal-side counterpart to <code>insert</code>. An out-of-range <code>index</code> (negative, or at or past the end) returns <code>list</code> unchanged.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#update_at/3\" class=\"fn-sig\"><code>function update_at(list, index, f)</code></a>\n    <span class=\"fn-summary\">Returns a new list with the element at zero-based <code>index</code> replaced by <code>f</code> applied to it. An out-of-range <code>index</code> (negative, or at or past the end) returns <code>list</code> unchanged — <code>f</code> is never called.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#update_at_loop/4\" class=\"fn-sig\"><code>helper update_at_loop(list, index, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>update_at</code>, carrying the already-passed prefix in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#take/2\" class=\"fn-sig\"><code>function take(list, n)</code></a>\n    <span class=\"fn-summary\">Returns the first <code>n</code> elements of <code>list</code>. Returns <code>[]</code> when <code>n</code> is zero or negative, or the whole list when <code>n</code> exceeds its length.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#take_loop/3\" class=\"fn-sig\"><code>helper take_loop(list, n, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>take</code>, building the kept prefix in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#drop/2\" class=\"fn-sig\"><code>function drop(list, n)</code></a>\n    <span class=\"fn-summary\">Returns <code>list</code> without its first <code>n</code> elements. When <code>n</code> is zero or negative, returns <code>list</code> unchanged; when <code>n</code> exceeds the length, returns <code>[]</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#take_while/2\" class=\"fn-sig\"><code>function take_while(list, f)</code></a>\n    <span class=\"fn-summary\">Returns the longest prefix of <code>list</code> for which <code>f</code> is truthy — a predicate-based cutoff where <code>take</code> is count-based. Stops at the first element for which <code>f</code> is falsy.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#take_while_loop/3\" class=\"fn-sig\"><code>helper take_while_loop(list, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>take_while</code>, building the kept prefix in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#drop_while/2\" class=\"fn-sig\"><code>function drop_while(list, f)</code></a>\n    <span class=\"fn-summary\">Returns <code>list</code> with its longest prefix for which <code>f</code> is truthy removed — a predicate-based cutoff where <code>drop</code> is count-based. Stops dropping at the first element for which <code>f</code> is falsy, and returns from there on.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#slice/3\" class=\"fn-sig\"><code>function slice(list, start, count)</code></a>\n    <span class=\"fn-summary\">Returns <code>count</code> elements of <code>list</code> starting at zero-based <code>start</code>. Implemented as <code>take(drop(list, start), count)</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#group_by/2\" class=\"fn-sig\"><code>function group_by(list, f)</code></a>\n    <span class=\"fn-summary\">Groups the elements of <code>list</code> by the key returned by <code>f</code>, returning a map from key to the list of elements sharing that key, in original order.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#group_by_loop/3\" class=\"fn-sig\"><code>helper group_by_loop(list, f, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>group_by</code>. Builds each key&#39;s group in reverse (each new element is cheap to prepend); <code>group_by</code> fixes the order of every group with a final <code>reverse</code> once grouping is done.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#chunk_every/2\" class=\"fn-sig\"><code>function chunk_every(list, n)</code></a>\n    <span class=\"fn-summary\">Splits <code>list</code> into consecutive chunks of <code>n</code> elements each; the final chunk may be shorter than <code>n</code>. <code>n</code> must be positive.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#chunk_every_loop/3\" class=\"fn-sig\"><code>helper chunk_every_loop(list, n, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>chunk_every</code>, building the chunks in reverse.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#split_chunk/3\" class=\"fn-sig\"><code>helper split_chunk(list, n, acc)</code></a>\n    <span class=\"fn-summary\">(private) Splits <code>list</code> into its first <code>n</code> elements and the rest, as <code>(chunk, rest)</code> — <code>chunk_every_loop</code>&#39;s own take/drop in one pass, since it cannot call the public <code>take</code> or <code>drop</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#join/2\" class=\"fn-sig\"><code>function join(list, sep)</code></a>\n    <span class=\"fn-summary\">Joins the elements of <code>list</code> into a single string separated by <code>sep</code>. Elements are assumed to be strings. The empty list yields <code>&quot;&quot;</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#join_loop/3\" class=\"fn-sig\"><code>helper join_loop(list, sep, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>join</code>, threading the string built so far as <code>acc</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#intersperse/2\" class=\"fn-sig\"><code>function intersperse(list, sep)</code></a>\n    <span class=\"fn-summary\">Returns a new list with <code>sep</code> inserted between every pair of adjacent elements. The generic counterpart to <code>join</code>, which does the same thing but for strings specifically, concatenated into one.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#intersperse_loop/3\" class=\"fn-sig\"><code>helper intersperse_loop(list, sep, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>intersperse</code>, building the result in reverse.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#sample/1\" class=\"fn-sig\"><code>function sample(list)</code></a>\n    <span class=\"fn-summary\">Returns one element of <code>list</code>, chosen uniformly at random, or <code>nil</code> when <code>list</code> is empty. Not cryptographic — see <code><a href=\"#/Kernel:random\">Kernel.random</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#shuffle/1\" class=\"fn-sig\"><code>function shuffle(list)</code></a>\n    <span class=\"fn-summary\">Returns a new list with the elements of <code>list</code> in random order. Every ordering is equally likely. Not cryptographic — see <code><a href=\"#/Kernel:random\">Kernel.random</a></code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#shuffle_loop/2\" class=\"fn-sig\"><code>helper shuffle_loop(list, acc)</code></a>\n    <span class=\"fn-summary\">(private) Tail-recursive helper for <code>shuffle</code>, building the shuffled result in reverse.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#remove_at/2\" class=\"fn-sig\"><code>helper remove_at(list, index)</code></a>\n    <span class=\"fn-summary\">(private) <code>delete_at</code>, for <code>shuffle_loop</code> since it cannot call the public <code>delete_at</code>. Built on the same <code>split_chunk</code> <code>chunk_every_loop</code> uses, dropping the one element right after the split.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"range/2\">\n  <h3 class=\"fn-title\"><code>function range(from, to)</code> <a class=\"anchor\" href=\"#range/2\">¶</a></h3>\n<p>The integers from <code>from</code> to <code>to</code>, inclusive at both ends, moving in whichever direction gets there: ascending when <code>from &lt;= to</code>, descending when <code>from &gt; to</code> — the one function for both counting up and counting down.</p>\n<pre><code><span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">5</span>)   <span class=\"tok-com\"># == [1, 2, 3, 4, 5]</span>\n<span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">5</span>, <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == [5, 4, 3, 2, 1]</span>\n<span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == [3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>from</code></dt>\n  <dd>the first integer</dd>\n  <dt><code>to</code></dt>\n  <dd>the last integer</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> counting from <code>from</code> to <code>to</code>, inclusive at both ends</p>\n</div>\n<div class=\"fn-detail\" id=\"range_loop/4\">\n  <h3 class=\"fn-title\"><code>helper range_loop(current, stop, step, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#range_loop/4\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>range</code>, counting from <code>current</code> to <code>stop</code> by <code>step</code> and building the run in reverse for <code>range</code> to <code>reverse</code> back into order.</p>\n</div>\n<div class=\"fn-detail\" id=\"map/2\">\n  <h3 class=\"fn-title\"><code>function map(list, f)</code> <a class=\"anchor\" href=\"#map/2\">¶</a></h3>\n<p>Applies <code>f</code> to every element of <code>list</code>, returning a new list of the results. Preserves order and length.</p>\n<pre><code><span class=\"tok-type\">List</span>.map([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-kw\">do</span> x -&gt; x * <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == [2, 4, 6]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to map over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of <code>f</code> applied to each element, same order and length</p>\n</div>\n<div class=\"fn-detail\" id=\"map_loop/3\">\n  <h3 class=\"fn-title\"><code>helper map_loop(list, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#map_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>map</code>, building the mapped results in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"each/2\">\n  <h3 class=\"fn-title\"><code>function each(list, f)</code> <a class=\"anchor\" href=\"#each/2\">¶</a></h3>\n<p>Calls <code>f</code> once per element, discarding the results. Intended for side effects (printing, etc.); returns <code>nil</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.each([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>], <span class=\"tok-kw\">do</span> x -&gt; print(x))</code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to iterate over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda called once per element for its side effect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"with_index/1\">\n  <h3 class=\"fn-title\"><code>function with_index(list)</code> <a class=\"anchor\" href=\"#with_index/1\">¶</a></h3>\n<p>Pairs every element of <code>list</code> with its zero-based index.</p>\n<pre><code><span class=\"tok-type\">List</span>.with_index([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>, <span class=\"tok-str\">&quot;c&quot;</span>])   <span class=\"tok-com\"># == [(&quot;a&quot;, 0), (&quot;b&quot;, 1), (&quot;c&quot;, 2)]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to index</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code>(element, index)</code> tuples, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"with_index_loop/3\">\n  <h3 class=\"fn-title\"><code>helper with_index_loop(list, index, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#with_index_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper carrying the current <code>index</code> and a reverse-order accumulator for <code>with_index</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"map_with_index/2\">\n  <h3 class=\"fn-title\"><code>function map_with_index(list, f)</code> <a class=\"anchor\" href=\"#map_with_index/2\">¶</a></h3>\n<p>Like <code>map</code>, but <code>f</code> also receives the zero-based index of the element it is applied to.</p>\n<pre><code><span class=\"tok-type\">List</span>.map_with_index([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>], <span class=\"tok-kw\">do</span> x, i -&gt; <span class=\"tok-str\">&quot;#{i}:#{x}&quot;</span>)   <span class=\"tok-com\"># == [&quot;0:a&quot;, &quot;1:b&quot;]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to map over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda <code>(element, index)</code> returning the replacement</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of <code>f</code> applied to each <code>(element, index)</code> pair</p>\n</div>\n<div class=\"fn-detail\" id=\"map_with_index_loop/4\">\n  <h3 class=\"fn-title\"><code>helper map_with_index_loop(list, f, index, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#map_with_index_loop/4\">¶</a></h3>\n<p>(private) Tail-recursive helper carrying the current <code>index</code> and a reverse-order accumulator for <code>map_with_index</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"each_with_index/2\">\n  <h3 class=\"fn-title\"><code>function each_with_index(list, f)</code> <a class=\"anchor\" href=\"#each_with_index/2\">¶</a></h3>\n<p>Like <code>each</code>, but <code>f</code> also receives the zero-based index of the element it is called with. Intended for side effects; returns <code>nil</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.each_with_index([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>], <span class=\"tok-kw\">do</span> x, i -&gt; print(<span class=\"tok-str\">&quot;#{i}:#{x}&quot;</span>))</code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to iterate over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda <code>(element, index)</code> called once per element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n<div class=\"fn-detail\" id=\"each_with_index_loop/3\">\n  <h3 class=\"fn-title\"><code>helper each_with_index_loop(list, f, index)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#each_with_index_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper carrying the current <code>index</code> for <code>each_with_index</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"reduce/3\">\n  <h3 class=\"fn-title\"><code>function reduce(list, acc, f)</code> <a class=\"anchor\" href=\"#reduce/3\">¶</a></h3>\n<p>Folds <code>list</code> left-to-right, threading an accumulator. <code>f</code> receives the accumulator and the current element, and its return becomes the next accumulator. Returns the final accumulator.</p>\n<pre><code><span class=\"tok-type\">List</span>.reduce([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">0</span>, <span class=\"tok-kw\">do</span> acc, x -&gt; acc + x)   <span class=\"tok-com\"># == 6</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to fold</dd>\n  <dt><code>acc</code></dt>\n  <dd>the initial accumulator value</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda <code>(acc, element)</code> returning the next accumulator</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the final accumulator after folding every element</p>\n</div>\n<div class=\"fn-detail\" id=\"sum/1\">\n  <h3 class=\"fn-title\"><code>function sum(list)</code> <a class=\"anchor\" href=\"#sum/1\">¶</a></h3>\n<p>Returns the sum of the elements of <code>list</code> (numeric). The sum of <code>[]</code> is 0.</p>\n<pre><code><span class=\"tok-type\">List</span>.sum([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])   <span class=\"tok-com\"># == 6</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list of numbers</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the numeric sum (<code>0</code> for an empty list)</p>\n</div>\n<div class=\"fn-detail\" id=\"product/1\">\n  <h3 class=\"fn-title\"><code>function product(list)</code> <a class=\"anchor\" href=\"#product/1\">¶</a></h3>\n<p>Returns the product of the elements of <code>list</code> (numeric). The product of <code>[]</code> is 1.</p>\n<pre><code><span class=\"tok-type\">List</span>.product([<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>])   <span class=\"tok-com\"># == 24</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list of numbers</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the numeric product (<code>1</code> for an empty list)</p>\n</div>\n<div class=\"fn-detail\" id=\"count/2\">\n  <h3 class=\"fn-title\"><code>function count(list, f)</code> <a class=\"anchor\" href=\"#count/2\">¶</a></h3>\n<p>Returns the number of elements for which <code>f</code> is truthy.</p>\n<pre><code><span class=\"tok-type\">List</span>.count([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to test</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Integer\">Integer</a></code> count of elements for which <code>f</code> is truthy</p>\n</div>\n<div class=\"fn-detail\" id=\"min/1\">\n  <h3 class=\"fn-title\"><code>function min(list)</code> <a class=\"anchor\" href=\"#min/1\">¶</a></h3>\n<p>Returns the smallest element of <code>list</code>, or <code>nil</code> if empty.</p>\n<pre><code><span class=\"tok-type\">List</span>.min([<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>])   <span class=\"tok-com\"># == 1</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list of mutually comparable elements</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the smallest element, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"max/1\">\n  <h3 class=\"fn-title\"><code>function max(list)</code> <a class=\"anchor\" href=\"#max/1\">¶</a></h3>\n<p>Returns the largest element of <code>list</code>, or <code>nil</code> if empty.</p>\n<pre><code><span class=\"tok-type\">List</span>.max([<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>])   <span class=\"tok-com\"># == 3</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list of mutually comparable elements</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the largest element, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"min_of/2\">\n  <h3 class=\"fn-title\"><code>helper min_of(a, b)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#min_of/2\">¶</a></h3>\n<p>(private) Two-argument minimum that tolerates a <code>nil</code> operand, used as the reducing step for <code>min</code>.</p>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the accumulated minimum so far (or <code>nil</code>)</dd>\n  <dt><code>b</code></dt>\n  <dd>the current element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the smaller of <code>a</code> and <code>b</code>, treating <code>nil</code> as absent</p>\n</div>\n<div class=\"fn-detail\" id=\"max_of/2\">\n  <h3 class=\"fn-title\"><code>helper max_of(a, b)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#max_of/2\">¶</a></h3>\n<p>(private) Two-argument maximum that tolerates a <code>nil</code> operand, used as the reducing step for <code>max</code>.</p>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the accumulated maximum so far (or <code>nil</code>)</dd>\n  <dt><code>b</code></dt>\n  <dd>the current element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the larger of <code>a</code> and <code>b</code>, treating <code>nil</code> as absent</p>\n</div>\n<div class=\"fn-detail\" id=\"filter/2\">\n  <h3 class=\"fn-title\"><code>function filter(list, f)</code> <a class=\"anchor\" href=\"#filter/2\">¶</a></h3>\n<p>Returns a new list containing only the elements of <code>list</code> for which <code>f</code> is truthy, in their original order.</p>\n<pre><code><span class=\"tok-type\">List</span>.filter([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == [2, 4]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to filter</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of the elements for which <code>f</code> is truthy, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"filter_loop/3\">\n  <h3 class=\"fn-title\"><code>helper filter_loop(list, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#filter_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>filter</code>, building the kept elements in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"reject/2\">\n  <h3 class=\"fn-title\"><code>function reject(list, f)</code> <a class=\"anchor\" href=\"#reject/2\">¶</a></h3>\n<p>Inverse of <code>filter</code>: returns the elements for which <code>f</code> is falsy.</p>\n<pre><code><span class=\"tok-type\">List</span>.reject([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == [1, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to filter</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of the elements for which <code>f</code> is falsy, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"partition/2\">\n  <h3 class=\"fn-title\"><code>function partition(list, f)</code> <a class=\"anchor\" href=\"#partition/2\">¶</a></h3>\n<p>Splits <code>list</code> into a tuple of two lists: those for which <code>f</code> is truthy and those for which it is falsy. Relative order is preserved in each half.</p>\n<pre><code><span class=\"tok-type\">List</span>.partition([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))\n<span class=\"tok-com\"># == ([2, 4], [1, 3])</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to split</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code>(truthy, falsy)</code> tuple of two lists, each in original order</p>\n</div>\n<div class=\"fn-detail\" id=\"uniq/1\">\n  <h3 class=\"fn-title\"><code>function uniq(list)</code> <a class=\"anchor\" href=\"#uniq/1\">¶</a></h3>\n<p>Returns a new list with duplicate values removed, keeping the first occurrence of each. Equality uses <code>==</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.uniq([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">2</span>])   <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to deduplicate</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list with duplicates removed, keeping first occurrences</p>\n</div>\n<div class=\"fn-detail\" id=\"uniq_loop/2\">\n  <h3 class=\"fn-title\"><code>helper uniq_loop(list, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#uniq_loop/2\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>uniq</code>, building the kept first-occurrences in reverse. Order in <code>acc</code> does not matter for the membership check, only for the caller&#39;s final <code>reverse</code>, so a value is kept the first time it is seen and skipped every time after.</p>\n</div>\n<div class=\"fn-detail\" id=\"already_kept/2\">\n  <h3 class=\"fn-title\"><code>helper already_kept(acc, value)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#already_kept/2\">¶</a></h3>\n<p>(private) Whether <code>value</code> is already in <code>acc</code>, by <code>==</code> — <code>uniq_loop</code>&#39;s own membership check, since it cannot reach the public <code>contains</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"dedup/1\">\n  <h3 class=\"fn-title\"><code>function dedup(list)</code> <a class=\"anchor\" href=\"#dedup/1\">¶</a></h3>\n<p>Returns a new list with <em>consecutive</em> duplicate values collapsed to one, using <code>==</code>. Unlike <code>uniq</code>, a repeat elsewhere in the list (not adjacent to its first occurrence) is left alone.</p>\n<pre><code><span class=\"tok-type\">List</span>.dedup([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>])   <span class=\"tok-com\"># == [1, 2, 1, 3]</span>\n<span class=\"tok-type\">List</span>.uniq([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>])    <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to collapse</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list with consecutive duplicates removed</p>\n</div>\n<div class=\"fn-detail\" id=\"dedup_from/3\">\n  <h3 class=\"fn-title\"><code>helper dedup_from(list, last, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#dedup_from/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>dedup</code>, carrying the last element kept — so an arbitrarily long run collapses to its first element rather than comparing only adjacent pairs — and a reverse-order accumulator.</p>\n</div>\n<div class=\"fn-detail\" id=\"dedup_by/2\">\n  <h3 class=\"fn-title\"><code>function dedup_by(list, f)</code> <a class=\"anchor\" href=\"#dedup_by/2\">¶</a></h3>\n<p>Like <code>dedup</code>, but two elements collapse when <code>f</code> returns the same key for both, rather than when they are themselves <code>==</code>. Keeps the first element of each run, like <code>dedup</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.dedup_by([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))\n<span class=\"tok-com\"># == [1, 2, 5]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to collapse</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda returning the key to compare consecutive elements by</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list with consecutive same-key elements removed</p>\n</div>\n<div class=\"fn-detail\" id=\"dedup_by_from/4\">\n  <h3 class=\"fn-title\"><code>helper dedup_by_from(list, key, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#dedup_by_from/4\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>dedup_by</code>, carrying the key of the last element kept and a reverse-order accumulator.</p>\n</div>\n<div class=\"fn-detail\" id=\"concat/1\">\n  <h3 class=\"fn-title\"><code>function concat(list)</code> <a class=\"anchor\" href=\"#concat/1\">¶</a></h3>\n<p>Flattens one level: given a list of lists, concatenates them in order.</p>\n<pre><code><span class=\"tok-type\">List</span>.concat([[<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>], [<span class=\"tok-lit\">3</span>], [<span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>]])   <span class=\"tok-com\"># == [1, 2, 3, 4, 5]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list whose elements are themselves lists</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a single list, the inner lists concatenated in order</p>\n</div>\n<div class=\"fn-detail\" id=\"flatten/1\">\n  <h3 class=\"fn-title\"><code>function flatten(list)</code> <a class=\"anchor\" href=\"#flatten/1\">¶</a></h3>\n<p>Flattens <code>list</code> all the way down, however deeply its elements are themselves nested lists — unlike <code>concat</code>, which only drops one level. A non-list element is kept as-is.</p>\n<pre><code><span class=\"tok-type\">List</span>.flatten([<span class=\"tok-lit\">1</span>, [<span class=\"tok-lit\">2</span>, [<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]], [<span class=\"tok-lit\">5</span>]])   <span class=\"tok-com\"># == [1, 2, 3, 4, 5]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list, possibly containing nested lists at any depth</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a single flat list of every non-list element, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"flatten_loop/2\">\n  <h3 class=\"fn-title\"><code>helper flatten_loop(list, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#flatten_loop/2\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>flatten</code>: walks <code>list</code> one element at a time, so stack use is bounded by <code>list</code>&#39;s own length rather than its total (flattened) element count — a nested list still recurses into <code>flatten_loop</code> itself, bounded instead by its nesting depth. <code>flatten_loop(head, [])</code> is already <code>reverse(flatten(head))</code> — the public round trip through <code>flatten</code> would reverse it twice — so a nested list&#39;s own reversed accumulator prepends directly onto <code>acc</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"flat_map/2\">\n  <h3 class=\"fn-title\"><code>function flat_map(list, f)</code> <a class=\"anchor\" href=\"#flat_map/2\">¶</a></h3>\n<p>Maps <code>f</code> over <code>list</code> and flattens the result one level. Equivalent to <code>concat(map(list, f))</code> — handy when <code>f</code> returns a list per element.</p>\n<pre><code><span class=\"tok-type\">List</span>.flat_map([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>], <span class=\"tok-kw\">do</span> x -&gt; [x, x])   <span class=\"tok-com\"># == [1, 1, 2, 2]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to map over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda returning a list for each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the mapped lists concatenated in order</p>\n</div>\n<div class=\"fn-detail\" id=\"zip/2\">\n  <h3 class=\"fn-title\"><code>function zip(a, b)</code> <a class=\"anchor\" href=\"#zip/2\">¶</a></h3>\n<p>Pairs up <code>a</code> and <code>b</code> element-wise. Stops at the shorter list — a trailing tail on the longer one has nothing to pair with, so it is dropped.</p>\n<pre><code><span class=\"tok-type\">List</span>.zip([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], [<span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">:b</span>])   <span class=\"tok-com\"># == [(1, :a), (2, :b)]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first list</dd>\n  <dt><code>b</code></dt>\n  <dd>the second list</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code>(a_element, b_element)</code> tuples, one per shared index</p>\n</div>\n<div class=\"fn-detail\" id=\"zip_loop/3\">\n  <h3 class=\"fn-title\"><code>helper zip_loop(a, b, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#zip_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>zip</code>, building pairs in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"zip_with/3\">\n  <h3 class=\"fn-title\"><code>function zip_with(a, b, f)</code> <a class=\"anchor\" href=\"#zip_with/3\">¶</a></h3>\n<p>Combines <code>a</code> and <code>b</code> element-wise through <code>f</code> instead of pairing them into tuples. Stops at the shorter list, same as <code>zip</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.zip_with([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], [<span class=\"tok-lit\">10</span>, <span class=\"tok-lit\">20</span>], <span class=\"tok-kw\">do</span> x, y -&gt; x + y)   <span class=\"tok-com\"># == [11, 22]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the first list</dd>\n  <dt><code>b</code></dt>\n  <dd>the second list</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda <code>(a_element, b_element)</code> returning the combined value</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code>f</code> applied to each pair, one per shared index</p>\n</div>\n<div class=\"fn-detail\" id=\"zip_with_loop/4\">\n  <h3 class=\"fn-title\"><code>helper zip_with_loop(a, b, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#zip_with_loop/4\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>zip_with</code>, building results in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"unzip/1\">\n  <h3 class=\"fn-title\"><code>function unzip(pairs)</code> <a class=\"anchor\" href=\"#unzip/1\">¶</a></h3>\n<p>Splits a list of <code>(x, y)</code> tuples into a tuple of two lists — the inverse of <code>zip</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.unzip([(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">:a</span>), (<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">:b</span>)])   <span class=\"tok-com\"># == ([1, 2], [:a, :b])</span></code></pre>\n<dl class=\"params\">\n  <dt><code>pairs</code></dt>\n  <dd>a list of two-element tuples</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code>(xs, ys)</code> tuple: the first elements, then the second elements</p>\n</div>\n<div class=\"fn-detail\" id=\"reverse/1\">\n  <h3 class=\"fn-title\"><code>function reverse(list)</code> <a class=\"anchor\" href=\"#reverse/1\">¶</a></h3>\n<p>Returns a new list with the elements of <code>list</code> in reverse order.</p>\n<pre><code><span class=\"tok-type\">List</span>.reverse([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])   <span class=\"tok-com\"># == [3, 2, 1]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to reverse</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list with the elements in reverse order</p>\n</div>\n<div class=\"fn-detail\" id=\"reverse_loop/2\">\n  <h3 class=\"fn-title\"><code>helper reverse_loop(list, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#reverse_loop/2\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>reverse</code>. Also the reversal other helpers reach for when they need to flip an accumulator of their own — they cannot call the public <code>reverse</code> — so this stays a plain, two-arg loop rather than folding into <code>reverse</code> itself.</p>\n</div>\n<div class=\"fn-detail\" id=\"sort/1\">\n  <h3 class=\"fn-title\"><code>function sort(list)</code> <a class=\"anchor\" href=\"#sort/1\">¶</a></h3>\n<p>Returns a new list with the elements of <code>list</code> sorted in ascending order using <code>&lt;</code>. Elements must be mutually comparable.</p>\n<pre><code><span class=\"tok-type\">List</span>.sort([<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>])   <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list of mutually comparable elements</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list sorted in ascending order</p>\n</div>\n<div class=\"fn-detail\" id=\"sorted_insert/2\">\n  <h3 class=\"fn-title\"><code>helper sorted_insert(list, x)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#sorted_insert/2\">¶</a></h3>\n<p>(private) Inserts <code>x</code> into the already-sorted <code>list</code> at the first position where it is <code>&lt;=</code> the current head, preserving order. Named apart from the public <code>insert</code>, which places a value at an index.</p>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>an already-sorted list</dd>\n  <dt><code>x</code></dt>\n  <dd>the element to insert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>list</code> with <code>x</code> inserted so the result stays sorted</p>\n</div>\n<div class=\"fn-detail\" id=\"sorted_insert_loop/3\">\n  <h3 class=\"fn-title\"><code>helper sorted_insert_loop(list, x, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#sorted_insert_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>sorted_insert</code>, carrying the already-passed prefix in reverse until it finds where <code>x</code> belongs.</p>\n</div>\n<div class=\"fn-detail\" id=\"sort_by/2\">\n  <h3 class=\"fn-title\"><code>function sort_by(list, f)</code> <a class=\"anchor\" href=\"#sort_by/2\">¶</a></h3>\n<p>Returns a new list sorted in ascending order of <code>f(element)</code>, rather than the elements themselves — the way to sort a list of structs or tuples by one field.</p>\n<pre><code><span class=\"tok-type\">List</span>.sort_by([<span class=\"tok-str\">&quot;ccc&quot;</span>, <span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;bb&quot;</span>], <span class=\"tok-kw\">do</span> s -&gt; <span class=\"tok-type\">String</span>.length(s))\n<span class=\"tok-com\"># == [&quot;a&quot;, &quot;bb&quot;, &quot;ccc&quot;]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to sort</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda returning the key to sort by</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list sorted in ascending order of <code>f</code>&#39;s result</p>\n</div>\n<div class=\"fn-detail\" id=\"sorted_insert_by/3\">\n  <h3 class=\"fn-title\"><code>helper sorted_insert_by(list, x, f)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#sorted_insert_by/3\">¶</a></h3>\n<p>(private) Inserts <code>x</code> into the already-<code>sort_by</code>-sorted <code>list</code> at the first position where <code>f(x)</code> is <code>&lt;=</code> the current head&#39;s key.</p>\n</div>\n<div class=\"fn-detail\" id=\"sorted_insert_by_loop/4\">\n  <h3 class=\"fn-title\"><code>helper sorted_insert_by_loop(list, x, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#sorted_insert_by_loop/4\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>sorted_insert_by</code>, carrying the already-passed prefix in reverse until it finds where <code>x</code> belongs.</p>\n</div>\n<div class=\"fn-detail\" id=\"max_by/2\">\n  <h3 class=\"fn-title\"><code>function max_by(list, f)</code> <a class=\"anchor\" href=\"#max_by/2\">¶</a></h3>\n<p>Returns the element of <code>list</code> for which <code>f</code> is largest, or <code>nil</code> if empty. The way to find the largest of a list of structs or tuples by one field.</p>\n<pre><code><span class=\"tok-type\">List</span>.max_by([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;ccc&quot;</span>, <span class=\"tok-str\">&quot;bb&quot;</span>], <span class=\"tok-kw\">do</span> s -&gt; <span class=\"tok-type\">String</span>.length(s))   <span class=\"tok-com\"># == &quot;ccc&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to search</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda returning the key to compare by</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the element with the largest <code>f</code>, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"min_by/2\">\n  <h3 class=\"fn-title\"><code>function min_by(list, f)</code> <a class=\"anchor\" href=\"#min_by/2\">¶</a></h3>\n<p>Returns the element of <code>list</code> for which <code>f</code> is smallest, or <code>nil</code> if empty.</p>\n<pre><code><span class=\"tok-type\">List</span>.min_by([<span class=\"tok-str\">&quot;ccc&quot;</span>, <span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;bb&quot;</span>], <span class=\"tok-kw\">do</span> s -&gt; <span class=\"tok-type\">String</span>.length(s))   <span class=\"tok-com\"># == &quot;a&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to search</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda returning the key to compare by</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the element with the smallest <code>f</code>, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"max_of_by/3\">\n  <h3 class=\"fn-title\"><code>helper max_of_by(a, b, f)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#max_of_by/3\">¶</a></h3>\n<p>(private) Two-argument, key-based maximum tolerating a <code>nil</code> operand — the reducing step for <code>max_by</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"min_of_by/3\">\n  <h3 class=\"fn-title\"><code>helper min_of_by(a, b, f)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#min_of_by/3\">¶</a></h3>\n<p>(private) Two-argument, key-based minimum tolerating a <code>nil</code> operand — the reducing step for <code>min_by</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"find/2\">\n  <h3 class=\"fn-title\"><code>function find(list, f)</code> <a class=\"anchor\" href=\"#find/2\">¶</a></h3>\n<p>Returns the first element for which <code>f</code> is truthy, or <code>nil</code> if none match.</p>\n<pre><code><span class=\"tok-type\">List</span>.find([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; x &gt; <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == 3</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to search</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the first element for which <code>f</code> is truthy, or <code>nil</code> if none match</p>\n</div>\n<div class=\"fn-detail\" id=\"find_index/2\">\n  <h3 class=\"fn-title\"><code>function find_index(list, f)</code> <a class=\"anchor\" href=\"#find_index/2\">¶</a></h3>\n<p>Returns the zero-based index of the first element for which <code>f</code> is truthy, or <code>nil</code> if none match.</p>\n<pre><code><span class=\"tok-type\">List</span>.find_index([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; x &gt; <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == 2</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to search</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the zero-based <code><a href=\"#/Integer\">Integer</a></code> index of the first match, or <code>nil</code> if none</p>\n</div>\n<div class=\"fn-detail\" id=\"find_index_loop/3\">\n  <h3 class=\"fn-title\"><code>helper find_index_loop(list, f, index)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#find_index_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper carrying the current <code>index</code> for <code>find_index</code>.</p>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the remaining list to search</dd>\n  <dt><code>f</code></dt>\n  <dd>the predicate do</dd>\n  <dt><code>index</code></dt>\n  <dd>the index of <code>list</code>&#39;s head in the original list</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the index of the first match, or <code>nil</code> if none match</p>\n</div>\n<div class=\"fn-detail\" id=\"index_of/2\">\n  <h3 class=\"fn-title\"><code>function index_of(list, value)</code> <a class=\"anchor\" href=\"#index_of/2\">¶</a></h3>\n<p>Returns the zero-based index of the first element equal to <code>value</code> (using <code>==</code>), or <code>nil</code> if it is not present. <code>find_index</code> takes a predicate; this is the by-value shorthand, matching <code>contains</code>&#39;s by-value search.</p>\n<pre><code><span class=\"tok-type\">List</span>.index_of([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == 2</span>\n<span class=\"tok-type\">List</span>.index_of([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-lit\">9</span>)   <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to search</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to look for (compared with <code>==</code>)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the zero-based <code><a href=\"#/Integer\">Integer</a></code> index of the first match, or <code>nil</code> if none</p>\n</div>\n<div class=\"fn-detail\" id=\"contains/2\">\n  <h3 class=\"fn-title\"><code>function contains(list, value)</code> <a class=\"anchor\" href=\"#contains/2\">¶</a></h3>\n<p>Returns <code>true</code> if <code>value</code> is present in <code>list</code> (using <code>==</code>), else <code>false</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.contains([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == true</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to search</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to look for (compared with <code>==</code>)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>value</code> is in <code>list</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"all/2\">\n  <h3 class=\"fn-title\"><code>function all(list, f)</code> <a class=\"anchor\" href=\"#all/2\">¶</a></h3>\n<p>Returns <code>true</code> if <code>f</code> is truthy for every element (trivially <code>true</code> for an empty list). Short-circuits on the first falsy element.</p>\n<pre><code><span class=\"tok-type\">List</span>.all([<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">6</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == true</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to test</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>f</code> is truthy for every element, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"any/2\">\n  <h3 class=\"fn-title\"><code>function any(list, f)</code> <a class=\"anchor\" href=\"#any/2\">¶</a></h3>\n<p>Returns <code>true</code> if <code>f</code> is truthy for at least one element (<code>false</code> for an empty list). Short-circuits on the first truthy element.</p>\n<pre><code><span class=\"tok-type\">List</span>.any([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">5</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to test</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>f</code> is truthy for at least one element, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"first/1\">\n  <h3 class=\"fn-title\"><code>function first(list)</code> <a class=\"anchor\" href=\"#first/1\">¶</a></h3>\n<p>Returns the first element of <code>list</code>, or <code>nil</code> if empty.</p>\n<pre><code><span class=\"tok-type\">List</span>.first([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])   <span class=\"tok-com\"># == 1</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to read from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the first element, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"last/1\">\n  <h3 class=\"fn-title\"><code>function last(list)</code> <a class=\"anchor\" href=\"#last/1\">¶</a></h3>\n<p>Returns the last element of <code>list</code>, or <code>nil</code> if empty. Walks the whole list.</p>\n<pre><code><span class=\"tok-type\">List</span>.last([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])   <span class=\"tok-com\"># == 3</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to read from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the last element, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"insert/3\">\n  <h3 class=\"fn-title\"><code>function insert(list, index, value)</code> <a class=\"anchor\" href=\"#insert/3\">¶</a></h3>\n<p>Returns a new list with <code>value</code> placed at zero-based <code>index</code>, moving the element currently there and everything after it one position along.</p>\n<p>An <code>index</code> at or past the end appends, and a negative one inserts at the front — the position is clamped rather than being an error, which matches <code>take</code>/<code>drop</code> treating out-of-range counts as the nearest sensible edge.</p>\n<pre><code><span class=\"tok-type\">List</span>.insert([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">:x</span>)    <span class=\"tok-com\"># == [1, :x, 2, 3]</span>\n<span class=\"tok-type\">List</span>.insert([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">:x</span>)    <span class=\"tok-com\"># == [:x, 1, 2, 3]</span>\n<span class=\"tok-type\">List</span>.insert([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">99</span>, <span class=\"tok-lit\">:x</span>)   <span class=\"tok-com\"># == [1, 2, 3, :x]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to insert into</dd>\n  <dt><code>index</code></dt>\n  <dd>the zero-based position the value should end up at</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to insert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list, one element longer</p>\n</div>\n<div class=\"fn-detail\" id=\"delete/2\">\n  <h3 class=\"fn-title\"><code>function delete(list, value)</code> <a class=\"anchor\" href=\"#delete/2\">¶</a></h3>\n<p>Returns a new list with the first element equal to <code>value</code> (using <code>==</code>) removed. Every later occurrence, if any, is left alone — unlike <code>filter(list, do x -&gt; x != value)</code>, which removes every match.</p>\n<pre><code><span class=\"tok-type\">List</span>.delete([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == [2, 1, 3]</span>\n<span class=\"tok-type\">List</span>.delete([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">9</span>)      <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to remove from</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to remove the first occurrence of</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list, one element shorter if <code>value</code> was found</p>\n</div>\n<div class=\"fn-detail\" id=\"delete_loop/3\">\n  <h3 class=\"fn-title\"><code>helper delete_loop(list, value, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#delete_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>delete</code>, carrying the already-passed prefix in reverse until it finds <code>value</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"delete_at/2\">\n  <h3 class=\"fn-title\"><code>function delete_at(list, index)</code> <a class=\"anchor\" href=\"#delete_at/2\">¶</a></h3>\n<p>Returns a new list with the element at zero-based <code>index</code> removed — the removal-side counterpart to <code>insert</code>. An out-of-range <code>index</code> (negative, or at or past the end) returns <code>list</code> unchanged.</p>\n<pre><code><span class=\"tok-type\">List</span>.delete_at([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">1</span>)    <span class=\"tok-com\"># == [1, 3]</span>\n<span class=\"tok-type\">List</span>.delete_at([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">99</span>)   <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to remove from</dd>\n  <dt><code>index</code></dt>\n  <dd>the zero-based position to remove</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list, one element shorter if <code>index</code> was in range</p>\n</div>\n<div class=\"fn-detail\" id=\"update_at/3\">\n  <h3 class=\"fn-title\"><code>function update_at(list, index, f)</code> <a class=\"anchor\" href=\"#update_at/3\">¶</a></h3>\n<p>Returns a new list with the element at zero-based <code>index</code> replaced by <code>f</code> applied to it. An out-of-range <code>index</code> (negative, or at or past the end) returns <code>list</code> unchanged — <code>f</code> is never called.</p>\n<pre><code><span class=\"tok-type\">List</span>.update_at([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">1</span>, <span class=\"tok-kw\">do</span> x -&gt; x * <span class=\"tok-lit\">10</span>)   <span class=\"tok-com\"># == [1, 20, 3]</span>\n<span class=\"tok-type\">List</span>.update_at([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">99</span>, <span class=\"tok-kw\">do</span> x -&gt; x * <span class=\"tok-lit\">10</span>)  <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to update</dd>\n  <dt><code>index</code></dt>\n  <dd>the zero-based position to replace</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda from the old value to the new one</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list with <code>index</code> updated, or <code>list</code> unchanged if out of range</p>\n</div>\n<div class=\"fn-detail\" id=\"update_at_loop/4\">\n  <h3 class=\"fn-title\"><code>helper update_at_loop(list, index, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#update_at_loop/4\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>update_at</code>, carrying the already-passed prefix in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"take/2\">\n  <h3 class=\"fn-title\"><code>function take(list, n)</code> <a class=\"anchor\" href=\"#take/2\">¶</a></h3>\n<p>Returns the first <code>n</code> elements of <code>list</code>. Returns <code>[]</code> when <code>n</code> is zero or negative, or the whole list when <code>n</code> exceeds its length.</p>\n<pre><code><span class=\"tok-type\">List</span>.take([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == [1, 2]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to take from</dd>\n  <dt><code>n</code></dt>\n  <dd>how many leading elements to keep</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of the first <code>n</code> elements (fewer if <code>list</code> is shorter)</p>\n</div>\n<div class=\"fn-detail\" id=\"take_loop/3\">\n  <h3 class=\"fn-title\"><code>helper take_loop(list, n, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#take_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>take</code>, building the kept prefix in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"drop/2\">\n  <h3 class=\"fn-title\"><code>function drop(list, n)</code> <a class=\"anchor\" href=\"#drop/2\">¶</a></h3>\n<p>Returns <code>list</code> without its first <code>n</code> elements. When <code>n</code> is zero or negative, returns <code>list</code> unchanged; when <code>n</code> exceeds the length, returns <code>[]</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.drop([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == [3, 4]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to drop from</dd>\n  <dt><code>n</code></dt>\n  <dd>how many leading elements to skip</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>list</code> without its first <code>n</code> elements</p>\n</div>\n<div class=\"fn-detail\" id=\"take_while/2\">\n  <h3 class=\"fn-title\"><code>function take_while(list, f)</code> <a class=\"anchor\" href=\"#take_while/2\">¶</a></h3>\n<p>Returns the longest prefix of <code>list</code> for which <code>f</code> is truthy — a predicate-based cutoff where <code>take</code> is count-based. Stops at the first element for which <code>f</code> is falsy.</p>\n<pre><code><span class=\"tok-type\">List</span>.take_while([<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>, <span class=\"tok-lit\">6</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == [2, 4]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to take from</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of the leading elements for which <code>f</code> is truthy</p>\n</div>\n<div class=\"fn-detail\" id=\"take_while_loop/3\">\n  <h3 class=\"fn-title\"><code>helper take_while_loop(list, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#take_while_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>take_while</code>, building the kept prefix in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"drop_while/2\">\n  <h3 class=\"fn-title\"><code>function drop_while(list, f)</code> <a class=\"anchor\" href=\"#drop_while/2\">¶</a></h3>\n<p>Returns <code>list</code> with its longest prefix for which <code>f</code> is truthy removed — a predicate-based cutoff where <code>drop</code> is count-based. Stops dropping at the first element for which <code>f</code> is falsy, and returns from there on.</p>\n<pre><code><span class=\"tok-type\">List</span>.drop_while([<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>, <span class=\"tok-lit\">6</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))   <span class=\"tok-com\"># == [5, 6]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to drop from</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda applied to each element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>list</code> without its leading elements for which <code>f</code> is truthy</p>\n</div>\n<div class=\"fn-detail\" id=\"slice/3\">\n  <h3 class=\"fn-title\"><code>function slice(list, start, count)</code> <a class=\"anchor\" href=\"#slice/3\">¶</a></h3>\n<p>Returns <code>count</code> elements of <code>list</code> starting at zero-based <code>start</code>. Implemented as <code>take(drop(list, start), count)</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.slice([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>], <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == [2, 3, 4]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to slice</dd>\n  <dt><code>start</code></dt>\n  <dd>the zero-based position to start at</dd>\n  <dt><code>count</code></dt>\n  <dd>how many elements to take</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of up to <code>count</code> elements from <code>start</code></p>\n</div>\n<div class=\"fn-detail\" id=\"group_by/2\">\n  <h3 class=\"fn-title\"><code>function group_by(list, f)</code> <a class=\"anchor\" href=\"#group_by/2\">¶</a></h3>\n<p>Groups the elements of <code>list</code> by the key returned by <code>f</code>, returning a map from key to the list of elements sharing that key, in original order.</p>\n<p><code>f</code> must return a valid map key — an <code><a href=\"#/Integer\">Integer</a></code>, <code><a href=\"#/String\">String</a></code> or <code>Symbol</code>. A predicate returning a boolean is the common slip; yield a symbol instead:</p>\n<pre><code><span class=\"tok-type\">List</span>.group_by([<span class=\"tok-lit\">:ok</span>, <span class=\"tok-lit\">:error</span>, <span class=\"tok-lit\">:ok</span>], <span class=\"tok-kw\">do</span> x -&gt; x)\n<span class=\"tok-com\"># == {ok: [:ok, :ok], error: [:error]}</span></code></pre>\n<pre><code>parity = <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-kw\">cond</span>\n  <span class=\"tok-type\">Integer</span>.is_even(x) -&gt; <span class=\"tok-lit\">:even</span>\n  <span class=\"tok-lit\">true</span> -&gt; <span class=\"tok-lit\">:odd</span>\n<span class=\"tok-type\">List</span>.group_by([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], parity)\n<span class=\"tok-com\"># == {odd: [1, 3], even: [2, 4]}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to group</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda returning the grouping key for an element</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Map\">Map</a></code> from key to the list of elements with that key, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"group_by_loop/3\">\n  <h3 class=\"fn-title\"><code>helper group_by_loop(list, f, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#group_by_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>group_by</code>. Builds each key&#39;s group in reverse (each new element is cheap to prepend); <code>group_by</code> fixes the order of every group with a final <code>reverse</code> once grouping is done.</p>\n</div>\n<div class=\"fn-detail\" id=\"chunk_every/2\">\n  <h3 class=\"fn-title\"><code>function chunk_every(list, n)</code> <a class=\"anchor\" href=\"#chunk_every/2\">¶</a></h3>\n<p>Splits <code>list</code> into consecutive chunks of <code>n</code> elements each; the final chunk may be shorter than <code>n</code>. <code>n</code> must be positive.</p>\n<pre><code><span class=\"tok-type\">List</span>.chunk_every([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>], <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == [[1, 2], [3, 4], [5]]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to chunk</dd>\n  <dt><code>n</code></dt>\n  <dd>the (positive) chunk size</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a list of chunks, the last possibly shorter than <code>n</code></p>\n</div>\n<div class=\"fn-detail\" id=\"chunk_every_loop/3\">\n  <h3 class=\"fn-title\"><code>helper chunk_every_loop(list, n, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#chunk_every_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>chunk_every</code>, building the chunks in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"split_chunk/3\">\n  <h3 class=\"fn-title\"><code>helper split_chunk(list, n, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#split_chunk/3\">¶</a></h3>\n<p>(private) Splits <code>list</code> into its first <code>n</code> elements and the rest, as <code>(chunk, rest)</code> — <code>chunk_every_loop</code>&#39;s own take/drop in one pass, since it cannot call the public <code>take</code> or <code>drop</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"join/2\">\n  <h3 class=\"fn-title\"><code>function join(list, sep)</code> <a class=\"anchor\" href=\"#join/2\">¶</a></h3>\n<p>Joins the elements of <code>list</code> into a single string separated by <code>sep</code>. Elements are assumed to be strings. The empty list yields <code>&quot;&quot;</code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.join([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>, <span class=\"tok-str\">&quot;c&quot;</span>], <span class=\"tok-str\">&quot;, &quot;</span>)   <span class=\"tok-com\"># == &quot;a, b, c&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>a list of strings to join</dd>\n  <dt><code>sep</code></dt>\n  <dd>the separator string placed between elements</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a single <code><a href=\"#/String\">String</a></code> of the elements joined by <code>sep</code> (<code>&quot;&quot;</code> when empty)</p>\n</div>\n<div class=\"fn-detail\" id=\"join_loop/3\">\n  <h3 class=\"fn-title\"><code>helper join_loop(list, sep, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#join_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>join</code>, threading the string built so far as <code>acc</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"intersperse/2\">\n  <h3 class=\"fn-title\"><code>function intersperse(list, sep)</code> <a class=\"anchor\" href=\"#intersperse/2\">¶</a></h3>\n<p>Returns a new list with <code>sep</code> inserted between every pair of adjacent elements. The generic counterpart to <code>join</code>, which does the same thing but for strings specifically, concatenated into one.</p>\n<pre><code><span class=\"tok-type\">List</span>.intersperse([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-lit\">0</span>)   <span class=\"tok-com\"># == [1, 0, 2, 0, 3]</span>\n<span class=\"tok-type\">List</span>.intersperse([<span class=\"tok-lit\">1</span>], <span class=\"tok-lit\">0</span>)         <span class=\"tok-com\"># == [1]</span>\n<span class=\"tok-type\">List</span>.intersperse([], <span class=\"tok-lit\">0</span>)          <span class=\"tok-com\"># == []</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to intersperse</dd>\n  <dt><code>sep</code></dt>\n  <dd>the value to insert between elements</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list with <code>sep</code> between every original pair of elements</p>\n</div>\n<div class=\"fn-detail\" id=\"intersperse_loop/3\">\n  <h3 class=\"fn-title\"><code>helper intersperse_loop(list, sep, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#intersperse_loop/3\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>intersperse</code>, building the result in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"sample/1\">\n  <h3 class=\"fn-title\"><code>function sample(list)</code> <a class=\"anchor\" href=\"#sample/1\">¶</a></h3>\n<p>Returns one element of <code>list</code>, chosen uniformly at random, or <code>nil</code> when <code>list</code> is empty. Not cryptographic — see <code><a href=\"#/Kernel:random\">Kernel.random</a></code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.sample([<span class=\"tok-lit\">:only</span>])   <span class=\"tok-com\"># == :only</span>\n<span class=\"tok-type\">List</span>.sample([])        <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to sample from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a randomly chosen element, or <code>nil</code> if <code>list</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"shuffle/1\">\n  <h3 class=\"fn-title\"><code>function shuffle(list)</code> <a class=\"anchor\" href=\"#shuffle/1\">¶</a></h3>\n<p>Returns a new list with the elements of <code>list</code> in random order. Every ordering is equally likely. Not cryptographic — see <code><a href=\"#/Kernel:random\">Kernel.random</a></code>.</p>\n<pre><code><span class=\"tok-type\">List</span>.shuffle([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])\n| <span class=\"tok-type\">List</span>.sort()   <span class=\"tok-com\"># == [1, 2, 3]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to shuffle</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list, <code>list</code>&#39;s elements in random order</p>\n</div>\n<div class=\"fn-detail\" id=\"shuffle_loop/2\">\n  <h3 class=\"fn-title\"><code>helper shuffle_loop(list, acc)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#shuffle_loop/2\">¶</a></h3>\n<p>(private) Tail-recursive helper for <code>shuffle</code>, building the shuffled result in reverse.</p>\n</div>\n<div class=\"fn-detail\" id=\"remove_at/2\">\n  <h3 class=\"fn-title\"><code>helper remove_at(list, index)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#remove_at/2\">¶</a></h3>\n<p>(private) <code>delete_at</code>, for <code>shuffle_loop</code> since it cannot call the public <code>delete_at</code>. Built on the same <code>split_chunk</code> <code>chunk_every_loop</code> uses, dropping the one element right after the split.</p>\n</div>\n</section>\n"},"Map":{"title":"Map — Ramos","heading":"Map","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Map — operations on key/value maps.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Map — operations on key/value maps.</p>\n<p><code>{name: &quot;andrew&quot;}</code> is a <code><a href=\"#/Map\">Map</a></code>. A key is an <code><a href=\"#/Integer\">Integer</a></code>, a <code><a href=\"#/String\">String</a></code> or a <code>Symbol</code> — nothing else, so every map can be written as a literal: <code>{name: 1}</code> (the bare-name shorthand for the symbol <code>:name</code>), <code>{&quot;host&quot;: 1}</code> and <code>{8080: 1}</code>. Putting a key of any other type is an error.</p>\n<p>Maps are immutable and insertion-ordered: <code>put</code> on an existing key replaces its value in place, a new key is appended, and every operation returns a new map rather than changing the one it was given.</p>\n<pre><code>config = {host: <span class=\"tok-str\">&quot;localhost&quot;</span>, port: <span class=\"tok-lit\">8080</span>}\n<span class=\"tok-type\">Map</span>.get(config, <span class=\"tok-lit\">:host</span>, <span class=\"tok-str\">&quot;?&quot;</span>)   <span class=\"tok-com\"># == &quot;localhost&quot;</span></code></pre>\n<p>The basics — <code>put</code>, <code>get</code>, <code>delete</code>, <code>has_key</code>, <code>keys</code>, <code>entries</code>, <code>from_list</code> — are host primitives. Everything that takes a lambda is written in Ramos on top of them, because a native cannot call back into the interpreter to run one.</p>\n<p>Kernel already covers the rest, so this module does not repeat it: <code>size(m)</code> counts the pairs and <code>to_list(m)</code> gives the values (also <code>values</code>, below, which reads as <code><a href=\"#/Map\">Map</a></code>&#39;s own the way <code>keys</code> does). For the pairs themselves, use <code>entries</code>.</p>\n<p>Conventions:</p>\n<ul><li><code>get</code> takes the value to fall back to, so a miss is never a surprise.</li><li>key types never collide: <code>{a: 1}</code> and <code>{&quot;a&quot;: 1}</code> are different maps.</li><li>a predicate always takes the <code>(key, value)</code> pair and comes last: <code>filter</code> and <code>reject</code> screen the whole map with one.</li><li>merging is the <code>++</code> operator, with the right map winning a clash; reach for <code>merge_with</code> when a clash should combine instead of overwrite.</li><li>equality ignores order: <code>{a: 1, b: 2} == {b: 2, a: 1}</code> is <code>true</code>.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#entries/1\" class=\"fn-sig\"><code>function entries(map)</code></a>\n    <span class=\"fn-summary\">The map&#39;s <code>(key, value)</code> pairs, in insertion order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#put/3\" class=\"fn-sig\"><code>function put(map, key, value)</code></a>\n    <span class=\"fn-summary\">A new map with <code>key</code> set to <code>value</code>. An existing key keeps its position; a new one is appended. The key must be an <code><a href=\"#/Integer\">Integer</a></code>, <code><a href=\"#/String\">String</a></code> or <code>Symbol</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#get/3\" class=\"fn-sig\"><code>function get(map, key, default)</code></a>\n    <span class=\"fn-summary\">The value stored under <code>key</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#has_key/2\" class=\"fn-sig\"><code>function has_key(map, key)</code></a>\n    <span class=\"fn-summary\">Whether <code>map</code> holds <code>key</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#keys/1\" class=\"fn-sig\"><code>function keys(map)</code></a>\n    <span class=\"fn-summary\">The map&#39;s keys, in insertion order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#values/1\" class=\"fn-sig\"><code>function values(map)</code></a>\n    <span class=\"fn-summary\">The map&#39;s values, in insertion order — the counterpart to <code>keys</code>. <code><a href=\"#/Kernel:to_list\">Kernel.to_list(map)</a></code> gives the same list; this is the name that reads as <code><a href=\"#/Map\">Map</a></code>&#39;s own, the way <code>keys</code> does.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#delete/2\" class=\"fn-sig\"><code>function delete(map, key)</code></a>\n    <span class=\"fn-summary\">A new map without <code>key</code>. Removing a key that is not there is not an error.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#update/3\" class=\"fn-sig\"><code>function update(map, key, f)</code></a>\n    <span class=\"fn-summary\">A new map with <code>key</code>&#39;s value replaced by <code>f</code> applied to it. A key that is not there is left alone, so <code>update</code> never adds one.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#put_new/3\" class=\"fn-sig\"><code>function put_new(map, key, value)</code></a>\n    <span class=\"fn-summary\">A new map with <code>key</code> set to <code>value</code>, but only when <code>key</code> is not already there — the insert-only counterpart to <code>update</code>, which only touches a key that already exists. An existing key is left exactly as it was.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#merge_with/3\" class=\"fn-sig\"><code>function merge_with(a, b, f)</code></a>\n    <span class=\"fn-summary\">Merges <code>a</code> and <code>b</code> like <code>++</code>, except a key present in both is resolved by <code>f(a_value, b_value)</code> instead of <code>b</code>&#39;s value silently winning. A key present in only one map keeps its value untouched.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#merge_pair/3\" class=\"fn-sig\"><code>helper merge_pair(acc, pair, f)</code></a>\n    <span class=\"fn-summary\">(private) One step of <code>merge_with</code>&#39;s fold: puts <code>pair</code>&#39;s key into <code>acc</code>, combining with the existing value through <code>f</code> when the key already exists there. Goes through <code>native</code> directly rather than the public <code>put</code>/<code>get</code>/<code>has_key</code>, which a helper cannot call.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#filter/2\" class=\"fn-sig\"><code>function filter(map, f)</code></a>\n    <span class=\"fn-summary\">The pairs for which <code>f</code> is truthy, in order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#reject/2\" class=\"fn-sig\"><code>function reject(map, f)</code></a>\n    <span class=\"fn-summary\">Inverse of <code>filter</code>: the pairs for which <code>f</code> is falsy.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#apply_pair/2\" class=\"fn-sig\"><code>helper apply_pair(f, pair)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#map_values/2\" class=\"fn-sig\"><code>function map_values(map, f)</code></a>\n    <span class=\"fn-summary\">A new map with each value replaced by <code>f</code> applied to it. Keys are kept.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#map_keys/2\" class=\"fn-sig\"><code>function map_keys(map, f)</code></a>\n    <span class=\"fn-summary\">A new map with each key replaced by <code>f</code> applied to it. Values are kept; if <code>f</code> maps two keys onto one, the later pair wins.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#map_entries/2\" class=\"fn-sig\"><code>function map_entries(map, f)</code></a>\n    <span class=\"fn-summary\">A new map built by replacing each pair with the <code>(key, value)</code> tuple <code>f</code> returns for it.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_list/1\" class=\"fn-sig\"><code>function from_list(pairs)</code></a>\n    <span class=\"fn-summary\">A map built from a list of <code>(key, value)</code> tuples, in order. A repeated key keeps its first position and takes its last value.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"entries/1\">\n  <h3 class=\"fn-title\"><code>function entries(map)</code> <a class=\"anchor\" href=\"#entries/1\">¶</a></h3>\n<p>The map&#39;s <code>(key, value)</code> pairs, in insertion order.</p>\n<pre><code><span class=\"tok-type\">Map</span>.entries({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>})   <span class=\"tok-com\"># == [(:a, 1), (:b, 2)]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code>(key, value)</code> tuples, in insertion order</p>\n</div>\n<div class=\"fn-detail\" id=\"put/3\">\n  <h3 class=\"fn-title\"><code>function put(map, key, value)</code> <a class=\"anchor\" href=\"#put/3\">¶</a></h3>\n<p>A new map with <code>key</code> set to <code>value</code>. An existing key keeps its position; a new one is appended. The key must be an <code><a href=\"#/Integer\">Integer</a></code>, <code><a href=\"#/String\">String</a></code> or <code>Symbol</code>.</p>\n<pre><code><span class=\"tok-type\">Map</span>.put({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:b</span>, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == {a: 1, b: 2}</span>\n<span class=\"tok-type\">Map</span>.put({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">9</span>)   <span class=\"tok-com\"># == {a: 9}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to build on</dd>\n  <dt><code>key</code></dt>\n  <dd>the key to set</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to store</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> with <code>key</code> set to <code>value</code></p>\n</div>\n<div class=\"fn-detail\" id=\"get/3\">\n  <h3 class=\"fn-title\"><code>function get(map, key, default)</code> <a class=\"anchor\" href=\"#get/3\">¶</a></h3>\n<p>The value stored under <code>key</code>.</p>\n<pre><code><span class=\"tok-type\">Map</span>.get({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">0</span>)   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Map</span>.get({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:b</span>, <span class=\"tok-lit\">0</span>)   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to read</dd>\n  <dt><code>key</code></dt>\n  <dd>the key to look up</dd>\n  <dt><code>default</code></dt>\n  <dd>what to return when the map has no such key</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the value under <code>key</code>, or <code>default</code> when there is none</p>\n</div>\n<div class=\"fn-detail\" id=\"has_key/2\">\n  <h3 class=\"fn-title\"><code>function has_key(map, key)</code> <a class=\"anchor\" href=\"#has_key/2\">¶</a></h3>\n<p>Whether <code>map</code> holds <code>key</code>.</p>\n<pre><code><span class=\"tok-type\">Map</span>.has_key({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:a</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Map</span>.has_key({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:b</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to test</dd>\n  <dt><code>key</code></dt>\n  <dd>the key to look for</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when the key is present, <code>false</code> otherwise</p>\n</div>\n<div class=\"fn-detail\" id=\"keys/1\">\n  <h3 class=\"fn-title\"><code>function keys(map)</code> <a class=\"anchor\" href=\"#keys/1\">¶</a></h3>\n<p>The map&#39;s keys, in insertion order.</p>\n<pre><code><span class=\"tok-type\">Map</span>.keys({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>})   <span class=\"tok-com\"># == [:a, :b]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of the keys, in insertion order</p>\n</div>\n<div class=\"fn-detail\" id=\"values/1\">\n  <h3 class=\"fn-title\"><code>function values(map)</code> <a class=\"anchor\" href=\"#values/1\">¶</a></h3>\n<p>The map&#39;s values, in insertion order — the counterpart to <code>keys</code>. <code><a href=\"#/Kernel:to_list\">Kernel.to_list(map)</a></code> gives the same list; this is the name that reads as <code><a href=\"#/Map\">Map</a></code>&#39;s own, the way <code>keys</code> does.</p>\n<pre><code><span class=\"tok-type\">Map</span>.values({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>})   <span class=\"tok-com\"># == [1, 2]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of the values, in insertion order</p>\n</div>\n<div class=\"fn-detail\" id=\"delete/2\">\n  <h3 class=\"fn-title\"><code>function delete(map, key)</code> <a class=\"anchor\" href=\"#delete/2\">¶</a></h3>\n<p>A new map without <code>key</code>. Removing a key that is not there is not an error.</p>\n<pre><code><span class=\"tok-type\">Map</span>.delete({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>}, <span class=\"tok-lit\">:a</span>)   <span class=\"tok-com\"># == {b: 2}</span>\n<span class=\"tok-type\">Map</span>.delete({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:z</span>)         <span class=\"tok-com\"># == {a: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to remove from</dd>\n  <dt><code>key</code></dt>\n  <dd>the key to drop</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> without <code>key</code></p>\n</div>\n<div class=\"fn-detail\" id=\"update/3\">\n  <h3 class=\"fn-title\"><code>function update(map, key, f)</code> <a class=\"anchor\" href=\"#update/3\">¶</a></h3>\n<p>A new map with <code>key</code>&#39;s value replaced by <code>f</code> applied to it. A key that is not there is left alone, so <code>update</code> never adds one.</p>\n<pre><code><span class=\"tok-type\">Map</span>.update({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:a</span>, <span class=\"tok-kw\">do</span> v -&gt; v + <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == {a: 2}</span>\n<span class=\"tok-type\">Map</span>.update({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:z</span>, <span class=\"tok-kw\">do</span> v -&gt; v + <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == {a: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to update</dd>\n  <dt><code>key</code></dt>\n  <dd>the key whose value to replace</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda from the old value to the new one</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> with <code>key</code> updated, or the original when it is absent</p>\n</div>\n<div class=\"fn-detail\" id=\"put_new/3\">\n  <h3 class=\"fn-title\"><code>function put_new(map, key, value)</code> <a class=\"anchor\" href=\"#put_new/3\">¶</a></h3>\n<p>A new map with <code>key</code> set to <code>value</code>, but only when <code>key</code> is not already there — the insert-only counterpart to <code>update</code>, which only touches a key that already exists. An existing key is left exactly as it was.</p>\n<pre><code>`key` was already there</code></pre>\n<pre><code><span class=\"tok-type\">Map</span>.put_new({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:b</span>, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == {a: 1, b: 2}</span>\n<span class=\"tok-type\">Map</span>.put_new({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">9</span>)   <span class=\"tok-com\"># == {a: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to build on</dd>\n  <dt><code>key</code></dt>\n  <dd>the key to set if absent</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to store when <code>key</code> is not already present</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> with <code>key</code> set to <code>value</code>, or <code>map</code> unchanged if</p>\n</div>\n<div class=\"fn-detail\" id=\"merge_with/3\">\n  <h3 class=\"fn-title\"><code>function merge_with(a, b, f)</code> <a class=\"anchor\" href=\"#merge_with/3\">¶</a></h3>\n<p>Merges <code>a</code> and <code>b</code> like <code>++</code>, except a key present in both is resolved by <code>f(a_value, b_value)</code> instead of <code>b</code>&#39;s value silently winning. A key present in only one map keeps its value untouched.</p>\n<pre><code><span class=\"tok-type\">Map</span>.merge_with({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>}, {b: <span class=\"tok-lit\">10</span>, c: <span class=\"tok-lit\">3</span>}, <span class=\"tok-kw\">do</span> x, y -&gt; x + y)\n<span class=\"tok-com\"># == {a: 1, b: 12, c: 3}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>a</code></dt>\n  <dd>the base map</dd>\n  <dt><code>b</code></dt>\n  <dd>the map to merge in</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda <code>(a_value, b_value)</code> resolving a colliding key</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code>, <code>a</code> and <code>b</code> merged with <code>f</code> deciding collisions</p>\n</div>\n<div class=\"fn-detail\" id=\"merge_pair/3\">\n  <h3 class=\"fn-title\"><code>helper merge_pair(acc, pair, f)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#merge_pair/3\">¶</a></h3>\n<p>(private) One step of <code>merge_with</code>&#39;s fold: puts <code>pair</code>&#39;s key into <code>acc</code>, combining with the existing value through <code>f</code> when the key already exists there. Goes through <code>native</code> directly rather than the public <code>put</code>/<code>get</code>/<code>has_key</code>, which a helper cannot call.</p>\n</div>\n<div class=\"fn-detail\" id=\"filter/2\">\n  <h3 class=\"fn-title\"><code>function filter(map, f)</code> <a class=\"anchor\" href=\"#filter/2\">¶</a></h3>\n<p>The pairs for which <code>f</code> is truthy, in order.</p>\n<pre><code><span class=\"tok-type\">Map</span>.filter({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>}, <span class=\"tok-kw\">do</span> k, v -&gt; v &gt; <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == {b: 2}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to filter</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda <code>(key, value)</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> of the pairs <code>f</code> accepted</p>\n</div>\n<div class=\"fn-detail\" id=\"reject/2\">\n  <h3 class=\"fn-title\"><code>function reject(map, f)</code> <a class=\"anchor\" href=\"#reject/2\">¶</a></h3>\n<p>Inverse of <code>filter</code>: the pairs for which <code>f</code> is falsy.</p>\n<pre><code><span class=\"tok-type\">Map</span>.reject({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>}, <span class=\"tok-kw\">do</span> k, v -&gt; v &gt; <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == {a: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to filter</dd>\n  <dt><code>f</code></dt>\n  <dd>a predicate lambda <code>(key, value)</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> of the pairs <code>f</code> rejected</p>\n</div>\n<div class=\"fn-detail\" id=\"apply_pair/2\">\n  <h3 class=\"fn-title\"><code>helper apply_pair(f, pair)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#apply_pair/2\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"map_values/2\">\n  <h3 class=\"fn-title\"><code>function map_values(map, f)</code> <a class=\"anchor\" href=\"#map_values/2\">¶</a></h3>\n<p>A new map with each value replaced by <code>f</code> applied to it. Keys are kept.</p>\n<pre><code><span class=\"tok-type\">Map</span>.map_values({a: <span class=\"tok-lit\">1</span>, b: <span class=\"tok-lit\">2</span>}, <span class=\"tok-kw\">do</span> v -&gt; v * <span class=\"tok-lit\">10</span>)   <span class=\"tok-com\"># == {a: 10, b: 20}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to walk</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda from a value to its replacement</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> with the same keys and mapped values</p>\n</div>\n<div class=\"fn-detail\" id=\"map_keys/2\">\n  <h3 class=\"fn-title\"><code>function map_keys(map, f)</code> <a class=\"anchor\" href=\"#map_keys/2\">¶</a></h3>\n<p>A new map with each key replaced by <code>f</code> applied to it. Values are kept; if <code>f</code> maps two keys onto one, the later pair wins.</p>\n<pre><code><span class=\"tok-type\">Map</span>.map_keys({<span class=\"tok-lit\">1</span>: <span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">2</span>: <span class=\"tok-lit\">:b</span>}, <span class=\"tok-kw\">do</span> k -&gt; k * <span class=\"tok-lit\">10</span>)   <span class=\"tok-com\"># == {10: :a, 20: :b}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to walk</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda from a key to its replacement</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> with mapped keys and the same values</p>\n</div>\n<div class=\"fn-detail\" id=\"map_entries/2\">\n  <h3 class=\"fn-title\"><code>function map_entries(map, f)</code> <a class=\"anchor\" href=\"#map_entries/2\">¶</a></h3>\n<p>A new map built by replacing each pair with the <code>(key, value)</code> tuple <code>f</code> returns for it.</p>\n<pre><code><span class=\"tok-type\">Map</span>.map_entries({a: <span class=\"tok-lit\">1</span>}, <span class=\"tok-kw\">do</span> k, v -&gt; (k, v + <span class=\"tok-lit\">1</span>))   <span class=\"tok-com\"># == {a: 2}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>map</code></dt>\n  <dd>the map to walk</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda <code>(key, value)</code> returning the replacement pair</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Map\">Map</a></code> of the returned pairs</p>\n</div>\n<div class=\"fn-detail\" id=\"from_list/1\">\n  <h3 class=\"fn-title\"><code>function from_list(pairs)</code> <a class=\"anchor\" href=\"#from_list/1\">¶</a></h3>\n<p>A map built from a list of <code>(key, value)</code> tuples, in order. A repeated key keeps its first position and takes its last value.</p>\n<pre><code><span class=\"tok-type\">Map</span>.from_list([(<span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">1</span>), (<span class=\"tok-lit\">:b</span>, <span class=\"tok-lit\">2</span>)])   <span class=\"tok-com\"># == {a: 1, b: 2}</span>\n<span class=\"tok-type\">Map</span>.from_list([])                   <span class=\"tok-com\"># == {}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>pairs</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of <code>(key, value)</code> tuples</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Map\">Map</a></code> those pairs describe</p>\n</div>\n</section>\n"},"Module":{"title":"Module — Ramos","heading":"Module","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Module — introspecting and calling into modules by value.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Module — introspecting and calling into modules by value.</p>\n<p>A module is itself a value in Ramos: <code><a href=\"#/Kernel\">Kernel</a></code> (or <code><a href=\"#/Integer\">Integer</a></code>, or any module written) can be bound, passed around, and stored — <code>is_module</code> tests for one and <code>type_of</code> names it. This module is what you do with one once you have it, when the function to call is not known at the call site.</p>\n<pre><code><span class=\"tok-com\"># ramos doctest setup</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">Greeter</span>\n  <span class=\"tok-kw\">function</span> hello(name)\n    <span class=\"tok-str\">&quot;hi #{name}&quot;</span></code></pre>\n<pre><code><span class=\"tok-kw\">helper</span> secret()\n  <span class=\"tok-lit\">42</span></code></pre>\n<pre><code><span class=\"tok-type\">Module</span>.functions(<span class=\"tok-type\">Greeter</span>)                    <span class=\"tok-com\"># == [&quot;hello&quot;]</span>\n<span class=\"tok-type\">Module</span>.apply(<span class=\"tok-type\">Greeter</span>, <span class=\"tok-str\">&quot;hello&quot;</span>, [<span class=\"tok-str\">&quot;Andrew&quot;</span>])    <span class=\"tok-com\"># == &quot;hi Andrew&quot;</span></code></pre>\n<p>Conventions:</p>\n<ul><li>both functions only ever see a module&#39;s <em>public</em> functions — <code>helper</code> stays exactly as unreachable from here as it is from a written <code>Greeter.secret()</code> call outside the module.</li><li>a function is named with a <code><a href=\"#/String\">String</a></code> (<code>&quot;hello&quot;</code>), not a <code>Symbol</code> — matching <code>type_of</code>, since a module&#39;s own name is a <code><a href=\"#/String\">String</a></code> too.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#functions/1\" class=\"fn-sig\"><code>function functions(m)</code></a>\n    <span class=\"fn-summary\">The names of the public functions <code>m</code> defines, in declaration order. <code>helper</code> functions are never included — they are not callable from outside <code>m</code> either.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#apply/3\" class=\"fn-sig\"><code>function apply(m, fn_name, args)</code></a>\n    <span class=\"fn-summary\">Resolves <code>fn_name</code> on module <code>m</code> and calls it with <code>args</code>, a list of its arguments — like <code><a href=\"#/Kernel:apply\">Kernel.apply</a></code>, but the function is looked up by name on a module rather than held as a lambda. Use it when <em>which</em> function to call is only known at runtime (a dispatch table, a plugin name read from config); otherwise call the function directly.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"functions/1\">\n  <h3 class=\"fn-title\"><code>function functions(m)</code> <a class=\"anchor\" href=\"#functions/1\">¶</a></h3>\n<p>The names of the public functions <code>m</code> defines, in declaration order. <code>helper</code> functions are never included — they are not callable from outside <code>m</code> either.</p>\n<pre><code><span class=\"tok-type\">Module</span>.functions(<span class=\"tok-type\">Greeter</span>)   <span class=\"tok-com\"># == [&quot;hello&quot;]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>m</code></dt>\n  <dd>the module to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of <code><a href=\"#/String\">String</a></code> function names, in declaration order</p>\n</div>\n<div class=\"fn-detail\" id=\"apply/3\">\n  <h3 class=\"fn-title\"><code>function apply(m, fn_name, args)</code> <a class=\"anchor\" href=\"#apply/3\">¶</a></h3>\n<p>Resolves <code>fn_name</code> on module <code>m</code> and calls it with <code>args</code>, a list of its arguments — like <code><a href=\"#/Kernel:apply\">Kernel.apply</a></code>, but the function is looked up by name on a module rather than held as a lambda. Use it when <em>which</em> function to call is only known at runtime (a dispatch table, a plugin name read from config); otherwise call the function directly.</p>\n<p>Has no Ramos body: the interpreter intercepts the call, because resolving <code>fn_name</code> and running it needs the evaluator, which a native cannot reach. Raises if <code>m</code> has no public function named <code>fn_name</code>.</p>\n<pre><code><span class=\"tok-type\">Module</span>.apply(<span class=\"tok-type\">Integer</span>, <span class=\"tok-str\">&quot;abs&quot;</span>, [-<span class=\"tok-lit\">7</span>])   <span class=\"tok-com\"># == 7</span></code></pre>\n<dl class=\"params\">\n  <dt><code>m</code></dt>\n  <dd>the module to call a function on</dd>\n  <dt><code>fn_name</code></dt>\n  <dd>the function&#39;s name, as a <code><a href=\"#/String\">String</a></code></dd>\n  <dt><code>args</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of the arguments to pass</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whatever the resolved function returns</p>\n</div>\n</section>\n"},"NaiveDateTime":{"title":"NaiveDateTime — Ramos","heading":"NaiveDateTime","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>NaiveDateTime — a calendar date and a time of day, with no time zone. <code><a href=\"#/Date\">Date</a></code> plus <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>NaiveDateTime — a calendar date and a time of day, with no time zone. <code><a href=\"#/Date\">Date</a></code> plus <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>.</p>\n<p>&quot;Naive&quot; is the standard term for this shape: a wall-clock reading with no attached zone or offset, so it cannot be converted to a precise instant on its own. Reach for <code><a href=\"#/DateTime\">DateTime</a></code> when the offset from UTC matters — logging a timestamp, scheduling something relative to now, comparing readings taken in different places.</p>\n<pre><code>dt = <span class=\"tok-type\">NaiveDateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">9</span>, <span class=\"tok-lit\">13</span>, <span class=\"tok-lit\">45</span>, <span class=\"tok-lit\">30</span>)\ndt.hour     <span class=\"tok-com\"># == 13</span>\ndt.minute   <span class=\"tok-com\"># == 45</span></code></pre>\n<p>As with <code><a href=\"#/Date\">Date</a></code>, an out-of-range field normalizes rather than raising — <code>second: 60</code> rolls into the next minute, <code>hour: -1</code> into the previous day&#39;s last hour:</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">9</span>, <span class=\"tok-lit\">23</span>, <span class=\"tok-lit\">59</span>, <span class=\"tok-lit\">60</span>)   <span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 10, hour: 0, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<p>Conventions:</p>\n<ul><li><code>compare</code> follows <code><a href=\"#/Integer:compare\">Integer.compare</a></code>: <code>-1</code>, <code>0</code>, <code>1</code>.</li><li><code>to_iso</code>/<code>parse</code> round-trip through <code>&quot;YYYY-MM-DDTHH:MM:SS.mmm&quot;</code>, always with milliseconds — the precision <code>now</code> reads the clock at.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#new/6\" class=\"fn-sig\"><code>function new(year, month, day, hour, minute, second)</code></a>\n    <span class=\"fn-summary\">Builds a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> with <code>millisecond: 0</code>. Every field past <code>year</code> normalizes when it overflows or underflows its natural range, the same way <code><a href=\"#/Date:new\">Date.new</a></code>&#39;s <code>day</code> does.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#now/0\" class=\"fn-sig\"><code>function now()</code></a>\n    <span class=\"fn-summary\">The current date and time, read from the wall clock as UTC.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_epoch_millis/1\" class=\"fn-sig\"><code>function from_epoch_millis(ms)</code></a>\n    <span class=\"fn-summary\">The <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> that is <code>ms</code> milliseconds after 1970-01-01T00:00:00.000 (millisecond <code>0</code>). Negative values reach before the epoch.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#from_epoch_millis_raw/1\" class=\"fn-sig\"><code>helper from_epoch_millis_raw(ms)</code></a>\n    <span class=\"fn-summary\">(private) <code>from_epoch_millis</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#parse/1\" class=\"fn-sig\"><code>function parse(text)</code></a>\n    <span class=\"fn-summary\">Parses <code>&quot;YYYY-MM-DDTHH:MM:SS&quot;</code> or <code>&quot;YYYY-MM-DD HH:MM:SS&quot;</code> — either a <code>T</code> or a plain space between the date and the time, as <code>to_iso</code> renders or as the <code>N&quot;...&quot;</code> sigil is written. The seconds&#39; fractional part is optional and any number of digits (padded or truncated to milliseconds): <code>&quot;...:30&quot;</code>, <code>&quot;...:30.5&quot;</code> and <code>&quot;...:30.123456&quot;</code> are all valid, meaning <code>0</code>, <code>500</code> and <code>123</code> milliseconds respectively.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#date/1\" class=\"fn-sig\"><code>function date(self)</code></a>\n    <span class=\"fn-summary\">The <code><a href=\"#/Date\">Date</a></code> half of <code>self</code>, dropping the time of day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_epoch_millis/1\" class=\"fn-sig\"><code>function to_epoch_millis(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> as milliseconds since 1970-01-01T00:00:00.000.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_iso/1\" class=\"fn-sig\"><code>function to_iso(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> rendered as <code>&quot;YYYY-MM-DDTHH:MM:SS.mmm&quot;</code>, zero-padded.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_days/2\" class=\"fn-sig\"><code>function add_days(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> days, keeping the time of day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_weeks/2\" class=\"fn-sig\"><code>function add_weeks(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> weeks (<code>n * 7</code> days), keeping the time of day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_months/2\" class=\"fn-sig\"><code>function add_months(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> months, keeping the time of day — <code><a href=\"#/Date:add_months\">Date.add_months</a></code> applied to the date half, with the day-overflow normalization that implies.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_hours/2\" class=\"fn-sig\"><code>function add_hours(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> hours.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_minutes/2\" class=\"fn-sig\"><code>function add_minutes(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> minutes.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_seconds/2\" class=\"fn-sig\"><code>function add_seconds(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> seconds.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(self, other)</code></a>\n    <span class=\"fn-summary\">Orders two date-times as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> uses, since there is no built-in <code>&lt;</code>/<code>&gt;</code> for structs.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#diff_millis/2\" class=\"fn-sig\"><code>function diff_millis(self, other)</code></a>\n    <span class=\"fn-summary\">The number of milliseconds from <code>other</code> to <code>self</code> — positive when <code>self</code> is later, negative when earlier.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#day_of_week/1\" class=\"fn-sig\"><code>function day_of_week(self)</code></a>\n    <span class=\"fn-summary\">The day of the week <code>self</code> falls on. Delegates to <code><a href=\"#/Date:day_of_week\">Date.day_of_week</a></code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#raw_millis/7\" class=\"fn-sig\"><code>helper raw_millis(year, month, day, hour, minute, second, millisecond)</code></a>\n    <span class=\"fn-summary\">(private) Milliseconds since the epoch for the given fields, computed straight from the unchecked <code>date_to_epoch_day</code> native — the shared arithmetic behind <code>new</code>, <code>parse</code> and <code>to_epoch_millis</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#shift/2\" class=\"fn-sig\"><code>helper shift(self, millis)</code></a>\n    <span class=\"fn-summary\">(private) <code>self</code> moved by <code>millis</code> milliseconds. Backs the <code>add_*</code> family.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#floor_div/2\" class=\"fn-sig\"><code>helper floor_div(a, b)</code></a>\n    <span class=\"fn-summary\">(private) <code>a</code> divided by <code>b</code>, rounding toward negative infinity — unlike <code>/</code>, which truncates toward zero. <code>b</code> is assumed positive. Needed to turn a possibly-negative millisecond count into the right epoch day: <code>%</code> (floor modulo) already gives the right millisecond-of-day on its own, but <code>/</code> alone would round a negative count the wrong way.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#millis_from_fraction/1\" class=\"fn-sig\"><code>helper millis_from_fraction(digits)</code></a>\n    <span class=\"fn-summary\">(private) A fractional-second string of any length as milliseconds: <code>&quot;5&quot;</code> is <code>500</code>, <code>&quot;123456&quot;</code> truncates to <code>123</code>. Backs <code>parse</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#pad/2\" class=\"fn-sig\"><code>helper pad(n, width)</code></a>\n    <span class=\"fn-summary\">(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"new/6\">\n  <h3 class=\"fn-title\"><code>function new(year, month, day, hour, minute, second)</code> <a class=\"anchor\" href=\"#new/6\">¶</a></h3>\n<p>Builds a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> with <code>millisecond: 0</code>. Every field past <code>year</code> normalizes when it overflows or underflows its natural range, the same way <code><a href=\"#/Date:new\">Date.new</a></code>&#39;s <code>day</code> does.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">9</span>, <span class=\"tok-lit\">13</span>, <span class=\"tok-lit\">45</span>, <span class=\"tok-lit\">30</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 45, second: 30, millisecond: 0}</span>\n<span class=\"tok-type\">NaiveDateTime</span>.new(<span class=\"tok-lit\">2024</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">9</span>, <span class=\"tok-lit\">24</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 10, hour: 0, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>year</code></dt>\n  <dd>the calendar year, any integer</dd>\n  <dt><code>month</code></dt>\n  <dd>the month, <code>1</code>..<code>12</code></dd>\n  <dt><code>day</code></dt>\n  <dd>the day of month; out-of-range values roll over</dd>\n  <dt><code>hour</code></dt>\n  <dd>the hour, <code>0</code>..<code>23</code>; out-of-range values roll over</dd>\n  <dt><code>minute</code></dt>\n  <dd>the minute, <code>0</code>..<code>59</code>; out-of-range values roll over</dd>\n  <dt><code>second</code></dt>\n  <dd>the second, <code>0</code>..<code>59</code>; out-of-range values roll over</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"now/0\">\n  <h3 class=\"fn-title\"><code>function now()</code> <a class=\"anchor\" href=\"#now/0\">¶</a></h3>\n<p>The current date and time, read from the wall clock as UTC.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.now().year &gt; <span class=\"tok-lit\">2000</span>   <span class=\"tok-com\"># == true</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the current <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_epoch_millis/1\">\n  <h3 class=\"fn-title\"><code>function from_epoch_millis(ms)</code> <a class=\"anchor\" href=\"#from_epoch_millis/1\">¶</a></h3>\n<p>The <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> that is <code>ms</code> milliseconds after 1970-01-01T00:00:00.000 (millisecond <code>0</code>). Negative values reach before the epoch.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.from_epoch_millis(<span class=\"tok-lit\">0</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 1970, month: 1, day: 1, hour: 0, minute: 0, second: 0, millisecond: 0}</span>\n<span class=\"tok-type\">NaiveDateTime</span>.from_epoch_millis(-<span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 1969, month: 12, day: 31, hour: 23, minute: 59, second: 59, millisecond: 999}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>ms</code></dt>\n  <dd>milliseconds since the epoch, as an <code><a href=\"#/Integer\">Integer</a></code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_epoch_millis_raw/1\">\n  <h3 class=\"fn-title\"><code>helper from_epoch_millis_raw(ms)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#from_epoch_millis_raw/1\">¶</a></h3>\n<p>(private) <code>from_epoch_millis</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</p>\n</div>\n<div class=\"fn-detail\" id=\"parse/1\">\n  <h3 class=\"fn-title\"><code>function parse(text)</code> <a class=\"anchor\" href=\"#parse/1\">¶</a></h3>\n<p>Parses <code>&quot;YYYY-MM-DDTHH:MM:SS&quot;</code> or <code>&quot;YYYY-MM-DD HH:MM:SS&quot;</code> — either a <code>T</code> or a plain space between the date and the time, as <code>to_iso</code> renders or as the <code>N&quot;...&quot;</code> sigil is written. The seconds&#39; fractional part is optional and any number of digits (padded or truncated to milliseconds): <code>&quot;...:30&quot;</code>, <code>&quot;...:30.5&quot;</code> and <code>&quot;...:30.123456&quot;</code> are all valid, meaning <code>0</code>, <code>500</code> and <code>123</code> milliseconds respectively.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.parse(<span class=\"tok-str\">&quot;2024-02-09T13:45:30.500&quot;</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 45, second: 30, millisecond: 500}</span>\n<span class=\"tok-type\">NaiveDateTime</span>.parse(<span class=\"tok-str\">&quot;2024-02-09 13:45:30.123456&quot;</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 9, hour: 13, minute: 45, second: 30, millisecond: 123}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>text</code></dt>\n  <dd>a date-time in <code>&quot;YYYY-MM-DD[T ]HH:MM:SS[.fraction]&quot;</code> form</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"date/1\">\n  <h3 class=\"fn-title\"><code>function date(self)</code> <a class=\"anchor\" href=\"#date/1\">¶</a></h3>\n<p>The <code><a href=\"#/Date\">Date</a></code> half of <code>self</code>, dropping the time of day.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.date(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">13</span>})\n<span class=\"tok-com\"># == Date{year: 2024, month: 2, day: 9}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Date\">Date</a></code> self falls on</p>\n</div>\n<div class=\"fn-detail\" id=\"to_epoch_millis/1\">\n  <h3 class=\"fn-title\"><code>function to_epoch_millis(self)</code> <a class=\"anchor\" href=\"#to_epoch_millis/1\">¶</a></h3>\n<p><code>self</code> as milliseconds since 1970-01-01T00:00:00.000.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.to_epoch_millis(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">1970</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> millisecond count, negative before the epoch</p>\n</div>\n<div class=\"fn-detail\" id=\"to_iso/1\">\n  <h3 class=\"fn-title\"><code>function to_iso(self)</code> <a class=\"anchor\" href=\"#to_iso/1\">¶</a></h3>\n<p><code>self</code> rendered as <code>&quot;YYYY-MM-DDTHH:MM:SS.mmm&quot;</code>, zero-padded.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.to_iso(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">3</span>, minute: <span class=\"tok-lit\">5</span>, second: <span class=\"tok-lit\">9</span>, millisecond: <span class=\"tok-lit\">7</span>})\n<span class=\"tok-com\"># == &quot;2024-02-09T03:05:09.007&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to render</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the ISO-8601-shaped string</p>\n</div>\n<div class=\"fn-detail\" id=\"add_days/2\">\n  <h3 class=\"fn-title\"><code>function add_days(self, n)</code> <a class=\"anchor\" href=\"#add_days/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> days, keeping the time of day.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.add_days(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">28</span>, hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 29, hour: 12}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many days to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_weeks/2\">\n  <h3 class=\"fn-title\"><code>function add_weeks(self, n)</code> <a class=\"anchor\" href=\"#add_weeks/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> weeks (<code>n * 7</code> days), keeping the time of day.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.add_weeks(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>, hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 1, day: 8, hour: 12}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many weeks to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_months/2\">\n  <h3 class=\"fn-title\"><code>function add_months(self, n)</code> <a class=\"anchor\" href=\"#add_months/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> months, keeping the time of day — <code><a href=\"#/Date:add_months\">Date.add_months</a></code> applied to the date half, with the day-overflow normalization that implies.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.add_months(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">15</span>, hour: <span class=\"tok-lit\">12</span>}, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 15, hour: 12}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many months to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_hours/2\">\n  <h3 class=\"fn-title\"><code>function add_hours(self, n)</code> <a class=\"anchor\" href=\"#add_hours/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> hours.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.add_hours(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">23</span>}, <span class=\"tok-lit\">2</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 10, hour: 1}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many hours to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_minutes/2\">\n  <h3 class=\"fn-title\"><code>function add_minutes(self, n)</code> <a class=\"anchor\" href=\"#add_minutes/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> minutes.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.add_minutes(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, hour: <span class=\"tok-lit\">0</span>, minute: <span class=\"tok-lit\">30</span>}, <span class=\"tok-lit\">45</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 9, hour: 1, minute: 15}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many minutes to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_seconds/2\">\n  <h3 class=\"fn-title\"><code>function add_seconds(self, n)</code> <a class=\"anchor\" href=\"#add_seconds/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> seconds.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.add_seconds(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">2024</span>, month: <span class=\"tok-lit\">2</span>, day: <span class=\"tok-lit\">9</span>, second: <span class=\"tok-lit\">45</span>}, <span class=\"tok-lit\">30</span>)\n<span class=\"tok-com\"># == NaiveDateTime{year: 2024, month: 2, day: 9, minute: 1, second: 15}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting date-time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many seconds to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(self, other)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two date-times as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> uses, since there is no built-in <code>&lt;</code>/<code>&gt;</code> for structs.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.compare(<span class=\"tok-type\">NaiveDateTime</span>{hour: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">NaiveDateTime</span>{hour: <span class=\"tok-lit\">2</span>})   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">NaiveDateTime</span>.compare(<span class=\"tok-type\">NaiveDateTime</span>{hour: <span class=\"tok-lit\">2</span>}, <span class=\"tok-type\">NaiveDateTime</span>{hour: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">NaiveDateTime</span>.compare(<span class=\"tok-type\">NaiveDateTime</span>{hour: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">NaiveDateTime</span>{hour: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the first date-time</dd>\n  <dt><code>other</code></dt>\n  <dd>the second date-time</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>self</code> is earlier, <code>1</code> when later, <code>0</code> when equal</p>\n</div>\n<div class=\"fn-detail\" id=\"diff_millis/2\">\n  <h3 class=\"fn-title\"><code>function diff_millis(self, other)</code> <a class=\"anchor\" href=\"#diff_millis/2\">¶</a></h3>\n<p>The number of milliseconds from <code>other</code> to <code>self</code> — positive when <code>self</code> is later, negative when earlier.</p>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.diff_millis(<span class=\"tok-type\">NaiveDateTime</span>{second: <span class=\"tok-lit\">10</span>}, <span class=\"tok-type\">NaiveDateTime</span>{second: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 9000</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the later (or earlier) date-time</dd>\n  <dt><code>other</code></dt>\n  <dd>the date-time to measure from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> millisecond count</p>\n</div>\n<div class=\"fn-detail\" id=\"day_of_week/1\">\n  <h3 class=\"fn-title\"><code>function day_of_week(self)</code> <a class=\"anchor\" href=\"#day_of_week/1\">¶</a></h3>\n<p>The day of the week <code>self</code> falls on. Delegates to <code><a href=\"#/Date:day_of_week\">Date.day_of_week</a></code>.</p>\n<pre><code>`:thursday`, `:friday`, `:saturday`</code></pre>\n<pre><code><span class=\"tok-type\">NaiveDateTime</span>.day_of_week(<span class=\"tok-type\">NaiveDateTime</span>{year: <span class=\"tok-lit\">1970</span>, month: <span class=\"tok-lit\">1</span>, day: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == :thursday</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the date-time to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> one of <code>:sunday</code>, <code>:monday</code>, <code>:tuesday</code>, <code>:wednesday</code>,</p>\n</div>\n<div class=\"fn-detail\" id=\"raw_millis/7\">\n  <h3 class=\"fn-title\"><code>helper raw_millis(year, month, day, hour, minute, second, millisecond)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#raw_millis/7\">¶</a></h3>\n<p>(private) Milliseconds since the epoch for the given fields, computed straight from the unchecked <code>date_to_epoch_day</code> native — the shared arithmetic behind <code>new</code>, <code>parse</code> and <code>to_epoch_millis</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"shift/2\">\n  <h3 class=\"fn-title\"><code>helper shift(self, millis)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#shift/2\">¶</a></h3>\n<p>(private) <code>self</code> moved by <code>millis</code> milliseconds. Backs the <code>add_*</code> family.</p>\n</div>\n<div class=\"fn-detail\" id=\"floor_div/2\">\n  <h3 class=\"fn-title\"><code>helper floor_div(a, b)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#floor_div/2\">¶</a></h3>\n<p>(private) <code>a</code> divided by <code>b</code>, rounding toward negative infinity — unlike <code>/</code>, which truncates toward zero. <code>b</code> is assumed positive. Needed to turn a possibly-negative millisecond count into the right epoch day: <code>%</code> (floor modulo) already gives the right millisecond-of-day on its own, but <code>/</code> alone would round a negative count the wrong way.</p>\n</div>\n<div class=\"fn-detail\" id=\"millis_from_fraction/1\">\n  <h3 class=\"fn-title\"><code>helper millis_from_fraction(digits)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#millis_from_fraction/1\">¶</a></h3>\n<p>(private) A fractional-second string of any length as milliseconds: <code>&quot;5&quot;</code> is <code>500</code>, <code>&quot;123456&quot;</code> truncates to <code>123</code>. Backs <code>parse</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"pad/2\">\n  <h3 class=\"fn-title\"><code>helper pad(n, width)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#pad/2\">¶</a></h3>\n<p>(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</p>\n</div>\n</section>\n"},"String":{"title":"String — Ramos","heading":"String","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>String — operations on immutable, UTF-8 text.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>String — operations on immutable, UTF-8 text.</p>\n<p>Strings in Ramos are double-quoted (<code>&quot;hi&quot;</code>), interpolated with <code>#{name}</code>, and concatenated with <code>&lt;&gt;</code>. Like every value in Ramos they are immutable: every function here returns a new string rather than mutating its input.</p>\n<p>Unlike lists, strings have no <code>head | tail</code> destructuring in the language, so the character-level primitives (indexing, slicing, casing, splitting, reversing) delegate to the host via <code>native</code>. Concatenation is the <code>&lt;&gt;</code> operator rather than a function here, and the rest compose through pipes just like the <code><a href=\"#/List\">List</a></code> module:</p>\n<pre><code><span class=\"tok-str\">&quot;  Hello World  &quot;</span>\n| <span class=\"tok-type\">String</span>.trim()\n| <span class=\"tok-type\">String</span>.downcase()\n| <span class=\"tok-type\">String</span>.replace(<span class=\"tok-str\">&quot; &quot;</span>, <span class=\"tok-str\">&quot;_&quot;</span>)     <span class=\"tok-com\"># == &quot;hello_world&quot;</span></code></pre>\n<p>Joining a list of strings back into one is <code><a href=\"#/List:join\">List.join</a></code> (not here), the natural inverse of <code>split</code>. Numeric parsing lives on <code><a href=\"#/Kernel\">Kernel</a></code> (<code>to_integer</code> / <code>to_float</code>), and so does emptiness: <code>is_empty(s)</code> already covers strings, so this module does not repeat it.</p>\n<p>Conventions:</p>\n<ul><li>indexes are zero-based and count Unicode code points.</li><li><code>at</code> returns a single-character string; Ramos has no separate char type.</li><li>search functions that may miss return <code>nil</code>.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#length/1\" class=\"fn-sig\"><code>function length(s)</code></a>\n    <span class=\"fn-summary\">Returns the number of Unicode code points in <code>s</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#at/2\" class=\"fn-sig\"><code>function at(s, index)</code></a>\n    <span class=\"fn-summary\">Returns the single-character string at zero-based <code>index</code>, or <code>nil</code> if out of range. Ramos has no separate char type, so the result is a <code><a href=\"#/String\">String</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#slice/3\" class=\"fn-sig\"><code>function slice(s, start, count)</code></a>\n    <span class=\"fn-summary\">Returns <code>count</code> characters of <code>s</code> starting at zero-based <code>start</code>. <code>start</code> beyond the end yields <code>&quot;&quot;</code>; <code>count</code> is clipped to the available tail.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#contains/2\" class=\"fn-sig\"><code>function contains(s, sub)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>sub</code> occurs anywhere within <code>s</code>, else <code>false</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#starts_with/2\" class=\"fn-sig\"><code>function starts_with(s, prefix)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>s</code> begins with <code>prefix</code>, else <code>false</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#ends_with/2\" class=\"fn-sig\"><code>function ends_with(s, suffix)</code></a>\n    <span class=\"fn-summary\">Returns <code>true</code> if <code>s</code> ends with <code>suffix</code>, else <code>false</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#find/2\" class=\"fn-sig\"><code>function find(s, sub)</code></a>\n    <span class=\"fn-summary\">Returns the zero-based index of the first occurrence of <code>sub</code> in <code>s</code>, or <code>nil</code> if it is not present.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#replace/3\" class=\"fn-sig\"><code>function replace(s, pattern, replacement)</code></a>\n    <span class=\"fn-summary\">Returns a copy of <code>s</code> with every (non-overlapping) occurrence of <code>pattern</code> replaced by <code>replacement</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#split/2\" class=\"fn-sig\"><code>function split(s, sep)</code></a>\n    <span class=\"fn-summary\">Splits <code>s</code> at each occurrence of <code>sep</code>, returning a list of parts. Empty <code>sep</code> splits into one-character strings. To rejoin, use <code><a href=\"#/List:join\">List.join</a></code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#upcase/1\" class=\"fn-sig\"><code>function upcase(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with each ASCII letter uppercased.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#downcase/1\" class=\"fn-sig\"><code>function downcase(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with each ASCII letter lowercased.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#trim/1\" class=\"fn-sig\"><code>function trim(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with leading and trailing whitespace removed.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#trim_left/1\" class=\"fn-sig\"><code>function trim_left(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with leading whitespace removed.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#trim_right/1\" class=\"fn-sig\"><code>function trim_right(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with trailing whitespace removed.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#reverse/1\" class=\"fn-sig\"><code>function reverse(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with its characters in reverse order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#repeat/2\" class=\"fn-sig\"><code>function repeat(s, n)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> concatenated with itself <code>n</code> times. <code>n &lt;= 0</code> yields <code>&quot;&quot;</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_list/1\" class=\"fn-sig\"><code>function to_list(s)</code></a>\n    <span class=\"fn-summary\">Splits <code>s</code> into a list of single-character strings, one per Unicode code point. The inverse of <code>from_list</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#pad_left/3\" class=\"fn-sig\"><code>function pad_left(s, width, pad_char)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> left-padded with <code>pad_char</code> until it reaches at least <code>width</code> characters. Already-long-enough <code>s</code> comes back unchanged. <code>pad_char</code> is a single character; a longer one may overshoot <code>width</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#pad_right/3\" class=\"fn-sig\"><code>function pad_right(s, width, pad_char)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> right-padded with <code>pad_char</code> until it reaches at least <code>width</code> characters. Already-long-enough <code>s</code> comes back unchanged. <code>pad_char</code> is a single character; a longer one may overshoot <code>width</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#capitalize/1\" class=\"fn-sig\"><code>function capitalize(s)</code></a>\n    <span class=\"fn-summary\">Returns <code>s</code> with its first character uppercased and the rest lowercased. <code>&quot;&quot;</code> comes back unchanged.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_list/1\" class=\"fn-sig\"><code>function from_list(chars)</code></a>\n    <span class=\"fn-summary\">Concatenates a list of single-character strings into one string. The inverse of <code>to_list</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"length/1\">\n  <h3 class=\"fn-title\"><code>function length(s)</code> <a class=\"anchor\" href=\"#length/1\">¶</a></h3>\n<p>Returns the number of Unicode code points in <code>s</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.length(<span class=\"tok-str\">&quot;hello&quot;</span>)   <span class=\"tok-com\"># == 5</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to measure</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the <code><a href=\"#/Integer\">Integer</a></code> count of code points in <code>s</code></p>\n</div>\n<div class=\"fn-detail\" id=\"at/2\">\n  <h3 class=\"fn-title\"><code>function at(s, index)</code> <a class=\"anchor\" href=\"#at/2\">¶</a></h3>\n<p>Returns the single-character string at zero-based <code>index</code>, or <code>nil</code> if out of range. Ramos has no separate char type, so the result is a <code><a href=\"#/String\">String</a></code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.at(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-lit\">1</span>)   <span class=\"tok-com\"># == &quot;e&quot;</span>\n<span class=\"tok-type\">String</span>.at(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-lit\">9</span>)   <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to read from</dd>\n  <dt><code>index</code></dt>\n  <dd>the zero-based code-point position to read</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the single-character <code><a href=\"#/String\">String</a></code> at <code>index</code>, or <code>nil</code> if out of range</p>\n</div>\n<div class=\"fn-detail\" id=\"slice/3\">\n  <h3 class=\"fn-title\"><code>function slice(s, start, count)</code> <a class=\"anchor\" href=\"#slice/3\">¶</a></h3>\n<p>Returns <code>count</code> characters of <code>s</code> starting at zero-based <code>start</code>. <code>start</code> beyond the end yields <code>&quot;&quot;</code>; <code>count</code> is clipped to the available tail.</p>\n<pre><code><span class=\"tok-type\">String</span>.slice(<span class=\"tok-str\">&quot;hello world&quot;</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">5</span>)   <span class=\"tok-com\"># == &quot;hello&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to slice</dd>\n  <dt><code>start</code></dt>\n  <dd>the zero-based code-point position to start at</dd>\n  <dt><code>count</code></dt>\n  <dd>how many characters to take</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> of up to <code>count</code> characters from <code>start</code></p>\n</div>\n<div class=\"fn-detail\" id=\"contains/2\">\n  <h3 class=\"fn-title\"><code>function contains(s, sub)</code> <a class=\"anchor\" href=\"#contains/2\">¶</a></h3>\n<p>Returns <code>true</code> if <code>sub</code> occurs anywhere within <code>s</code>, else <code>false</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.contains(<span class=\"tok-str\">&quot;hello world&quot;</span>, <span class=\"tok-str\">&quot;world&quot;</span>)   <span class=\"tok-com\"># == true</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to search within</dd>\n  <dt><code>sub</code></dt>\n  <dd>the substring to look for</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>sub</code> occurs in <code>s</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"starts_with/2\">\n  <h3 class=\"fn-title\"><code>function starts_with(s, prefix)</code> <a class=\"anchor\" href=\"#starts_with/2\">¶</a></h3>\n<p>Returns <code>true</code> if <code>s</code> begins with <code>prefix</code>, else <code>false</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.starts_with(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-str\">&quot;he&quot;</span>)   <span class=\"tok-com\"># == true</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to test</dd>\n  <dt><code>prefix</code></dt>\n  <dd>the prefix to look for</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>s</code> begins with <code>prefix</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"ends_with/2\">\n  <h3 class=\"fn-title\"><code>function ends_with(s, suffix)</code> <a class=\"anchor\" href=\"#ends_with/2\">¶</a></h3>\n<p>Returns <code>true</code> if <code>s</code> ends with <code>suffix</code>, else <code>false</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.ends_with(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-str\">&quot;lo&quot;</span>)   <span class=\"tok-com\"># == true</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to test</dd>\n  <dt><code>suffix</code></dt>\n  <dd>the suffix to look for</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> if <code>s</code> ends with <code>suffix</code>, else <code>false</code></p>\n</div>\n<div class=\"fn-detail\" id=\"find/2\">\n  <h3 class=\"fn-title\"><code>function find(s, sub)</code> <a class=\"anchor\" href=\"#find/2\">¶</a></h3>\n<p>Returns the zero-based index of the first occurrence of <code>sub</code> in <code>s</code>, or <code>nil</code> if it is not present.</p>\n<pre><code><span class=\"tok-type\">String</span>.find(<span class=\"tok-str\">&quot;hello world&quot;</span>, <span class=\"tok-str\">&quot;world&quot;</span>)   <span class=\"tok-com\"># == 6</span>\n<span class=\"tok-type\">String</span>.find(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-str\">&quot;z&quot;</span>)             <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to search within</dd>\n  <dt><code>sub</code></dt>\n  <dd>the substring to look for</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the zero-based <code><a href=\"#/Integer\">Integer</a></code> index of the first match, or <code>nil</code> if none</p>\n</div>\n<div class=\"fn-detail\" id=\"replace/3\">\n  <h3 class=\"fn-title\"><code>function replace(s, pattern, replacement)</code> <a class=\"anchor\" href=\"#replace/3\">¶</a></h3>\n<p>Returns a copy of <code>s</code> with every (non-overlapping) occurrence of <code>pattern</code> replaced by <code>replacement</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.replace(<span class=\"tok-str\">&quot;a-b-c&quot;</span>, <span class=\"tok-str\">&quot;-&quot;</span>, <span class=\"tok-str\">&quot;_&quot;</span>)   <span class=\"tok-com\"># == &quot;a_b_c&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to rewrite</dd>\n  <dt><code>pattern</code></dt>\n  <dd>the substring to replace</dd>\n  <dt><code>replacement</code></dt>\n  <dd>the string to substitute for each <code>pattern</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> with every <code>pattern</code> replaced by <code>replacement</code></p>\n</div>\n<div class=\"fn-detail\" id=\"split/2\">\n  <h3 class=\"fn-title\"><code>function split(s, sep)</code> <a class=\"anchor\" href=\"#split/2\">¶</a></h3>\n<p>Splits <code>s</code> at each occurrence of <code>sep</code>, returning a list of parts. Empty <code>sep</code> splits into one-character strings. To rejoin, use <code><a href=\"#/List:join\">List.join</a></code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.split(<span class=\"tok-str\">&quot;a,b,c&quot;</span>, <span class=\"tok-str\">&quot;,&quot;</span>)   <span class=\"tok-com\"># == [&quot;a&quot;, &quot;b&quot;, &quot;c&quot;]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to split</dd>\n  <dt><code>sep</code></dt>\n  <dd>the separator to split on (<code>&quot;&quot;</code> splits into characters)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of the <code><a href=\"#/String\">String</a></code> parts</p>\n</div>\n<div class=\"fn-detail\" id=\"upcase/1\">\n  <h3 class=\"fn-title\"><code>function upcase(s)</code> <a class=\"anchor\" href=\"#upcase/1\">¶</a></h3>\n<p>Returns <code>s</code> with each ASCII letter uppercased.</p>\n<pre><code><span class=\"tok-type\">String</span>.upcase(<span class=\"tok-str\">&quot;hello&quot;</span>)   <span class=\"tok-com\"># == &quot;HELLO&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to uppercase</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> with each ASCII letter uppercased</p>\n</div>\n<div class=\"fn-detail\" id=\"downcase/1\">\n  <h3 class=\"fn-title\"><code>function downcase(s)</code> <a class=\"anchor\" href=\"#downcase/1\">¶</a></h3>\n<p>Returns <code>s</code> with each ASCII letter lowercased.</p>\n<pre><code><span class=\"tok-type\">String</span>.downcase(<span class=\"tok-str\">&quot;HELLO&quot;</span>)   <span class=\"tok-com\"># == &quot;hello&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to lowercase</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> with each ASCII letter lowercased</p>\n</div>\n<div class=\"fn-detail\" id=\"trim/1\">\n  <h3 class=\"fn-title\"><code>function trim(s)</code> <a class=\"anchor\" href=\"#trim/1\">¶</a></h3>\n<p>Returns <code>s</code> with leading and trailing whitespace removed.</p>\n<pre><code><span class=\"tok-type\">String</span>.trim(<span class=\"tok-str\">&quot;  hi  &quot;</span>)   <span class=\"tok-com\"># == &quot;hi&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to trim</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> without leading or trailing whitespace</p>\n</div>\n<div class=\"fn-detail\" id=\"trim_left/1\">\n  <h3 class=\"fn-title\"><code>function trim_left(s)</code> <a class=\"anchor\" href=\"#trim_left/1\">¶</a></h3>\n<p>Returns <code>s</code> with leading whitespace removed.</p>\n<pre><code><span class=\"tok-type\">String</span>.trim_left(<span class=\"tok-str\">&quot;  hi  &quot;</span>)   <span class=\"tok-com\"># == &quot;hi  &quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to trim</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> without leading whitespace</p>\n</div>\n<div class=\"fn-detail\" id=\"trim_right/1\">\n  <h3 class=\"fn-title\"><code>function trim_right(s)</code> <a class=\"anchor\" href=\"#trim_right/1\">¶</a></h3>\n<p>Returns <code>s</code> with trailing whitespace removed.</p>\n<pre><code><span class=\"tok-type\">String</span>.trim_right(<span class=\"tok-str\">&quot;  hi  &quot;</span>)   <span class=\"tok-com\"># == &quot;  hi&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to trim</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> without trailing whitespace</p>\n</div>\n<div class=\"fn-detail\" id=\"reverse/1\">\n  <h3 class=\"fn-title\"><code>function reverse(s)</code> <a class=\"anchor\" href=\"#reverse/1\">¶</a></h3>\n<p>Returns <code>s</code> with its characters in reverse order.</p>\n<pre><code><span class=\"tok-type\">String</span>.reverse(<span class=\"tok-str\">&quot;hello&quot;</span>)   <span class=\"tok-com\"># == &quot;olleh&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to reverse</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> with the code points of <code>s</code> in reverse order</p>\n</div>\n<div class=\"fn-detail\" id=\"repeat/2\">\n  <h3 class=\"fn-title\"><code>function repeat(s, n)</code> <a class=\"anchor\" href=\"#repeat/2\">¶</a></h3>\n<p>Returns <code>s</code> concatenated with itself <code>n</code> times. <code>n &lt;= 0</code> yields <code>&quot;&quot;</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.repeat(<span class=\"tok-str\">&quot;ab&quot;</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == &quot;ababab&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to repeat</dd>\n  <dt><code>n</code></dt>\n  <dd>how many times to repeat <code>s</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code> of <code>s</code> repeated <code>n</code> times (<code>&quot;&quot;</code> when <code>n &lt;= 0</code>)</p>\n</div>\n<div class=\"fn-detail\" id=\"to_list/1\">\n  <h3 class=\"fn-title\"><code>function to_list(s)</code> <a class=\"anchor\" href=\"#to_list/1\">¶</a></h3>\n<p>Splits <code>s</code> into a list of single-character strings, one per Unicode code point. The inverse of <code>from_list</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.to_list(<span class=\"tok-str\">&quot;abc&quot;</span>)   <span class=\"tok-com\"># == [&quot;a&quot;, &quot;b&quot;, &quot;c&quot;]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to explode</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of single-character <code><a href=\"#/String\">String</a></code>s, one per code point</p>\n</div>\n<div class=\"fn-detail\" id=\"pad_left/3\">\n  <h3 class=\"fn-title\"><code>function pad_left(s, width, pad_char)</code> <a class=\"anchor\" href=\"#pad_left/3\">¶</a></h3>\n<p>Returns <code>s</code> left-padded with <code>pad_char</code> until it reaches at least <code>width</code> characters. Already-long-enough <code>s</code> comes back unchanged. <code>pad_char</code> is a single character; a longer one may overshoot <code>width</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.pad_left(<span class=\"tok-str\">&quot;7&quot;</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-str\">&quot;0&quot;</span>)     <span class=\"tok-com\"># == &quot;007&quot;</span>\n<span class=\"tok-type\">String</span>.pad_left(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-str\">&quot;0&quot;</span>) <span class=\"tok-com\"># == &quot;hello&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to pad</dd>\n  <dt><code>width</code></dt>\n  <dd>the minimum length of the result</dd>\n  <dt><code>pad_char</code></dt>\n  <dd>the single-character string to pad with</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>s</code>, left-padded to at least <code>width</code> characters</p>\n</div>\n<div class=\"fn-detail\" id=\"pad_right/3\">\n  <h3 class=\"fn-title\"><code>function pad_right(s, width, pad_char)</code> <a class=\"anchor\" href=\"#pad_right/3\">¶</a></h3>\n<p>Returns <code>s</code> right-padded with <code>pad_char</code> until it reaches at least <code>width</code> characters. Already-long-enough <code>s</code> comes back unchanged. <code>pad_char</code> is a single character; a longer one may overshoot <code>width</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.pad_right(<span class=\"tok-str\">&quot;7&quot;</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-str\">&quot;0&quot;</span>)     <span class=\"tok-com\"># == &quot;700&quot;</span>\n<span class=\"tok-type\">String</span>.pad_right(<span class=\"tok-str\">&quot;hello&quot;</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-str\">&quot;0&quot;</span>) <span class=\"tok-com\"># == &quot;hello&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to pad</dd>\n  <dt><code>width</code></dt>\n  <dd>the minimum length of the result</dd>\n  <dt><code>pad_char</code></dt>\n  <dd>the single-character string to pad with</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>s</code>, right-padded to at least <code>width</code> characters</p>\n</div>\n<div class=\"fn-detail\" id=\"capitalize/1\">\n  <h3 class=\"fn-title\"><code>function capitalize(s)</code> <a class=\"anchor\" href=\"#capitalize/1\">¶</a></h3>\n<p>Returns <code>s</code> with its first character uppercased and the rest lowercased. <code>&quot;&quot;</code> comes back unchanged.</p>\n<pre><code><span class=\"tok-type\">String</span>.capitalize(<span class=\"tok-str\">&quot;hello WORLD&quot;</span>)   <span class=\"tok-com\"># == &quot;Hello world&quot;</span>\n<span class=\"tok-type\">String</span>.capitalize(<span class=\"tok-str\">&quot;&quot;</span>)              <span class=\"tok-com\"># == &quot;&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>s</code></dt>\n  <dd>the string to capitalize</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/String\">String</a></code>, first character up, rest down</p>\n</div>\n<div class=\"fn-detail\" id=\"from_list/1\">\n  <h3 class=\"fn-title\"><code>function from_list(chars)</code> <a class=\"anchor\" href=\"#from_list/1\">¶</a></h3>\n<p>Concatenates a list of single-character strings into one string. The inverse of <code>to_list</code>.</p>\n<pre><code><span class=\"tok-type\">String</span>.from_list([<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>, <span class=\"tok-str\">&quot;c&quot;</span>])   <span class=\"tok-com\"># == &quot;abc&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>chars</code></dt>\n  <dd>a list of strings to concatenate</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a single <code><a href=\"#/String\">String</a></code>, the elements of <code>chars</code> joined in order</p>\n</div>\n</section>\n"},"Struct":{"title":"Struct — Ramos","heading":"Struct","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Struct — reading and replacing the attributes of a struct instance.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Struct — reading and replacing the attributes of a struct instance.</p>\n<p>A <code>struct</code> declares its attributes once, with defaults, and an instance is built with the literal form: <code>Person{name: &quot;Andrew&quot;, age: 40}</code>. The shape is fixed at definition time, so these functions never add or drop an attribute — naming one the struct does not declare is an error rather than a silent extra.</p>\n<p>Instances are immutable like every other value: <code>put</code> returns a <em>new</em> instance and leaves the original alone.</p>\n<pre><code>andrew = <span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}\nolder =\n  andrew\n  | <span class=\"tok-type\">Struct</span>.put(<span class=\"tok-lit\">:age</span>, <span class=\"tok-lit\">41</span>)</code></pre>\n<p><code>Person</code> is the struct the examples below are written against:</p>\n<pre><code><span class=\"tok-com\"># ramos doctest setup</span>\n<span class=\"tok-kw\">struct</span> <span class=\"tok-type\">Person</span>\n  <span class=\"tok-kw\">attributes</span>\n    name: <span class=\"tok-lit\">nil</span>\n    age: <span class=\"tok-lit\">0</span></code></pre>\n<p>The field-assignment form is sugar for exactly that call, rebinding the name it reads from:</p>\n<pre><code>andrew.age = <span class=\"tok-lit\">41</span>     <span class=\"tok-com\"># andrew = Struct.put(andrew, :age, 41)</span></code></pre>\n<p>Reading a field needs no function at all — <code>andrew.name</code> is the dot form, and <code>get</code> exists for the piped and defaulted cases.</p>\n<p>Conventions:</p>\n<ul><li>attributes are named with symbols: <code>:age</code>, matching <code><a href=\"#/Map\">Map</a></code> keys.</li><li>a struct&#39;s own functions are called on the instance (<code>andrew.hello()</code>), which passes it as <code>self</code>.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#get/3\" class=\"fn-sig\"><code>function get(instance, attribute, default)</code></a>\n    <span class=\"fn-summary\">The value of <code>attribute</code>, or <code>default</code> when the struct has no such attribute. <code>instance.attribute</code> is the direct form; this one pipes and takes a fallback.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#put/3\" class=\"fn-sig\"><code>function put(instance, attribute, value)</code></a>\n    <span class=\"fn-summary\">A new instance with <code>attribute</code> set to <code>value</code>. Raises when the struct does not declare <code>attribute</code>, since an instance&#39;s shape is its struct&#39;s.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#update/3\" class=\"fn-sig\"><code>function update(instance, attribute, f)</code></a>\n    <span class=\"fn-summary\">A new instance with <code>attribute</code> replaced by <code>f</code> applied to its current value — <code>put</code> when the new value depends on the old one.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#keys/1\" class=\"fn-sig\"><code>function keys(instance)</code></a>\n    <span class=\"fn-summary\">The struct&#39;s attribute names, in declaration order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#values/1\" class=\"fn-sig\"><code>function values(instance)</code></a>\n    <span class=\"fn-summary\">The struct&#39;s attribute values, in declaration order.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_map/1\" class=\"fn-sig\"><code>function to_map(instance)</code></a>\n    <span class=\"fn-summary\">The instance as a <code><a href=\"#/Map\">Map</a></code> keyed by attribute name — the bridge to every <code><a href=\"#/Map\">Map</a></code> function, since a struct has a fixed shape and a map does not.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#is_a/2\" class=\"fn-sig\"><code>function is_a(instance, struct_module)</code></a>\n    <span class=\"fn-summary\">Whether <code>instance</code> is an instance of <code>struct_module</code>. False for every non-struct value, so it is safe to ask of anything.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"get/3\">\n  <h3 class=\"fn-title\"><code>function get(instance, attribute, default)</code> <a class=\"anchor\" href=\"#get/3\">¶</a></h3>\n<p>The value of <code>attribute</code>, or <code>default</code> when the struct has no such attribute. <code>instance.attribute</code> is the direct form; this one pipes and takes a fallback.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.get(<span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>}, <span class=\"tok-lit\">:name</span>, <span class=\"tok-str\">&quot;anonymous&quot;</span>)   <span class=\"tok-com\"># == &quot;Andrew&quot;</span>\n<span class=\"tok-type\">Struct</span>.get(<span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>}, <span class=\"tok-lit\">:nickname</span>, <span class=\"tok-lit\">nil</span>)       <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the struct instance to read</dd>\n  <dt><code>attribute</code></dt>\n  <dd>the attribute name, as a symbol</dd>\n  <dt><code>default</code></dt>\n  <dd>the value to return when the attribute is not declared</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the attribute&#39;s value, or <code>default</code></p>\n</div>\n<div class=\"fn-detail\" id=\"put/3\">\n  <h3 class=\"fn-title\"><code>function put(instance, attribute, value)</code> <a class=\"anchor\" href=\"#put/3\">¶</a></h3>\n<p>A new instance with <code>attribute</code> set to <code>value</code>. Raises when the struct does not declare <code>attribute</code>, since an instance&#39;s shape is its struct&#39;s.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.put(<span class=\"tok-type\">Person</span>{age: <span class=\"tok-lit\">40</span>}, <span class=\"tok-lit\">:age</span>, <span class=\"tok-lit\">41</span>)   <span class=\"tok-com\"># == Person{name: nil, age: 41}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the struct instance to build on</dd>\n  <dt><code>attribute</code></dt>\n  <dd>the attribute to set, as a symbol</dd>\n  <dt><code>value</code></dt>\n  <dd>the value to store</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new instance of the same struct</p>\n</div>\n<div class=\"fn-detail\" id=\"update/3\">\n  <h3 class=\"fn-title\"><code>function update(instance, attribute, f)</code> <a class=\"anchor\" href=\"#update/3\">¶</a></h3>\n<p>A new instance with <code>attribute</code> replaced by <code>f</code> applied to its current value — <code>put</code> when the new value depends on the old one.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.update(<span class=\"tok-type\">Person</span>{age: <span class=\"tok-lit\">40</span>}, <span class=\"tok-lit\">:age</span>, <span class=\"tok-kw\">do</span> n -&gt; n + <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == Person{name: nil, age: 41}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the struct instance to build on</dd>\n  <dt><code>attribute</code></dt>\n  <dd>the attribute to update, as a symbol</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda from the old value to the new one</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new instance of the same struct</p>\n</div>\n<div class=\"fn-detail\" id=\"keys/1\">\n  <h3 class=\"fn-title\"><code>function keys(instance)</code> <a class=\"anchor\" href=\"#keys/1\">¶</a></h3>\n<p>The struct&#39;s attribute names, in declaration order.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.keys(<span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>})   <span class=\"tok-com\"># == [:name, :age]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the struct instance to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of symbols</p>\n</div>\n<div class=\"fn-detail\" id=\"values/1\">\n  <h3 class=\"fn-title\"><code>function values(instance)</code> <a class=\"anchor\" href=\"#values/1\">¶</a></h3>\n<p>The struct&#39;s attribute values, in declaration order.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.values(<span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>})   <span class=\"tok-com\"># == [&quot;Andrew&quot;, 40]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the struct instance to inspect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of values</p>\n</div>\n<div class=\"fn-detail\" id=\"to_map/1\">\n  <h3 class=\"fn-title\"><code>function to_map(instance)</code> <a class=\"anchor\" href=\"#to_map/1\">¶</a></h3>\n<p>The instance as a <code><a href=\"#/Map\">Map</a></code> keyed by attribute name — the bridge to every <code><a href=\"#/Map\">Map</a></code> function, since a struct has a fixed shape and a map does not.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.to_map(<span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>})\n<span class=\"tok-com\"># == {name: &quot;Andrew&quot;, age: 40}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the struct instance to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Map\">Map</a></code> from attribute symbol to value</p>\n</div>\n<div class=\"fn-detail\" id=\"is_a/2\">\n  <h3 class=\"fn-title\"><code>function is_a(instance, struct_module)</code> <a class=\"anchor\" href=\"#is_a/2\">¶</a></h3>\n<p>Whether <code>instance</code> is an instance of <code>struct_module</code>. False for every non-struct value, so it is safe to ask of anything.</p>\n<pre><code><span class=\"tok-type\">Struct</span>.is_a(<span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>}, <span class=\"tok-type\">Person</span>)   <span class=\"tok-com\"># == true</span>\n<span class=\"tok-type\">Struct</span>.is_a(<span class=\"tok-lit\">42</span>, <span class=\"tok-type\">Person</span>)                       <span class=\"tok-com\"># == false</span></code></pre>\n<dl class=\"params\">\n  <dt><code>instance</code></dt>\n  <dd>the value to test</dd>\n  <dt><code>struct_module</code></dt>\n  <dd>the struct to test against, as a module</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>true</code> when <code>instance</code> was built from <code>struct_module</code></p>\n</div>\n</section>\n"},"Test":{"title":"Test — Ramos","heading":"Test","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Test — the trait that marks a module as a test module.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Test — the trait that marks a module as a test module.</p>\n<p>A test module is an ordinary module that implements <code><a href=\"#/Test\">Test</a></code>. Every public function whose name begins with <code>test_</code> is a test, and <code>ramos test</code> runs them. Nothing registers itself and there is no test DSL: a test is a function, and a failing <code>assert</code> is what makes it fail.</p>\n<pre><code><span class=\"tok-com\"># src/test/my_app/user_test.rmo</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">UserTest</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Test</span></code></pre>\n<pre><code><span class=\"tok-kw\">alias</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">User</span></code></pre>\n<pre><code><span class=\"tok-kw\">function</span> test_user_hello()\n  user = <span class=\"tok-type\">User</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>}\n  assert(user.hello() == <span class=\"tok-str\">&quot;Ola Andrew&quot;</span>)</code></pre>\n<p>Tests live under <code>src/test/</code>, and follow the same rules as any other file: the namespace is the path, so <code>MyApp.UserTest</code> is <code>src/test/my_app/user_test.rmo</code>. A test module&#39;s name must end in <code><a href=\"#/Test\">Test</a></code>, which is what makes it read as one in a failure report. A test reaches the code it exercises through the ordinary <code>alias</code>, resolved against <code>src/</code>.</p>\n<p><code>ramos test</code> with no argument runs everything under <code>src/test/</code>; with a file argument it runs just that file.</p>\n<p>The trait requires nothing — it is a marker, so being run is something a module opts into rather than something its name causes. A test module may define helpers alongside its tests; only public <code>test_</code> functions are called, and each is called with no arguments.</p>\n<p>A test stops at its first failed <code>assert</code>, and the run continues with the next test. See <code><a href=\"#/Kernel:assert\">Kernel.assert</a></code> for what a failure reports.</p>\n</section>\n"},"Thread":{"title":"Thread — Ramos","heading":"Thread","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Thread — run a computation on another thread and collect its result.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Thread — run a computation on another thread and collect its result.</p>\n<p>A thread is a one-shot piece of parallel work. <code>start</code> starts a zero-argument lambda on its own thread and hands back a handle at once, without waiting; <code>await</code> waits on the handle and returns what the lambda produced. Between the two, the caller and the spawned work run at the same time.</p>\n<pre><code>t = <span class=\"tok-type\">Thread</span>.start(<span class=\"tok-kw\">do</span> -&gt; slow_sum())\nother_work()                        <span class=\"tok-com\"># runs while the thread does</span>\ntotal = <span class=\"tok-type\">Thread</span>.await(t)             <span class=\"tok-com\"># now wait for it</span></code></pre>\n<p>To fan several out and gather them, start them all, then <code>await_all</code> the handles — every thread is already running, so the wait is for the slowest, not the sum:</p>\n<pre><code>[<span class=\"tok-kw\">do</span> -&gt; a(), <span class=\"tok-kw\">do</span> -&gt; b(), <span class=\"tok-kw\">do</span> -&gt; c()]\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> f -&gt; <span class=\"tok-type\">Thread</span>.start(f))\n| <span class=\"tok-type\">Thread</span>.await_all()</code></pre>\n<p>The concurrency is real. Two threads each sleeping 300ms finish in ~300ms, not 600 — the sleeps overlap.</p>\n<p>A thread produces a <em></em>value<em></em>, where an [[Actor]] holds <em></em>state<em></em> and answers messages over its lifetime. Reach for a thread to fan a batch of independent work out and gather it back; reach for an actor for something long-lived that many callers talk to.</p>\n<p>What crosses to the worker is the lambda alone — it carries its own captured scope. Everything else is fresh, exactly as for an actor: the worker has its own root and <em></em>cannot see the caller&#39;s running actors<em></em>, so a started lambda cannot message an actor. A failure inside the lambda is not lost; it surfaces as a runtime error at the <code>await</code> that collects it.</p>\n<p>A thread&#39;s output is <em></em>live<em></em>: it prints to the same place as the caller, as it happens, so a started <code>println</code> may land in either order relative to the caller&#39;s — though never split, since a whole line is written at once. (An actor is the opposite: it buffers and delivers its output at the reply, so actor lines never interleave.)</p>\n<p><em></em>Await what you start.<em></em> A handle is a value, so it can be held, passed, and awaited from anywhere — but a thread nobody awaits is detached: its <em>result</em> is never collected, and it may be cut off when the program ends. There is no registry of threads and no automatic wait at program end; the handle is the only way back to the work.</p>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#start/1\" class=\"fn-sig\"><code>function start(f)</code></a>\n    <span class=\"fn-summary\">Starts <code>f</code>, a lambda of no arguments, on its own thread and returns a handle at once — the caller does not wait. Pass the handle to <code>await</code> for the result.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#await/1\" class=\"fn-sig\"><code>function await(t)</code></a>\n    <span class=\"fn-summary\">Waits for the thread <code>t</code> to finish and returns what its lambda produced. A failure inside the lambda comes back here as a runtime error. The thread&#39;s output is written now, once; awaiting the same handle again returns the same value without printing twice.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#await_all/1\" class=\"fn-sig\"><code>function await_all(threads)</code></a>\n    <span class=\"fn-summary\">Waits for every thread in <code>threads</code> and returns their results as a list, in the same order as the handles — however the threads happened to finish. Since each was already running, this waits for the slowest, not the sum.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#map/2\" class=\"fn-sig\"><code>function map(list, f)</code></a>\n    <span class=\"fn-summary\">A parallel <code><a href=\"#/List:map\">List.map</a></code>: <code>f</code> is applied to every element on its own thread, and the results are gathered in order. Every element&#39;s work overlaps, so a list of slow, independent computations finishes in about the time of the slowest rather than their sum.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#each/2\" class=\"fn-sig\"><code>function each(list, f)</code></a>\n    <span class=\"fn-summary\">Like <code>map</code>, but for side effects: <code>f</code> runs on its own thread for every element, all of them are waited for, and <code>nil</code> comes back. Use it to run a batch of independent effects at once and be sure they have all finished.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"start/1\">\n  <h3 class=\"fn-title\"><code>function start(f)</code> <a class=\"anchor\" href=\"#start/1\">¶</a></h3>\n<p>Starts <code>f</code>, a lambda of no arguments, on its own thread and returns a handle at once — the caller does not wait. Pass the handle to <code>await</code> for the result.</p>\n<p>Has no Ramos body beyond the native call: starting a thread runs a lambda, which an ordinary native handler cannot do — reaching back into the evaluator to run it — so the interpreter intercepts this particular <code>native</code> call rather than dispatching it through the plain native table.</p>\n<pre><code>t = <span class=\"tok-type\">Thread</span>.start(<span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">6</span> * <span class=\"tok-lit\">7</span>)\n<span class=\"tok-type\">Thread</span>.await(t)   <span class=\"tok-com\"># == 42</span></code></pre>\n<dl class=\"params\">\n  <dt><code>f</code></dt>\n  <dd>a lambda of no arguments</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Thread\">Thread</a></code> handle</p>\n</div>\n<div class=\"fn-detail\" id=\"await/1\">\n  <h3 class=\"fn-title\"><code>function await(t)</code> <a class=\"anchor\" href=\"#await/1\">¶</a></h3>\n<p>Waits for the thread <code>t</code> to finish and returns what its lambda produced. A failure inside the lambda comes back here as a runtime error. The thread&#39;s output is written now, once; awaiting the same handle again returns the same value without printing twice.</p>\n<p>Has no Ramos body beyond the native call, for the reason given on <code>start</code>.</p>\n<pre><code><span class=\"tok-type\">Thread</span>.await(<span class=\"tok-type\">Thread</span>.start(<span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-str\">&quot;done&quot;</span>))   <span class=\"tok-com\"># == &quot;done&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>t</code></dt>\n  <dd>a <code><a href=\"#/Thread\">Thread</a></code> handle from <code>start</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> whatever the started lambda returned</p>\n</div>\n<div class=\"fn-detail\" id=\"await_all/1\">\n  <h3 class=\"fn-title\"><code>function await_all(threads)</code> <a class=\"anchor\" href=\"#await_all/1\">¶</a></h3>\n<p>Waits for every thread in <code>threads</code> and returns their results as a list, in the same order as the handles — however the threads happened to finish. Since each was already running, this waits for the slowest, not the sum.</p>\n<p>A failure in any of them surfaces as a runtime error, from the first handle whose thread failed. Pair it with <code>start</code> to run a batch at once:</p>\n<pre><code>[<span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">1</span>, <span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">2</span> + <span class=\"tok-lit\">2</span>]\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> f -&gt; <span class=\"tok-type\">Thread</span>.start(f))\n| <span class=\"tok-type\">Thread</span>.await_all()                <span class=\"tok-com\"># == [2, 4]</span></code></pre>\n<pre><code><span class=\"tok-type\">Thread</span>.await_all([<span class=\"tok-type\">Thread</span>.start(<span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">:a</span>), <span class=\"tok-type\">Thread</span>.start(<span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">:b</span>)])   <span class=\"tok-com\"># == [:a, :b]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>threads</code></dt>\n  <dd>a <code><a href=\"#/List\">List</a></code> of <code><a href=\"#/Thread\">Thread</a></code> handles from <code>start</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/List\">List</a></code> of their results, in the order of the handles</p>\n</div>\n<div class=\"fn-detail\" id=\"map/2\">\n  <h3 class=\"fn-title\"><code>function map(list, f)</code> <a class=\"anchor\" href=\"#map/2\">¶</a></h3>\n<p>A parallel <code><a href=\"#/List:map\">List.map</a></code>: <code>f</code> is applied to every element on its own thread, and the results are gathered in order. Every element&#39;s work overlaps, so a list of slow, independent computations finishes in about the time of the slowest rather than their sum.</p>\n<p><code>f</code> runs on another thread, so it cannot reach the caller&#39;s actors — the same rule as <code>start</code>. Order is preserved: the result lines up with <code>list</code> however the threads happen to finish.</p>\n<pre><code><span class=\"tok-type\">Thread</span>.map([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-kw\">do</span> n -&gt; n * n)   <span class=\"tok-com\"># == [1, 4, 9]</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to map over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda applied to each element, on its own thread</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new list of <code>f</code> applied to each element, in the original order</p>\n</div>\n<div class=\"fn-detail\" id=\"each/2\">\n  <h3 class=\"fn-title\"><code>function each(list, f)</code> <a class=\"anchor\" href=\"#each/2\">¶</a></h3>\n<p>Like <code>map</code>, but for side effects: <code>f</code> runs on its own thread for every element, all of them are waited for, and <code>nil</code> comes back. Use it to run a batch of independent effects at once and be sure they have all finished.</p>\n<pre><code><span class=\"tok-type\">Thread</span>.each([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-kw\">do</span> n -&gt; n)   <span class=\"tok-com\"># == nil</span></code></pre>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to iterate over</dd>\n  <dt><code>f</code></dt>\n  <dd>a lambda run once per element, on its own thread, for its effect</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>nil</code></p>\n</div>\n</section>\n"},"Time":{"title":"Time — Ramos","heading":"Time","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Time — a time of day: <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>, with no date and no time zone.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Time — a time of day: <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>, with no date and no time zone.</p>\n<pre><code>t = <span class=\"tok-type\">Time</span>.new(<span class=\"tok-lit\">13</span>, <span class=\"tok-lit\">45</span>, <span class=\"tok-lit\">30</span>)\nt.hour     <span class=\"tok-com\"># == 13</span>\nt.minute   <span class=\"tok-com\"># == 45</span></code></pre>\n<p>There is no day to carry into, so every field wraps at its own boundary instead of normalizing into the next one the way <code><a href=\"#/Date\">Date</a></code>&#39;s <code>day</code> does — <code>add_hours</code> past <code>23</code> wraps back to <code>0</code>:</p>\n<pre><code><span class=\"tok-type\">Time</span>.add_hours(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">23</span>}, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == Time{hour: 1, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<p>Conventions:</p>\n<ul><li><code>compare</code> follows <code><a href=\"#/Integer:compare\">Integer.compare</a></code>: <code>-1</code>, <code>0</code>, <code>1</code>.</li><li><code>to_iso</code>/<code>parse</code> round-trip through <code>&quot;HH:MM:SS.mmm&quot;</code>; <code>parse</code> also accepts a bare <code>&quot;HH:MM:SS&quot;</code> (millisecond <code>0</code>) and a fractional part of any precision (<code>&quot;HH:MM:SS.5&quot;</code> is 500ms, <code>&quot;HH:MM:SS.123456&quot;</code> truncates to <code>123</code>ms) — this is also what the <code>T&quot;...&quot;</code> sigil parses.</li><li>see <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> for this paired with a calendar date.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#new/3\" class=\"fn-sig\"><code>function new(hour, minute, second)</code></a>\n    <span class=\"fn-summary\">Builds a <code><a href=\"#/Time\">Time</a></code> with <code>millisecond: 0</code>. Every field wraps at its own boundary rather than raising — <code>hour: 24</code> is <code>0</code>, <code>hour: -1</code> is <code>23</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#now/0\" class=\"fn-sig\"><code>function now()</code></a>\n    <span class=\"fn-summary\">The current time of day, read from the wall clock as UTC.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_millis_of_day/1\" class=\"fn-sig\"><code>function from_millis_of_day(ms)</code></a>\n    <span class=\"fn-summary\">The <code><a href=\"#/Time\">Time</a></code> that is <code>ms</code> milliseconds after midnight, wrapping at a day (<code>86400000</code> ms) either direction.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#from_millis_of_day_raw/1\" class=\"fn-sig\"><code>helper from_millis_of_day_raw(ms)</code></a>\n    <span class=\"fn-summary\">(private) <code>from_millis_of_day</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#parse/1\" class=\"fn-sig\"><code>function parse(text)</code></a>\n    <span class=\"fn-summary\">Parses <code>&quot;HH:MM:SS&quot;</code> or <code>&quot;HH:MM:SS.fraction&quot;</code>, <code>fraction</code> any number of digits (padded or truncated to milliseconds) — the shape <code>to_iso</code> renders, and what the <code>T&quot;...&quot;</code> sigil uses.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_millis_of_day/1\" class=\"fn-sig\"><code>function to_millis_of_day(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> as milliseconds since midnight.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#to_millis_of_day_raw/1\" class=\"fn-sig\"><code>helper to_millis_of_day_raw(self)</code></a>\n    <span class=\"fn-summary\">(private) <code>to_millis_of_day</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_iso/1\" class=\"fn-sig\"><code>function to_iso(self)</code></a>\n    <span class=\"fn-summary\"><code>self</code> rendered as <code>&quot;HH:MM:SS.mmm&quot;</code>, zero-padded.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_hours/2\" class=\"fn-sig\"><code>function add_hours(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> hours, wrapping at a day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_minutes/2\" class=\"fn-sig\"><code>function add_minutes(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> minutes, wrapping at a day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#add_seconds/2\" class=\"fn-sig\"><code>function add_seconds(self, n)</code></a>\n    <span class=\"fn-summary\"><code>self</code> shifted by <code>n</code> seconds, wrapping at a day.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(self, other)</code></a>\n    <span class=\"fn-summary\">Orders two times as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> uses, since there is no built-in <code>&lt;</code>/<code>&gt;</code> for structs.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#diff_millis/2\" class=\"fn-sig\"><code>function diff_millis(self, other)</code></a>\n    <span class=\"fn-summary\">The number of milliseconds from <code>other</code> to <code>self</code> — positive when <code>self</code> is later, negative when earlier. Both are read as times on the same day, so this does not wrap.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#raw_millis/4\" class=\"fn-sig\"><code>helper raw_millis(hour, minute, second, millisecond)</code></a>\n    <span class=\"fn-summary\">(private) Milliseconds since midnight for the given fields, unwrapped — shared arithmetic behind <code>new</code>, <code>parse</code> and <code>to_millis_of_day</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#shift/2\" class=\"fn-sig\"><code>helper shift(self, millis)</code></a>\n    <span class=\"fn-summary\">(private) <code>self</code> moved by <code>millis</code> milliseconds, wrapping at a day. Backs the <code>add_*</code> family.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#millis_from_fraction/1\" class=\"fn-sig\"><code>helper millis_from_fraction(digits)</code></a>\n    <span class=\"fn-summary\">(private) A fractional-second string of any length as milliseconds: <code>&quot;5&quot;</code> is <code>500</code>, <code>&quot;123456&quot;</code> truncates to <code>123</code>. Backs <code>parse</code>.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#pad/2\" class=\"fn-sig\"><code>helper pad(n, width)</code></a>\n    <span class=\"fn-summary\">(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"new/3\">\n  <h3 class=\"fn-title\"><code>function new(hour, minute, second)</code> <a class=\"anchor\" href=\"#new/3\">¶</a></h3>\n<p>Builds a <code><a href=\"#/Time\">Time</a></code> with <code>millisecond: 0</code>. Every field wraps at its own boundary rather than raising — <code>hour: 24</code> is <code>0</code>, <code>hour: -1</code> is <code>23</code>.</p>\n<pre><code><span class=\"tok-type\">Time</span>.new(<span class=\"tok-lit\">13</span>, <span class=\"tok-lit\">45</span>, <span class=\"tok-lit\">30</span>)   <span class=\"tok-com\"># == Time{hour: 13, minute: 45, second: 30, millisecond: 0}</span>\n<span class=\"tok-type\">Time</span>.new(<span class=\"tok-lit\">24</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>)     <span class=\"tok-com\"># == Time{hour: 0, minute: 0, second: 0, millisecond: 0}</span>\n<span class=\"tok-type\">Time</span>.new(-<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">0</span>, <span class=\"tok-lit\">0</span>)     <span class=\"tok-com\"># == Time{hour: 23, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>hour</code></dt>\n  <dd>the hour; wraps modulo 24</dd>\n  <dt><code>minute</code></dt>\n  <dd>the minute; wraps modulo 60</dd>\n  <dt><code>second</code></dt>\n  <dd>the second; wraps modulo 60</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"now/0\">\n  <h3 class=\"fn-title\"><code>function now()</code> <a class=\"anchor\" href=\"#now/0\">¶</a></h3>\n<p>The current time of day, read from the wall clock as UTC.</p>\n<pre><code><span class=\"tok-type\">Time</span>.now().hour &lt; <span class=\"tok-lit\">24</span>   <span class=\"tok-com\"># == true</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> the current <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_millis_of_day/1\">\n  <h3 class=\"fn-title\"><code>function from_millis_of_day(ms)</code> <a class=\"anchor\" href=\"#from_millis_of_day/1\">¶</a></h3>\n<p>The <code><a href=\"#/Time\">Time</a></code> that is <code>ms</code> milliseconds after midnight, wrapping at a day (<code>86400000</code> ms) either direction.</p>\n<pre><code><span class=\"tok-type\">Time</span>.from_millis_of_day(<span class=\"tok-lit\">0</span>)        <span class=\"tok-com\"># == Time{hour: 0, minute: 0, second: 0, millisecond: 0}</span>\n<span class=\"tok-type\">Time</span>.from_millis_of_day(-<span class=\"tok-lit\">1</span>)       <span class=\"tok-com\"># == Time{hour: 23, minute: 59, second: 59, millisecond: 999}</span>\n<span class=\"tok-type\">Time</span>.from_millis_of_day(<span class=\"tok-lit\">86400000</span>) <span class=\"tok-com\"># == Time{hour: 0, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>ms</code></dt>\n  <dd>milliseconds since midnight, as an <code><a href=\"#/Integer\">Integer</a></code>; any value wraps</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_millis_of_day_raw/1\">\n  <h3 class=\"fn-title\"><code>helper from_millis_of_day_raw(ms)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#from_millis_of_day_raw/1\">¶</a></h3>\n<p>(private) <code>from_millis_of_day</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</p>\n</div>\n<div class=\"fn-detail\" id=\"parse/1\">\n  <h3 class=\"fn-title\"><code>function parse(text)</code> <a class=\"anchor\" href=\"#parse/1\">¶</a></h3>\n<p>Parses <code>&quot;HH:MM:SS&quot;</code> or <code>&quot;HH:MM:SS.fraction&quot;</code>, <code>fraction</code> any number of digits (padded or truncated to milliseconds) — the shape <code>to_iso</code> renders, and what the <code>T&quot;...&quot;</code> sigil uses.</p>\n<pre><code><span class=\"tok-type\">Time</span>.parse(<span class=\"tok-str\">&quot;13:45:30&quot;</span>)       <span class=\"tok-com\"># == Time{hour: 13, minute: 45, second: 30, millisecond: 0}</span>\n<span class=\"tok-type\">Time</span>.parse(<span class=\"tok-str\">&quot;13:45:30.5&quot;</span>)     <span class=\"tok-com\"># == Time{hour: 13, minute: 45, second: 30, millisecond: 500}</span>\n<span class=\"tok-type\">Time</span>.parse(<span class=\"tok-str\">&quot;13:45:30.123456&quot;</span>)\n<span class=\"tok-com\"># == Time{hour: 13, minute: 45, second: 30, millisecond: 123}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>text</code></dt>\n  <dd>a time of day in that form</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"to_millis_of_day/1\">\n  <h3 class=\"fn-title\"><code>function to_millis_of_day(self)</code> <a class=\"anchor\" href=\"#to_millis_of_day/1\">¶</a></h3>\n<p><code>self</code> as milliseconds since midnight.</p>\n<pre><code><span class=\"tok-type\">Time</span>.to_millis_of_day(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">0</span>, minute: <span class=\"tok-lit\">0</span>, second: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 1000</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the time to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> in <code>0</code>..<code>86399999</code></p>\n</div>\n<div class=\"fn-detail\" id=\"to_millis_of_day_raw/1\">\n  <h3 class=\"fn-title\"><code>helper to_millis_of_day_raw(self)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#to_millis_of_day_raw/1\">¶</a></h3>\n<p>(private) <code>to_millis_of_day</code>&#39;s own body, so <code>shift</code> can reach it without calling the public function.</p>\n</div>\n<div class=\"fn-detail\" id=\"to_iso/1\">\n  <h3 class=\"fn-title\"><code>function to_iso(self)</code> <a class=\"anchor\" href=\"#to_iso/1\">¶</a></h3>\n<p><code>self</code> rendered as <code>&quot;HH:MM:SS.mmm&quot;</code>, zero-padded.</p>\n<pre><code><span class=\"tok-type\">Time</span>.to_iso(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">3</span>, minute: <span class=\"tok-lit\">5</span>, second: <span class=\"tok-lit\">9</span>, millisecond: <span class=\"tok-lit\">7</span>})   <span class=\"tok-com\"># == &quot;03:05:09.007&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the time to render</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the ISO-8601-shaped string</p>\n</div>\n<div class=\"fn-detail\" id=\"add_hours/2\">\n  <h3 class=\"fn-title\"><code>function add_hours(self, n)</code> <a class=\"anchor\" href=\"#add_hours/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> hours, wrapping at a day.</p>\n<pre><code><span class=\"tok-type\">Time</span>.add_hours(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">23</span>}, <span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == Time{hour: 1, minute: 0, second: 0, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many hours to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_minutes/2\">\n  <h3 class=\"fn-title\"><code>function add_minutes(self, n)</code> <a class=\"anchor\" href=\"#add_minutes/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> minutes, wrapping at a day.</p>\n<pre><code><span class=\"tok-type\">Time</span>.add_minutes(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">0</span>, minute: <span class=\"tok-lit\">30</span>}, <span class=\"tok-lit\">45</span>)   <span class=\"tok-com\"># == Time{hour: 1, minute: 15, second: 0, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many minutes to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"add_seconds/2\">\n  <h3 class=\"fn-title\"><code>function add_seconds(self, n)</code> <a class=\"anchor\" href=\"#add_seconds/2\">¶</a></h3>\n<p><code>self</code> shifted by <code>n</code> seconds, wrapping at a day.</p>\n<pre><code><span class=\"tok-type\">Time</span>.add_seconds(<span class=\"tok-type\">Time</span>{second: <span class=\"tok-lit\">45</span>}, <span class=\"tok-lit\">30</span>)   <span class=\"tok-com\"># == Time{hour: 0, minute: 1, second: 15, millisecond: 0}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the starting time</dd>\n  <dt><code>n</code></dt>\n  <dd>how many seconds to add (negative subtracts)</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/Time\">Time</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(self, other)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two times as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> uses, since there is no built-in <code>&lt;</code>/<code>&gt;</code> for structs.</p>\n<pre><code><span class=\"tok-type\">Time</span>.compare(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">2</span>})   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">Time</span>.compare(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">2</span>}, <span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 1</span>\n<span class=\"tok-type\">Time</span>.compare(<span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">1</span>}, <span class=\"tok-type\">Time</span>{hour: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the first time</dd>\n  <dt><code>other</code></dt>\n  <dd>the second time</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>self</code> is earlier, <code>1</code> when later, <code>0</code> when equal</p>\n</div>\n<div class=\"fn-detail\" id=\"diff_millis/2\">\n  <h3 class=\"fn-title\"><code>function diff_millis(self, other)</code> <a class=\"anchor\" href=\"#diff_millis/2\">¶</a></h3>\n<p>The number of milliseconds from <code>other</code> to <code>self</code> — positive when <code>self</code> is later, negative when earlier. Both are read as times on the same day, so this does not wrap.</p>\n<pre><code><span class=\"tok-type\">Time</span>.diff_millis(<span class=\"tok-type\">Time</span>{second: <span class=\"tok-lit\">10</span>}, <span class=\"tok-type\">Time</span>{second: <span class=\"tok-lit\">1</span>})   <span class=\"tok-com\"># == 9000</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the later (or earlier) time</dd>\n  <dt><code>other</code></dt>\n  <dd>the time to measure from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> an <code><a href=\"#/Integer\">Integer</a></code> millisecond count</p>\n</div>\n<div class=\"fn-detail\" id=\"raw_millis/4\">\n  <h3 class=\"fn-title\"><code>helper raw_millis(hour, minute, second, millisecond)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#raw_millis/4\">¶</a></h3>\n<p>(private) Milliseconds since midnight for the given fields, unwrapped — shared arithmetic behind <code>new</code>, <code>parse</code> and <code>to_millis_of_day</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"shift/2\">\n  <h3 class=\"fn-title\"><code>helper shift(self, millis)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#shift/2\">¶</a></h3>\n<p>(private) <code>self</code> moved by <code>millis</code> milliseconds, wrapping at a day. Backs the <code>add_*</code> family.</p>\n</div>\n<div class=\"fn-detail\" id=\"millis_from_fraction/1\">\n  <h3 class=\"fn-title\"><code>helper millis_from_fraction(digits)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#millis_from_fraction/1\">¶</a></h3>\n<p>(private) A fractional-second string of any length as milliseconds: <code>&quot;5&quot;</code> is <code>500</code>, <code>&quot;123456&quot;</code> truncates to <code>123</code>. Backs <code>parse</code>.</p>\n</div>\n<div class=\"fn-detail\" id=\"pad/2\">\n  <h3 class=\"fn-title\"><code>helper pad(n, width)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#pad/2\">¶</a></h3>\n<p>(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>to_iso</code>.</p>\n</div>\n</section>\n"},"TimeZone":{"title":"TimeZone — Ramos","heading":"TimeZone","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>TimeZone — a name paired with a fixed offset from UTC.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>TimeZone — a name paired with a fixed offset from UTC.</p>\n<p>There is no IANA time zone database here (no dependency ships one), so a <code><a href=\"#/TimeZone\">TimeZone</a></code> cannot answer &quot;what was the offset in Berlin on this date&quot; — daylight saving and historical rule changes are out of scope. What it <em>does</em> give a raw <code>offset_minutes</code> integer (as <code><a href=\"#/DateTime\">DateTime</a></code> takes) is a name to carry alongside it, and the shared <code>&quot;Z&quot;</code>/<code>±HH:MM</code> text form:</p>\n<pre><code>ist = <span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;IST&quot;</span>, <span class=\"tok-lit\">330</span>)\nist.name             <span class=\"tok-com\"># == &quot;IST&quot;</span>\nist.offset_minutes   <span class=\"tok-com\"># == 330</span>\n<span class=\"tok-type\">DateTime</span>.now_in(ist).offset_minutes   <span class=\"tok-com\"># == 330</span></code></pre>\n<p><code>utc</code>, <code>fixed</code>, and <code>from_offset_minutes</code> build one; <code><a href=\"#/DateTime:now_in\">DateTime.now_in</a></code> and <code><a href=\"#/DateTime:in_time_zone\">DateTime.in_time_zone</a></code> are the bridge to <code><a href=\"#/DateTime\">DateTime</a></code>. A <code><a href=\"#/DateTime\">DateTime</a></code> only ever keeps the numeric <code>offset_minutes</code>, not the name it arrived with, so reading it back with <code><a href=\"#/DateTime:time_zone\">DateTime.time_zone</a></code> gives a zone named after its offset (<code>&quot;+05:30&quot;</code>), not <code>&quot;IST&quot;</code> — a name is a label you attach going in, not one a <code><a href=\"#/DateTime\">DateTime</a></code> remembers on its own.</p>\n<p>Conventions:</p>\n<ul><li><code>offset_minutes</code> is checked against the real range any UTC offset can fall in, <code>-12:00</code>..<code>+14:00</code> (<code>-720</code>..<code>840</code>), but the <em>name</em> is never checked — <code><a href=\"#/TimeZone:fixed\">TimeZone.fixed(&quot;literally anything&quot;, 0)</a></code> is fine. A <code><a href=\"#/TimeZone\">TimeZone</a></code> only ever carries what its caller gives it.</li><li><code>compare</code> orders by <code>offset_minutes</code>, west to east.</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#utc/0\" class=\"fn-sig\"><code>function utc()</code></a>\n    <span class=\"fn-summary\">The UTC zone: <code>offset_minutes: 0</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#fixed/2\" class=\"fn-sig\"><code>function fixed(name, offset_minutes)</code></a>\n    <span class=\"fn-summary\">A zone called <code>name</code>, at a fixed <code>offset_minutes</code> from UTC.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_offset_minutes/1\" class=\"fn-sig\"><code>function from_offset_minutes(offset_minutes)</code></a>\n    <span class=\"fn-summary\">A zone named after its own offset — <code>&quot;UTC&quot;</code> at <code>0</code>, otherwise the <code>±HH:MM</code> text <code>offset_text</code> renders. For when a zone has no friendlier name to give it.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#parse/1\" class=\"fn-sig\"><code>function parse(text)</code></a>\n    <span class=\"fn-summary\">A zone parsed from <code>offset_text</code>&#39;s shape: <code>&quot;Z&quot;</code> for UTC, or a signed <code>&quot;±HH:MM&quot;</code>. The zone is named after the offset — this is the inverse of <code>from_offset_minutes</code>, not of <code>fixed</code> (a parsed zone has no way to recover a friendlier name).</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#offset_text/1\" class=\"fn-sig\"><code>function offset_text(offset_minutes)</code></a>\n    <span class=\"fn-summary\"><code>offset_minutes</code> rendered as <code>&quot;Z&quot;</code> (when zero) or a signed <code>&quot;±HH:MM&quot;</code> — the suffix <code><a href=\"#/DateTime:to_iso\">DateTime.to_iso</a></code> appends.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#parse_offset_text/1\" class=\"fn-sig\"><code>function parse_offset_text(text)</code></a>\n    <span class=\"fn-summary\">The inverse of <code>offset_text</code>: <code>&quot;Z&quot;</code> or a signed <code>&quot;±HH:MM&quot;</code> parsed back to a minute count.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#compare/2\" class=\"fn-sig\"><code>function compare(self, other)</code></a>\n    <span class=\"fn-summary\">Orders two zones by <code>offset_minutes</code>, west to east, as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> uses. Two zones at the same offset compare equal regardless of name.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#pad/2\" class=\"fn-sig\"><code>helper pad(n, width)</code></a>\n    <span class=\"fn-summary\">(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>offset_text</code>.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"utc/0\">\n  <h3 class=\"fn-title\"><code>function utc()</code> <a class=\"anchor\" href=\"#utc/0\">¶</a></h3>\n<p>The UTC zone: <code>offset_minutes: 0</code>.</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.utc()   <span class=\"tok-com\"># == TimeZone{name: &quot;UTC&quot;, offset_minutes: 0}</span></code></pre>\n<p class=\"returns\"><strong>Returns:</strong> <code>TimeZone{name: &quot;UTC&quot;, offset_minutes: 0}</code></p>\n</div>\n<div class=\"fn-detail\" id=\"fixed/2\">\n  <h3 class=\"fn-title\"><code>function fixed(name, offset_minutes)</code> <a class=\"anchor\" href=\"#fixed/2\">¶</a></h3>\n<p>A zone called <code>name</code>, at a fixed <code>offset_minutes</code> from UTC.</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;IST&quot;</span>, <span class=\"tok-lit\">330</span>)   <span class=\"tok-com\"># == TimeZone{name: &quot;IST&quot;, offset_minutes: 330}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>name</code></dt>\n  <dd>the zone&#39;s label — anything, not checked against <code>offset_minutes</code></dd>\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC, in minutes; must be <code>-720</code>..<code>840</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/TimeZone\">TimeZone</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"from_offset_minutes/1\">\n  <h3 class=\"fn-title\"><code>function from_offset_minutes(offset_minutes)</code> <a class=\"anchor\" href=\"#from_offset_minutes/1\">¶</a></h3>\n<p>A zone named after its own offset — <code>&quot;UTC&quot;</code> at <code>0</code>, otherwise the <code>±HH:MM</code> text <code>offset_text</code> renders. For when a zone has no friendlier name to give it.</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.from_offset_minutes(<span class=\"tok-lit\">0</span>)      <span class=\"tok-com\"># == TimeZone{name: &quot;UTC&quot;, offset_minutes: 0}</span>\n<span class=\"tok-type\">TimeZone</span>.from_offset_minutes(-<span class=\"tok-lit\">300</span>)   <span class=\"tok-com\"># == TimeZone{name: &quot;-05:00&quot;, offset_minutes: -300}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC, in minutes; must be <code>-720</code>..<code>840</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new <code><a href=\"#/TimeZone\">TimeZone</a></code>, named by its offset</p>\n</div>\n<div class=\"fn-detail\" id=\"parse/1\">\n  <h3 class=\"fn-title\"><code>function parse(text)</code> <a class=\"anchor\" href=\"#parse/1\">¶</a></h3>\n<p>A zone parsed from <code>offset_text</code>&#39;s shape: <code>&quot;Z&quot;</code> for UTC, or a signed <code>&quot;±HH:MM&quot;</code>. The zone is named after the offset — this is the inverse of <code>from_offset_minutes</code>, not of <code>fixed</code> (a parsed zone has no way to recover a friendlier name).</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.parse(<span class=\"tok-str\">&quot;Z&quot;</span>)        <span class=\"tok-com\"># == TimeZone{name: &quot;UTC&quot;, offset_minutes: 0}</span>\n<span class=\"tok-type\">TimeZone</span>.parse(<span class=\"tok-str\">&quot;-05:00&quot;</span>)   <span class=\"tok-com\"># == TimeZone{name: &quot;-05:00&quot;, offset_minutes: -300}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>text</code></dt>\n  <dd><code>&quot;Z&quot;</code> or a signed <code>&quot;±HH:MM&quot;</code> offset</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the corresponding <code><a href=\"#/TimeZone\">TimeZone</a></code></p>\n</div>\n<div class=\"fn-detail\" id=\"offset_text/1\">\n  <h3 class=\"fn-title\"><code>function offset_text(offset_minutes)</code> <a class=\"anchor\" href=\"#offset_text/1\">¶</a></h3>\n<p><code>offset_minutes</code> rendered as <code>&quot;Z&quot;</code> (when zero) or a signed <code>&quot;±HH:MM&quot;</code> — the suffix <code><a href=\"#/DateTime:to_iso\">DateTime.to_iso</a></code> appends.</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.offset_text(<span class=\"tok-lit\">0</span>)      <span class=\"tok-com\"># == &quot;Z&quot;</span>\n<span class=\"tok-type\">TimeZone</span>.offset_text(<span class=\"tok-lit\">330</span>)    <span class=\"tok-com\"># == &quot;+05:30&quot;</span>\n<span class=\"tok-type\">TimeZone</span>.offset_text(-<span class=\"tok-lit\">300</span>)   <span class=\"tok-com\"># == &quot;-05:00&quot;</span></code></pre>\n<dl class=\"params\">\n  <dt><code>offset_minutes</code></dt>\n  <dd>the offset from UTC, in minutes</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>&quot;Z&quot;</code> or a signed <code>&quot;±HH:MM&quot;</code> string</p>\n</div>\n<div class=\"fn-detail\" id=\"parse_offset_text/1\">\n  <h3 class=\"fn-title\"><code>function parse_offset_text(text)</code> <a class=\"anchor\" href=\"#parse_offset_text/1\">¶</a></h3>\n<p>The inverse of <code>offset_text</code>: <code>&quot;Z&quot;</code> or a signed <code>&quot;±HH:MM&quot;</code> parsed back to a minute count.</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.parse_offset_text(<span class=\"tok-str\">&quot;Z&quot;</span>)        <span class=\"tok-com\"># == 0</span>\n<span class=\"tok-type\">TimeZone</span>.parse_offset_text(<span class=\"tok-str\">&quot;+05:30&quot;</span>)   <span class=\"tok-com\"># == 330</span>\n<span class=\"tok-type\">TimeZone</span>.parse_offset_text(<span class=\"tok-str\">&quot;-05:00&quot;</span>)   <span class=\"tok-com\"># == -300</span></code></pre>\n<dl class=\"params\">\n  <dt><code>text</code></dt>\n  <dd><code>&quot;Z&quot;</code> or a signed <code>&quot;±HH:MM&quot;</code> offset</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the offset from UTC, in minutes</p>\n</div>\n<div class=\"fn-detail\" id=\"compare/2\">\n  <h3 class=\"fn-title\"><code>function compare(self, other)</code> <a class=\"anchor\" href=\"#compare/2\">¶</a></h3>\n<p>Orders two zones by <code>offset_minutes</code>, west to east, as <code>-1</code>, <code>0</code> or <code>1</code> — the shape <code><a href=\"#/Integer:compare\">Integer.compare</a></code> uses. Two zones at the same offset compare equal regardless of name.</p>\n<pre><code><span class=\"tok-type\">TimeZone</span>.compare(<span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;A&quot;</span>, -<span class=\"tok-lit\">300</span>), <span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;B&quot;</span>, <span class=\"tok-lit\">0</span>))   <span class=\"tok-com\"># == -1</span>\n<span class=\"tok-type\">TimeZone</span>.compare(<span class=\"tok-type\">TimeZone</span>.utc(), <span class=\"tok-type\">TimeZone</span>.fixed(<span class=\"tok-str\">&quot;Also UTC&quot;</span>, <span class=\"tok-lit\">0</span>))       <span class=\"tok-com\"># == 0</span></code></pre>\n<dl class=\"params\">\n  <dt><code>self</code></dt>\n  <dd>the first zone</dd>\n  <dt><code>other</code></dt>\n  <dd>the second zone</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> <code>-1</code> when <code>self</code> is west of <code>other</code>, <code>1</code> when east, <code>0</code> when level</p>\n</div>\n<div class=\"fn-detail\" id=\"pad/2\">\n  <h3 class=\"fn-title\"><code>helper pad(n, width)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#pad/2\">¶</a></h3>\n<p>(private) <code>n</code> rendered as decimal digits, left-padded with zeros to at least <code>width</code> characters. Backs <code>offset_text</code>.</p>\n</div>\n</section>\n"},"Tuple":{"title":"Tuple — Ramos","heading":"Tuple","content_class":"","body":"<section class=\"module-summary\">\n<h2>Summary</h2>\n  <p>Tuple — operations on fixed-size, immutable tuples.</p>\n</section>\n<section class=\"module-description\">\n<h2>Module documentation</h2>\n<p>Tuple — operations on fixed-size, immutable tuples.</p>\n<p>A tuple in Ramos is a small, fixed-arity record written with parentheses, e.g. <code>(1, 2)</code> or <code>(:ok, value)</code>. Tuples are immutable and heterogeneous, and their arity is fixed at creation — unlike lists, you cannot &quot;cons&quot; onto a tuple or recurse over its elements generically.</p>\n<p>Kernel already reads tuples — <code>size(t)</code>, <code>at(t, i)</code> and <code>to_list(t)</code> all work on one — so this module does not repeat them. What is left is the writer <code>set</code>, the constructor <code>from_list</code>, and the conveniences built on top. Reading by position is <code>at</code>, or destructuring when the arity is known:</p>\n<pre><code>result = (<span class=\"tok-lit\">:ok</span>, <span class=\"tok-lit\">42</span>)\nat(result, <span class=\"tok-lit\">0</span>)               <span class=\"tok-com\"># == :ok</span>\n(status, value) = result    <span class=\"tok-com\"># status == :ok, value == 42</span>\nto_list((<span class=\"tok-str\">&quot;a&quot;</span>, <span class=\"tok-str\">&quot;b&quot;</span>, <span class=\"tok-str\">&quot;c&quot;</span>))    <span class=\"tok-com\"># == [&quot;a&quot;, &quot;b&quot;, &quot;c&quot;]</span></code></pre>\n<p>Tagged tuples <code>(:ok, value)</code> / <code>(:error, reason)</code> are the idiomatic result type for fallible operations — see the README&#39;s pattern-matching section.</p>\n<p>Conventions:</p>\n<ul><li>indexes are zero-based.</li><li>out-of-range access returns <code>nil</code>; out-of-range <code>put</code> returns the tuple unchanged (tuples cannot change arity).</li></ul>\n</section>\n<section class=\"functions-summary\">\n<h2>Functions</h2>\n  <div class=\"fn-row\">\n    <a href=\"#set/3\" class=\"fn-sig\"><code>function set(tuple, index, value)</code></a>\n    <span class=\"fn-summary\">Returns a new tuple with the element at zero-based <code>index</code> replaced by <code>value</code>. An out-of-range <code>index</code> returns <code>tuple</code> unchanged — tuples cannot change arity.</span>\n  </div>\n  <div class=\"fn-row private\">\n    <a href=\"#replace_at/3\" class=\"fn-sig\"><code>helper replace_at(items, index, value)</code></a>\n    <span class=\"fn-summary\"></span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#from_list/1\" class=\"fn-sig\"><code>function from_list(list)</code></a>\n    <span class=\"fn-summary\">A tuple of the list&#39;s elements, in order — the inverse of Kernel&#39;s <code>to_list</code>. This is the only way to build a tuple whose arity is not written in the source.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#last/1\" class=\"fn-sig\"><code>function last(tuple)</code></a>\n    <span class=\"fn-summary\">Returns the last element of <code>tuple</code>.</span>\n  </div>\n  <div class=\"fn-row\">\n    <a href=\"#to_map/1\" class=\"fn-sig\"><code>function to_map(tuple)</code></a>\n    <span class=\"fn-summary\">Converts a tuple of <code>(key, value)</code> pairs into a map. Later pairs win on duplicate keys.</span>\n  </div>\n</section>\n<section class=\"functions-detail\">\n<div class=\"fn-detail\" id=\"set/3\">\n  <h3 class=\"fn-title\"><code>function set(tuple, index, value)</code> <a class=\"anchor\" href=\"#set/3\">¶</a></h3>\n<p>Returns a new tuple with the element at zero-based <code>index</code> replaced by <code>value</code>. An out-of-range <code>index</code> returns <code>tuple</code> unchanged — tuples cannot change arity.</p>\n<pre><code>is out of range</code></pre>\n<pre><code><span class=\"tok-type\">Tuple</span>.set((<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>), <span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">:x</span>)   <span class=\"tok-com\"># == (1, :x, 3)</span>\n<span class=\"tok-type\">Tuple</span>.set((<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>), <span class=\"tok-lit\">9</span>, <span class=\"tok-lit\">:x</span>)   <span class=\"tok-com\"># == (1, 2, 3)</span></code></pre>\n<dl class=\"params\">\n  <dt><code>tuple</code></dt>\n  <dd>the tuple to update</dd>\n  <dt><code>index</code></dt>\n  <dd>the zero-based position to replace</dd>\n  <dt><code>value</code></dt>\n  <dd>the new element to store at <code>index</code></dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a new tuple with <code>index</code> replaced, or <code>tuple</code> unchanged if <code>index</code></p>\n</div>\n<div class=\"fn-detail\" id=\"replace_at/3\">\n  <h3 class=\"fn-title\"><code>helper replace_at(items, index, value)</code> <span class=\"badge private\">private</span> <a class=\"anchor\" href=\"#replace_at/3\">¶</a></h3>\n</div>\n<div class=\"fn-detail\" id=\"from_list/1\">\n  <h3 class=\"fn-title\"><code>function from_list(list)</code> <a class=\"anchor\" href=\"#from_list/1\">¶</a></h3>\n<p>A tuple of the list&#39;s elements, in order — the inverse of Kernel&#39;s <code>to_list</code>. This is the only way to build a tuple whose arity is not written in the source.</p>\n<pre><code><span class=\"tok-type\">Tuple</span>.from_list([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>])   <span class=\"tok-com\"># == (1, 2, 3)</span>\n<span class=\"tok-type\">Tuple</span>.from_list([<span class=\"tok-lit\">:ok</span>, <span class=\"tok-lit\">42</span>])   <span class=\"tok-com\"># == (:ok, 42)</span></code></pre>\n<p>An empty list yields the zero-arity tuple, which has no literal form — <code>()</code> is not something you can write.</p>\n<dl class=\"params\">\n  <dt><code>list</code></dt>\n  <dd>the list to convert</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Tuple\">Tuple</a></code> of the list&#39;s elements, in order</p>\n</div>\n<div class=\"fn-detail\" id=\"last/1\">\n  <h3 class=\"fn-title\"><code>function last(tuple)</code> <a class=\"anchor\" href=\"#last/1\">¶</a></h3>\n<p>Returns the last element of <code>tuple</code>.</p>\n<pre><code><span class=\"tok-type\">Tuple</span>.last((<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>))   <span class=\"tok-com\"># == 3</span></code></pre>\n<dl class=\"params\">\n  <dt><code>tuple</code></dt>\n  <dd>the tuple to read from</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> the last element, or <code>nil</code> if <code>tuple</code> is empty</p>\n</div>\n<div class=\"fn-detail\" id=\"to_map/1\">\n  <h3 class=\"fn-title\"><code>function to_map(tuple)</code> <a class=\"anchor\" href=\"#to_map/1\">¶</a></h3>\n<p>Converts a tuple of <code>(key, value)</code> pairs into a map. Later pairs win on duplicate keys.</p>\n<pre><code><span class=\"tok-type\">Tuple</span>.to_map(((<span class=\"tok-lit\">:a</span>, <span class=\"tok-lit\">1</span>), (<span class=\"tok-lit\">:b</span>, <span class=\"tok-lit\">2</span>)))   <span class=\"tok-com\"># == {a: 1, b: 2}</span></code></pre>\n<dl class=\"params\">\n  <dt><code>tuple</code></dt>\n  <dd>a tuple whose elements are <code>(key, value)</code> pair tuples</dd>\n</dl>\n<p class=\"returns\"><strong>Returns:</strong> a <code><a href=\"#/Map\">Map</a></code> built from the pairs, later pairs winning on duplicate keys</p>\n</div>\n</section>\n"},"examples":{"title":"Examples — Ramos","heading":null,"content_class":"examples","body":"<h1>Examples</h1>\n<p>One short program per language feature, each taken straight from the fixtures in <code>tests/fixtures/features/</code> — so every snippet here lexes and parses as real Ramos.</p>\n<p>For the prose version of the same ground, see the <a href=\"#/guide\">language guide</a>.</p>\n<ul class=\"example-toc\">\n  <li><a href=\"#actor\">Actor</a> <span class=\"example-summary\">a module implements <code><a href=\"#/Actor\">Actor</a></code> by defining <code>call</code>, which returns <code>(reply, new_state)</code>. <code>cast</code> has no reply: it returns only the new state, and runs later — before the next call, or at program end.</span></li>\n  <li><a href=\"#alias\">Alias</a> <span class=\"example-summary\"><code>alias</code> gives a module a shorter name, cutting the verbosity of long namespaces. It works for pure modules and struct modules alike.</span></li>\n  <li><a href=\"#arithmetic\">Arithmetic</a> <span class=\"example-summary\">arithmetic operators. Whitespace around a binary operator is required: <code>1 + 2</code>, never <code>1+2</code>.</span></li>\n  <li><a href=\"#case\">Case</a> <span class=\"example-summary\"><code>case</code> matches a value against patterns top to bottom and runs the first match. <code>_</code> is the wildcard. Where <code>if</code> branches on a condition and <code>cond</code> on a chain of them, <code>case</code> branches on the <em>shape</em> of a value.</span></li>\n  <li><a href=\"#comments\">Comments</a> <span class=\"example-summary\">a comment runs from <code>#</code> to the end of the line.</span></li>\n  <li><a href=\"#comparison\">Comparison</a> <span class=\"example-summary\">comparison operators, each returning a bool.</span></li>\n  <li><a href=\"#cond\">Cond</a> <span class=\"example-summary\"><code>cond</code> takes the first arm whose condition is truthy. It is <code>case</code> over a sequence of guards, for when no single value is matched.</span></li>\n  <li><a href=\"#destructuring\">Destructuring</a> <span class=\"example-summary\">patterns work in plain assignment, not just in <code>case</code>.</span></li>\n  <li><a href=\"#doc_comments\">Doc comments</a> <span class=\"example-summary\">documentation lives inline, as a comment block placed immediately below the definition it describes. <code>@module_doc</code> documents a module, <code>@doc</code> a function; each marker is followed by an empty <code>#</code> line.</span></li>\n  <li><a href=\"#error_tuples\">Error tuples</a> <span class=\"example-summary\">Ramos has no exceptions. A function that can fail returns a tagged tuple instead: <code>(:ok, value)</code> on success, <code>(:error, (:type, msg))</code> on failure. The error is an ordinary value, so it is matched like any other.</span></li>\n  <li><a href=\"#guards\">Guards</a> <span class=\"example-summary\">a <code>when</code> guard sits between a pattern and its <code>-&gt;</code>, narrowing a match beyond the pattern&#39;s shape.</span></li>\n  <li><a href=\"#if\">If</a> <span class=\"example-summary\"><code>if</code> is the two-branch conditional: a condition, an indented body, and an optional <code>else</code>. Any value works as the condition, since only <code>false</code> and <code>nil</code> are falsy. There is no <code>else if</code> — a chain of conditions is <code>cond</code>.</span></li>\n  <li><a href=\"#implements\">Implements</a> <span class=\"example-summary\">a struct declares the traits it implements with <code>implements</code>, at the top of the struct body. It must define every function the trait requires; the trait&#39;s defaults come for free.</span></li>\n  <li><a href=\"#lambdas\">Lambdas</a> <span class=\"example-summary\">anonymous functions. Lambdas are values, so they can be bound, passed and piped.</span></li>\n  <li><a href=\"#list_concat\">List concat</a> <span class=\"example-summary\"><code>++</code> concatenates two lists, returning a new one.</span></li>\n  <li><a href=\"#literals\">Literals</a> <span class=\"example-summary\">one of every literal Ramos has.</span></li>\n  <li><a href=\"#logical\">Logical</a> <span class=\"example-summary\">the word-form logical operators. Ramos has no <code>&amp;&amp;</code>, <code>||</code> or <code>!</code>.</span></li>\n  <li><a href=\"#map_merge\">Map merge</a> <span class=\"example-summary\"><code>++</code> merges two maps, returning a new one.</span></li>\n  <li><a href=\"#module\">Module</a> <span class=\"example-summary\">a pure module is a namespace of functions with no per-instance state. It is itself a value, so it can be passed around and inspected.</span></li>\n  <li><a href=\"#multiline_strings\">Multiline strings</a> <span class=\"example-summary\"><code>&quot;&quot;&quot;</code> opens a multiline string. The opening quotes are followed straight by a newline, content sits one level (2 spaces) past the opening line, and the closing quotes stand alone at the opening line&#39;s indentation. Every content line keeps its trailing newline.</span></li>\n  <li><a href=\"#namespaced_module\">Namespaced module</a> <span class=\"example-summary\">module names are CamelCase and may be namespaced with <code>.</code>. The namespace maps to the file path: <code>MyApp.Business.SystemUser</code> lives at <code>src/my_app/business/system_user.rmo</code>.</span></li>\n  <li><a href=\"#pipe\">Pipe</a> <span class=\"example-summary\"><code>|</code> passes the value on its left as the <em>first argument</em> to the module function on its right, so data flows top to bottom. It always starts its own line, at the same indentation as what comes before it.</span></li>\n  <li><a href=\"#private_functions\">Private functions</a> <span class=\"example-summary\"><code>helper</code> defines a private function, callable only from inside its own module. <code>function</code> is public.</span></li>\n  <li><a href=\"#run\">Run</a> <span class=\"example-summary\"><code>run</code> is a block of matches. Statements run in order; the first <code>pattern = value</code> that does not match ends the block and <em>is</em> its result, so a chain of validations reads flat instead of as nested cases.</span></li>\n  <li><a href=\"#sigils\">Sigils</a> <span class=\"example-summary\">a letter hugging a string desugars to a <code>.parse</code> call.</span></li>\n  <li><a href=\"#string_concat\">String concat</a> <span class=\"example-summary\"><code>&lt;&gt;</code> concatenates two strings.</span></li>\n  <li><a href=\"#string_interpolation\">String interpolation</a> <span class=\"example-summary\"><code>#{...}</code> embeds an expression inside a string.</span></li>\n  <li><a href=\"#struct\">Struct</a> <span class=\"example-summary\">a struct declares a typed record. <code>attributes</code> names each field with its default, and instances are immutable: every update returns a new one.</span></li>\n  <li><a href=\"#struct_errors\">Struct errors</a> <span class=\"example-summary\">a struct makes a good error payload once a symbol and a message aren&#39;t enough detail: it destructures in <code>case</code> like any other pattern, field by field, including matching on a literal field value.</span></li>\n  <li><a href=\"#trait\">Trait</a> <span class=\"example-summary\">a trait declares a contract of functions. A function <em>with</em> a body is a default implementation; a function <em>without</em> one is required, and every implementer must define it.</span></li>\n  <li><a href=\"#variables\">Variables</a> <span class=\"example-summary\">assignment binds a name to a value.</span></li>\n</ul>\n<section class=\"example\" id=\"actor\">\n  <h2>Actor <a class=\"anchor\" href=\"#actor\">¶</a></h2>\n  <p class=\"example-summary\">a module implements <code><a href=\"#/Actor\">Actor</a></code> by defining <code>call</code>, which returns <code>(reply, new_state)</code>. <code>cast</code> has no reply: it returns only the new state, and runs later — before the next call, or at program end.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Cache</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Actor</span>\n\n  <span class=\"tok-kw\">function</span> call(f, args, state, config)\n    <span class=\"tok-kw\">case</span> f\n      <span class=\"tok-lit\">:get</span> -&gt;\n        [key] = args\n        (<span class=\"tok-type\">Map</span>.get(state, key, <span class=\"tok-lit\">nil</span>), state)\n      <span class=\"tok-lit\">:set</span> -&gt;\n        [key, value] = args\n        (<span class=\"tok-lit\">:ok</span>, <span class=\"tok-type\">Map</span>.put(state, key, value))\n\n  <span class=\"tok-kw\">function</span> cast(f, args, state, config)\n    [key] = args\n    <span class=\"tok-type\">Map</span>.delete(state, key)</code></pre>\n<p>client half — callers never write <code>call_actor</code> / <code>cast_actor</code></p>\n<pre><code>  <span class=\"tok-kw\">function</span> start()\n    start_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, {}, {})\n\n  <span class=\"tok-kw\">function</span> get(key)\n    call_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:get</span>, [key])\n\n  <span class=\"tok-kw\">function</span> set(key, value)\n    call_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:set</span>, [key, value])\n  <span class=\"tok-kw\">function</span> clear(key)\n    cast_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:clear</span>, [key])</code></pre>\n</section>\n<section class=\"example\" id=\"alias\">\n  <h2>Alias <a class=\"anchor\" href=\"#alias\">¶</a></h2>\n  <p class=\"example-summary\"><code>alias</code> gives a module a shorter name, cutting the verbosity of long namespaces. It works for pure modules and struct modules alike.</p>\n<pre><code><span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span> <span class=\"tok-kw\">as</span> <span class=\"tok-type\">Geo</span>\n<span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span>.<span class=\"tok-type\">Circle</span> <span class=\"tok-kw\">as</span> <span class=\"tok-type\">Circle</span>\n<span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span>.<span class=\"tok-type\">Rectangle</span> <span class=\"tok-kw\">as</span> <span class=\"tok-type\">Rect</span>\n\narea = <span class=\"tok-type\">Geo</span>.circle_area(<span class=\"tok-lit\">5</span>)   <span class=\"tok-com\"># == 78.5</span>\ncircle = <span class=\"tok-type\">Circle</span>.area(<span class=\"tok-lit\">5</span>)\nrectangle = <span class=\"tok-type\">Rect</span>.area(<span class=\"tok-lit\">10</span>, <span class=\"tok-lit\">20</span>)</code></pre>\n<p>Without <code>as</code>, the local name is the path&#39;s last segment — what most aliases want. <code>as</code> is for breaking a collision between two of them.</p>\n<pre><code><span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Drawing</span>.<span class=\"tok-type\">Shapes</span>.<span class=\"tok-type\">Ellipse</span>\n\nellipse = <span class=\"tok-type\">Ellipse</span>.area(<span class=\"tok-lit\">10</span>, <span class=\"tok-lit\">20</span>)</code></pre>\n<p>A module aliases the modules it uses at the top of its own body.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Report</span>\n  <span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span>.<span class=\"tok-type\">Circle</span>\n  <span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Drawing</span>.<span class=\"tok-type\">Shapes</span>.<span class=\"tok-type\">Ellipse</span> <span class=\"tok-kw\">as</span> <span class=\"tok-type\">Oval</span>\n\n  <span class=\"tok-kw\">function</span> areas(r)\n    (<span class=\"tok-type\">Circle</span>.area(r), <span class=\"tok-type\">Oval</span>.area(r, r))</code></pre>\n</section>\n<section class=\"example\" id=\"arithmetic\">\n  <h2>Arithmetic <a class=\"anchor\" href=\"#arithmetic\">¶</a></h2>\n  <p class=\"example-summary\">arithmetic operators. Whitespace around a binary operator is required: <code>1 + 2</code>, never <code>1+2</code>.</p>\n<pre><code>sum = <span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">2</span>\ndifference = <span class=\"tok-lit\">10</span> - <span class=\"tok-lit\">4</span>\nproduct = <span class=\"tok-lit\">3</span> * <span class=\"tok-lit\">4</span>\nquotient = <span class=\"tok-lit\">10</span> / <span class=\"tok-lit\">3</span>          <span class=\"tok-com\"># int / int truncates toward zero  # == 3</span>\nremainder = -<span class=\"tok-lit\">7</span> % <span class=\"tok-lit\">3</span>         <span class=\"tok-com\"># follows the sign of the divisor  # == 2</span>\npower = <span class=\"tok-lit\">2</span> ** <span class=\"tok-lit\">10</span>            <span class=\"tok-com\"># == 1024</span>\nnegated = -<span class=\"tok-lit\">5</span>               <span class=\"tok-com\"># unary minus may hug its operand</span></code></pre>\n<p>Mixing an int and a float widens the int to a float.</p>\n<pre><code>widened = <span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">1.5</span>          <span class=\"tok-com\"># == 2.5</span>\nfloat_division = <span class=\"tok-lit\">10</span> / <span class=\"tok-lit\">4.0</span>  <span class=\"tok-com\"># == 2.5</span></code></pre>\n<p>Precedence: <code>**</code> binds tighter than unary minus, <code>*</code> tighter than <code>+</code>.</p>\n<pre><code>precedence = -<span class=\"tok-lit\">2</span> ** <span class=\"tok-lit\">2</span> + <span class=\"tok-lit\">3</span> * <span class=\"tok-lit\">4</span>   <span class=\"tok-com\"># == (-(2 ** 2)) + 12 == 8</span></code></pre>\n</section>\n<section class=\"example\" id=\"case\">\n  <h2>Case <a class=\"anchor\" href=\"#case\">¶</a></h2>\n  <p class=\"example-summary\"><code>case</code> matches a value against patterns top to bottom and runs the first match. <code>_</code> is the wildcard. Where <code>if</code> branches on a condition and <code>cond</code> on a chain of them, <code>case</code> branches on the <em>shape</em> of a value.</p>\n<pre><code>x = <span class=\"tok-lit\">2</span></code></pre>\n<p>A block whose value is assigned starts on the line after <code>=</code>.</p>\n<pre><code>matched =\n  <span class=\"tok-kw\">case</span> x\n    <span class=\"tok-lit\">1</span> -&gt; <span class=\"tok-lit\">:one</span>\n    <span class=\"tok-lit\">2</span> -&gt; <span class=\"tok-lit\">:two</span>\n    _ -&gt; <span class=\"tok-lit\">:other</span></code></pre>\n<p>An arm body can be a block: keep the <code>-&gt;</code> at the end of the arm and indent the body; its last expression is the arm&#39;s value.</p>\n<pre><code>described =\n  <span class=\"tok-kw\">case</span> [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>]\n    [] -&gt; <span class=\"tok-str\">&quot;empty&quot;</span>\n    [only] -&gt; <span class=\"tok-str\">&quot;single: #{only}&quot;</span>\n    [head | tail] -&gt;\n      rest = size(tail)\n      <span class=\"tok-str\">&quot;head #{head} and #{rest} more&quot;</span></code></pre>\n</section>\n<section class=\"example\" id=\"comments\">\n  <h2>Comments <a class=\"anchor\" href=\"#comments\">¶</a></h2>\n  <p class=\"example-summary\">a comment runs from <code>#</code> to the end of the line.</p>\n<p>A comment can stand on its own line, above the code it describes.</p>\n<pre><code>x = <span class=\"tok-lit\">1</span>  <span class=\"tok-com\"># ...or trail a statement.</span></code></pre>\n<p>Blank comment lines are ignored, and so are the blank lines around them:</p>\n<p>A <code>#</code> inside a string is just a character, not a comment.</p>\n<pre><code>marker = <span class=\"tok-str\">&quot;# not a comment&quot;</span>\nprint(marker)</code></pre>\n</section>\n<section class=\"example\" id=\"comparison\">\n  <h2>Comparison <a class=\"anchor\" href=\"#comparison\">¶</a></h2>\n  <p class=\"example-summary\">comparison operators, each returning a bool.</p>\n<pre><code>equal = <span class=\"tok-lit\">1</span> == <span class=\"tok-lit\">1</span>\nnot_equal = <span class=\"tok-lit\">1</span> != <span class=\"tok-lit\">2</span>\nless = <span class=\"tok-lit\">1</span> &lt; <span class=\"tok-lit\">2</span>\ngreater = <span class=\"tok-lit\">1</span> &gt; <span class=\"tok-lit\">2</span>\nless_or_equal = <span class=\"tok-lit\">1</span> &lt;= <span class=\"tok-lit\">2</span>\ngreater_or_equal = <span class=\"tok-lit\">1</span> &gt;= <span class=\"tok-lit\">2</span></code></pre>\n<p>Comparison binds looser than arithmetic, so the sum happens first.</p>\n<pre><code>in_range = <span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">2</span> * <span class=\"tok-lit\">3</span> == <span class=\"tok-lit\">7</span></code></pre>\n</section>\n<section class=\"example\" id=\"cond\">\n  <h2>Cond <a class=\"anchor\" href=\"#cond\">¶</a></h2>\n  <p class=\"example-summary\"><code>cond</code> takes the first arm whose condition is truthy. It is <code>case</code> over a sequence of guards, for when no single value is matched.</p>\n<pre><code>x = <span class=\"tok-lit\">5</span></code></pre>\n<p>A block whose value is assigned starts on the line after <code>=</code>.</p>\n<pre><code>sign =\n  <span class=\"tok-kw\">cond</span>\n    x &gt; <span class=\"tok-lit\">0</span> -&gt; <span class=\"tok-lit\">:positive</span>\n    x &lt; <span class=\"tok-lit\">0</span> -&gt; <span class=\"tok-lit\">:negative</span>\n    <span class=\"tok-lit\">true</span> -&gt; <span class=\"tok-lit\">:zero</span></code></pre>\n<p><code>true -&gt;</code> is the idiomatic catch-all arm. Arms take blocks too.</p>\n<pre><code>label =\n  <span class=\"tok-kw\">cond</span>\n    x &gt; <span class=\"tok-lit\">100</span> -&gt;\n      big = x * <span class=\"tok-lit\">2</span>\n      <span class=\"tok-str\">&quot;big: #{big}&quot;</span>\n    <span class=\"tok-lit\">true</span> -&gt; <span class=\"tok-str\">&quot;small&quot;</span></code></pre>\n</section>\n<section class=\"example\" id=\"destructuring\">\n  <h2>Destructuring <a class=\"anchor\" href=\"#destructuring\">¶</a></h2>\n  <p class=\"example-summary\">patterns work in plain assignment, not just in <code>case</code>.</p>\n<p>Lists destructure with the cons operator <code>|</code>.</p>\n<pre><code>[head | tail] = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>]              <span class=\"tok-com\"># head == 1, tail == [2, 3]</span>\n[first, second | rest] = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]  <span class=\"tok-com\"># first == 1, second == 2, rest == [3, 4]</span></code></pre>\n<p>Tuples are the idiomatic way to unpack a small fixed record.</p>\n<pre><code>person = (<span class=\"tok-str\">&quot;Andrew&quot;</span>, <span class=\"tok-lit\">40</span>)\n(name, age) = person                   <span class=\"tok-com\"># name == &quot;Andrew&quot;, age == 40</span></code></pre>\n<p>Any combination nests, and <code>_</code> discards.</p>\n<pre><code>((a, _), [b | _]) = ((<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>), [<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>])   <span class=\"tok-com\"># a == 1, b == 3</span></code></pre>\n<p>Maps and struct instances destructure by field.</p>\n<pre><code>{name: bound_name} = {name: <span class=\"tok-str\">&quot;andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}\n<span class=\"tok-type\">Person</span>{age: bound_age} = <span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}</code></pre>\n</section>\n<section class=\"example\" id=\"doc_comments\">\n  <h2>Doc comments <a class=\"anchor\" href=\"#doc_comments\">¶</a></h2>\n  <p class=\"example-summary\">documentation lives inline, as a comment block placed immediately below the definition it describes. <code>@module_doc</code> documents a module, <code>@doc</code> a function; each marker is followed by an empty <code>#</code> line.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Greeter</span></code></pre>\n<p>@module_doc</p>\n<p>Greeter — builds greetings.</p>\n<pre><code><span class=\"tok-type\">Greeter</span>.greet(<span class=\"tok-str\">&quot;Andrew&quot;</span>)   <span class=\"tok-com\"># == &quot;Ola Andrew&quot;</span></code></pre>\n<pre><code>  <span class=\"tok-kw\">function</span> greet(name)</code></pre>\n<p>@doc</p>\n<p>Returns the greeting for <code>name</code>.</p>\n<pre><code><span class=\"tok-type\">Greeter</span>.greet(<span class=\"tok-str\">&quot;Andrew&quot;</span>)   <span class=\"tok-com\"># == &quot;Ola Andrew&quot;</span></code></pre>\n<pre><code>    &quot;Ola #{name}&quot;</code></pre>\n</section>\n<section class=\"example\" id=\"error_tuples\">\n  <h2>Error tuples <a class=\"anchor\" href=\"#error_tuples\">¶</a></h2>\n  <p class=\"example-summary\">Ramos has no exceptions. A function that can fail returns a tagged tuple instead: <code>(:ok, value)</code> on success, <code>(:error, (:type, msg))</code> on failure. The error is an ordinary value, so it is matched like any other.</p>\n<pre><code><span class=\"tok-kw\">function</span> withdraw(balance, amount)\n  <span class=\"tok-kw\">cond</span>\n    amount &lt;= <span class=\"tok-lit\">0</span> -&gt; (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:invalid_amount</span>, <span class=\"tok-str\">&quot;amount must be positive&quot;</span>))\n    amount &gt; balance -&gt; (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:insufficient_funds</span>, <span class=\"tok-str\">&quot;balance is too low&quot;</span>))\n    <span class=\"tok-lit\">true</span> -&gt; (<span class=\"tok-lit\">:ok</span>, balance - amount)</code></pre>\n<p><code>case</code> reads the tag, and the error&#39;s own shape destructures in the pattern, so each failure is handled where it is named.</p>\n<pre><code>result =\n  <span class=\"tok-kw\">case</span> withdraw(<span class=\"tok-lit\">100</span>, <span class=\"tok-lit\">250</span>)\n    (<span class=\"tok-lit\">:ok</span>, remaining) -&gt; <span class=\"tok-str\">&quot;left: #{remaining}&quot;</span>\n    (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:invalid_amount</span>, message)) -&gt; <span class=\"tok-str\">&quot;bad request: #{message}&quot;</span>\n    (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:insufficient_funds</span>, message)) -&gt; <span class=\"tok-str\">&quot;declined: #{message}&quot;</span>\n    (<span class=\"tok-lit\">:error</span>, (_, message)) -&gt; <span class=\"tok-str\">&quot;failed: #{message}&quot;</span>\n\nprint(result)</code></pre>\n<p><code>run</code> chains steps that each return <code>(:ok, _)</code>, stopping at the first one that does not match — the flat alternative to nesting a <code>case</code> per step.</p>\n<pre><code>total =\n  <span class=\"tok-kw\">run</span>\n    (<span class=\"tok-lit\">:ok</span>, after_rent) = withdraw(<span class=\"tok-lit\">1000</span>, <span class=\"tok-lit\">400</span>)\n    (<span class=\"tok-lit\">:ok</span>, after_food) = withdraw(after_rent, <span class=\"tok-lit\">200</span>)\n    after_food\n\nprint(total)</code></pre>\n</section>\n<section class=\"example\" id=\"guards\">\n  <h2>Guards <a class=\"anchor\" href=\"#guards\">¶</a></h2>\n  <p class=\"example-summary\">a <code>when</code> guard sits between a pattern and its <code>-&gt;</code>, narrowing a match beyond the pattern&#39;s shape.</p>\n<pre><code><span class=\"tok-kw\">function</span> classify(list)\n  <span class=\"tok-kw\">case</span> list\n    [head | _] <span class=\"tok-kw\">when</span> head &gt; <span class=\"tok-lit\">100</span> -&gt; <span class=\"tok-lit\">:big</span>\n    [head | _] <span class=\"tok-kw\">when</span> head &gt; <span class=\"tok-lit\">0</span> -&gt; <span class=\"tok-lit\">:small</span>\n    [_ | _] -&gt; <span class=\"tok-lit\">:non_positive</span>\n    [] -&gt; <span class=\"tok-lit\">:empty</span></code></pre>\n<p>Guards are plain expressions, so they combine with <code>and</code> / <code>or</code> / <code>not</code>.</p>\n<pre><code><span class=\"tok-kw\">function</span> admits(user)\n  <span class=\"tok-kw\">case</span> user\n    ({name: n, age: a}, <span class=\"tok-lit\">:admin</span>) <span class=\"tok-kw\">when</span> a &gt; <span class=\"tok-lit\">18</span> <span class=\"tok-kw\">and</span> <span class=\"tok-kw\">not</span> (n == <span class=\"tok-str\">&quot;&quot;</span>) -&gt; <span class=\"tok-str\">&quot;Admin #{n}&quot;</span>\n    ({name: n}, <span class=\"tok-lit\">:guest</span>) -&gt; <span class=\"tok-str\">&quot;Guest #{n}&quot;</span>\n    _ -&gt; <span class=\"tok-str\">&quot;Unknown&quot;</span></code></pre>\n</section>\n<section class=\"example\" id=\"if\">\n  <h2>If <a class=\"anchor\" href=\"#if\">¶</a></h2>\n  <p class=\"example-summary\"><code>if</code> is the two-branch conditional: a condition, an indented body, and an optional <code>else</code>. Any value works as the condition, since only <code>false</code> and <code>nil</code> are falsy. There is no <code>else if</code> — a chain of conditions is <code>cond</code>.</p>\n<pre><code>x = <span class=\"tok-lit\">5</span>\n\n<span class=\"tok-kw\">if</span> x &gt; <span class=\"tok-lit\">3</span>\n  print(<span class=\"tok-str\">&quot;big&quot;</span>)\n<span class=\"tok-kw\">else</span>\n  print(<span class=\"tok-str\">&quot;small&quot;</span>)</code></pre>\n<p><code>else</code> is optional; without one, the branch not taken yields <code>nil</code>.</p>\n<pre><code><span class=\"tok-kw\">if</span> x &gt; <span class=\"tok-lit\">100</span>\n  print(<span class=\"tok-str\">&quot;never&quot;</span>)</code></pre>\n<p><code>if</code> is an expression, so it can be assigned — the block starts on its own line, like any other block on the right of <code>=</code>.</p>\n<pre><code>grade =\n  <span class=\"tok-kw\">if</span> x &gt; <span class=\"tok-lit\">3</span>\n    <span class=\"tok-lit\">:high</span>\n  <span class=\"tok-kw\">else</span>\n    <span class=\"tok-lit\">:low</span></code></pre>\n<p>Any truthy value: <code>is_truthy</code> is the same rule written down.</p>\n<pre><code><span class=\"tok-kw\">if</span> <span class=\"tok-str\">&quot;text&quot;</span>\n  print(is_truthy(x))</code></pre>\n<p>One guarded statement fits on one line: <code>&lt;statement&gt; when &lt;condition&gt;</code> — the same conditional, so no <code>else</code> half and no assignment on the left.</p>\n<pre><code>print(<span class=\"tok-str\">&quot;big&quot;</span>) <span class=\"tok-kw\">when</span> x &gt; <span class=\"tok-lit\">3</span></code></pre>\n</section>\n<section class=\"example\" id=\"implements\">\n  <h2>Implements <a class=\"anchor\" href=\"#implements\">¶</a></h2>\n  <p class=\"example-summary\">a struct declares the traits it implements with <code>implements</code>, at the top of the struct body. It must define every function the trait requires; the trait&#39;s defaults come for free.</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">Person</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Helloable</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Describable</span>\n\n  <span class=\"tok-kw\">attributes</span>\n    name: <span class=\"tok-lit\">nil</span>\n    age: <span class=\"tok-lit\">0</span>\n\n  <span class=\"tok-kw\">function</span> is_over_eighteen(<span class=\"tok-kw\">self</span>)   <span class=\"tok-com\"># required by Helloable</span>\n    <span class=\"tok-kw\">self</span>.age &gt; <span class=\"tok-lit\">18</span></code></pre>\n</section>\n<section class=\"example\" id=\"lambdas\">\n  <h2>Lambdas <a class=\"anchor\" href=\"#lambdas\">¶</a></h2>\n  <p class=\"example-summary\">anonymous functions. Lambdas are values, so they can be bound, passed and piped.</p>\n<p>Single-expression: use <code>-&gt;</code> and stay on one line.</p>\n<pre><code>add = <span class=\"tok-kw\">do</span> x, y -&gt; x + y\nthree = add(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>)</code></pre>\n<p>Multi-statement: drop the <code>-&gt;</code>, indent the body, last expression returns. The body is a block, so — like a <code>case</code> — it starts on the line after the <code>=</code>.</p>\n<pre><code>double_then_add =\n  <span class=\"tok-kw\">do</span> x, y\n    z = x + y\n    z * <span class=\"tok-lit\">2</span>\n\nten = double_then_add(<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>)</code></pre>\n<p>No parameters, and passed straight into a function call.</p>\n<pre><code>answer = <span class=\"tok-kw\">do</span> -&gt; <span class=\"tok-lit\">42</span>\ndoubled = <span class=\"tok-type\">List</span>.map([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-kw\">do</span> x -&gt; x * <span class=\"tok-lit\">2</span>)</code></pre>\n</section>\n<section class=\"example\" id=\"list_concat\">\n  <h2>List concat <a class=\"anchor\" href=\"#list_concat\">¶</a></h2>\n  <p class=\"example-summary\"><code>++</code> concatenates two lists, returning a new one.</p>\n<pre><code>joined = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>] ++ [<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]   <span class=\"tok-com\"># == [1, 2, 3, 4]</span>\nprepended = [<span class=\"tok-lit\">0</span>] ++ joined   <span class=\"tok-com\"># == [0, 1, 2, 3, 4]</span>\nwith_empty = [] ++ [<span class=\"tok-lit\">1</span>]      <span class=\"tok-com\"># == [1]</span></code></pre>\n<p><code>[head | tail]</code> is the cons form — a literal/pattern shape, not an operator.</p>\n<pre><code>[head | tail] = joined</code></pre>\n</section>\n<section class=\"example\" id=\"literals\">\n  <h2>Literals <a class=\"anchor\" href=\"#literals\">¶</a></h2>\n  <p class=\"example-summary\">one of every literal Ramos has.</p>\n<pre><code>int = <span class=\"tok-lit\">42</span>\nfloat = <span class=\"tok-lit\">3.14</span>\nstring = <span class=\"tok-str\">&quot;andrew&quot;</span>\nsymbol = <span class=\"tok-lit\">:symbol</span>\nyes = <span class=\"tok-lit\">true</span>\nno = <span class=\"tok-lit\">false</span>\nnothing = <span class=\"tok-lit\">nil</span>\nlist = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>]\ntuple = (<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>)\nmap = {name: <span class=\"tok-str\">&quot;andrew&quot;</span>}</code></pre>\n<p>Only <code>false</code> and <code>nil</code> are falsy...</p>\n<pre><code>falsy = [<span class=\"tok-lit\">false</span>, <span class=\"tok-lit\">nil</span>]</code></pre>\n<p>...everything else is truthy, including 0, &quot;&quot; and [].</p>\n<pre><code>truthy = [<span class=\"tok-lit\">0</span>, <span class=\"tok-str\">&quot;&quot;</span>, []]</code></pre>\n</section>\n<section class=\"example\" id=\"logical\">\n  <h2>Logical <a class=\"anchor\" href=\"#logical\">¶</a></h2>\n  <p class=\"example-summary\">the word-form logical operators. Ramos has no <code>&amp;&amp;</code>, <code>||</code> or <code>!</code>.</p>\n<pre><code>both = <span class=\"tok-lit\">true</span> <span class=\"tok-kw\">and</span> <span class=\"tok-lit\">false</span>   <span class=\"tok-com\"># `and` short-circuits: a false left side skips the right</span>\neither = <span class=\"tok-lit\">true</span> <span class=\"tok-kw\">or</span> <span class=\"tok-lit\">false</span>  <span class=\"tok-com\"># `or` short-circuits too</span>\nnegated = <span class=\"tok-kw\">not</span> <span class=\"tok-lit\">true</span></code></pre>\n<p>Precedence, loosest to tightest: <code>or</code> &lt; <code>and</code> &lt; <code>not</code> &lt; comparison.</p>\n<pre><code>mixed = <span class=\"tok-lit\">1</span> &lt; <span class=\"tok-lit\">2</span> <span class=\"tok-kw\">and</span> <span class=\"tok-kw\">not</span> (<span class=\"tok-lit\">3</span> &gt; <span class=\"tok-lit\">4</span>) <span class=\"tok-kw\">or</span> <span class=\"tok-lit\">false</span></code></pre>\n</section>\n<section class=\"example\" id=\"map_merge\">\n  <h2>Map merge <a class=\"anchor\" href=\"#map_merge\">¶</a></h2>\n  <p class=\"example-summary\"><code>++</code> merges two maps, returning a new one.</p>\n<pre><code>merged = {a: <span class=\"tok-lit\">1</span>} ++ {b: <span class=\"tok-lit\">2</span>}      <span class=\"tok-com\"># == {a: 1, b: 2}</span>\noverridden = {a: <span class=\"tok-lit\">1</span>} ++ {a: <span class=\"tok-lit\">2</span>}  <span class=\"tok-com\"># right side wins on duplicate keys  # == {a: 2}</span>\ndefaults = {} ++ merged</code></pre>\n<p>Maps are insertion-ordered and immutable — <code>++</code> returns a new map.</p>\n<pre><code>nested = {user: {name: <span class=\"tok-str\">&quot;andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}, active: <span class=\"tok-lit\">true</span>}</code></pre>\n</section>\n<section class=\"example\" id=\"module\">\n  <h2>Module <a class=\"anchor\" href=\"#module\">¶</a></h2>\n  <p class=\"example-summary\">a pure module is a namespace of functions with no per-instance state. It is itself a value, so it can be passed around and inspected.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">PersonUtils</span>\n  <span class=\"tok-kw\">function</span> hello(person)\n    print(<span class=\"tok-str\">&quot;Ola #{person.name}, eu tenho #{person.age}&quot;</span>)\n\n  <span class=\"tok-kw\">function</span> hi_all(people)\n    people\n    | <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> p -&gt; <span class=\"tok-str\">&quot;hi #{p.name}&quot;</span>)\n    | <span class=\"tok-type\">List</span>.join(<span class=\"tok-str\">&quot;, &quot;</span>)</code></pre>\n</section>\n<section class=\"example\" id=\"multiline_strings\">\n  <h2>Multiline strings <a class=\"anchor\" href=\"#multiline_strings\">¶</a></h2>\n  <p class=\"example-summary\"><code>&quot;&quot;&quot;</code> opens a multiline string. The opening quotes are followed straight by a newline, content sits one level (2 spaces) past the opening line, and the closing quotes stand alone at the opening line&#39;s indentation. Every content line keeps its trailing newline.</p>\n<p>Like <code>case</code> or <code>run</code>, a multiline string being assigned starts on the line after the <code>=</code>, so the assignment does not hang open across the content.</p>\n<pre><code>str =\n  <span class=\"tok-str\">&quot;&quot;&quot;\n    Hi there\n\n    This is a multiline string\n  &quot;&quot;&quot;</span>\n\nprint(str)</code></pre>\n<p>Here the string opens at the function-body level, so &quot;Usage: ...&quot; is flush left in the value while &quot;  -h ...&quot; keeps its two extra spaces. Interpolation works exactly as in single-line strings.</p>\n<pre><code><span class=\"tok-kw\">function</span> usage(name)\n  <span class=\"tok-str\">&quot;&quot;&quot;\n    Usage: #{name} &lt;file&gt;\n      -h  show this help\n  &quot;&quot;&quot;</span></code></pre>\n</section>\n<section class=\"example\" id=\"namespaced_module\">\n  <h2>Namespaced module <a class=\"anchor\" href=\"#namespaced_module\">¶</a></h2>\n  <p class=\"example-summary\">module names are CamelCase and may be namespaced with <code>.</code>. The namespace maps to the file path: <code>MyApp.Business.SystemUser</code> lives at <code>src/my_app/business/system_user.rmo</code>.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">Business</span>.<span class=\"tok-type\">SystemUser</span>\n  <span class=\"tok-kw\">function</span> create(name)\n    (<span class=\"tok-lit\">:ok</span>, name)\n\n  <span class=\"tok-kw\">function</span> describe()\n    <span class=\"tok-str\">&quot;a namespaced module&quot;</span></code></pre>\n</section>\n<section class=\"example\" id=\"pipe\">\n  <h2>Pipe <a class=\"anchor\" href=\"#pipe\">¶</a></h2>\n  <p class=\"example-summary\"><code>|</code> passes the value on its left as the <em>first argument</em> to the module function on its right, so data flows top to bottom. It always starts its own line, at the same indentation as what comes before it.</p>\n<pre><code>shouted = [<span class=\"tok-str\">&quot;ana&quot;</span>, <span class=\"tok-str\">&quot;bob&quot;</span>]\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> n -&gt; <span class=\"tok-type\">String</span>.upcase(n))</code></pre>\n<p>Pipes chain, each on its own line.</p>\n<pre><code>sentence = [<span class=\"tok-str\">&quot;ana&quot;</span>, <span class=\"tok-str\">&quot;bob&quot;</span>]\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> n -&gt; <span class=\"tok-type\">String</span>.upcase(n))\n| <span class=\"tok-type\">List</span>.join(<span class=\"tok-str\">&quot;, &quot;</span>)</code></pre>\n<p><code>a</code> then <code>| M.f(b)</code> means exactly <code>M.f(a, b)</code> — these two are the same call.</p>\n<pre><code>piped = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]\n| <span class=\"tok-type\">List</span>.filter(<span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))\nplain = <span class=\"tok-type\">List</span>.filter([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))</code></pre>\n</section>\n<section class=\"example\" id=\"private_functions\">\n  <h2>Private functions <a class=\"anchor\" href=\"#private_functions\">¶</a></h2>\n  <p class=\"example-summary\"><code>helper</code> defines a private function, callable only from inside its own module. <code>function</code> is public.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Accounts</span>\n  <span class=\"tok-kw\">function</span> create(name)\n    (<span class=\"tok-lit\">:ok</span>, normalize(name))   <span class=\"tok-com\"># a public function may call a private one</span>\n\n  <span class=\"tok-kw\">helper</span> normalize(name)\n    <span class=\"tok-type\">String</span>.downcase(name)</code></pre>\n</section>\n<section class=\"example\" id=\"run\">\n  <h2>Run <a class=\"anchor\" href=\"#run\">¶</a></h2>\n  <p class=\"example-summary\"><code>run</code> is a block of matches. Statements run in order; the first <code>pattern = value</code> that does not match ends the block and <em>is</em> its result, so a chain of validations reads flat instead of as nested cases.</p>\n<p>Bindings flow to later statements; the result is the last one&#39;s value, or the value that failed to match, e.g. (:error, (:invalid_price, &quot;...&quot;)). The block starts on the line after <code>=</code>, as every assigned block must.</p>\n<pre><code>total =\n  <span class=\"tok-kw\">run</span>\n    (<span class=\"tok-lit\">:ok</span>, price) = fetch_price(item)\n    (<span class=\"tok-lit\">:ok</span>, qty) = fetch_quantity(item)\n    price * qty</code></pre>\n<p>A <code>run</code> may close with a <code>case</code> written without a subject: the block&#39;s result is the subject, so success and failure are handled side by side.</p>\n<pre><code><span class=\"tok-kw\">run</span>\n  <span class=\"tok-lit\">:ok</span> = validate_id(data)\n  <span class=\"tok-lit\">:ok</span> = validate_name(data)\n  <span class=\"tok-lit\">:ok</span> = validate_age(data)\n<span class=\"tok-kw\">case</span>\n  <span class=\"tok-lit\">:ok</span> -&gt;\n    print(<span class=\"tok-str\">&quot;valid&quot;</span>)\n\n  err -&gt;\n    print(<span class=\"tok-str\">&quot;rejected: #{err}&quot;</span>)</code></pre>\n</section>\n<section class=\"example\" id=\"sigils\">\n  <h2>Sigils <a class=\"anchor\" href=\"#sigils\">¶</a></h2>\n  <p class=\"example-summary\">a letter hugging a string desugars to a <code>.parse</code> call.</p>\n<pre><code>date = <span class=\"tok-str\">D&quot;2024-02-29&quot;</span>                    <span class=\"tok-com\"># == Date.parse(&quot;2024-02-29&quot;)</span>\ntime = <span class=\"tok-str\">T&quot;13:45:30&quot;</span>                      <span class=\"tok-com\"># == Time.parse(&quot;13:45:30&quot;)</span>\nnaive = <span class=\"tok-str\">N&quot;2024-02-09T13:45:30.500&quot;</span>      <span class=\"tok-com\"># == NaiveDateTime.parse(...)</span>\nzoned = <span class=\"tok-str\">U&quot;2024-02-09T13:45:30.500Z&quot;</span>     <span class=\"tok-com\"># == DateTime.parse(...)</span></code></pre>\n</section>\n<section class=\"example\" id=\"string_concat\">\n  <h2>String concat <a class=\"anchor\" href=\"#string_concat\">¶</a></h2>\n  <p class=\"example-summary\"><code>&lt;&gt;</code> concatenates two strings.</p>\n<pre><code>joined = <span class=\"tok-str\">&quot;a&quot;</span> &lt;&gt; <span class=\"tok-str\">&quot;b&quot;</span>                  <span class=\"tok-com\"># == &quot;ab&quot;</span>\ngreeting = <span class=\"tok-str\">&quot;Ola&quot;</span> &lt;&gt; <span class=\"tok-str\">&quot; &quot;</span> &lt;&gt; <span class=\"tok-str\">&quot;Andrew&quot;</span>  <span class=\"tok-com\"># left-associative</span></code></pre>\n<p><code>&lt;&gt;</code> binds looser than arithmetic, so <code>1 + 2</code> is evaluated first.</p>\n<pre><code>counted = <span class=\"tok-str\">&quot;total: &quot;</span> &lt;&gt; to_string(<span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">2</span>)</code></pre>\n</section>\n<section class=\"example\" id=\"string_interpolation\">\n  <h2>String interpolation <a class=\"anchor\" href=\"#string_interpolation\">¶</a></h2>\n  <p class=\"example-summary\"><code>#{...}</code> embeds an expression inside a string.</p>\n<pre><code>name = <span class=\"tok-str\">&quot;andrew&quot;</span>\ngreeting = <span class=\"tok-str\">&quot;Ola #{name}, you have #{1 + 1} new messages&quot;</span></code></pre>\n<p>Any expression works, including calls...</p>\n<pre><code>report = <span class=\"tok-str\">&quot;size: #{size([1, 2, 3])}&quot;</span></code></pre>\n<p>...and a string holding an interpolation of its own.</p>\n<pre><code>nested = <span class=\"tok-str\">&quot;outer #{&quot;inner #{name}&quot;}&quot;</span></code></pre>\n<p>Escapes: \\n \\t \\&quot; \\\\ and \\#{, which suppresses interpolation.</p>\n<pre><code>escaped = <span class=\"tok-str\">&quot;line\\n\\ttab \\&quot;quoted\\&quot; backslash \\\\ literal \\#{name}&quot;</span></code></pre>\n</section>\n<section class=\"example\" id=\"struct\">\n  <h2>Struct <a class=\"anchor\" href=\"#struct\">¶</a></h2>\n  <p class=\"example-summary\">a struct declares a typed record. <code>attributes</code> names each field with its default, and instances are immutable: every update returns a new one.</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">Person</span>\n  <span class=\"tok-kw\">attributes</span>\n    name: <span class=\"tok-lit\">nil</span>\n    age: <span class=\"tok-lit\">0</span>\n\n  <span class=\"tok-kw\">function</span> hello(<span class=\"tok-kw\">self</span>)\n    print(<span class=\"tok-str\">&quot;Ola #{self.name}, eu tenho #{self.age}&quot;</span>)</code></pre>\n<p>Construction is the map-literal syntax prefixed by the struct name; omitted fields take their declared defaults, and field access is dot syntax.</p>\n<pre><code>andrew = <span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}\nandrew.hello()\n\nwith_defaults = <span class=\"tok-type\">Person</span>{}   <span class=\"tok-com\"># == Person{name: nil, age: 0}</span>\noldest =\n  andrew\n  | <span class=\"tok-type\">Struct</span>.put(<span class=\"tok-lit\">:age</span>, <span class=\"tok-lit\">41</span>)\n  | <span class=\"tok-type\">Struct</span>.update(<span class=\"tok-lit\">:age</span>, <span class=\"tok-kw\">do</span> a -&gt; a + <span class=\"tok-lit\">1</span>)</code></pre>\n<p><code>andrew.age = 41</code> rebinds <code>andrew</code> to a new instance, sugar for that same call.</p>\n<pre><code>andrew.age = <span class=\"tok-lit\">41</span>\n\nfields = <span class=\"tok-type\">Struct</span>.keys(andrew)             <span class=\"tok-com\"># == [:name, :age]</span>\nas_map = <span class=\"tok-type\">Struct</span>.to_map(oldest)           <span class=\"tok-com\"># == {name: &quot;Andrew&quot;, age: 42}</span>\nis_person = <span class=\"tok-type\">Struct</span>.is_a(andrew, <span class=\"tok-type\">Person</span>)  <span class=\"tok-com\"># == true</span></code></pre>\n</section>\n<section class=\"example\" id=\"struct_errors\">\n  <h2>Struct errors <a class=\"anchor\" href=\"#struct_errors\">¶</a></h2>\n  <p class=\"example-summary\">a struct makes a good error payload once a symbol and a message aren&#39;t enough detail: it destructures in <code>case</code> like any other pattern, field by field, including matching on a literal field value.</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">DeclineReason</span>\n  <span class=\"tok-kw\">attributes</span>\n    code: <span class=\"tok-lit\">0</span>\n    message: <span class=\"tok-lit\">nil</span>\n\n<span class=\"tok-kw\">function</span> charge(amount)\n  <span class=\"tok-kw\">cond</span>\n    amount &lt;= <span class=\"tok-lit\">0</span> -&gt; (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">DeclineReason</span>{code: <span class=\"tok-lit\">555</span>, message: <span class=\"tok-str\">&quot;must be positive&quot;</span>})\n    amount &gt; <span class=\"tok-lit\">10000</span> -&gt; (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">DeclineReason</span>{code: <span class=\"tok-lit\">556</span>, message: <span class=\"tok-str\">&quot;too large&quot;</span>})\n    <span class=\"tok-lit\">true</span> -&gt; (<span class=\"tok-lit\">:ok</span>, amount)\n\nresult =\n  <span class=\"tok-kw\">case</span> charge(<span class=\"tok-lit\">50000</span>)\n    (<span class=\"tok-lit\">:ok</span>, charged) -&gt; <span class=\"tok-str\">&quot;charged #{charged}&quot;</span>\n    (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">DeclineReason</span>{code: <span class=\"tok-lit\">556</span>, message: message}) -&gt; <span class=\"tok-str\">&quot;too large: #{message}&quot;</span>\n    (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">DeclineReason</span>{message: message}) -&gt; <span class=\"tok-str\">&quot;declined: #{message}&quot;</span>\n\nprint(result)</code></pre>\n</section>\n<section class=\"example\" id=\"trait\">\n  <h2>Trait <a class=\"anchor\" href=\"#trait\">¶</a></h2>\n  <p class=\"example-summary\">a trait declares a contract of functions. A function <em>with</em> a body is a default implementation; a function <em>without</em> one is required, and every implementer must define it.</p>\n<pre><code><span class=\"tok-kw\">trait</span> <span class=\"tok-type\">Helloable</span>\n  <span class=\"tok-kw\">function</span> hello(<span class=\"tok-kw\">self</span>)\n    print(<span class=\"tok-str\">&quot;Ola #{self.name}, eu tenho #{self.age}&quot;</span>)\n\n  <span class=\"tok-kw\">function</span> describe(<span class=\"tok-kw\">self</span>)\n    <span class=\"tok-str\">&quot;#{self.name} (#{self.age})&quot;</span>\n\n  <span class=\"tok-kw\">function</span> is_over_eighteen(<span class=\"tok-kw\">self</span>)   <span class=\"tok-com\"># required, no body</span></code></pre>\n</section>\n<section class=\"example\" id=\"variables\">\n  <h2>Variables <a class=\"anchor\" href=\"#variables\">¶</a></h2>\n  <p class=\"example-summary\">assignment binds a name to a value.</p>\n<pre><code>x = <span class=\"tok-lit\">100</span>\nname = <span class=\"tok-str\">&quot;andrew&quot;</span>\npi = <span class=\"tok-lit\">3.14</span></code></pre>\n<p>Rebinding, not mutation: <code>x</code> now names a new value, and 100 was never changed.</p>\n<pre><code>x = x + <span class=\"tok-lit\">1</span></code></pre>\n<p>Names are snake_case, <code>a-z_</code> only — no <code>?</code> or <code>!</code>.</p>\n<pre><code>main_user_name = name</code></pre>\n</section>\n"},"programs":{"title":"Programs — Ramos","heading":null,"content_class":"examples","body":"<h1>Programs</h1>\n<p>Complete, runnable programs from <code>examples/</code> — each with an entrypoint, run with <code>ramos run</code>. Where the Examples page shows one language feature in isolation, these show several working together.</p>\n<p>For one small program per language feature instead, see the <a href=\"#/examples\">Examples</a> page.</p>\n<p>New to the language? Start with the <a href=\"#/guide\">language guide</a>.</p>\n<ul class=\"example-toc\">\n  <li><a href=\"#hello_world\">Hello world</a> <span class=\"example-summary\">a tiny Ramos program: a Main entrypoint that does some arithmetic, maps a list through a private helper, and prints the results.</span></li>\n  <li><a href=\"#structs\">Structs</a> <span class=\"example-summary\">structs end to end: attributes and methods, a trait with a required function, struct-shaped error results destructured in <code>case</code>, and <code><a href=\"#/Struct\">Struct</a></code>&#39;s introspection functions. Run with <code>ramos run examples/structs/main.rmo</code>.</span></li>\n</ul>\n<section class=\"example\" id=\"hello_world\">\n  <h2>Hello world <a class=\"anchor\" href=\"#hello_world\">¶</a></h2>\n  <p class=\"example-summary\">a tiny Ramos program: a Main entrypoint that does some arithmetic, maps a list through a private helper, and prints the results.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Main</span>\n  <span class=\"tok-kw\">function</span> main()\n    total = (<span class=\"tok-lit\">2</span> + <span class=\"tok-lit\">3</span>) * <span class=\"tok-lit\">4</span> - <span class=\"tok-lit\">6</span> / <span class=\"tok-lit\">2</span>\n    doubled = double_all([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>])\n    println(<span class=\"tok-str\">&quot;total is #{total}&quot;</span>)\n    println(<span class=\"tok-str\">&quot;doubled is #{doubled}&quot;</span>)\n\n  <span class=\"tok-kw\">helper</span> double_all(list)\n    <span class=\"tok-kw\">case</span> list\n      [] -&gt; []\n      [head | tail] -&gt; [head * <span class=\"tok-lit\">2</span>] ++ double_all(tail)</code></pre>\n</section>\n<section class=\"example\" id=\"structs\">\n  <h2>Structs <a class=\"anchor\" href=\"#structs\">¶</a></h2>\n  <p class=\"example-summary\">structs end to end: attributes and methods, a trait with a required function, struct-shaped error results destructured in <code>case</code>, and <code><a href=\"#/Struct\">Struct</a></code>&#39;s introspection functions. Run with <code>ramos run examples/structs/main.rmo</code>.</p>\n<p class=\"program-file\"><code>account.rmo</code></p>\n<p>a struct with attributes, methods that take <code>self</code>, and a trait it implements. Every update returns a new instance; nothing here mutates.</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">Account</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Reportable</span>\n\n  <span class=\"tok-kw\">attributes</span>\n    owner: <span class=\"tok-lit\">nil</span>\n    balance: <span class=\"tok-lit\">0</span>\n\n  <span class=\"tok-kw\">function</span> summary(<span class=\"tok-kw\">self</span>)\n    <span class=\"tok-str\">&quot;#{self.owner}: #{self.balance}&quot;</span>\n\n  <span class=\"tok-kw\">function</span> deposit(<span class=\"tok-kw\">self</span>, amount)\n    <span class=\"tok-kw\">self</span>\n    | <span class=\"tok-type\">Struct</span>.put(<span class=\"tok-lit\">:balance</span>, <span class=\"tok-kw\">self</span>.balance + amount)\n\n  <span class=\"tok-kw\">function</span> withdraw(<span class=\"tok-kw\">self</span>, amount)\n    <span class=\"tok-kw\">cond</span>\n      amount &lt;= <span class=\"tok-lit\">0</span> -&gt;\n        (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">WithdrawalError</span>{code: <span class=\"tok-lit\">400</span>, message: <span class=\"tok-str\">&quot;amount must be positive&quot;</span>})\n      amount &gt; <span class=\"tok-kw\">self</span>.balance -&gt;\n        (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">WithdrawalError</span>{code: <span class=\"tok-lit\">402</span>, message: <span class=\"tok-str\">&quot;insufficient funds&quot;</span>})\n      <span class=\"tok-lit\">true</span> -&gt;\n        updated =\n          <span class=\"tok-kw\">self</span>\n          | <span class=\"tok-type\">Struct</span>.put(<span class=\"tok-lit\">:balance</span>, <span class=\"tok-kw\">self</span>.balance - amount)\n        (<span class=\"tok-lit\">:ok</span>, updated)</code></pre>\n<p class=\"program-file\"><code>reportable.rmo</code></p>\n<p>a trait implemented by anything that can describe itself. <code>summary</code> has no body: it is required, and every implementer must define it.</p>\n<pre><code><span class=\"tok-kw\">trait</span> <span class=\"tok-type\">Reportable</span>\n  <span class=\"tok-kw\">function</span> summary(<span class=\"tok-kw\">self</span>)</code></pre>\n<p class=\"program-file\"><code>withdrawal_error.rmo</code></p>\n<p>a struct used as an error payload: a plain <code>:error</code> symbol has nowhere to carry a code and a message, so <code>Account.withdraw</code> returns one of these instead.</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">WithdrawalError</span>\n  <span class=\"tok-kw\">attributes</span>\n    code: <span class=\"tok-lit\">0</span>\n    message: <span class=\"tok-lit\">nil</span></code></pre>\n<p class=\"program-file\"><code>main.rmo</code></p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Main</span>\n  <span class=\"tok-kw\">function</span> main()\n    account = <span class=\"tok-type\">Account</span>{owner: <span class=\"tok-str\">&quot;Andrew&quot;</span>, balance: <span class=\"tok-lit\">100</span>}\n    println(account.summary())\n\n    account = account.deposit(<span class=\"tok-lit\">50</span>)\n    println(account.summary())\n\n    account = attempt_withdrawal(account, <span class=\"tok-lit\">30</span>)\n    account = attempt_withdrawal(account, <span class=\"tok-lit\">1000</span>)\n    account = attempt_withdrawal(account, -<span class=\"tok-lit\">5</span>)\n\n    println(<span class=\"tok-str\">&quot;fields: #{Struct.keys(account)}&quot;</span>)\n    println(<span class=\"tok-str\">&quot;as a map: #{Struct.to_map(account)}&quot;</span>)\n    println(<span class=\"tok-str\">&quot;is an Account: #{Struct.is_a(account, Account)}&quot;</span>)\n\n  <span class=\"tok-kw\">helper</span> attempt_withdrawal(account, amount)\n    <span class=\"tok-kw\">case</span> account.withdraw(amount)\n      (<span class=\"tok-lit\">:ok</span>, updated) -&gt;\n        println(<span class=\"tok-str\">&quot;withdrew #{amount}, new balance #{updated.balance}&quot;</span>)\n        updated\n      (<span class=\"tok-lit\">:error</span>, <span class=\"tok-type\">WithdrawalError</span>{message: message}) -&gt;\n        println(<span class=\"tok-str\">&quot;declined #{amount}: #{message}&quot;</span>)\n        account</code></pre>\n</section>\n"},"guide":{"title":"Language guide — Ramos","heading":null,"content_class":"guide","body":"<h1>Language guide</h1>\n<ul class=\"guide-toc\">\n  <li><a href=\"#philosophy\">Philosophy</a></li>\n  <li><a href=\"#hello-world\">Hello, world</a></li>\n  <li><a href=\"#keywords\">Keywords</a></li>\n  <li><a href=\"#types\">Types</a></li>\n  <li><a href=\"#syntax\">Syntax</a></li>\n  <li><a href=\"#standard-library\">Standard library</a></li>\n  <li><a href=\"#command-line\">Command line</a></li>\n  <li><a href=\"#the-repl\">The REPL</a></li>\n  <li><a href=\"#project-layout\">Project layout</a></li>\n  <li><a href=\"#editor-support\">Editor support</a></li>\n</ul>\n<p>Ramos (plant branches) is a small, <strong>indentation-driven</strong>, <strong>immutable</strong>, and <strong>strict</strong> functional programming language. It borrows its feel from Elixir and Ruby: everything is an expression, values are persistent and immutable.</p>\n<ul>\n  <li>Immutable (like Elixir)</li>\n  <li>Indentation driven (like Python)</li>\n  <li>Strict</li>\n  <li>Uses actor model (like Elixir)</li>\n  <li>Allows kind of oo style <code>person.hello()</code> (like Ruby)</li>\n  <li>Backed on rust</li>\n</ul>\n<h2 id=\"philosophy\">Philosophy</h2>\n<p>Ramos (plant branches) is a small, indentation-driven language built around four ideas: values are <strong>immutable</strong>, the interpreter enforces a <strong>strict</strong> set of rules rather than leaving them to style guides, the language is deliberately <strong>limited</strong> in how many ways it lets you do the same thing, and where a choice remains, it is an <strong>opinionated</strong> one.</p>\n<h3 id=\"immutable\">Immutable</h3>\n<p>Every value in Ramos is persistent and immutable: a list, tuple, map, or struct instance handed to a function is exactly the value the caller has, unaffected by anything the function does with it. &quot;Changing&quot; something really means building a new value and rebinding a name to it — <code><a href=\"#/Map:put\">Map.put</a></code>, <code><a href=\"#/List:insert\">List.insert</a></code>, <code><a href=\"#/Struct:update\">Struct.update</a></code> all return a new value rather than mutating their argument. There is no in-place update anywhere in the language, and no <code>RefCell</code>-style escape hatch back into one.</p>\n<h3 id=\"strict\">Strict</h3>\n<p>These are not style suggestions: violating any of them is a hard error that stops the interpreter before your program runs. They exist so that all Ramos code looks the same, everywhere. <code>ramos check</code>/<code>run</code> prints a wrong/correct snippet under the message for every one of them, so the fix is visible right there in the terminal — the examples below are the same ones.</p>\n<p><strong>Layout</strong></p>\n<ul>\n  <li><strong>2 space indentation</strong> — exactly 2 spaces per indentation level, never tabs</li>\n  <li><strong>Whitespace around operators</strong> — <code>x = 1</code> is correct, <code>x=1</code> is an error</li>\n  <li><strong>Whitespace after commas</strong> — <code>function test(n, x)</code> is correct, <code>function test(n,x)</code> is an error (applies to arguments, lists, tuples and maps)</li>\n  <li><strong>Parentheses on calls</strong> — <code>foo()</code>, never bare <code>foo</code>; a bare name is always a variable or a field access</li>\n  <li><strong>Assigned blocks start on their own line</strong> — when the value being assigned is an <code>if</code>, <code>case</code>, <code>cond</code> or <code>run</code> block, a <code>&quot;&quot;&quot;</code> multiline string, or a <code>do</code> lambda with an indented body, it begins on the line <em>after</em> the <code>=</code>, indented one level:</li>\n</ul>\n<pre><code>  # error: the block hangs off the end of the assignment\n  result = case value\n    true -&gt; :ok\n    _ -&gt; :error\n\n  # correct\n  result =\n    case value\n      true -&gt; :ok\n      _ -&gt; :error\n\n  # the same rule for a multiline string\n  banner =\n    &quot;&quot;&quot;\n      Ramos\n    &quot;&quot;&quot;\n\n  # and for a `do` whose body is a block\n  double =\n    do x\n      y = x * 2\n      y</code></pre>\n<p>Otherwise the value starts on the <code>=</code> line but finishes several lines below it, and the deeper the surrounding code, the harder it is to see where the assignment ends. A value that <em>does</em> end on the <code>=</code> line is unaffected, so the single-expression lambda <code>f = do x -&gt; x + 1</code> is correct — the <code>-&gt;</code> is what makes it one line rather than a block.</p>\n<ul>\n  <li><strong>One <code>:</code> on a map key</strong> — a symbol key is <code>{name: 1}</code>; the leading colon of <code>{:name: 1}</code> writes the symbol twice over and is an error</li>\n  <li><strong><code>end</code> is alone on its line, or not at all</strong> — it is an optional, purely decorative block-closing marker (see <a href=\"#indentation\">Indentation</a>), never required and never a value, so <code>x = end</code> or <code>foo() end</code> is an error</li>\n  <li><strong><code>|</code> starts its own line</strong> — never <code>x | f()</code>; write <code>x</code> then <code>| f()</code> on the next line, at the same indentation (see <a href=\"#pipes\">Pipes</a>). Since newlines inside <code>(</code> <code>[</code> <code>{</code> are just whitespace, this also means a pipe can never sit inside a call&#39;s arguments, a list, or a tuple — only as a complete statement or the value of an assignment</li>\n  <li><strong>A <code>do</code> lambda passed directly as a call argument fits on one line</strong> — newlines inside <code>(</code> are whitespace, so a multi-line, arrow-form lambda written in place (as opposed to the indented-body form, which cannot appear inside <code>(</code> at all — see <a href=\"#lambdas\">Lambdas</a>) would still parse, just not where it visually looks like it does. Name it first instead:</li>\n</ul>\n<pre><code>  <span class=\"tok-com\"># error: the lambda is a call argument but spans more than one line</span>\n  <span class=\"tok-type\">SomeProcess</span>.process_and_call_back(\n    [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>],\n    <span class=\"tok-kw\">do</span> processed_value -&gt;\n      print(<span class=\"tok-str\">&quot;DONE #{processed_value * 100}&quot;</span>)\n  )\n\n  <span class=\"tok-com\"># correct: bind it, then pass the name</span>\n  callback =\n    <span class=\"tok-kw\">do</span> processed_value\n      print(<span class=\"tok-str\">&quot;DONE #{processed_value * 100}&quot;</span>)\n\n  <span class=\"tok-type\">SomeProcess</span>.process_and_call_back([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], callback)\n\n  <span class=\"tok-com\"># also correct: it already fits on one line</span>\n  <span class=\"tok-type\">SomeProcess</span>.process_and_call_back([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-kw\">do</span> v -&gt; print(<span class=\"tok-str\">&quot;DONE #{v * 100}&quot;</span>))</code></pre>\n<ul>\n  <li><strong>Once a call&#39;s arguments spill past one line, every argument is on its own line</strong> — the first cannot share the line with <code>(</code> either:</li>\n</ul>\n<pre><code>  <span class=\"tok-com\"># error: the first argument shares the line with `(`</span>\n  <span class=\"tok-type\">SomeProcess</span>.process([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>],\n    <span class=\"tok-str\">&quot;a&quot;</span>\n  )\n\n  <span class=\"tok-com\"># correct</span>\n  <span class=\"tok-type\">SomeProcess</span>.process(\n    [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>],\n    <span class=\"tok-str\">&quot;a&quot;</span>\n  )\n\n  <span class=\"tok-com\"># also correct — the whole call fits on one line</span>\n  <span class=\"tok-type\">SomeProcess</span>.process([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>], <span class=\"tok-str\">&quot;a&quot;</span>)</code></pre>\n<ul>\n  <li><strong>No <code>do</code> lambda reaches an actor</strong> — <code>start_actor</code>, <code>call_actor</code> and <code>cast_actor</code> never accept one as an argument, even nested in a list, tuple or map literal. See <a href=\"#actors\">Actors</a> for why</li>\n</ul>\n<p><strong>Naming</strong></p>\n<ul>\n  <li><strong>Modules are CamelCase</strong>, <code>[a-zA-Z]</code> only — <code>SystemUser</code>, optionally namespaced as <code>MyApp.Business.SystemUser</code></li>\n  <li><strong>Variables and functions are snake_case</strong>, <code>[a-z_]</code> only — <code>main_user_name</code>, <code>read_safely</code>. No trailing <code>?</code> or <code>!</code> as in Ruby or Elixir</li>\n</ul>\n<p><strong>Files</strong></p>\n<ul>\n  <li><strong><code>.rmo</code> extension</strong> — no other extension is loaded</li>\n  <li><strong>One definition per file</strong> — a file holds exactly one module, trait or struct</li>\n  <li><strong>File name matches the definition</strong> — <code>SystemUser</code> lives in <code>system_user.rmo</code></li>\n  <li><strong>Path follows the namespace</strong> — <code>MyApp.Business.SystemUser</code> lives at <code>src/my_app/business/system_user.rmo</code></li>\n</ul>\n<h3 id=\"limited\">Limited</h3>\n<p>Ramos is intentionally <strong>limited</strong>. Its design philosophy is to offer fewer ways to do things and to enforce a consistent code style strictly. Rather than providing multiple syntaxes or constructs for the same operation, Ramos gives you one clear, idiomatic way. This reduces decision fatigue, makes code more predictable, and helps teams maintain a uniform style across codebases.</p>\n<h3 id=\"opinionated\">Opinionated</h3>\n<p>Ramos is also deliberately <strong>opinionated</strong>. You won&#39;t find ternary operators or symbolic logical operators (<code>&amp;&amp;</code>, <code>||</code>, <code>!</code>), and <code>if</code> stops at two branches — <code>else if</code> is a <code>cond</code>. Ramos provides <code>if</code>, <code>cond</code>, and <code>case</code> for branching, and <code>and</code>, <code>or</code>, and <code>not</code> for logical operations. There are exactly two spaces for indentation — no tabs, no configurable spacing. This strictness is by design.</p>\n<p>This README is a tour of the language. For the full standard library, see the module sources under <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/stdlib/\"><code>stdlib/</code></a>.</p>\n<h2 id=\"hello-world\">Hello, world</h2>\n<pre><code><span class=\"tok-com\"># comments start with # and run to the end of the line</span>\nprint(<span class=\"tok-str\">&quot;Ola #{name}&quot;</span>)\n\ngreet = <span class=\"tok-kw\">do</span> name -&gt; <span class=\"tok-str\">&quot;Ola #{name}&quot;</span>\nprint(greet(<span class=\"tok-str\">&quot;Andrew&quot;</span>))</code></pre>\n<p><code>print</code> lives in the <code><a href=\"#/Kernel\">Kernel</a></code> module — and <code><a href=\"#/Kernel\">Kernel</a></code> is the one module that is implicitly in scope everywhere, so <code>print(x)</code> is sugar for <code><a href=\"#/Kernel:print\">Kernel.print(x)</a></code> and you never write the <code><a href=\"#/Kernel\">Kernel.</a></code> prefix (or <code>alias</code> it). See the <a href=\"#standard-library\">Standard library</a> section for the rest of what <code><a href=\"#/Kernel\">Kernel</a></code> provides.</p>\n<h3 id=\"entrypoints\">Entrypoints</h3>\n<p>A runnable Ramos program starts from an <strong>entrypoint</strong>: a <code>.rmo</code> file whose module exposes a public <code>function main()</code>. <code>ramos run</code> loads the file and calls that module&#39;s <code>main()</code> with no arguments:</p>\n<pre><code><span class=\"tok-com\"># app.rmo</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">App</span>\n  <span class=\"tok-kw\">function</span> main()\n    print(<span class=\"tok-str\">&quot;Ola world&quot;</span>)</code></pre>\n<pre><code>ramos <span class=\"tok-kw\">run</span> app.rmo        <span class=\"tok-com\"># calls App.main()</span></code></pre>\n<p><code>main()</code> is only the way in, not the whole module: an entrypoint may define whatever other functions it needs, public or private. Keeping it thin is still the habit worth having — <code>helper</code> functions break <code>main()</code> up without widening what the module exposes:</p>\n<pre><code><span class=\"tok-com\"># app.rmo</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">App</span>\n  <span class=\"tok-kw\">function</span> main()\n    [<span class=\"tok-str\">&quot;Andrew&quot;</span>, <span class=\"tok-str\">&quot;Ana&quot;</span>]\n    | <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> name -&gt; greeting(name))\n    | <span class=\"tok-type\">List</span>.each(<span class=\"tok-kw\">do</span> line -&gt; print(line))\n\n  <span class=\"tok-kw\">helper</span> greeting(name)          <span class=\"tok-com\"># private helper, fine</span>\n    <span class=\"tok-str\">&quot;Ola #{name}&quot;</span></code></pre>\n<p>Most real work still belongs in other modules, and an entrypoint reaches those through <code>alias</code>, which drops a namespaced module&#39;s prefix so you can call it by its short name:</p>\n<pre><code><span class=\"tok-com\"># cli.rmo</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">Cli</span>\n  <span class=\"tok-kw\">alias</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">Business</span>.<span class=\"tok-type\">Greeter</span>\n\n  <span class=\"tok-kw\">function</span> main()\n    <span class=\"tok-type\">Greeter</span>.greet_all([<span class=\"tok-str\">&quot;Andrew&quot;</span>, <span class=\"tok-str\">&quot;Ana&quot;</span>])</code></pre>\n<p>Alias several modules the same way; each <code>alias</code> line sits at the top of the entrypoint body:</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Cli</span>\n  <span class=\"tok-kw\">alias</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">Business</span>.<span class=\"tok-type\">Greeter</span>\n  <span class=\"tok-kw\">alias</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">Reporting</span>.<span class=\"tok-type\">Summary</span>\n\n  <span class=\"tok-kw\">function</span> main()\n    [<span class=\"tok-str\">&quot;Andrew&quot;</span>, <span class=\"tok-str\">&quot;Ana&quot;</span>]\n    | <span class=\"tok-type\">Greeter</span>.greet_all()\n    | <span class=\"tok-type\">Summary</span>.print_report()</code></pre>\n<p>A codebase can have <strong>as many entrypoints as it needs</strong> — one per runnable program — each in its own file, following the same naming rules as any other module (<code>App</code> → <code>app.rmo</code>, <code>MyApp.Cli</code> → <code>src/my_app/cli.rmo</code>).</p>\n<p><code>ramos new &lt;name&gt;</code> scaffolds one of these for you — see <a href=\"#starting-a-project\">Starting a project</a>.</p>\n<p>Bare top-level code, like the <a href=\"#hello-world\">Hello, world</a> snippet above, still runs for quick experiments — <code>ramos run</code> executes a file&#39;s top-level statements when it has no entrypoint module. But a real program you ship is structured as an entrypoint.</p>\n<h2 id=\"keywords\">Keywords</h2>\n<p>Ramos has a small set of reserved keywords. Each serves a specific purpose; there are no redundant constructs.</p>\n<div class=\"table-wrap\">\n<table>\n<thead>\n  <tr><th>Keyword</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n  <tr><td><code>module</code></td><td>Define a namespace of functions</td></tr>\n  <tr><td><code>struct</code></td><td>Define a typed record with fields</td></tr>\n  <tr><td><code>trait</code></td><td>Define a contract of functions for structs to implement</td></tr>\n  <tr><td><code>implements</code></td><td>Declare that a struct implements a trait</td></tr>\n  <tr><td><code>attributes</code></td><td>Declare struct fields with default values</td></tr>\n  <tr><td><code>function</code></td><td>Define a public function</td></tr>\n  <tr><td><code>helper</code></td><td>Define a private function (module-scoped)</td></tr>\n  <tr><td><code>case</code></td><td>Pattern match a value against patterns</td></tr>\n  <tr><td><code>if</code></td><td>Branch two ways on a condition (no <code>else if</code> — use <code>cond</code>)</td></tr>\n  <tr><td><code>else</code></td><td>The other branch of an <code>if</code></td></tr>\n  <tr><td><code>cond</code></td><td>Branch on a sequence of boolean conditions</td></tr>\n  <tr><td><code>run</code></td><td>Run a block of matches, halting on the first that fails</td></tr>\n  <tr><td><code>do</code></td><td>Create an anonymous function (a lambda)</td></tr>\n  <tr><td><code>alias</code></td><td>Create a shorter name for a module</td></tr>\n  <tr><td><code>as</code></td><td>Rename a module in an <code>alias</code> statement (defaults to the last segment)</td></tr>\n  <tr><td><code>self</code></td><td>Reference the current struct instance or module</td></tr>\n  <tr><td><code>true</code></td><td>Boolean true value</td></tr>\n  <tr><td><code>false</code></td><td>Boolean false value</td></tr>\n  <tr><td><code>nil</code></td><td>Represents absence of value</td></tr>\n  <tr><td><code>when</code></td><td>Guard clause in a <code>case</code> pattern, or on a single trailing statement</td></tr>\n  <tr><td><code>and</code></td><td>Logical AND (short-circuits)</td></tr>\n  <tr><td><code>or</code></td><td>Logical OR (short-circuits)</td></tr>\n  <tr><td><code>not</code></td><td>Logical NOT</td></tr>\n  <tr><td><code>_</code></td><td>Wildcard pattern (matches anything)</td></tr>\n</tbody>\n</table>\n</div>\n<h2 id=\"types\">Types</h2>\n<p>Practically every value is tied to a module — <code>42</code> to <code><a href=\"#/Integer\">Integer</a></code>, <code>&quot;hi&quot;</code> to <code><a href=\"#/String\">String</a></code>, <code>[1,2,3]</code> to <code><a href=\"#/List\">List</a></code>, and so on. See the <a href=\"#standard-library\">Standard library</a> section for what each module provides.</p>\n<div class=\"table-wrap\">\n<table>\n<thead>\n  <tr><th>Value</th><th>Module</th></tr>\n</thead>\n<tbody>\n  <tr><td><code>42</code></td><td><code><a href=\"#/Integer\">Integer</a></code></td></tr>\n  <tr><td><code>3.14</code></td><td><code><a href=\"#/Float\">Float</a></code></td></tr>\n  <tr><td><code>&quot;andrew&quot;</code></td><td><code><a href=\"#/String\">String</a></code></td></tr>\n  <tr><td><code>:symbol</code></td><td><code>Symbol</code></td></tr>\n  <tr><td><code>true</code> / <code>false</code></td><td><code>Bool</code></td></tr>\n  <tr><td><code>[1, 2, 3]</code></td><td><code><a href=\"#/List\">List</a></code></td></tr>\n  <tr><td><code>(1, 2, 3)</code></td><td><code><a href=\"#/Tuple\">Tuple</a></code></td></tr>\n  <tr><td><code>{name: &quot;andrew&quot;}</code></td><td><code><a href=\"#/Map\">Map</a></code></td></tr>\n  <tr><td>module</td><td><code><a href=\"#/Module\">Module</a></code></td></tr>\n  <tr><td>struct instance</td><td><code><a href=\"#/Struct\">Struct</a></code></td></tr>\n</tbody>\n</table>\n</div>\n<pre><code>x = <span class=\"tok-lit\">100</span>\ny = <span class=\"tok-lit\">100.5</span>\ns = <span class=\"tok-str\">&quot;andrew&quot;</span>\nsym = <span class=\"tok-lit\">:symbol</span>\nb = <span class=\"tok-lit\">true</span> <span class=\"tok-kw\">or</span> <span class=\"tok-lit\">false</span>\nn = <span class=\"tok-lit\">nil</span>\nlist = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>]\ntuple = (<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>)\nmap = {name: <span class=\"tok-str\">&quot;andrew&quot;</span>}</code></pre>\n<h3 id=\"pure-modules\">Pure modules</h3>\n<p>A <strong>pure module</strong> is a namespace of functions with no per-instance state. It is itself a value, so it can be passed around and inspected.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">PersonUtils</span>\n  <span class=\"tok-kw\">function</span> hello(person)\n    print(<span class=\"tok-str\">&quot;Ola #{person.name}, eu tenho #{person.age}&quot;</span>)\n\n  <span class=\"tok-kw\">helper</span> secret()          <span class=\"tok-com\"># helper = private, only callable from inside the module</span>\n    <span class=\"tok-lit\">42</span>\n\n<span class=\"tok-type\">PersonUtils</span>.hello(andrew)\nandrew\n| <span class=\"tok-type\">PersonUtils</span>.hello()</code></pre>\n<p>A name resolves to exactly one function: Ramos does not overload on arity, and <code>function</code> and <code>helper</code> share the one namespace. Defining a name twice in the same body is an error rather than a second definition that could never be reached:</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Dup</span>\n  <span class=\"tok-kw\">function</span> twice(x)\n    x * <span class=\"tok-lit\">2</span>\n\n  <span class=\"tok-kw\">function</span> twice(x, y)     <span class=\"tok-com\"># error: `Dup` defines `twice` more than once</span>\n    x + y</code></pre>\n<p>A <code>helper</code> may call another <code>helper</code>, and a <code>function</code> may call a <code>helper</code> — that is the whole point of one. What it may not do is call back into its own module&#39;s <code>function</code>s: the direction only runs one way, so a helper cannot widen its own reach by reaching for the public surface it exists to support. This is checked the same way a duplicate name is, at parse time:</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Payments</span>\n  <span class=\"tok-kw\">function</span> charge(amount)\n    log(amount)\n    amount\n\n  <span class=\"tok-kw\">helper</span> log(amount)\n    charge(amount)     <span class=\"tok-com\"># error: `Payments.log` is a `helper` and calls</span>\n                        <span class=\"tok-com\"># `Payments.charge`, a `function` in the same module</span></code></pre>\n<p>There is no <code>const</code>: a constant is a function that takes no arguments, so it is defined and called like everything else. One construct covers both, and the call parentheses say plainly that a name is being resolved in a module.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">SystemUser</span>\n  <span class=\"tok-kw\">function</span> default_email()\n    <span class=\"tok-str\">&quot;system@ramos.com.br&quot;</span>\n\n  <span class=\"tok-kw\">function</span> max_retries()\n    <span class=\"tok-lit\">3</span>\n\nprint(<span class=\"tok-type\">SystemUser</span>.default_email())\nprint(<span class=\"tok-type\">SystemUser</span>.max_retries())</code></pre>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Greet</span>\n  <span class=\"tok-kw\">function</span> hi_all(people)\n    people\n    | <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> p -&gt; <span class=\"tok-str\">&quot;hi #{p.name}&quot;</span>)\n    | <span class=\"tok-type\">List</span>.join(<span class=\"tok-str\">&quot;, &quot;</span>)</code></pre>\n<h3 id=\"alias\">Alias</h3>\n<p><code>alias</code> drops a module&#39;s namespace prefix so you can call it by its <strong>last segment</strong> alone, cutting the verbosity of long names:</p>\n<pre><code><span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span>.<span class=\"tok-type\">Shapes</span>.<span class=\"tok-type\">Circle</span>       <span class=\"tok-com\"># now referred to as `Circle`</span>\n\n<span class=\"tok-type\">Circle</span>.area(<span class=\"tok-lit\">5</span>)  <span class=\"tok-com\"># == 78.5</span></code></pre>\n<p>The local name defaults to that last segment, so most aliases need no extra syntax. <code>as</code> <strong>renames</strong> the module — pick a different short name, chiefly to avoid a collision when two aliased modules would otherwise claim the same one:</p>\n<pre><code><span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span> <span class=\"tok-kw\">as</span> <span class=\"tok-type\">Geo</span>              <span class=\"tok-com\"># rename to a shorter name</span>\n\n<span class=\"tok-type\">Geo</span>.circle_area(<span class=\"tok-lit\">5</span>)  <span class=\"tok-com\"># == 78.5</span></code></pre>\n<pre><code><span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Geometry</span>.<span class=\"tok-type\">Circle</span>              <span class=\"tok-com\"># -&gt; Circle</span>\n<span class=\"tok-kw\">alias</span> <span class=\"tok-type\">Drawing</span>.<span class=\"tok-type\">Circle</span> <span class=\"tok-kw\">as</span> <span class=\"tok-type\">Ellipse</span>    <span class=\"tok-com\"># would clash with Circle, so rename it</span>\n\n<span class=\"tok-type\">Circle</span>.area(<span class=\"tok-lit\">5</span>)\n<span class=\"tok-type\">Ellipse</span>.area(<span class=\"tok-lit\">10</span>, <span class=\"tok-lit\">20</span>)</code></pre>\n<p>Aliases work the same for pure modules and struct modules.</p>\n<h3 id=\"struct-modules\">Struct modules</h3>\n<p>A <strong>struct module</strong> is a module that also produces typed record instances. <code>attributes</code> declares a field with its default. Instances are created with the map-literal syntax prefixed by the struct name — any field you don&#39;t supply takes its declared default. Instances are immutable maps under the hood, tagged with the struct name.</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">Person</span>\n  <span class=\"tok-kw\">attributes</span>\n    name: <span class=\"tok-lit\">nil</span>\n    age: <span class=\"tok-lit\">0</span>\n\n  <span class=\"tok-kw\">function</span> hello(<span class=\"tok-kw\">self</span>)\n    print(<span class=\"tok-str\">&quot;Ola #{self.name}, eu tenho #{self.age}&quot;</span>)\n\nandrew = <span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}\nandrew.hello()\n\nwith_defaults = <span class=\"tok-type\">Person</span>{}      <span class=\"tok-com\"># == Person{name: nil, age: 0}</span></code></pre>\n<p>Construction mirrors pattern matching: the same <code>Person{name: n}</code> shape that builds an instance also destructures one in <code>case</code> patterns.</p>\n<p>Field access is dot syntax, and updates return a new instance:</p>\n<pre><code>andrew.name                       <span class=\"tok-com\"># == &quot;Andrew&quot;</span>\nolder =\n  andrew\n  | <span class=\"tok-type\">Struct</span>.put(<span class=\"tok-lit\">:age</span>, <span class=\"tok-lit\">41</span>)</code></pre>\n<p>Setting a field has its own form, which is sugar for exactly that call:</p>\n<pre><code>andrew.age = <span class=\"tok-lit\">41</span>                   <span class=\"tok-com\"># andrew = Struct.put(andrew, :age, 41)</span></code></pre>\n<p>It <strong>rebinds</strong> rather than mutates: the instance <code>andrew</code> pointed at is untouched, and the name is bound to a new one. That is why the left side has to be a plain variable — there has to be a name to rebind, so <code>f().age = 41</code> is an error rather than a value quietly thrown away.</p>\n<h3 id=\"traits\">Traits</h3>\n<p>Traits declare a contract of functions. A function <strong>with a body</strong> is a default implementation; a function <strong>without a body</strong> is required and every implementer must define it. A struct declares its traits with the <code>implements</code> keyword.</p>\n<pre><code><span class=\"tok-kw\">trait</span> <span class=\"tok-type\">Helloable</span>\n  <span class=\"tok-kw\">function</span> hello(<span class=\"tok-kw\">self</span>)\n    print(<span class=\"tok-str\">&quot;Ola #{self.name}, eu tenho #{self.age}&quot;</span>)\n\n  <span class=\"tok-kw\">function</span> is_over_eighteen(<span class=\"tok-kw\">self</span>)         <span class=\"tok-com\"># required, no body</span>\n\n<span class=\"tok-kw\">struct</span> <span class=\"tok-type\">Person</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Helloable</span>\n\n  <span class=\"tok-kw\">attributes</span>\n    name: <span class=\"tok-lit\">nil</span>\n    age: <span class=\"tok-lit\">0</span>\n\n  <span class=\"tok-kw\">function</span> is_over_eighteen(<span class=\"tok-kw\">self</span>)\n    <span class=\"tok-kw\">self</span>.age &gt; <span class=\"tok-lit\">18</span>\n\nandrew = <span class=\"tok-type\">Person</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>, age: <span class=\"tok-lit\">40</span>}\n\n<span class=\"tok-kw\">case</span> andrew.is_over_eighteen()\n  <span class=\"tok-lit\">true</span>  -&gt; andrew.hello()         <span class=\"tok-com\"># uses the trait&#39;s default hello</span>\n  _     -&gt; <span class=\"tok-lit\">:none</span></code></pre>\n<p>There is no built-in <code>Error</code> trait, because an error is not a special kind of value — see <a href=\"#error-handling\">Error handling</a>. When a symbol and a message are not enough detail, pass a struct as <code>exception</code>&#39;s message — it destructures in <code>case</code> like any other pattern:</p>\n<pre><code><span class=\"tok-kw\">struct</span> <span class=\"tok-type\">DeclineReason</span>\n  <span class=\"tok-kw\">attributes</span>\n    code: <span class=\"tok-lit\">0</span>\n    message: <span class=\"tok-lit\">nil</span>\n\n<span class=\"tok-kw\">function</span> charge(amount)\n  <span class=\"tok-kw\">cond</span>\n    amount &lt;= <span class=\"tok-lit\">0</span> -&gt; exception(<span class=\"tok-lit\">:declined</span>, <span class=\"tok-type\">DeclineReason</span>{code: <span class=\"tok-lit\">555</span>, message: <span class=\"tok-str\">&quot;must be positive&quot;</span>})\n    amount &gt; <span class=\"tok-lit\">10000</span> -&gt; exception(<span class=\"tok-lit\">:declined</span>, <span class=\"tok-type\">DeclineReason</span>{code: <span class=\"tok-lit\">556</span>, message: <span class=\"tok-str\">&quot;too large&quot;</span>})\n    <span class=\"tok-lit\">true</span> -&gt; (<span class=\"tok-lit\">:ok</span>, amount)\n\n<span class=\"tok-kw\">case</span> charge(<span class=\"tok-lit\">50000</span>)\n  (<span class=\"tok-lit\">:ok</span>, charged) -&gt; print(<span class=\"tok-str\">&quot;charged #{charged}&quot;</span>)\n  (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:declined</span>, <span class=\"tok-type\">DeclineReason</span>{code: <span class=\"tok-lit\">556</span>, message: message})) -&gt; print(<span class=\"tok-str\">&quot;too large: #{message}&quot;</span>)\n  (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:declined</span>, <span class=\"tok-type\">DeclineReason</span>{message: message})) -&gt; print(<span class=\"tok-str\">&quot;declined: #{message}&quot;</span>)</code></pre>\n<h3 id=\"pattern-matching\">Pattern matching</h3>\n<p><code>case</code> matches a value against patterns left-to-right and runs the body of the first match. <code>_</code> is the wildcard. Patterns can bind variables and match tuples, structs, and lists:</p>\n<pre><code><span class=\"tok-kw\">case</span> result\n  (<span class=\"tok-lit\">:ok</span>, value)    -&gt; value\n  (<span class=\"tok-lit\">:error</span>, reason) -&gt; print(<span class=\"tok-str\">&quot;failed: #{reason}&quot;</span>)\n  _                -&gt; <span class=\"tok-lit\">:unknown</span></code></pre>\n<p>Lists can be pattern matched with the cons operator <code>|</code>:</p>\n<pre><code><span class=\"tok-kw\">case</span> numbers\n  []          -&gt; <span class=\"tok-str\">&quot;empty&quot;</span>\n  [x]         -&gt; <span class=\"tok-str\">&quot;single: #{x}&quot;</span>\n  [head | _]  -&gt; <span class=\"tok-str\">&quot;head is #{head}&quot;</span>\n\n<span class=\"tok-com\"># Direct destructuring assignment</span>\n[head | tail] = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>]           <span class=\"tok-com\"># head == 1, tail == [2, 3]</span>\n[first, second | rest] = [<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]  <span class=\"tok-com\"># first == 1, second == 2, rest == [3, 4]</span></code></pre>\n<p>Tuples destructure in plain assignments too, which is the idiomatic way to unpack small fixed records:</p>\n<pre><code>person = (<span class=\"tok-str\">&quot;Andrew&quot;</span>, <span class=\"tok-lit\">40</span>)\n(name, age) = person          <span class=\"tok-com\"># name == &quot;Andrew&quot;, age == 40</span></code></pre>\n<p>Nested pattern matching works for any combination:</p>\n<pre><code><span class=\"tok-kw\">case</span> user\n  ({name: n, age: a}, <span class=\"tok-lit\">:admin</span>) <span class=\"tok-kw\">when</span> a &gt; <span class=\"tok-lit\">18</span> -&gt; <span class=\"tok-str\">&quot;Admin #{n}&quot;</span>\n  ({name: n}, <span class=\"tok-lit\">:guest</span>) -&gt; <span class=\"tok-str\">&quot;Guest #{n}&quot;</span>\n  _ -&gt; <span class=\"tok-str\">&quot;Unknown&quot;</span>\n\n<span class=\"tok-com\"># Destructuring nested structures</span>\n((first, _), [head | _]) = ((<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>), [<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>, <span class=\"tok-lit\">5</span>])\n<span class=\"tok-com\"># first == 1, head == 3</span></code></pre>\n<p>Tagged tuples like <code>(:ok, value)</code> / <code>(:error, reason)</code> are the standard result type for fallible operations.</p>\n<p>Each name in a pattern binds <strong>once</strong>. <code>(p, p)</code> looks like it asks for the two positions to be equal, but there is no pin operator to ask that with, so the second <code>p</code> would just rebind and the pattern could never fail — it is rejected instead:</p>\n<pre><code>(p, p) = (<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>)     <span class=\"tok-com\"># error: a pattern cannot bind `p` twice</span>\n(a, b) = (<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>)     <span class=\"tok-com\"># correct — compare them afterwards if that is the intent</span>\n(_, _) = (<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>)     <span class=\"tok-com\"># fine: `_` binds nothing, so it may repeat</span></code></pre>\n<p>A struct pattern is held to the struct&#39;s declared attributes, exactly as construction is. Naming one that does not exist can never match, so it is an error rather than a silently skipped branch:</p>\n<pre><code><span class=\"tok-kw\">case</span> andrew\n  <span class=\"tok-type\">Person</span>{nickname: n} -&gt; n     <span class=\"tok-com\"># error: `Person` has no attribute `nickname`</span>\n  _ -&gt; <span class=\"tok-lit\">:none</span></code></pre>\n<p>A <strong>map</strong> pattern is different: a map has no fixed shape, so a key that is not there is an ordinary non-match, not an error.</p>\n<p><code>cond</code> is just <code>case</code> over a sequence of guard expressions, useful when no single value is being matched.</p>\n<h2 id=\"syntax\">Syntax</h2>\n<h3 id=\"comments\">Comments</h3>\n<pre><code><span class=\"tok-com\"># single line comment, everything after # is ignored</span></code></pre>\n<h3 id=\"documentation-comments\">Documentation comments</h3>\n<p>Modules and functions carry their documentation <em>inline</em>, as a comment block placed <strong>immediately below</strong> the definition it describes. A module doc starts with <code>@module_doc</code>; a function doc starts with <code>@doc</code>. In both cases the marker is followed by an empty <code>#</code> line, then the prose:</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">List</span>\n  <span class=\"tok-com\"># @module_doc</span>\n  <span class=\"tok-com\">#</span>\n  <span class=\"tok-com\"># List — operations on persistent, immutable lists.</span>\n  <span class=\"tok-com\">#</span>\n  <span class=\"tok-com\">#   [1, 2, 3] | List.map(do x -&gt; x * 2)   # == [2, 4, 6]</span>\n\n  <span class=\"tok-kw\">function</span> map(list, f)\n    <span class=\"tok-com\"># @doc</span>\n    <span class=\"tok-com\">#</span>\n    <span class=\"tok-com\"># Applies `f` to every element of `list`, returning a new list of the</span>\n    <span class=\"tok-com\"># results. Preserves order and length.</span>\n    <span class=\"tok-com\">#</span>\n    <span class=\"tok-com\">#   List.map([1, 2, 3], do x -&gt; x * 2)   # == [2, 4, 6]</span>\n    <span class=\"tok-kw\">case</span> list\n      [] -&gt; []\n      [head | tail] -&gt; [f(head)] ++ map(tail, f)</code></pre>\n<p>The module doc is indented to the module body; a function doc is indented to the function body (so it lives inside the function it documents). Doc blocks are just comments, so every line begins with <code>#</code>, and blank <code>#</code> lines separate the summary from examples. See <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/stdlib/\"><code>stdlib/</code></a> for the convention applied across the standard library.</p>\n<p><strong>Examples in a doc block are tests.</strong> Any line carrying <code># ==</code> is a claim, and <code>ramos doctest</code> runs it:</p>\n<pre><code>ramos doctest --stdlib stdlib     # the standard library documents itself\nramos doctest ./mylib             # a project, against the embedded stdlib\nramos doctest                     # same as `ramos doctest .` — the current\n                                   # project, against the embedded stdlib</code></pre>\n<p><code>DIR/src</code> is searched recursively (its own <code>src/test/</code> is skipped — that&#39;s <code>ramos test</code>&#39;s), so a namespaced module&#39;s examples run too. An example has no <code>alias</code> in scope, though, so it calls a namespaced module by its full path: <code>MyApp.Business.Greeter.greet(...)</code>, not the short name a real caller would get from <code>alias</code>ing it.</p>\n<p>Each example runs as its own program, in its own empty directory, with the project&#39;s modules beside it — so an example must be self-contained. Write the file before reading it back, and start an actor before calling it:</p>\n<pre><code><span class=\"tok-com\">#   File.write(&quot;greeting.txt&quot;, &quot;hello\\n&quot;)</span>\n<span class=\"tok-com\">#   File.read(&quot;greeting.txt&quot;)   # == (:ok, &quot;hello\\n&quot;)</span></code></pre>\n<p>Lines above an assertion in the same snippet are its setup, and a blank line of prose ends the snippet (and the scope). When several examples in a file share a fixture, declare it once in the <code>@module_doc</code> under <code># ramos doctest setup</code>: that snippet is never asserted itself and runs before every example in the file.</p>\n<pre><code><span class=\"tok-com\">#   # ramos doctest setup</span>\n<span class=\"tok-com\">#   struct Person</span>\n<span class=\"tok-com\">#     attributes</span>\n<span class=\"tok-com\">#       name: nil</span></code></pre>\n<p>Everything after <code># ==</code> is parsed as Ramos, so a note like <code>(a directory)</code> after the expected value is a syntax error, not a comment — put it in the prose.</p>\n<h3 id=\"variables\">Variables</h3>\n<p>Assignment binds a name to a value. Bindings are immutable: a name can be <strong>rebound</strong> to a new value, but the underlying values themselves never mutate.</p>\n<pre><code>x = <span class=\"tok-lit\">100</span>\nname = <span class=\"tok-str\">&quot;andrew&quot;</span>\npi = <span class=\"tok-lit\">3.14</span>\n\nx = x + <span class=\"tok-lit\">1</span>          <span class=\"tok-com\"># rebinding, not mutation</span></code></pre>\n<h3 id=\"literals\">Literals</h3>\n<pre><code><span class=\"tok-lit\">42</span>            <span class=\"tok-com\"># int</span>\n<span class=\"tok-lit\">3.14</span>          <span class=\"tok-com\"># float</span>\n<span class=\"tok-str\">&quot;andrew&quot;</span>      <span class=\"tok-com\"># string</span>\n<span class=\"tok-lit\">:symbol</span>       <span class=\"tok-com\"># symbol</span>\n<span class=\"tok-lit\">true</span>          <span class=\"tok-com\"># bool</span>\n<span class=\"tok-lit\">false</span>         <span class=\"tok-com\"># bool</span>\n<span class=\"tok-lit\">nil</span>           <span class=\"tok-com\"># nil</span>\n[<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>]     <span class=\"tok-com\"># list</span>\n(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>)     <span class=\"tok-com\"># tuple</span>\n{name: <span class=\"tok-str\">&quot;andrew&quot;</span>}  <span class=\"tok-com\"># map</span></code></pre>\n<p>Only <code>false</code> and <code>nil</code> are falsy; everything else (including <code>0</code>, <code>&quot;&quot;</code> and <code>[]</code>) is truthy.</p>\n<h4 id=\"sigils\">Sigils</h4>\n<p>A letter hugging a string — no space — is a <strong>sigil</strong>: shorthand for parsing that string into one of the date/time types. It desugars before the parser ever sees a call, so <code>D&quot;...&quot;</code> and <code><a href=\"#/Date:parse\">Date.parse(&quot;...&quot;)</a></code> produce the same AST:</p>\n<pre><code><span class=\"tok-str\">D&quot;2024-02-29&quot;</span>                 <span class=\"tok-com\"># == Date.parse(&quot;2024-02-29&quot;)</span>\n<span class=\"tok-str\">T&quot;13:45:30&quot;</span>                   <span class=\"tok-com\"># == Time.parse(&quot;13:45:30&quot;)</span>\n<span class=\"tok-str\">N&quot;2024-02-09T13:45:30.500&quot;</span>    <span class=\"tok-com\"># == NaiveDateTime.parse(&quot;2024-02-09T13:45:30.500&quot;)</span>\n<span class=\"tok-str\">U&quot;2024-02-09T13:45:30.500Z&quot;</span>   <span class=\"tok-com\"># == DateTime.parse(&quot;2024-02-09T13:45:30.500Z&quot;)</span></code></pre>\n<p>The four letters are fixed — <code>D</code> (<code><a href=\"#/Date\">Date</a></code>), <code>T</code> (<code><a href=\"#/Time\">Time</a></code>), <code>N</code> (<code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code>), <code>U</code> (<code><a href=\"#/DateTime\">DateTime</a></code>) — any other letter hugging a <code>&quot;</code> is a lex error. A sigil is always a single literal: no <code>#{...}</code> interpolation and no multi-line form.</p>\n<h4 id=\"map-keys\">Map keys</h4>\n<p>A map key is a <strong>symbol</strong>, a <strong>string</strong> or an <strong>integer</strong> — nothing else, at any point, so every map can be written as a literal. A symbol key is a bare name followed by the <code>:</code> that separates it from its value — that is the only <code>:</code>, and <code>{:name: 1}</code> is an error:</p>\n<pre><code>{name: <span class=\"tok-str\">&quot;andrew&quot;</span>}      <span class=\"tok-com\"># symbol key  :name</span>\n{<span class=\"tok-str\">&quot;host&quot;</span>: <span class=\"tok-str\">&quot;local&quot;</span>}     <span class=\"tok-com\"># string key  &quot;host&quot;</span>\n{<span class=\"tok-lit\">8080</span>: <span class=\"tok-lit\">:http</span>}         <span class=\"tok-com\"># integer key 8080</span>\n\n{name: <span class=\"tok-lit\">1</span>} == {<span class=\"tok-str\">&quot;a&quot;</span>: <span class=\"tok-lit\">1</span>}   <span class=\"tok-com\"># == false — different key types never collide</span></code></pre>\n<p>Keys are literals: they are looked up by value, so a key cannot interpolate (<code>{&quot;#{host}&quot;: 1}</code> is an error). The same three forms work in patterns:</p>\n<pre><code><span class=\"tok-kw\">case</span> config\n  {<span class=\"tok-str\">&quot;host&quot;</span>: h, <span class=\"tok-lit\">8080</span>: p} -&gt; <span class=\"tok-str\">&quot;#{h}:#{p}&quot;</span>\n  _ -&gt; <span class=\"tok-str\">&quot;no match&quot;</span></code></pre>\n<p>The rule holds at runtime too: <code><a href=\"#/Map:put\">Map.put</a></code> rejects a key of any other type. So a grouping function must yield one of the three —</p>\n<pre><code><span class=\"tok-type\">List</span>.group_by([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))\n<span class=\"tok-com\"># runtime error: a Map key must be an Integer, String or Symbol, got Bool</span>\n\nparity = <span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-kw\">cond</span>\n  <span class=\"tok-type\">Integer</span>.is_even(x) -&gt; <span class=\"tok-lit\">:even</span>\n  <span class=\"tok-lit\">true</span> -&gt; <span class=\"tok-lit\">:odd</span>\n\n<span class=\"tok-type\">List</span>.group_by([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>], parity)   <span class=\"tok-com\"># == {odd: [1, 3], even: [2, 4]}</span></code></pre>\n<h3 id=\"indentation\">Indentation</h3>\n<p>Ramos is <strong>indentation driven</strong>, like Python. A keyword or clause followed by an indented line opens a block; the block ends as soon as indentation returns to a lower level. Two rules:</p>\n<ul>\n  <li>use <strong>exactly 2 spaces</strong> per indentation level</li>\n  <li><strong>never tabs</strong></li>\n</ul>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Geometry</span>\n  <span class=\"tok-kw\">function</span> area(r)\n    r * r * <span class=\"tok-lit\">3.14</span></code></pre>\n<p>Blocks are introduced by <code>module</code>, <code>struct</code>, <code>trait</code>, <code>function</code>, <code>if</code>, <code>case</code>, <code>cond</code> and <code>do</code>.</p>\n<p>Indentation is what actually closes a block — <code>end</code> is never required. It exists only as an optional visual marker for readers who like seeing where a block ends, and carries no meaning: the parser drops it before it ever matters, so a program reads identically with or without one. Put it, if you use it at all, on its own line, dedented back to the level of whatever it is marking the end of:</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Geometry</span>\n  <span class=\"tok-kw\">function</span> area(r)\n    r * r * <span class=\"tok-lit\">3.14</span>\n  end\nend</code></pre>\n<p>Nothing checks that an <code>end</code> lines up with the &quot;right&quot; block, or that there is one at all — this is decoration, not a rule, so unlike everything in the <strong>Strict rules</strong> section it is never wrong to leave it out. It only fails (<code>E0016</code>) when it isn&#39;t alone on its line — <code>x = end</code> or <code>foo() end</code> — since <code>end</code> was never meant to be a value.</p>\n<h3 id=\"operators\">Operators</h3>\n<p>Arithmetic:</p>\n<pre><code><span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">2</span>       <span class=\"tok-com\"># sum</span>\n<span class=\"tok-lit\">10</span> - <span class=\"tok-lit\">4</span>      <span class=\"tok-com\"># minus</span>\n<span class=\"tok-lit\">3</span> * <span class=\"tok-lit\">4</span>       <span class=\"tok-com\"># multiply</span>\n<span class=\"tok-lit\">10</span> / <span class=\"tok-lit\">3</span>      <span class=\"tok-com\"># divide (int / int -&gt; int, truncates toward zero)</span>\n-<span class=\"tok-lit\">7</span> % <span class=\"tok-lit\">3</span>      <span class=\"tok-com\"># modulo (non-negative, follows sign of divisor)</span>\n<span class=\"tok-lit\">2</span> ** <span class=\"tok-lit\">10</span>     <span class=\"tok-com\"># pow</span>\n-<span class=\"tok-lit\">5</span>          <span class=\"tok-com\"># neg (unary)</span></code></pre>\n<p>Mixing an <code><a href=\"#/Integer\">Integer</a></code> and a <code><a href=\"#/Float\">Float</a></code> widens the integer to a float:</p>\n<pre><code><span class=\"tok-lit\">1</span> + <span class=\"tok-lit\">1.5</span>     <span class=\"tok-com\"># == 2.5</span>\n<span class=\"tok-lit\">10</span> / <span class=\"tok-lit\">4.0</span>    <span class=\"tok-com\"># == 2.5 (float division; int / int truncates)</span></code></pre>\n<p>Comparison (return a <code>bool</code>):</p>\n<pre><code><span class=\"tok-lit\">1</span> == <span class=\"tok-lit\">1</span>      <span class=\"tok-com\"># equals?</span>\n<span class=\"tok-lit\">1</span> != <span class=\"tok-lit\">2</span>      <span class=\"tok-com\"># not equals?</span>\n<span class=\"tok-lit\">1</span> &lt; <span class=\"tok-lit\">2</span>       <span class=\"tok-com\"># less?</span>\n<span class=\"tok-lit\">1</span> &gt; <span class=\"tok-lit\">2</span>       <span class=\"tok-com\"># greater?</span>\n<span class=\"tok-lit\">1</span> &lt;= <span class=\"tok-lit\">2</span>      <span class=\"tok-com\"># less or equal?</span>\n<span class=\"tok-lit\">1</span> &gt;= <span class=\"tok-lit\">2</span>      <span class=\"tok-com\"># greater or equal?</span></code></pre>\n<p>Logical. There are <strong>no symbolic</strong> logical operators (<code>&amp;&amp;</code>, <code>||</code>, <code>!</code>); use the word forms. <code>and</code> / <code>or</code> short-circuit, and — unlike a language where they coerce to <code>bool</code> — each returns whichever operand decided the result, not <code>true</code>/<code>false</code>.</p>\n<pre><code><span class=\"tok-lit\">true</span> <span class=\"tok-kw\">and</span> <span class=\"tok-lit\">false</span>   <span class=\"tok-com\"># and</span>\n<span class=\"tok-lit\">true</span> <span class=\"tok-kw\">or</span> <span class=\"tok-lit\">false</span>    <span class=\"tok-com\"># or</span>\n<span class=\"tok-kw\">not</span> <span class=\"tok-lit\">true</span>         <span class=\"tok-com\"># not</span></code></pre>\n<p>That returned-operand behavior, combined with <code>and</code> binding tighter than <code>or</code> (see the precedence list at the top of this section), gives Ramos a working ternary even though it has no dedicated ternary syntax:</p>\n<pre><code>value = test <span class=\"tok-kw\">and</span> <span class=\"tok-lit\">1</span> <span class=\"tok-kw\">or</span> <span class=\"tok-lit\">100</span></code></pre>\n<p><code>and</code> binds tighter than <code>or</code>, so this reads as <code>(test and 1) or 100</code>. When <code>test</code> is truthy, <code>test and 1</code> short-circuits to <code>1</code>, and <code>1 or 100</code> — <code>1</code> being truthy — short-circuits right back to <code>1</code>. When <code>test</code> is falsy, <code>test and 1</code> short-circuits to <code>test</code> itself (falsy), so the <code>or</code> falls through and evaluates its right side: <code>100</code>. Either way, <code>value</code> ends up <code>1</code> when <code>test</code> is truthy and <code>100</code> when it is not — a ternary.</p>\n<p>This has the one gap the same trick has in every language that offers it: it breaks if the &quot;then&quot; branch is itself falsy. Since only <code>false</code> and <code>nil</code> are falsy (see <a href=\"#control-flow\">Control flow</a>), <code>test and false or 100</code> evaluates to <code>100</code> even when <code>test</code> is truthy, because the <code>and</code> produced <code>false</code>, which is falsy, so the <code>or</code> overrides it. Reach for <code>if</code> instead whenever the &quot;then&quot; value might be <code>false</code> or <code>nil</code>.</p>\n<p>String:</p>\n<pre><code><span class=\"tok-str\">&quot;a&quot;</span> &lt;&gt; <span class=\"tok-str\">&quot;b&quot;</span>   <span class=\"tok-com\"># concatenate</span></code></pre>\n<p>List:</p>\n<pre><code>[<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>] ++ [<span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]   <span class=\"tok-com\"># concatenate  # == [1, 2, 3, 4]</span></code></pre>\n<p>Map:</p>\n<pre><code>{a: <span class=\"tok-lit\">1</span>} ++ {b: <span class=\"tok-lit\">2</span>}   <span class=\"tok-com\"># merge  # == {a: 1, b: 2}</span>\n{a: <span class=\"tok-lit\">1</span>} ++ {a: <span class=\"tok-lit\">2</span>}   <span class=\"tok-com\"># right side wins on duplicate keys  # == {a: 2}</span></code></pre>\n<p>A range of integers is <code><a href=\"#/List:range\">List.range(from, to)</a></code> — an ordinary <code><a href=\"#/List\">List</a></code>, so every <code><a href=\"#/List\">List</a></code> function works on it. It goes up when <code>from &lt;= to</code>, down when <code>from &gt; to</code>, and is never empty for integer arguments (a single-element list when <code>from == to</code>).</p>\n<pre><code><span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">5</span>)                          <span class=\"tok-com\"># == [1, 2, 3, 4, 5]</span>\n<span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">5</span>, <span class=\"tok-lit\">1</span>)                          <span class=\"tok-com\"># == [5, 4, 3, 2, 1]</span>\n<span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">1</span>)                          <span class=\"tok-com\"># == [1]</span>\n<span class=\"tok-type\">List</span>.sum(<span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">100</span>))              <span class=\"tok-com\"># == 5050</span>\n<span class=\"tok-type\">List</span>.map(<span class=\"tok-type\">List</span>.range(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">3</span>), <span class=\"tok-kw\">do</span> n -&gt; n * n) <span class=\"tok-com\"># == [1, 4, 9]</span></code></pre>\n<p>See also the <strong>Pipes</strong> section below for <code>|</code>.</p>\n<h3 id=\"string-interpolation\">String interpolation</h3>\n<p>Use <code>#{...}</code> inside double-quoted strings</p>\n<pre><code>name = <span class=\"tok-str\">&quot;andrew&quot;</span>\nprint(<span class=\"tok-str\">&quot;Ola #{name}, you have #{1 + 1} new messages&quot;</span>)</code></pre>\n<p>Escape sequences such as <code>\\n</code>, <code>\\t</code>, <code>\\&quot;</code>, <code>\\\\</code> and <code>\\#{</code> are supported.</p>\n<h3 id=\"multiline-strings\">Multiline strings</h3>\n<p>Triple quotes open a <strong>multiline string</strong>. Like everything in Ramos, their shape is strict:</p>\n<ul>\n  <li>the opening <code>&quot;&quot;&quot;</code> is immediately followed by a newline</li>\n  <li>content lines are indented <strong>one level (2 spaces) past the line that opened the string</strong>; that prefix is stripped, and any deeper spaces are content</li>\n  <li>blank lines need no indentation and contribute a bare newline</li>\n  <li>the closing <code>&quot;&quot;&quot;</code> stands alone on its line, at the same indentation as the opening line</li>\n  <li>when it is being assigned, it opens on the line <em>after</em> the <code>=</code> — the same <a href=\"#strict\">strict rule</a> <code>case</code> and <code>run</code> follow</li>\n</ul>\n<p>Every content line keeps its trailing newline, so the value always ends with <code>\\n</code>. Interpolation and escapes work exactly as in single-line strings.</p>\n<pre><code>str =\n  <span class=\"tok-str\">&quot;&quot;&quot;\n    Hi there\n\n    This is a multiline string\n  &quot;&quot;&quot;</span>\n\nprint(str)\n\n<span class=\"tok-kw\">function</span> usage(name)\n  <span class=\"tok-str\">&quot;&quot;&quot;\n    Usage: #{name} &lt;file&gt;\n      -h  show this help\n  &quot;&quot;&quot;</span></code></pre>\n<p>In <code>usage</code>, the string opens at the function-body level, so its content is indented one level deeper and <code>&quot;Usage: ...&quot;</code> is flush left in the value, while <code>  -h ...</code> keeps its two extra spaces.</p>\n<h3 id=\"control-flow\">Control flow</h3>\n<p>Three constructs, each for a different shape of decision: <code>if</code> for two branches, <code>cond</code> for a chain of conditions, <code>case</code> for matching a value.</p>\n<pre><code><span class=\"tok-kw\">if</span> x &gt; <span class=\"tok-lit\">0</span>\n  <span class=\"tok-lit\">:positive</span>\n<span class=\"tok-kw\">else</span>\n  <span class=\"tok-lit\">:zero_or_less</span>\n\n<span class=\"tok-kw\">cond</span>\n  x &gt; <span class=\"tok-lit\">0</span> -&gt; <span class=\"tok-lit\">:positive</span>\n  x &lt; <span class=\"tok-lit\">0</span> -&gt; <span class=\"tok-lit\">:negative</span>\n  <span class=\"tok-lit\">true</span>  -&gt; <span class=\"tok-lit\">:zero</span>\n\n<span class=\"tok-kw\">case</span> x\n  <span class=\"tok-lit\">1</span> -&gt; <span class=\"tok-lit\">:one</span>\n  <span class=\"tok-lit\">2</span> -&gt; <span class=\"tok-lit\">:two</span>\n  _ -&gt; <span class=\"tok-lit\">:other</span></code></pre>\n<p><code>if</code> takes a condition, an indented body, and an optional <code>else</code>. There is <strong>no <code>else if</code></strong> — the moment you want a third branch, that is <code>cond</code>, and the interpreter says so:</p>\n<pre><code><span class=\"tok-kw\">if</span> x &gt; <span class=\"tok-lit\">8</span>\n  <span class=\"tok-lit\">:high</span>\n<span class=\"tok-kw\">else</span> <span class=\"tok-kw\">if</span> x &gt; <span class=\"tok-lit\">3</span>     <span class=\"tok-com\"># error: `else if` is not valid: `if` has exactly two</span>\n  <span class=\"tok-lit\">:mid</span>            <span class=\"tok-com\"># branches — use `cond` for a chain of conditions</span></code></pre>\n<p>A single guarded statement can be written on one line, with the condition trailing it after <code>when</code>:</p>\n<pre><code>print(<span class=\"tok-str\">&quot;big&quot;</span>) <span class=\"tok-kw\">when</span> x &gt; <span class=\"tok-lit\">3</span></code></pre>\n<p><code>when</code>, not <code>if</code>, so that <code>if</code> means exactly one thing — the two-branch block — and the trailing guard reads as the guard it already is in a <code>case</code> arm. The parser builds the same conditional either way, so a trailing <code>when</code> takes no <code>else</code> and guards exactly one statement.</p>\n<p>It cannot guard an assignment: the guarded statement has its own scope, so the binding in <code>x = 1 when ready</code> could never be read on the next line, and the interpreter rejects it rather than binding into nowhere.</p>\n<p><strong>Any value works as a condition</strong>, not just a boolean: only <code>false</code> and <code>nil</code> are falsy, so <code>0</code>, <code>&quot;&quot;</code> and <code>[]</code> all take the <code>if</code> branch. <code><a href=\"#/Kernel:is_truthy\">Kernel.is_truthy</a></code> is that rule written down (<code>not is_truthy(x)</code> is the inverse):</p>\n<pre><code>is_truthy(<span class=\"tok-lit\">0</span>)       <span class=\"tok-com\"># == true</span>\nis_truthy(<span class=\"tok-str\">&quot;&quot;</span>)      <span class=\"tok-com\"># == true</span>\nis_truthy(<span class=\"tok-lit\">nil</span>)     <span class=\"tok-com\"># == false</span>\nis_truthy(<span class=\"tok-lit\">false</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<p>All three are expressions, so they can be assigned. Like any block on the right of <code>=</code>, the construct starts on the <strong>next</strong> line, indented:</p>\n<pre><code>grade =\n  <span class=\"tok-kw\">if</span> score &gt; <span class=\"tok-lit\">8</span>\n    <span class=\"tok-lit\">:high</span>\n  <span class=\"tok-kw\">else</span>\n    <span class=\"tok-lit\">:low</span></code></pre>\n<p>Without an <code>else</code>, an <code>if</code> whose condition is falsy is <code>nil</code>.</p>\n<p>An arm body can also be a <strong>block</strong>: keep the <code>-&gt;</code> at the end of the arm and indent the body on the following lines; the last expression is the arm&#39;s value. A <code>when</code> guard sits between the pattern and the <code>-&gt;</code>. Both are used throughout the standard library:</p>\n<pre><code><span class=\"tok-kw\">case</span> list\n  [] -&gt; []\n  [head | tail] <span class=\"tok-kw\">when</span> head &gt; <span class=\"tok-lit\">0</span> -&gt;\n    doubled = head * <span class=\"tok-lit\">2</span>\n    [doubled] ++ keep_positive_doubled(tail)\n  [_ | tail] -&gt; keep_positive_doubled(tail)</code></pre>\n<h3 id=\"sequential-matching-with-run\">Sequential matching with <code>run</code></h3>\n<p><code>run</code> executes a block sequentially, like any other block — but every <code>pattern = value</code> in it is also a checkpoint. The first match that <strong>fails</strong> ends the block right there, and the value that failed to match becomes the block&#39;s result. If every match succeeds, the block&#39;s result is its last statement&#39;s value, exactly as usual.</p>\n<pre><code>result =\n  <span class=\"tok-kw\">run</span>\n    <span class=\"tok-lit\">:ok</span> = validate_number(<span class=\"tok-lit\">1</span>)\n    <span class=\"tok-lit\">:ok</span> = validate_string(<span class=\"tok-lit\">1</span>)\n    <span class=\"tok-lit\">:ok</span> = validate_symbol(<span class=\"tok-lit\">1</span>)\n\nprint(result) <span class=\"tok-com\"># (:error, (:invalid_string, &quot;1 is not a valid string&quot;))</span></code></pre>\n<p>Here <code>validate_string(1)</code> returns an error tuple (built with <code>exception(:invalid_string, &quot;1 is not a valid string&quot;)</code> — see <a href=\"#error-handling\">Error handling</a>), which does not match <code>:ok</code>. The block stops — <code>validate_symbol</code> is never called — and that error tuple is <code>result</code>. Had all three returned <code>:ok</code>, <code>result</code> would be <code>:ok</code>, the last statement&#39;s value.</p>\n<p>Without <code>run</code>, the same logic needs a <code>case</code> nested inside a <code>case</code> inside a <code>case</code>, one level deeper per validation, with the error path repeated in every one of them. <code>run</code> is the flat version of that.</p>\n<p>A <code>run</code> may close with a <code>case</code> written <strong>without a subject</strong> — the block&#39;s result <em>is</em> the subject — so the happy path and the failure path sit side by side at the same indentation:</p>\n<pre><code><span class=\"tok-kw\">run</span>\n  <span class=\"tok-lit\">:ok</span> = validate_id(data)\n  <span class=\"tok-lit\">:ok</span> = validate_name(data)\n  <span class=\"tok-lit\">:ok</span> = validate_age(data)\n  <span class=\"tok-lit\">:ok</span> = validate_birthday(data)\n<span class=\"tok-kw\">case</span>\n  <span class=\"tok-lit\">:ok</span> -&gt;\n    print(<span class=\"tok-str\">&quot;valid&quot;</span>)\n\n  err -&gt;\n    print(err)</code></pre>\n<p>Bindings made inside a <code>run</code> are visible to the statements after them in the same block, which is what lets each step build on the last:</p>\n<pre><code>total =\n  <span class=\"tok-kw\">run</span>\n    (<span class=\"tok-lit\">:ok</span>, price) = fetch_price(item)\n    (<span class=\"tok-lit\">:ok</span>, quantity) = fetch_quantity(item)\n    price * quantity</code></pre>\n<p>Those bindings do <strong>not</strong> escape the block. A <code>run</code> that halts part-way would otherwise leave some names bound and others not, depending on how far it got; keeping them inside means the code after a <code>run</code> never has to wonder.</p>\n<p><code>run</code> is how a chain of fallible steps stays flat. A failed match is an ordinary value, not an interruption — which is the whole model, described next.</p>\n<h3 id=\"error-handling\">Error handling</h3>\n<p>Ramos has no exceptions. Nothing interrupts execution, unwinds the stack, or jumps to a handler somewhere up the call chain. A function that can fail says so in what it <strong>returns</strong>: a tagged tuple, <code>(:ok, value)</code> on success and, on failure, whatever <code>exception</code> or <code>error</code> builds. There is no <code>raise</code> and no <code>catch</code> — these two are the standard way to report a failure, and every fallible function is expected to return through one of them rather than writing an error tuple by hand.</p>\n<p>Most failures are <code>exception</code>&#39;s: an expected outcome in the ordinary run of a program — a bad request, a declined charge — handled a line or two away. <code>exception(type, message)</code> returns <code>(:error, (type, message))</code>:</p>\n<pre><code><span class=\"tok-kw\">function</span> withdraw(balance, amount)\n  <span class=\"tok-kw\">cond</span>\n    amount &lt;= <span class=\"tok-lit\">0</span> -&gt; exception(<span class=\"tok-lit\">:invalid_amount</span>, <span class=\"tok-str\">&quot;amount must be positive&quot;</span>)\n    amount &gt; balance -&gt; exception(<span class=\"tok-lit\">:insufficient_funds</span>, <span class=\"tok-str\">&quot;balance is too low&quot;</span>)\n    <span class=\"tok-lit\">true</span> -&gt; (<span class=\"tok-lit\">:ok</span>, balance - amount)</code></pre>\n<ul>\n  <li><code>type</code> — a lowercase symbol naming <em>what</em> went wrong; the part code matches on.</li>\n  <li><code>message</code> — a <code><a href=\"#/String\">String</a></code> for a human to read, or a struct when a string is not enough detail (see <a href=\"#traits\">Traits</a> for a <code>DeclineReason</code> example).</li>\n</ul>\n<p>The error is an ordinary value, so it is read with the same <code>case</code> as anything else — and because the error carries its own tag, each failure is handled where it is named:</p>\n<pre><code><span class=\"tok-kw\">case</span> withdraw(<span class=\"tok-lit\">100</span>, <span class=\"tok-lit\">250</span>)\n  (<span class=\"tok-lit\">:ok</span>, remaining) -&gt; print(<span class=\"tok-str\">&quot;left: #{remaining}&quot;</span>)\n  (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:invalid_amount</span>, message)) -&gt; print(<span class=\"tok-str\">&quot;bad request: #{message}&quot;</span>)\n  (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:insufficient_funds</span>, message)) -&gt; print(<span class=\"tok-str\">&quot;declined: #{message}&quot;</span>)\n  (<span class=\"tok-lit\">:error</span>, (_, message)) -&gt; print(<span class=\"tok-str\">&quot;failed: #{message}&quot;</span>)</code></pre>\n<p><code>error(type, message)</code> is <code>exception</code>&#39;s twin for the rarer case where a stacktrace earns its cost: a lower-level or unexpected failure worth tracing back to where it happened, not a predictable business rule. It returns <code>(:error, (type, message, stacktrace))</code> — the same two fields, plus <code>current_stacktrace()</code> captured at the point of failure:</p>\n<pre><code>error(<span class=\"tok-lit\">:insufficient_funds</span>, <span class=\"tok-str\">&quot;balance is too low&quot;</span>)\n<span class=\"tok-com\"># == (:error, (:insufficient_funds, &quot;balance is too low&quot;, []))</span></code></pre>\n<p>Matching it back out just carries the extra position:</p>\n<pre><code><span class=\"tok-kw\">case</span> parse_document(text)\n  (<span class=\"tok-lit\">:ok</span>, doc) -&gt; doc\n  (<span class=\"tok-lit\">:error</span>, (<span class=\"tok-lit\">:malformed</span>, message, _)) -&gt; print(<span class=\"tok-str\">&quot;parse failed: #{message}&quot;</span>)</code></pre>\n<p><code>(type, message[, stacktrace])</code> is the convention the standard library follows: match on <code>type</code>, read <code>message</code>, and reach for <code>stacktrace</code> — when there is one — only while diagnosing. Where there is nothing useful to say beyond a bare tag, <code>(:error, reason)</code> with a symbol reason is enough — that is what <code><a href=\"#/File\">File</a></code> and <code><a href=\"#/Dir\">Dir</a></code> return (<code>:enoent</code>, <code>:eacces</code>, …), since those come straight from the OS rather than through <code>exception</code> or <code>error</code>.</p>\n<p>Because a failure is a value, a function that cannot handle one passes it along by returning it, and the choice is visible at every step rather than hidden in whether some caller installed a handler:</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">FileHandler</span>\n  <span class=\"tok-kw\">function</span> default_content()\n    <span class=\"tok-str\">&quot;Default content&quot;</span>\n\n  <span class=\"tok-kw\">function</span> read_safely(path)\n    <span class=\"tok-kw\">case</span> <span class=\"tok-type\">File</span>.read(path)\n      (<span class=\"tok-lit\">:ok</span>, text) -&gt; text\n      (<span class=\"tok-lit\">:error</span>, reason) -&gt;\n        print(<span class=\"tok-str\">&quot;Could not read #{path}: #{reason}&quot;</span>)\n        default_content()</code></pre>\n<p>Chains of fallible steps do not nest, because <a href=\"#run\"><code>run</code></a> flattens them: each line must match, and the first that does not becomes the block&#39;s value.</p>\n<pre><code>total =\n  <span class=\"tok-kw\">run</span>\n    (<span class=\"tok-lit\">:ok</span>, after_rent) = withdraw(<span class=\"tok-lit\">1000</span>, <span class=\"tok-lit\">400</span>)\n    (<span class=\"tok-lit\">:ok</span>, after_food) = withdraw(after_rent, <span class=\"tok-lit\">200</span>)\n    after_food</code></pre>\n<p>If <code>withdraw(1000, 400)</code> returns an <code>(:error, _)</code> tuple, it does not match <code>(:ok, after_rent)</code>; the block stops there and <code>total</code> is that error tuple. The happy path reads top to bottom, and the failure path needs no branch of its own.</p>\n<h3 id=\"pipes\">Pipes</h3>\n<p><code>|</code> is the pipe, and the only one: it passes the value on its left as the <strong>first argument</strong> to the module function on its right, so data flows top-to-bottom. It always starts its own line, at the same indentation as whatever comes before it — <code>x | f()</code> on one line is an error; write <code>x</code> and <code>| f()</code> on the next.</p>\n<pre><code>{}\n| <span class=\"tok-type\">Map</span>.put(<span class=\"tok-lit\">:new_value_a</span>, <span class=\"tok-lit\">100</span>)\n| <span class=\"tok-type\">Map</span>.put(<span class=\"tok-lit\">:new_value_b</span>, <span class=\"tok-lit\">50</span>)\n| <span class=\"tok-type\">Map</span>.put(<span class=\"tok-lit\">:new_value_c</span>, <span class=\"tok-lit\">1</span>)\n<span class=\"tok-com\"># == {new_value_a: 100, new_value_b: 50, new_value_c: 1}</span></code></pre>\n<p><code>a | M.f(b)</code> means exactly <code>M.f(a, b)</code>, so a pipeline is a chain of ordinary calls read in the order they happen. Naming the module at each step is the point: the value&#39;s type is on the page rather than inferred from the call.</p>\n<pre><code>[<span class=\"tok-str\">&quot;ana&quot;</span>, <span class=\"tok-str\">&quot;bob&quot;</span>]\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> n -&gt; <span class=\"tok-type\">String</span>.upcase(n))\n| <span class=\"tok-type\">List</span>.join(<span class=\"tok-str\">&quot;, &quot;</span>)\n<span class=\"tok-com\"># == &quot;ANA, BOB&quot;</span></code></pre>\n<h3 id=\"lambdas\">Lambdas</h3>\n<p><code>do</code> opens an anonymous function — a <strong>lambda</strong>. Single-expression lambdas use <code>-&gt;</code> and fit on one line:</p>\n<pre><code>add = <span class=\"tok-kw\">do</span> x, y -&gt; x + y\nadd(<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>)          <span class=\"tok-com\"># == 3</span></code></pre>\n<p>Multi-statement lambdas drop the <code>-&gt;</code> and indent the body; the last expression is the return value:</p>\n<pre><code>double_then_add =\n  <span class=\"tok-kw\">do</span> x, y\n    z = x + y\n    z * <span class=\"tok-lit\">2</span>\n\ndouble_then_add(<span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>)   <span class=\"tok-com\"># == 10</span></code></pre>\n<p>Because newlines are whitespace inside <code>(</code>, the indented-body form has no line of its own to indent onto once it is written inline — it can only follow a <code>=</code>, an arm&#39;s <code>-&gt;</code>, or another spot where a real newline is possible, never sit directly inside a call&#39;s parentheses. A <code>do</code> lambda passed straight into a call is therefore always the single-expression form, and per the <a href=\"#strict\">Strict rules</a> it must still fit on one line — assign a longer one to a name first, then pass the name.</p>\n<p>Lambdas are values, so they pipe naturally:</p>\n<pre><code>[<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>, <span class=\"tok-lit\">4</span>]\n| <span class=\"tok-type\">List</span>.filter(<span class=\"tok-kw\">do</span> x -&gt; <span class=\"tok-type\">Integer</span>.is_even(x))    <span class=\"tok-com\"># == [2, 4]</span>\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> x -&gt; x * <span class=\"tok-lit\">10</span>)                 <span class=\"tok-com\"># == [20, 40]</span></code></pre>\n<h4 id=\"closures\">Closures</h4>\n<p>A lambda closes over the scope it was written in, so its body can read the names that surrounded it:</p>\n<pre><code>v = <span class=\"tok-lit\">1</span>\nlb = <span class=\"tok-kw\">do</span> x -&gt; x + v\n\nlb(<span class=\"tok-lit\">2</span>)   <span class=\"tok-com\"># == 3</span></code></pre>\n<p>A lambda may not refer to the name it is being bound to (<code>f = do x -&gt; f(x)</code>): lambdas are anonymous and non-recursive, and a named <code>function</code> is how you recurse.</p>\n<h2 id=\"standard-library\">Standard library</h2>\n<p>Each type ships with a rich, pipe-friendly module. A few highlights (<code><a href=\"#/Kernel\">Kernel</a></code>, <code><a href=\"#/String\">String</a></code>, <code><a href=\"#/List\">List</a></code>, and <code><a href=\"#/Tuple\">Tuple</a></code> have inline-documented sources under <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/stdlib/\"><code>stdlib/</code></a>; the remaining modules are not in this repo yet):</p>\n<ul>\n  <li><strong><code><a href=\"#/Kernel\">Kernel</a></code></strong> — the only module whose functions you call <em>bare</em>: <code><a href=\"#/Kernel\">Kernel</a></code> is implicitly in scope everywhere, so <code>print(x)</code> is sugar for <code><a href=\"#/Kernel:print\">Kernel.print(x)</a></code> and no <code>alias</code> is ever needed for it. It hosts:</li>\n  <li><strong>console I/O</strong> — <code>print</code>, <code>println</code>, <code>new_line</code>, <code>read</code>, <code>read_all</code>, <code>read_password</code>, and <code>eprint</code>/<code>eprintln</code> for diagnostics on standard error</li>\n  <li><strong>process / CLI</strong> — <code>get_args</code>, <code>get_arg</code>, <code>get_env</code>, <code>sleep</code>, <code>exit</code></li>\n  <li><strong>time</strong> — <code>now</code> (wall clock) and <code>monotonic</code> (only moves forward, so it is the one to measure elapsed time with)</li>\n  <li><strong>randomness</strong> — <code>random</code>, <code>random_int</code> (not cryptographic)</li>\n  <li><strong>collections</strong> — <code>size</code>, <code>at</code>, <code>to_list</code>, each working on a list, tuple or map, which is why the type modules do not repeat them; plus <code>is_empty</code>, which also spans strings</li>\n  <li><strong>conversions</strong> — <code>to_string</code> (display form) and <code>inspect</code> (debug form: the rendering the REPL prints), <code>to_integer</code>, <code>to_float</code>, and <code>type_of</code></li>\n  <li><strong>predicates</strong> — the <code>is_*</code> family (<code>is_integer</code>, <code>is_string</code>, …) and <code>is_truthy</code>, the falsy rule written down</li>\n  <li><strong>calling</strong> — <code>apply(f, args)</code>, to call a lambda whose arity the call site does not know</li>\n  <li><strong>actors</strong> — <code>start_actor</code> / <code>call_actor</code> (see <a href=\"#actors\">Actors</a>)</li>\n  <li><strong>seams</strong> — <code>native(str, args)</code>, plus <code>current_stacktrace</code></li>\n</ul>\n<p>Every other module must be referenced by name (or <code>alias</code>ed).</p>\n<ul>\n  <li><strong><code><a href=\"#/Integer\">Integer</a></code></strong> — <code>compare</code>/<code>clamp</code>/<code>min</code>/<code>max</code>, <code>times</code>, <code>gcd</code>/<code>lcm</code>, <code>abs</code>/<code>sign</code>, <code>digits</code>, predicates (<code>is_even</code>, <code>is_odd</code>, <code>is_zero</code>, <code>is_positive</code>, <code>is_negative</code>). Counting up or down a range of integers is <code><a href=\"#/List:range\">List.range</a></code> — not repeated here.</li>\n  <li><strong><code><a href=\"#/Float\">Float</a></code></strong> — <code>round</code>/<code>floor</code>/<code>ceil</code> (all returning an <code><a href=\"#/Integer\">Integer</a></code>), <code>abs</code>/<code>sign</code>/<code>sqrt</code>/<code>min</code>/<code>max</code>/<code>clamp</code>/<code>compare</code>, transcendentals (<code>exp</code>/<code>log</code>/<code>log_two</code>/<code>log_ten</code>/<code>sin</code>/<code>cos</code>/<code>tan</code> — the only natives behind this module), constants (<code>pi()</code>, <code>e()</code>, <code>infinity</code>, <code>nan</code>), predicates (<code>is_nan</code>, <code>is_infinite</code>, <code>is_finite</code>, <code>is_positive</code>, <code>is_negative</code>).</li>\n  <li><strong><code><a href=\"#/String\">String</a></code></strong> — <code>&lt;&gt;</code> / <code>at</code> / <code>repeat</code>, casing (including <code>capitalize</code>), trimming, <code>split</code>/<code>replace</code> (joining a list of strings is <code><a href=\"#/List:join\">List.join</a></code>), <code>pad_left</code>/<code>pad_right</code>, <code>slice</code>, <code>find</code>/<code>contains</code>, conversions.</li>\n  <li><strong><code><a href=\"#/List\">List</a></code></strong> — <code>range</code> (either direction, ascending or descending), <code>map</code>/ <code>filter</code>/<code>reject</code>/<code>reduce</code>/<code>flat_map</code>, <code>sort</code>/<code>sort_by</code>/ <code>uniq</code>/<code>dedup</code>/<code>dedup_by</code>, <code>chunk_every</code>/<code>group_by</code>/<code>partition</code>, <code>insert</code>/<code>delete</code>/<code>delete_at</code>/<code>update_at</code>, <code>take_while</code>/ <code>drop_while</code>, <code>zip</code>/<code>zip_with</code>/<code>unzip</code>, <code>with_index</code>/<code>map_with_index</code>/ <code>each_with_index</code>, <code>flatten</code>, <code>intersperse</code>, <code>sample</code>/<code>shuffle</code>, slicing, search (<code>index_of</code> alongside <code>find</code>/<code>find_index</code>), aggregates (<code>max_by</code>/<code>min_by</code> alongside <code>max</code>/<code>min</code>), <code>all</code> (true when <code>f</code> holds for every element), and <code>any</code> (true when <code>f</code> holds for at least one).</li>\n  <li><strong><code><a href=\"#/Map\">Map</a></code></strong> — <code>get</code>/<code>put</code>/<code>put_new</code>/<code>delete</code>/<code>update</code>/<code>merge</code> (<code>++</code> is sugar for <code>merge</code>, <code>merge_with</code> resolves a clash instead of the right side winning), <code>filter</code>/<code>reject</code> to screen pairs with a <code>(key, value)</code> predicate, <code>keys</code>/<code>values</code>/<code>entries</code>/<code>from_list</code>, <code>map_keys</code>/<code>map_values</code>/<code>map_entries</code>, <code>has_key</code>. Keys are integers, strings and symbols only.</li>\n  <li><strong><code><a href=\"#/Tuple\">Tuple</a></code></strong> — <code>set</code>, <code>last</code>, <code>from_list</code>/<code>to_map</code>. Read by position with <code>at(t, 0)</code>, or destructure: <code>(a, b) = pair</code>.</li>\n  <li><strong><code><a href=\"#/Struct\">Struct</a></code></strong> — <code>get</code>/<code>put</code>/<code>update</code>, <code>to_map</code>, <code>is_a</code>, <code>keys</code>/<code>values</code>. Instances are built with the literal <code>Name{...}</code> syntax.</li>\n  <li><strong><code><a href=\"#/Date\">Date</a></code></strong> — a calendar date (<code>year</code>/<code>month</code>/<code>day</code>, no time-of-day or time zone): <code>new</code>/<code>today</code>/<code>from_epoch_day</code>/<code>parse</code>, <code>to_epoch_day</code>/<code>to_iso</code>, <code>add_days</code>/<code>add_weeks</code>/<code>add_months</code>/<code>add_years</code>, <code>compare</code>/<code>diff_days</code>/<code>day_of_week</code>, <code>is_leap_year</code>/<code>days_in_month</code>. An out-of-range <code>day</code> normalizes rather than raising.</li>\n  <li><strong><code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code></strong> — a <code><a href=\"#/Date\">Date</a></code> plus <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>, with no time zone: <code>new</code>/<code>now</code>/<code>from_epoch_millis</code>/<code>parse</code>, <code>to_epoch_millis</code>/<code>to_iso</code>, <code>add_days</code>/<code>add_weeks</code>/<code>add_months</code>/ <code>add_hours</code>/<code>add_minutes</code>/<code>add_seconds</code>, <code>compare</code>/<code>diff_millis</code>/ <code>day_of_week</code>, <code>date</code> (drops the time of day).</li>\n  <li><strong><code><a href=\"#/TimeZone\">TimeZone</a></code></strong> — a name paired with a fixed offset from UTC (no IANA database — no daylight saving, no historical rules, just <code>-720</code>..<code>840</code> minutes checked against the real-world range): <code>utc</code>/<code>fixed</code>/ <code>from_offset_minutes</code>/<code>parse</code>, <code>offset_text</code>/<code>parse_offset_text</code> (the shared <code>&quot;Z&quot;</code>/<code>±HH:MM</code> text form <code><a href=\"#/DateTime:to_iso\">DateTime.to_iso</a></code>/<code>parse</code> also use), <code>compare</code>.</li>\n  <li><strong><code><a href=\"#/DateTime\">DateTime</a></code></strong> — a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> plus a fixed <code>offset_minutes</code> from UTC (no time zone database — a plain numeric offset, not a zone name): <code>new</code>/<code>now</code>/<code>now_at</code>/<code>now_in</code>/<code>from_epoch_millis</code>/<code>from_naive</code>/<code>parse</code>, <code>to_utc_epoch_millis</code>/<code>to_iso</code>/<code>to_naive</code>/<code>to_utc</code>/<code>with_offset</code>/ <code>in_time_zone</code>/<code>time_zone</code>, <code>add_days</code>/<code>add_weeks</code>/<code>add_months</code>/ <code>add_hours</code>/<code>add_minutes</code>/<code>add_seconds</code>, <code>compare</code>/<code>diff_millis</code>/ <code>day_of_week</code>. <code>compare</code>/<code>diff_millis</code> order by the instant, not the printed local fields; <code>now_in</code>/<code>in_time_zone</code> take a <code><a href=\"#/TimeZone\">TimeZone</a></code>, the rest a bare <code>offset_minutes</code>.</li>\n  <li><strong><code><a href=\"#/Actor\">Actor</a></code></strong> — the trait a module implements to hold state and answer messages; driven by <code><a href=\"#/Kernel\">Kernel</a></code>&#39;s <code>start_actor</code> / <code>call_actor</code>.</li>\n  <li><strong><code><a href=\"#/Global\">Global</a></code></strong> — one process-wide map held by an actor: <code>start</code>, <code>get</code>, <code>put</code>, <code>clear</code>.</li>\n  <li><strong><code><a href=\"#/Config\">Config</a></code></strong> — the environment&#39;s <code>.env</code> file, read once at <code>start</code> and answered from memory: <code>get(section, key)</code>, <code>path</code>. Read-only. Shared mutable state; see <a href=\"#global\">Global</a> before reaching for it.</li>\n  <li><strong><code><a href=\"#/Thread\">Thread</a></code></strong> — one-shot parallel work: <code>start</code>, <code>await</code>, <code>await_all</code>, and a parallel <code>map</code>/<code>each</code>; see <a href=\"#threads\">Threads</a>.</li>\n  <li><strong><code><a href=\"#/Test\">Test</a></code></strong> — the marker trait a test module implements; see <a href=\"#tests\">Tests</a>.</li>\n  <li><strong><code><a href=\"#/Module\">Module</a></code></strong> — <code>functions</code> (a module&#39;s public function names) and <code>apply</code> (call one by name, given as a <code><a href=\"#/String\">String</a></code>, with its arguments as a list) — both only ever reach a module&#39;s public functions, the same as a written call from outside it would.</li>\n</ul>\n<h3 id=\"actors\">Actors</h3>\n<p>An <strong>actor</strong> is a named holder of state that answers messages. A module becomes one by implementing the <code><a href=\"#/Actor\">Actor</a></code> trait, which requires a single function: <code>call(f, args, state, config)</code>, returning a <code>(reply, new_state)</code> tuple. The reply goes back to the caller; <code>new_state</code> is what the actor holds from then on. Nothing mutates — the actor simply keeps the newest value.</p>\n<pre><code><span class=\"tok-kw\">module</span> <span class=\"tok-type\">Cache</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Actor</span>\n\n  <span class=\"tok-com\"># server</span>\n  <span class=\"tok-kw\">function</span> call(f, args, state, config)\n    <span class=\"tok-kw\">case</span> f\n      <span class=\"tok-lit\">:get</span> -&gt;\n        [key] = args\n        (<span class=\"tok-type\">Map</span>.get(state, key, <span class=\"tok-lit\">nil</span>), state)\n      <span class=\"tok-lit\">:set</span> -&gt;\n        [key, value] = args\n        (<span class=\"tok-lit\">:ok</span>, <span class=\"tok-type\">Map</span>.put(state, key, value))\n\n  <span class=\"tok-com\"># client</span>\n  <span class=\"tok-kw\">function</span> start()\n    start_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, {}, {})\n\n  <span class=\"tok-kw\">function</span> get(key)\n    call_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:get</span>, [key])\n\n  <span class=\"tok-kw\">function</span> set(key, value)\n    call_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:set</span>, [key, value])</code></pre>\n<pre><code><span class=\"tok-type\">Cache</span>.start()\n<span class=\"tok-type\">Cache</span>.set(<span class=\"tok-str\">&quot;key&quot;</span>, <span class=\"tok-str\">&quot;v2&quot;</span>)\nvalue = <span class=\"tok-type\">Cache</span>.get(<span class=\"tok-str\">&quot;key&quot;</span>)     <span class=\"tok-com\"># == &quot;v2&quot;</span></code></pre>\n<p>Two <code><a href=\"#/Kernel\">Kernel</a></code> functions drive them, so neither needs a prefix:</p>\n<div class=\"table-wrap\">\n<table>\n<thead>\n  <tr><th>Call</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n  <tr><td><code>start_actor(id, module, initial_state, config)</code></td><td>Register an actor under <code>id</code>. Returns <code>:ok</code>.</td></tr>\n  <tr><td><code>call_actor(id, module, op, args)</code></td><td>Send one message and wait; returns the handler&#39;s reply.</td></tr>\n  <tr><td><code>cast_actor(id, module, op, args)</code></td><td>Send one message without waiting; returns <code>:ok</code> at once.</td></tr>\n  <tr><td><code>list_actors()</code></td><td>Every running actor as an <code>(id, module)</code> pair, sorted by id.</td></tr>\n  <tr><td><code>actor_stats(id)</code></td><td>A <code><a href=\"#/Map\">Map</a></code> describing one actor.</td></tr>\n  <tr><td><code>is_actor_started(id)</code></td><td>Whether <code>id</code> names a running actor.</td></tr>\n  <tr><td><code>stop_actor(id)</code></td><td>Finish what was sent, then end the actor. Returns <code>:ok</code>.</td></tr>\n</tbody>\n</table>\n</div>\n<p>The <code>id</code> is what identifies a running actor, so one module can back many of them, each with its own state:</p>\n<pre><code><span class=\"tok-type\">Counter</span>.start(<span class=\"tok-lit\">:a</span>)\n<span class=\"tok-type\">Counter</span>.start(<span class=\"tok-lit\">:b</span>)      <span class=\"tok-com\"># independent state from :a</span></code></pre>\n<h4 id=\"inspecting-and-stopping\">Inspecting and stopping</h4>\n<p>Running actors can be listed and asked about themselves:</p>\n<pre><code>list_actors()             <span class=\"tok-com\"># == [(:alpha, Counter), (:beta, Gauge)]</span>\nis_actor_started(<span class=\"tok-lit\">:alpha</span>)  <span class=\"tok-com\"># == true</span>\nactor_stats(<span class=\"tok-lit\">:alpha</span>)\n<span class=\"tok-com\"># == {id: :alpha, module: &quot;Counter&quot;, calls: 2, casts: 1, pending: 1, alive: true}</span></code></pre>\n<p>Each row carries the module itself, not just its name, so a pair goes straight back into a call:</p>\n<pre><code>[(id, mod) | _] = list_actors()\ncall_actor(id, mod, <span class=\"tok-lit\">:ping</span>, [])</code></pre>\n<p><code>calls</code> and <code>casts</code> count messages <strong>sent</strong>. Because a cast is asynchronous it may still be running, so <code>pending</code> is the part not accounted for yet — it drops to zero once the handler has finished and its result been collected.</p>\n<p><code>stop_actor(id)</code> lets the actor finish everything already sent to it, then ends its thread and frees the id for reuse. Nothing in flight is discarded: the stop is queued behind the messages already in the mailbox. Sending to a stopped actor is an error, the same as sending to one that was never started.</p>\n<pre><code>stop_actor(<span class=\"tok-lit\">:beta</span>)         <span class=\"tok-com\"># == :ok</span>\nis_actor_started(<span class=\"tok-lit\">:beta</span>)   <span class=\"tok-com\"># == false</span></code></pre>\n<h4 id=\"call-and-cast\">call and cast</h4>\n<p><code>call</code> waits: the message goes to the actor&#39;s thread, the handler runs there, and its reply comes back before the next statement. <code>cast</code> does not — <code>:ok</code> comes back as soon as the message is sent, and the handler runs on the actor&#39;s thread while this one carries on. One actor handles its own mailbox in order, so a <code>call</code> always sees every message sent to that actor before it.</p>\n<p>The second callback returns only the new state, because a cast has no reply to send anywhere:</p>\n<pre><code><span class=\"tok-kw\">function</span> cast(f, args, state, config)\n  [key] = args\n  <span class=\"tok-type\">Map</span>.delete(state, key)</code></pre>\n<p><code>cast</code> is a trait <em>default</em>, not a requirement: unless a module defines its own, a cast runs that module&#39;s <code>call</code> and discards the reply. So an actor written only for <code>call</code> can be cast to without writing anything extra, and you override <code>cast</code> for work that only makes sense fire-and-forget.</p>\n<p>The concurrency is real, not just deferral. With a handler that sleeps 400ms:</p>\n<pre><code>cast_actor(<span class=\"tok-lit\">:s</span>, <span class=\"tok-type\">Sleeper</span>, <span class=\"tok-lit\">:work</span>, [])\nprint(<span class=\"tok-str\">&quot;main continued immediately&quot;</span>)\nsleep(<span class=\"tok-lit\">50</span>)</code></pre>\n<p>the whole program takes ~405ms, not 450ms — the two sleeps overlap. Two actors each sleeping 300ms finish in ~307ms, not 600ms.</p>\n<p>A handler&#39;s output is buffered on its thread and written by whichever thread receives it, so a line of actor output never lands inside a line of yours. One consequence: because a cast runs on its own thread, its output and any failure inside it surface when the reply is collected — at the next call to that actor, or at the end-of-program drain.</p>\n<p><code>config</code> is fixed for the actor&#39;s lifetime and handed to every <code>call</code> and <code>cast</code>; <code>state</code> is what advances. The convention is to keep <code>call</code> as the server half and wrap each operation in a plain function — the client half — so callers never write <code>call_actor</code> themselves.</p>\n<p>Mistakes are named rather than left to surface later: starting a module that does not implement <code><a href=\"#/Actor\">Actor</a></code>, starting the same id twice, calling an id that was never started, calling one id through another module&#39;s handler, and a <code>call</code> that returns something other than a <code>(reply, new_state)</code> pair are all errors. So is an actor calling itself — the re-entrant call would read the state from before the outer call and have its write overwritten.</p>\n<blockquote>\n<p><strong>Each actor owns its memory.</strong> An actor&#39;s thread gets a fresh root scope, so a handler cannot see the bindings around the call that started it, and its state is reachable only by sending a message. That memory does not include the actor registry either, so <strong>a handler cannot message another actor or itself</strong> — a self-call would block the actor waiting on a reply only it could produce. Actors are not yet a unit of failure isolation: an error in a handler propagates to whoever collects the reply rather than restarting the actor.</p>\n</blockquote>\n<p>For the same reason, <strong><code>start_actor</code>, <code>call_actor</code> and <code>cast_actor</code> never accept a <code>do</code> lambda</strong> — directly, or nested in a list, tuple or map literal argument:</p>\n<pre><code><span class=\"tok-com\"># error: a lambda cannot be passed to an actor</span>\ncall_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:process</span>, [<span class=\"tok-kw\">do</span> x -&gt; x + <span class=\"tok-lit\">1</span>])</code></pre>\n<p>A lambda closes over the scope it was written in, but a handler&#39;s thread cannot see that scope — the closure would carry bindings the actor could never actually reach. This check is a strict, syntactic one rather than full dataflow analysis, so it only catches a lambda written in place; it does not follow a lambda through a variable:</p>\n<pre><code><span class=\"tok-com\"># not caught at parse time, but just as broken at runtime</span>\ncb = <span class=\"tok-kw\">do</span> x -&gt; x + <span class=\"tok-lit\">1</span>\ncall_actor(<span class=\"tok-lit\">:cache</span>, <span class=\"tok-type\">Cache</span>, <span class=\"tok-lit\">:process</span>, [cb])</code></pre>\n<h4 id=\"global\">Global</h4>\n<p>The stdlib ships one actor already written: <code><a href=\"#/Global\">Global</a></code>, a single process-wide <code><a href=\"#/Map\">Map</a></code> behind the id <code>:global</code>.</p>\n<pre><code><span class=\"tok-type\">Global</span>.start()\n<span class=\"tok-type\">Global</span>.put(<span class=\"tok-lit\">:user</span>, <span class=\"tok-str\">&quot;andrew&quot;</span>)\n<span class=\"tok-type\">Global</span>.get(<span class=\"tok-lit\">:user</span>)             <span class=\"tok-com\"># == &quot;andrew&quot;</span>\n<span class=\"tok-type\">Global</span>.clear(<span class=\"tok-lit\">:user</span>)           <span class=\"tok-com\"># a cast: returns :ok at once</span></code></pre>\n<p><code>start</code> takes no arguments — there is exactly one of it — and <code>get</code> returns <code>nil</code> for a key that was never set. Only <code>get</code> waits: <code>put</code> and <code>clear</code> are casts that return <code>:ok</code> as soon as the message is sent, and a <code>get</code> behind them still sees the write, because the actor handles its mailbox in order.</p>\n<p><strong>Use it with caution.</strong> It is shared mutable state wearing an actor&#39;s clothes: a function that reads it declares nothing about what it needs, every reader and writer in the program queues behind one thread, the key space is flat and unscoped, and between a <code>get</code> and the <code>put</code> that acts on it another caller may have written the key. An actor handler cannot reach it at all, because an actor&#39;s thread cannot see the registry. Use it for what is genuinely process-wide and written once — config read at startup, a feature flag, a run id — and pass the value everywhere else.</p>\n<h4 id=\"config\">Config</h4>\n<p>The other actor the stdlib ships is <code><a href=\"#/Config\">Config</a></code>: the settings for this run, read once and answered from memory. It is <strong>read-only</strong> — there is no <code>put</code>.</p>\n<p><code>APP_ENV</code> names the environment, and the file is <code>.env</code> followed by its downcased name: <code>APP_ENV=PROD</code> reads <code>.env.prod</code>, <code>APP_ENV=staging</code> reads <code>.env.staging</code>, and an unset or empty <code>APP_ENV</code> reads the plain <code>.env</code>. The path is relative, so it resolves against the directory the program was run from, and <code><a href=\"#/Config:path\">Config.path()</a></code> reports the choice without reading anything.</p>\n<pre><code># .env.prod\n[database]\nhost = &quot;db.internal&quot;\nport = 5432\npassword = &#39;p#ss&#39;   # a comment after an unquoted value is trimmed off</code></pre>\n<pre><code><span class=\"tok-type\">Config</span>.start()                     <span class=\"tok-com\"># == :ok, or (:error, :enoent)</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;host&quot;</span>)     <span class=\"tok-com\"># == &quot;db.internal&quot;</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;port&quot;</span>)     <span class=\"tok-com\"># == &quot;5432&quot;</span>\n<span class=\"tok-type\">Config</span>.get(<span class=\"tok-str\">&quot;database&quot;</span>, <span class=\"tok-str\">&quot;nope&quot;</span>)     <span class=\"tok-com\"># == nil</span></code></pre>\n<p>The file is TOML, of the subset a settings file uses: <code>[section]</code> headers, <code>key = value</code> pairs, <code>#</code> comments, blank lines. Every value comes back as a <code><a href=\"#/String\">String</a></code> — <code>port = 5432</code> reads as <code>&quot;5432&quot;</code> — because the file cannot say what a value was meant to be, and guessing would let the same key return a different type on a different deploy. Reach for <code>to_integer</code> on the way out. Quotes are stripped, single or double, and a <code>#</code> inside them is part of the value; a key written before the first <code>[section]</code> has no section to be asked for, so it is skipped.</p>\n<p>Reading happens in <code>start</code>, not in the actor, so an unreadable file surfaces at the call that started it and the caller decides whether that is fatal:</p>\n<pre><code><span class=\"tok-kw\">case</span> <span class=\"tok-type\">Config</span>.start()\n  <span class=\"tok-lit\">:ok</span> -&gt; <span class=\"tok-kw\">run</span>()\n  (<span class=\"tok-lit\">:error</span>, <span class=\"tok-lit\">:enoent</span>) -&gt; run_with_defaults()</code></pre>\n<h3 id=\"threads\">Threads</h3>\n<p>Where an actor holds <strong>state</strong> and answers messages over its lifetime, a <strong>thread</strong> is a one-shot piece of parallel work that produces a <strong>value</strong>. <code><a href=\"#/Thread:start\">Thread.start</a></code> starts a zero-argument lambda on its own thread and hands back a handle at once; <code><a href=\"#/Thread:await\">Thread.await</a></code> waits on the handle and returns what the lambda produced.</p>\n<pre><code>t = <span class=\"tok-type\">Thread</span>.start(<span class=\"tok-kw\">do</span> -&gt; slow_sum())\nother_work()                  <span class=\"tok-com\"># runs while the thread does</span>\ntotal = <span class=\"tok-type\">Thread</span>.await(t)       <span class=\"tok-com\"># now wait for it</span></code></pre>\n<p>The parallelism is real: two threads each sleeping 300ms finish in ~300ms, not 600. To fan a batch out and gather it, start them all and <code><a href=\"#/Thread:await_all\">Thread.await_all</a></code> the handles — every thread is already running, so the wait is for the slowest, not the sum:</p>\n<pre><code>[<span class=\"tok-kw\">do</span> -&gt; a(), <span class=\"tok-kw\">do</span> -&gt; b(), <span class=\"tok-kw\">do</span> -&gt; c()]\n| <span class=\"tok-type\">List</span>.map(<span class=\"tok-kw\">do</span> f -&gt; <span class=\"tok-type\">Thread</span>.start(f))\n| <span class=\"tok-type\">Thread</span>.await_all()</code></pre>\n<p><code><a href=\"#/Thread:map\">Thread.map</a></code> wraps that pattern: it fans a list out one thread per element and gathers the results in order.</p>\n<pre><code><span class=\"tok-type\">Thread</span>.map([<span class=\"tok-lit\">1</span>, <span class=\"tok-lit\">2</span>, <span class=\"tok-lit\">3</span>], <span class=\"tok-kw\">do</span> n -&gt; n * n)   <span class=\"tok-com\"># == [1, 4, 9]</span></code></pre>\n<p>A started lambda carries its own captured scope, but — exactly like an actor&#39;s handler — it gets a fresh root and <strong>cannot see the caller&#39;s running actors</strong>. A failure inside the lambda is not lost; it surfaces as an error at the <code>await</code> that collects it. Its output is <strong>live</strong> — it prints to the same place as you, as it happens, so a started <code>println</code> may land in either order relative to yours, though never split mid-line. (An actor is the opposite: it buffers and delivers its output at the reply, so actor lines never interleave.)</p>\n<blockquote>\n<p><strong>Await what you start.</strong> A handle is a value you can hold, pass, and await from anywhere, but a thread nobody awaits is detached: its result is never collected, and it may be cut off when the program ends. There is no registry of threads and no automatic wait at the end — the handle is the only way back to the work.</p>\n</blockquote>\n<h3 id=\"tests\">Tests</h3>\n<p>A test is a function. There is no test DSL and nothing registers itself: a module implements <code><a href=\"#/Test\">Test</a></code>, and every public function whose name begins with <code>test_</code> is run by <code>ramos test</code>.</p>\n<pre><code><span class=\"tok-com\"># src/test/my_app/user_test.rmo</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">UserTest</span>\n  <span class=\"tok-com\"># @module_doc</span>\n  <span class=\"tok-com\"># The behaviour a User promises its callers.</span>\n  <span class=\"tok-kw\">implements</span> <span class=\"tok-type\">Test</span>\n\n  <span class=\"tok-kw\">alias</span> <span class=\"tok-type\">MyApp</span>.<span class=\"tok-type\">User</span>\n\n  <span class=\"tok-kw\">function</span> test_user_hello()\n    <span class=\"tok-com\"># @doc</span>\n    <span class=\"tok-com\"># A user greets by name.</span>\n    user = <span class=\"tok-type\">User</span>{name: <span class=\"tok-str\">&quot;Andrew&quot;</span>}\n    assert(user.hello() == <span class=\"tok-str\">&quot;Ola Andrew&quot;</span>)\n\n  <span class=\"tok-kw\">function</span> test_default_age()\n    <span class=\"tok-com\"># @doc</span>\n    <span class=\"tok-com\"># A user built with no age is 18, not 0.</span>\n    assert(<span class=\"tok-type\">User</span>{}.age == <span class=\"tok-lit\">0</span>)</code></pre>\n<pre><code>ramos test                # everything under the nearest src/test — found by\n                           # walking up from `.`, so this works from any\n                           # directory inside the project, not only its root\nramos test user           # only the files whose name or path contains &quot;user&quot;\nramos test --quietly      # results only, no doc lines</code></pre>\n<p>The report carries the <a href=\"#documentation-comments\">documentation comments</a> along with the results, so it says what the suite covers and not only that it ran:</p>\n<pre><code>MyApp.UserTest\n  The behaviour a User promises its callers.\n  ok test_user_hello\n     A user greets by name.\n  FAIL test_default_age\n      assertion failed: expected 0 to equal 18\n     A user built with no age is 18, not 0.\n\n2 test(s): 1 passed, 1 failed</code></pre>\n<p>A test with no <code>@doc</code> simply prints its name, and <code>--quietly</code> drops every doc line — for a CI log, or a run whose tests you already know by heart.</p>\n<p><code>ramos test</code> exits non-zero when anything fails, so it drops straight into a build script.</p>\n<p>Tests live under <strong><code>src/test/</code></strong> and follow the same file rules as everything else — the namespace is the path, so <code>MyApp.UserTest</code> is <code>src/test/my_app/user_test.rmo</code>. A test module&#39;s name must <strong>end in <code><a href=\"#/Test\">Test</a></code></strong>, and it must <code>implements Test</code>: the name makes it read as a test in a failure report, and the trait makes being run something a module opts into rather than something its name causes. A test reaches the code it exercises through the ordinary <code>alias</code>, resolved against <code>src/</code>.</p>\n<p>Only public <code>test_</code> functions run, each with no arguments, so a module can keep <code>helper</code> functions beside them. A test stops at its first failed <code>assert</code> and the run continues with the next test.</p>\n<h4 id=\"assert\">assert</h4>\n<p><code>assert(condition)</code> is a <code><a href=\"#/Kernel\">Kernel</a></code> function. It returns <code>:ok</code> when the condition holds — any truthy value, since only <code>false</code> and <code>nil</code> are falsy — and fails the test otherwise. A failure travels the ordinary error path; nothing is thrown, because Ramos has nothing to throw.</p>\n<p>When the condition is a comparison, the failure reports both sides, which is usually the whole story:</p>\n<pre><code>assert(name == <span class=\"tok-str\">&quot;andrew&quot;</span>)\n<span class=\"tok-com\"># assertion failed: expected &quot;Andrew&quot; to equal &quot;andrew&quot;</span>\n\nassert(age &lt; <span class=\"tok-lit\">18</span>)\n<span class=\"tok-com\"># assertion failed: expected 21 to be less than 18</span></code></pre>\n<p>A second argument replaces the generated message when the comparison is not the point:</p>\n<pre><code>assert(is_valid(user), <span class=\"tok-str\">&quot;a user with no email should be invalid&quot;</span>)</code></pre>\n<h2 id=\"command-line\">Command line</h2>\n<p>This repository <em>is</em> the interpreter — a Rust crate whose <code>src/</code> holds the implementation and whose <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/stdlib/\"><code>stdlib/</code></a> holds the Ramos standard library. Build it once and the <code>ramos</code> binary drives everything:</p>\n<pre><code>cargo build --release        # binary at target/release/ramos</code></pre>\n<p>The binary takes a subcommand and (except for the REPL) a single <code>.rmo</code> file:</p>\n<div class=\"table-wrap\">\n<table>\n<thead>\n  <tr><th>Command</th><th>What it does</th></tr>\n</thead>\n<tbody>\n  <tr><td><code>ramos new &lt;name&gt;</code></td><td>Scaffold a project: <code>&lt;name&gt;/src/&lt;snake_case&gt;/main.rmo</code> defining <code>&lt;CamelCase&gt;.Main</code></td></tr>\n  <tr><td><code>ramos run &lt;file.rmo&gt;</code></td><td>Execute a program — calls the file&#39;s <a href=\"#entrypoints\">entrypoint</a> <code>main()</code></td></tr>\n  <tr><td><code>ramos run &lt;dir&gt;</code></td><td>Run that directory&#39;s <code>main.rmo</code> (the shallowest one found)</td></tr>\n  <tr><td><code>ramos run</code></td><td>Same as <code>ramos run .</code> — run the current directory&#39;s <code>main.rmo</code></td></tr>\n  <tr><td><code>ramos learn</code></td><td>Print a crash course: every keyword, the syntax, and what not to do — no file needed, meant for a person or an AI agent to read cold</td></tr>\n  <tr><td><code>ramos repl</code></td><td>Start an interactive read-eval-print loop</td></tr>\n  <tr><td><code>ramos check &lt;file.rmo&gt;</code></td><td>Enforce the strict rules and parse the file <strong>without running it</strong></td></tr>\n  <tr><td><code>ramos ast &lt;file.rmo&gt;</code></td><td>Print the parsed abstract syntax tree</td></tr>\n  <tr><td><code>ramos lexer &lt;file.rmo&gt;</code></td><td>Print the raw token stream</td></tr>\n  <tr><td><code>ramos test [--quietly] [filter]</code></td><td>Run every test under the nearest <code>src/test</code> (found by walking up from <code>.</code>), or just the files whose name or path contains <code>filter</code> — <code>--quietly</code> leaves the <code>@doc</code> lines out</td></tr>\n  <tr><td><code>ramos doctest [--stdlib DIR] [DIR]</code></td><td>Run the <code># ==</code> examples in <code>DIR/src/*.rmo</code>&#39;s doc comments — <code>DIR</code> defaults to <code>.</code></td></tr>\n  <tr><td><code>ramos doc</code></td><td>Generate a Hexdocs-style reference for <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/stdlib/\"><code>stdlib/</code></a> into <code>docs/</code></td></tr>\n</tbody>\n</table>\n</div>\n<blockquote>\n<p><code>run</code> loads the standard library (embedded in the binary), then the entry file and every module it reaches, then calls the entrypoint&#39;s <code>main()</code> — or runs top-level statements when the file has none. <code>--stdlib DIR</code> reads the stdlib from disk instead, which is how the stdlib itself is developed.</p>\n<p><code>ramos doc</code> reads the inline <a href=\"#documentation-comments\"><code>@module_doc</code></a> / <code>@doc</code> comments out of every <code>.rmo</code> file under <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/stdlib/\"><code>stdlib/</code></a> and renders each module, plus a language guide built from the README, an examples guide built from the feature fixtures in <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/tests/fixtures/features/\"><code>tests/fixtures/features/</code></a> (each fixture&#39;s header comment becomes the prose, its code the snippets, so it can&#39;t drift from code that lexes and parses), and a programs guide built from <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/examples/\"><code>examples/</code></a>. Rather than one HTML file per page, it writes <code>docs/docs.json</code> — the rendered content, keyed by page — and a single static <code>docs/index.html</code> shell that fetches that JSON and presents it client-side, swapping pages in as the URL hash changes (<code>#/List</code>, <code>#/guide</code>, <code>#/examples</code>, <code>#/programs</code>). Options: <code>--stdlib DIR</code> (source, default <code>./stdlib</code>), <code>--out DIR</code> (output, default <code>./docs</code>), <code>--examples DIR</code> (fixtures, default <code>./tests/fixtures/features</code>), <code>--programs DIR</code> (default <code>./examples</code>) and <code>--readme FILE</code> (default <code>./README.md</code>) — a missing examples/programs dir or readme just drops that page. Serve <code>docs/</code> over HTTP and open <code>index.html</code> to browse it (the shell&#39;s <code>fetch(&quot;docs.json&quot;)</code> needs a real origin, so opening the file directly as <code>file://</code> won&#39;t load the data).</p>\n</blockquote>\n<h3 id=\"starting-a-project\">Starting a project</h3>\n<pre><code>ramos new pet-project</code></pre>\n<p>creates:</p>\n<pre><code>pet-project/\n└── src/\n    └── pet_project/\n        └── main.rmo</code></pre>\n<p>with an <a href=\"#entrypoints\">entrypoint</a> already in place:</p>\n<pre><code><span class=\"tok-com\"># pet-project/src/pet_project/main.rmo</span>\n<span class=\"tok-kw\">module</span> <span class=\"tok-type\">PetProject</span>.<span class=\"tok-type\">Main</span>\n  <span class=\"tok-kw\">function</span> main()\n    println(<span class=\"tok-str\">&quot;pet-project&quot;</span>)</code></pre>\n<p>The project name (<code>-</code> or <code>_</code> separated) becomes both a snake_case directory — following the same file-naming rule the module system uses everywhere else — and a CamelCase module name, so the project is runnable as soon as it exists:</p>\n<pre><code>ramos run pet-project    # finds and runs src/pet_project/main.rmo</code></pre>\n<h3 id=\"running-a-program\">Running a program</h3>\n<pre><code>ramos <span class=\"tok-kw\">run</span> app.rmo        <span class=\"tok-com\"># loads the stdlib, then app.rmo, then calls App.main()</span></code></pre>\n<p><code>run</code> calls the entrypoint&#39;s <code>main()</code> (see <a href=\"#entrypoints\">Entrypoints</a>); a file of bare top-level statements runs top-to-bottom instead.</p>\n<p>Point it at a directory and it runs that directory&#39;s <code>main.rmo</code> instead — and with no argument at all, <code>ramos run</code> is <code>ramos run .</code>, so the project a <a href=\"#starting-a-project\"><code>ramos new</code></a> scaffold produces is runnable from its own root with nothing more than:</p>\n<pre><code>ramos <span class=\"tok-kw\">run</span></code></pre>\n<h3 id=\"checking-without-running\">Checking without running</h3>\n<p><code>check</code> is <code>run</code> up to but not including execution — it loads the entry file and everything it reaches, enforcing every <a href=\"#strict\">strict rule</a> along the way and reporting the first violation with file, line, column and a source excerpt. Because it loads the whole program, it also catches a module that no file defines. Ideal for editors and CI:</p>\n<pre><code>ramos check src/my_app/business/system_user.rmo</code></pre>\n<h3 id=\"inspecting-the-lexer-and-ast\">Inspecting the lexer and AST</h3>\n<p>Two debug commands expose the interpreter&#39;s internals — useful when working on the language itself rather than a program:</p>\n<pre><code>ramos lexer app.rmo      <span class=\"tok-com\"># the INDENT / DEDENT / NEWLINE token stream</span>\nramos ast   app.rmo      <span class=\"tok-com\"># the parsed tree</span></code></pre>\n<p>Add <code>--dump</code> to either to print the source alongside the output. Both views are <strong>syntax coloured</strong> from the lexer&#39;s own token spans when stdout is a terminal; colour honours <code>NO_COLOR</code> and can be forced with <code>--color</code> / <code>--no-color</code>:</p>\n<pre><code>ramos ast --dump app.rmo --color | less -R   # keep colour through a pager\nramos lexer app.rmo --no-color               # force plain output</code></pre>\n<p>The interpreter runs on a large stack (256 MB, a virtual reservation that costs only the pages it touches), because the standard library recurses once per list element. <code>RAMA_STACK_SIZE</code> resizes it when a program recurses deeper still — or to prove the point with less:</p>\n<pre><code>RAMA_STACK_SIZE=1G ramos run deep.rmo    # 1 gibibyte; also accepts 512M, 64K, a byte count</code></pre>\n<h2 id=\"the-repl\">The REPL</h2>\n<pre><code>ramos repl</code></pre>\n<p>An interactive session: type an expression, see its value. The prompt is indentation-aware, so multi-line <code>case</code>, <code>cond</code>, <code>function</code> and <code>do</code> blocks carry across lines.</p>\n<h2 id=\"project-layout\">Project layout</h2>\n<pre><code>.\n├── README.md                # this file — language tour\n├── Cargo.toml               # the `ramos` Rust crate (lib + binary)\n├── src/                     # the interpreter (Rust): lexer, parser, interp/…\n├── stdlib/                  # standard-library modules in Ramos (documented inline)\n│   ├── kernel.rmo  integer.rmo  float.rmo  list.rmo\n│   ├── map.rmo     string.rmo   tuple.rmo\n│   └── file.rmo    dir.rmo\n├── tests/                   # Rust integration tests + .rmo fixtures\n└── editors/\n    └── neovim/              # syntax / indent / filetype support</code></pre>\n<h2 id=\"editor-support\">Editor support</h2>\n<p>Syntax highlighting, indentation and filetype detection for <code>*.rmo</code> are available for <strong>Neovim</strong>. See <a href=\"https://github.com/andrewaguiar/ramos/src/branch/main/editors/neovim/README.md\"><code>editors/neovim/README.md</code></a> for install options (symlink, <code>:packadd</code>, or <code>runtimepath</code>).</p>\n<h1 id=\"ramos\">ramos</h1>\n"},"":{"title":"Ramos","heading":null,"content_class":"index","body":"<h1>Ramos</h1>\n<p>A small, indentation-driven language that enforces a strict, opinionated style so Ramos code looks the same everywhere.</p>\n<h2 id=\"philosophy\">Philosophy</h2>\n<p>Ramos (plant branches) is a small, indentation-driven language built around four ideas: values are <strong>immutable</strong>, the interpreter enforces a <strong>strict</strong> set of rules rather than leaving them to style guides, the language is deliberately <strong>limited</strong> in how many ways it lets you do the same thing, and where a choice remains, it is an <strong>opinionated</strong> one.</p>\n<p><a href=\"#/guide:philosophy\">Read more in the language guide</a>.</p>\n<h2 id=\"quick-hello-world\">Quick hello world</h2>\n<pre><code><span class=\"tok-com\"># comments start with # and run to the end of the line</span>\nprint(<span class=\"tok-str\">&quot;Ola #{name}&quot;</span>)\n\ngreet = <span class=\"tok-kw\">do</span> name -&gt; <span class=\"tok-str\">&quot;Ola #{name}&quot;</span>\nprint(greet(<span class=\"tok-str\">&quot;Andrew&quot;</span>))</code></pre>\n<p><code>print</code> lives in the <code><a href=\"#/Kernel\">Kernel</a></code> module — and <code><a href=\"#/Kernel\">Kernel</a></code> is the one module that is implicitly in scope everywhere, so <code>print(x)</code> is sugar for <code><a href=\"#/Kernel:print\">Kernel.print(x)</a></code> and you never write the <code><a href=\"#/Kernel\">Kernel.</a></code> prefix (or <code>alias</code> it). See the <a href=\"#standard-library\">Standard library</a> section for the rest of what <code><a href=\"#/Kernel\">Kernel</a></code> provides.</p>\n<h2 id=\"keywords\">Keywords</h2>\n<p>Ramos has a small set of reserved keywords. Each serves a specific purpose; there are no redundant constructs.</p>\n<div class=\"table-wrap\">\n<table>\n<thead>\n  <tr><th>Keyword</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n  <tr><td><code>module</code></td><td>Define a namespace of functions</td></tr>\n  <tr><td><code>struct</code></td><td>Define a typed record with fields</td></tr>\n  <tr><td><code>trait</code></td><td>Define a contract of functions for structs to implement</td></tr>\n  <tr><td><code>implements</code></td><td>Declare that a struct implements a trait</td></tr>\n  <tr><td><code>attributes</code></td><td>Declare struct fields with default values</td></tr>\n  <tr><td><code>function</code></td><td>Define a public function</td></tr>\n  <tr><td><code>helper</code></td><td>Define a private function (module-scoped)</td></tr>\n  <tr><td><code>case</code></td><td>Pattern match a value against patterns</td></tr>\n  <tr><td><code>if</code></td><td>Branch two ways on a condition (no <code>else if</code> — use <code>cond</code>)</td></tr>\n  <tr><td><code>else</code></td><td>The other branch of an <code>if</code></td></tr>\n  <tr><td><code>cond</code></td><td>Branch on a sequence of boolean conditions</td></tr>\n  <tr><td><code>run</code></td><td>Run a block of matches, halting on the first that fails</td></tr>\n  <tr><td><code>do</code></td><td>Create an anonymous function (a lambda)</td></tr>\n  <tr><td><code>alias</code></td><td>Create a shorter name for a module</td></tr>\n  <tr><td><code>as</code></td><td>Rename a module in an <code>alias</code> statement (defaults to the last segment)</td></tr>\n  <tr><td><code>self</code></td><td>Reference the current struct instance or module</td></tr>\n  <tr><td><code>true</code></td><td>Boolean true value</td></tr>\n  <tr><td><code>false</code></td><td>Boolean false value</td></tr>\n  <tr><td><code>nil</code></td><td>Represents absence of value</td></tr>\n  <tr><td><code>when</code></td><td>Guard clause in a <code>case</code> pattern, or on a single trailing statement</td></tr>\n  <tr><td><code>and</code></td><td>Logical AND (short-circuits)</td></tr>\n  <tr><td><code>or</code></td><td>Logical OR (short-circuits)</td></tr>\n  <tr><td><code>not</code></td><td>Logical NOT</td></tr>\n  <tr><td><code>_</code></td><td>Wildcard pattern (matches anything)</td></tr>\n</tbody>\n</table>\n</div>\n<h2 id=\"standard-library\">Standard library</h2>\n<p>This is the reference for the modules that ship with Ramos. <code>Kernel</code> is always in scope; everything else is reached via its module name or an <code>alias</code>.</p>\n<p>New to the language? Start with the <a href=\"#/guide\">language guide</a>, then browse the <a href=\"#/examples\">examples</a> — one small program per feature, or look at the <a href=\"#/programs\">programs</a> page for several features working together.</p>\n<ul class=\"module-list\">\n  <li>\n    <a href=\"#/Actor\" class=\"module-name\">Actor</a>\n    <p class=\"module-summary\">Actor — a named process that holds state and answers messages.</p>\n  </li>\n  <li>\n    <a href=\"#/Config\" class=\"module-name\">Config</a>\n    <p class=\"module-summary\">Config — the environment&#39;s <code>.env</code> file, read once and answered from memory.</p>\n  </li>\n  <li>\n    <a href=\"#/Date\" class=\"module-name\">Date</a>\n    <p class=\"module-summary\">Date — a calendar date: year, month, and day, with no time-of-day or time zone. Built on the proleptic Gregorian calendar.</p>\n  </li>\n  <li>\n    <a href=\"#/DateTime\" class=\"module-name\">DateTime</a>\n    <p class=\"module-summary\">DateTime — a <code><a href=\"#/NaiveDateTime\">NaiveDateTime</a></code> plus a fixed offset from UTC, so it names a precise instant rather than just a wall-clock reading.</p>\n  </li>\n  <li>\n    <a href=\"#/Dir\" class=\"module-name\">Dir</a>\n    <p class=\"module-summary\">Dir — inspecting and manipulating directories on the host filesystem.</p>\n  </li>\n  <li>\n    <a href=\"#/File\" class=\"module-name\">File</a>\n    <p class=\"module-summary\">File — reading, writing, and managing files on the host filesystem.</p>\n  </li>\n  <li>\n    <a href=\"#/Float\" class=\"module-name\">Float</a>\n    <p class=\"module-summary\">Float — operations on floating-point numbers.</p>\n  </li>\n  <li>\n    <a href=\"#/Global\" class=\"module-name\">Global</a>\n    <p class=\"module-summary\">Global — one process-wide map, held by an actor.</p>\n  </li>\n  <li>\n    <a href=\"#/Integer\" class=\"module-name\">Integer</a>\n    <p class=\"module-summary\">Integer — operations on whole numbers.</p>\n  </li>\n  <li>\n    <a href=\"#/Kernel\" class=\"module-name\">Kernel</a>\n    <p class=\"module-summary\">Kernel — the implicit, always-in-scope module of built-ins.</p>\n  </li>\n  <li>\n    <a href=\"#/List\" class=\"module-name\">List</a>\n    <p class=\"module-summary\">List — operations on persistent, immutable lists.</p>\n  </li>\n  <li>\n    <a href=\"#/Map\" class=\"module-name\">Map</a>\n    <p class=\"module-summary\">Map — operations on key/value maps.</p>\n  </li>\n  <li>\n    <a href=\"#/Module\" class=\"module-name\">Module</a>\n    <p class=\"module-summary\">Module — introspecting and calling into modules by value.</p>\n  </li>\n  <li>\n    <a href=\"#/NaiveDateTime\" class=\"module-name\">NaiveDateTime</a>\n    <p class=\"module-summary\">NaiveDateTime — a calendar date and a time of day, with no time zone. <code><a href=\"#/Date\">Date</a></code> plus <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>.</p>\n  </li>\n  <li>\n    <a href=\"#/String\" class=\"module-name\">String</a>\n    <p class=\"module-summary\">String — operations on immutable, UTF-8 text.</p>\n  </li>\n  <li>\n    <a href=\"#/Struct\" class=\"module-name\">Struct</a>\n    <p class=\"module-summary\">Struct — reading and replacing the attributes of a struct instance.</p>\n  </li>\n  <li>\n    <a href=\"#/Test\" class=\"module-name\">Test</a>\n    <p class=\"module-summary\">Test — the trait that marks a module as a test module.</p>\n  </li>\n  <li>\n    <a href=\"#/Thread\" class=\"module-name\">Thread</a>\n    <p class=\"module-summary\">Thread — run a computation on another thread and collect its result.</p>\n  </li>\n  <li>\n    <a href=\"#/Time\" class=\"module-name\">Time</a>\n    <p class=\"module-summary\">Time — a time of day: <code>hour</code>/<code>minute</code>/<code>second</code>/<code>millisecond</code>, with no date and no time zone.</p>\n  </li>\n  <li>\n    <a href=\"#/TimeZone\" class=\"module-name\">TimeZone</a>\n    <p class=\"module-summary\">TimeZone — a name paired with a fixed offset from UTC.</p>\n  </li>\n  <li>\n    <a href=\"#/Tuple\" class=\"module-name\">Tuple</a>\n    <p class=\"module-summary\">Tuple — operations on fixed-size, immutable tuples.</p>\n  </li>\n</ul>\n"}}}